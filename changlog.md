# DNF90 Operation Log

## 2026-09-08 - complete party scene fan-out and bound database/network stalls

- Reproduced two existing Windows/Go source regressions on the unmodified `4caa2fa` baseline: a leader's `op16` committed the follower runtime but the follower never received `op29`, and a non-leader opening the selector left the leader in town. The prepared-follower path was incorrectly rejected by the generic "only the leader may request op16" gate even though the server, not the follower, was executing that path. The authority check now applies only to real client requests; a server-prepared follower consumes the already-validated leader plan and receives the complete op28/op29 chain with its stable party index.
- Town publication could install a resident session in `gameSessions` without assigning a nonzero session generation. Central party bootstrap correctly rejects generation zero, so that edge path could leave a locally visible two-member roster with no authoritative party graph and make selector fan-out a no-op. Resident promotion now establishes the same generation invariant as the normal bind path. The previously failing leader/follower selector synchronization regression now passes without a preparatory test-only bootstrap.
- Dungeon entry removes each member from town presence, but the confirmed return chain did not put that member back. A confirmed typed-op24 return now preserves its exact transition, finishes the actor/HUD chain, restores the town position owner, then republishes the character into the authoritative town registry. This closes the one-sided post-dungeon state where one client displayed two party rows and the other displayed only itself until a channel change.
- Town presence and expert-job stores are now scoped by `(channel, town, area)`, not only `(town, area)`. Equal town/area ids on different game ports can no longer see, invite, move, or replay stores across channels. Initial town, reconnect, normal area change, movement, and dungeon return all attach the resident channel explicitly.
- Reliability/performance hardening: every game socket write now has a five-second deadline and an incomplete peer projection closes that peer for a clean reconnect instead of silently preserving divergent actor sets; stalled event-loop shutdown forcibly removes socket/town/central-party/session-index ownership; aligned repository routes and dungeon discard writes are bounded; unrelated inventory/quest/mail/skill transactions no longer hold `party.mu`; generated runtime MySQL DSNs now include connect/read/write timeouts. These changes bound failure amplification without changing the MySQL schema or replacing MySQL.
- Windows/amd64 verification with Go 1.27.1: `go test -buildvcs=false -count=1 ./...` passes, including the two regressions that fail on `4caa2fa`, the new channel-isolation test, confirmed-return presence test, socket-deadline test, and controller DSN timeout assertion. Real two-client game acceptance remains required.
- `deploy/windows/runtime.version` is now `2026.09.08.3` because server and generated runtime configuration behaviour changed. No generated EXE, runtime config, database, PVF, or client DLL is committed.

## 2026-09-08 - publish initial town co-presence at the op24 boundary

- Two clients could select the same channel and stand in the same town area without seeing each other; reselecting a channel made them visible. The channel change path already registered the player in `OnlinePlayerManager` and installed repository-backed remote actors in both directions, while first town entry and channel reconnect completed the local op24 transition without ever publishing that presence.
- Added one shared post-op24 town-presence publication boundary. Initial character-list town entry and channel reconnect now register the selected character, promote the resident socket, install mode0/mode1/op9/op23 remote state in both directions, and replay town-scoped expert stores. `SET_USER_AREA` uses the same helper so these entry paths cannot drift again.
- The source regression now starts with one registered resident, publishes a newcomer, requires both characters to be registered in the same area, and checks both clients receive the other actor in the proved order. The solo case also requires registration without any self-transition replay.
- `deploy/windows/runtime.version` is now `2026.09.08.2`. Per request this remains a source-only blind repair: no Go tests, server launch, client launch, database operation, or two-client acceptance test was performed on this machine.

## 2026-09-08 - bind party invitations and dungeon entry to one authority

- The reported two-client captures show three facets of the same split-brain party state: the acceptor becomes leader even though the other character sent the invitation, one client renders two party rows while the other renders only itself, and the two clients later enter mismatched dungeon states before both disconnect. This is a party authority/synchronization failure, not evidence of a MySQL performance problem.
- The existing accept path tried to prefer the invitation target, but an invitation sent while the inviter had no central party still recorded party id `0`, and the response path explicitly discarded the recorded party id. A party invite now creates or resolves a hidden authoritative inviter lobby before forwarding the prompt, records that exact party generation, and accepts only while the same inviter still leads that same generation. A transient acceptor-side op12 singleton can no longer become the accepted party's authority.
- Party snapshot fan-out now attempts every live member and joins any delivery errors instead of returning after the first failed socket. Selector entry refreshes the initiator and passive member rosters from the central manager, including when the selector was already open, and leader op16 rejects a non-leader requester rather than silently entering it as a solo run. Dungeon preflight also replaces stale per-session membership caches with the authoritative snapshot before comparing members.
- Added source regressions for a quick invite allocating a nonzero hidden inviter party, an acceptor with a transient self-led party retaining the inviter as leader and slot zero, and a response bound to the wrong party generation leaving membership untouched.
- `deploy/windows/runtime.version` is now `2026.09.08.1` because `dnfbridge` party and dungeon-entry behaviour changed. Per request this is a source-only blind repair: no Go tests, server launch, client launch, DLL rebuild, database migration, or two-client acceptance test was performed on this machine.

## 2026-08-18 - bound the dungeon pickup writes that could freeze a run

- Live play reported three symptoms: picking an item up lagged, sometimes needed several attempts, and after a boss died the run occasionally never settled -- the end-run button and the return-to-town button both did nothing and the client had to be killed. The reporter also noted it worsened the longer a session ran.
- `handleCurrentDungeonPickup` takes `session.dungeon.mu` on entry and holds it through all 366 lines via `defer`. Inside that lock it commits through the repositories at three points: `CharacterAssets` for gold, `AccountAssets` for a discarded-item restore, and `CharacterItems` for an ordinary item. All three passed `context.Background()`, so none of them had a deadline.
- An unbounded commit under that lock does not merely delay one pickup. It freezes the whole dungeon session, because every other dungeon packet for that session needs the same mutex — which matches the reported settle/return-to-town hang exactly, and matches "worse the longer you play" as the character's item and asset rows grow.
- The three writes now run under `currentDungeonPickupWriteTimeout` (5s) and cancel as soon as they return. A stalled database becomes a rejected pickup that leaves the drop `available`, which the existing `game-dungeon-pickup-blocked` path already handles and the client already retries. `TestCurrentDungeonPickupWriteCarriesDeadline` asserts the write carries a deadline and that a rejected write does not consume the drop.
- This bounds the failure; it does not remove the database from the lock. An ordinary pickup still serializes its commits inside `session.dungeon.mu`, so the steady-state latency of a healthy database is unchanged. Moving the commit out of the lock is a larger change with real duplicate-grant risk and is not attempted here.
- `deploy/windows/runtime.version` incremented to `2026.08.18.1` because `dnfbridge` behaviour changed.
- Verification on macOS with go1.26.5: `gofmt`, `go vet ./internal/services/dnfbridge`, the three pickup regressions, and the full `dnfbridge` suite excluding the macOS-unavailable `127.0.0.2` bind test. Source passes; **no Windows client acceptance was performed for this change.**

## 2026-08-17 - install the selected-page null guard from the worker that actually runs

- Entering the first scene still crashed after the op3 guard shipped, but at a different site: `0xC0000005` at `0x017D6C0E` reading `0x00001840`, with `ecx=0`. Client traces separate the two cleanly. `0x00E44ED6` stops at 20:02 and never recurs once the op3 guard reports `result=1`; `0x017D6C0E` appears at 14:57, 18:56, 18:59, 20:37 and again at 21:55:11, eleven seconds after that guard installed. The two crashes share a symptom and nothing else.
- Disassembly located the defect. `0x017D6C0E` is `sub_17D6BE0+0x2E`, `mov ebx, [ecx+0x1840]`, reached before any test of `ecx`. All five callers — `0x01D1A823`, `0x01D2A4C9`, `0x01D33685`, `0x01D90B37`, `0x0340B2A6` — end in the identical `call sub_1892A50` / `mov ecx, eax` / `call sub_17D6BE0`, so a null return from `sub_1892A50` is passed straight through as `this`. The two crash dumps recorded 78 minutes apart carry a byte-identical stack tail, so the path is deterministic rather than a race.
- `ProxySelectedPageApply` already implements exactly this guard. It never ran. Its only installation site is in `InstallWorkerInner`, and that function has no callers: `Start90CNPatch` creates one thread, `InstallWorker`, which calls `InstallTransportWorkerInner` alone. The traces corroborate it — across 54 sessions `selected page apply skipped null this` appears zero times, and so do every diagnostic the same function would have emitted (`auto-repair auxiliary equipment compatibility installed`, `selector create gate trace`), while no `prologue mismatch`, no `already hooked`, and no `native targets did not reach verified bytes` was ever logged. The code was never reached, not failing.
- `InstallSelectedPageNullGuard` now installs that hook from `InstallTransportWorkerInner`, non-fatally, alongside the op3 guard. It logs `selected page null guard installed target=…` on success and `result=%d` either way, so the next trace states plainly whether the guard is present.
- Scope held to the one guard with evidence. `InstallWorkerInner` holds 26 dormant hooks; comparing them against the live traces shows the transport, codec and route hooks are duplicated under different labels by the worker that does run, and that `creature rename`, `dungeon manual pickup container repair` and `op24 loading gate` are installed by separate helpers. Fourteen remain genuinely dormant, four of them null guards. They stay dormant here: some change behaviour rather than only rejecting a null pointer, and none has live evidence behind it. `0x0262EBC0` reading `+0x4C8` was seen once and matches the documented Aura actor-owner offset, but that link is unverified.
- No server, launcher, controller, database, configuration or PVF behaviour changed, so `deploy/windows/runtime.version` is unchanged. **`90CN.dll` was not rebuilt: this host has no MSVC toolchain.** `deploy/assets/client-compatibility.json` therefore still pins the previous DLL, and the pin must be updated from `REBUILD_CLIENT_PATCH.bat` output before packaging. Source change only; **no build and no client acceptance were performed.**

## 2026-08-17 - answer the legacy-framed class1/op1 endpoint request

- Every channel whose ID fits the current EXE's `u8` town-owner field left the client on `连接中` until its 600-second Error2 watchdog. Channel IDs above `255` were unaffected, because their bootstrap already sends the proactive class1/op1 endpoint success before the client asks. Nine live sessions confirmed the split exactly: ports `10121`, `10251`, and `10253` always hung, while `10508` always reached character selection.
- Live capture `game-000121` on channel `253` proved the cause. After the class0/op1 CHANNELINFO notice the current EXE answers on the legacy frame, `cmd=1 type=1 ENUM_CMDPACKET_LOGIN` with a 594-byte body carrying the outer login token, not on the upper envelope. `handleGameCommand` discarded that request unconditionally, so `handleCurrentGameEndpointRequest` was never reached and the one-shot endpoint success was never sent.
- The legacy `op1` route now completes the same handshake behind the same three gates as the upper route: the proved 594-byte body plus the existing 590/598-byte shapes, CHANNELINFO already committed, and one success per TCP session. Unproved body lengths and requests arriving before CHANNELINFO stay write-silent. CHANNELINFO itself, the committed town-owner byte, and the upper-frame handler are unchanged.
- Added regressions for the 594-byte legacy request completing the handshake exactly once, and for the write-silent unproved-length and pre-CHANNELINFO cases. `gofmt`, `go vet ./internal/services/dnfbridge`, the complete `dnfbridge` suite, every `internal/modules/dnf/...` test, and the control suite pass.
- Verified on a live client: channel `251` now reports `game-endpoint-success-sent request_body_len=594 reason=current_exe_legacy_channelinfo_triggered_login_request` and reaches character selection and the first scene. No PVF, database, client asset, or generated runtime configuration was changed.

## 2026-08-17 - restore the omitted null check in the current EXE op3 user-state handler

- Entering the first scene crashed the client with `0xC0000005` at `0x00E44ED6`, reading `0x0000000C`. Five occurrences across two accounts, two jobs, three character slots, and both tutorial dungeons (`7115`/`53127` and `7145`/`70576`) carried byte-identical `eip`, `esp`, `ebp`, `eax`, `ecx`, `edx`, `esi`, and `edi`. Server packet logs disproved a server cause: for the same character in the same dungeon, a crashing run and a successful run emitted an identical first 163 events, and the successful run went on to persist tutorial completion and reach town.
- Disassembly of the current EXE isolated the defect. `sub_1D88A10`, the class0/op3 user-state handler, reads a UI object with `sub_1D93780`, a lookup whose default and per-id branches both end in `xor eax, eax`, and then loads that result into `edi` and passes it as `this` with no null check. Its two neighbouring lookups in the same function do check and branch to `sub_1D88A10+0x194`. With a null `this`, `sub_25B69E0` stores it at `[ebp-0x14]`, runs 236 instructions, then `lea ecx, [esi+8]` and `sub_1E44ED0`'s `mov edx, [ecx+4]` dereference `0xC`, matching the reported fault address and `ecx=8` exactly.
- `90CN.dll` now installs `InstallUserStateNullUiGuard`, restoring the missing check. It verifies the seven bytes `8B F8 E8 D8 BA 90 00` at `sub_1D88A10+0x181` before writing, relocates both displaced instructions into a 21-byte stub, and branches on the lookup result to the handler's own native null label. The branch is taken before the crashing call's two arguments are pushed, so no stack cleanup is invented and no calling convention is assumed. The non-null path executes the original instruction sequence unchanged.
- The transport-only boundary is preserved: the guard adds no business behaviour, reads no packet, and writes no game state. `client-compatibility.json` pins the rebuilt DLL as one unit with the source.
- Verified live on the character and dungeon that reproduced the crash twice within the preceding hour. `user-state null UI guard installed target=0x01D88B91 ... result=1` precedes a clean `lua enter_dungeon confirmed key=1` in the same map `53127`, with no exception recorded after the guard was installed.
- `REBUILD_CLIENT_PATCH.bat` also probes `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools`, which the previous list omitted, so the documented rebuild entry point works on an x86-installed VS2022 Build Tools host.

## 2026-08-17 - repair the LOGIN runtime refresh instead of masking a stale runtime

- Root cause of the reported black-console block: the runtime refresh forced a rebuild of `DNF90Control.exe` *before* stopping the owned service. On a tester machine holding the older ignored EXE set with no Go installation, `LOGIN.bat` stopped at “Go was not found” even though the installed controller was perfectly able to stop the service. `LOGIN.bat` now stops through the installed controller first and only then replaces the runtime.
- Reverted a harmful intermediate fix. `DNF90Build.version` is written only by a controller build of the current source, so a missing marker proves the installed executables predate the marker itself and therefore cannot contain the first-character fixes. Stamping the current `runtime.version` onto those executables let `LOGIN.bat` report a stale runtime as current, pinned the install to pre-fix binaries permanently, and would have made the first-character regression look unfixed on every retest. `LOGIN.bat` no longer writes that marker under any condition.
- When the runtime cannot be rebuilt (no Go) and does not match the source, `LOGIN.bat` now refuses with a specific recovery path, distinguishing an incomplete `runtime\bin` from a stale one, rather than starting executables that would reproduce the very bugs this build repairs.
- Restored the controller refresh at the correct point. A running controller cannot overwrite its own image, and `DNF90Control.exe` is not one of the three binaries the controller builds, so the force flag now sits on the build step after the stop. Without it the controller would never be updated again while the version marker claimed it was current.
- Go discovery for standard Windows installations that are not on PATH is probed through single-line `call :probe_go` in all three scripts. `%ProgramFiles(x86)%` expands to a value containing parentheses, so probing it inside an `if exist ... (` block can terminate the block early.
- The controller accepts the wrapper-discovered absolute Go executable. Release validation rejects a package whose tracked `runtime.version` and shipped `DNF90Build.version` differ, in addition to requiring all four EXEs, so a package can no longer ship binaries that disagree with its own source.
- The release builder now pins `GOOS=windows GOARCH=amd64 CGO_ENABLED=0` and uses slash-separated package paths. Previously it inherited the host target, so a packaging run on a non-Windows host produced host binaries under `.exe` names that still satisfied every payload rule — a package that validated clean and could not run. The server uses no cgo, so this stays a pure cross-build.
- `deploy/windows/runtime.version` incremented because `cmd/server/control` behaviour changed; it now reads `2026.08.17.3` after the legacy-endpoint merge also changed `dnfbridge`.
- README distinguishes a complete release ZIP from a source checkout: `runtime/bin` is ignored by Git, so a copied checkout can never contain the executables a player needs.
- Verification: `go build ./...`, `go vet` on control/release/dnfbridge, and `go test ./cmd/server/release ./internal/services/dnfbridge` pass on macOS with go1.26.5. `./cmd/server/control` keeps two failures, `TestResolveBinaryInstallStateRejectsEscapingPath` and `TestValidateInstance/absolute_asset`, which reproduce identically on the unmodified HEAD and are Windows path-semantics tests. Source passes; **no Windows client acceptance was performed for this change.**

## 2026-08-17 - one-click launcher and first-created-character lifecycle

- `LOGIN.bat` is now the only normal entry point. A tracked runtime version updates the ignored local EXEs once when source changes, then daily launches remain immediate. The launcher selects and validates `DNF.exe`, persists only `client.directory` atomically, starts the service, registers or verifies the selected credential slot, and launches the client without manual `START.bat`, `STATUS.bat`, or JSON editing.
- Replaced the two stacked account forms with one compact native Windows form: account-slot segmented controls, saved credentials, client picker, primary `进入游戏`, secondary `注册并进入`, four visible stages, high-DPI layout, Microsoft YaHei UI typography, restrained neutral/blue/green/red states, and bounded Chinese errors. Registration now continues directly into the game.
- A successful first-character creation now treats its non-empty roster refresh as authoritative ordinary-selection state. It clears an unbound channel-reconnect guess, a superseded deferred roster, and the empty-roster slot probe before the following `op4`. The regression starts with all three stale states, creates character `1`, and requires its immediate selection to stay out of the reconnect route.
- Validation passed `gofmt`, `git diff --check`, package listing, every `internal/modules/dnf/...` test, launcher/release suites, the complete `dnfbridge` suite excluding the macOS-unavailable `127.0.0.2` bind test, the control suite excluding its two Windows-path-semantics tests, and the required native/Windows `go vet` sets. Windows/amd64 candidates built successfully: launcher `4,253,184` bytes/SHA256 `E8437FE8A60CBEAD83484339ED783FC3D95AB8BF47B693C520568722A3CF460B`, control `11,358,720`/`1C2EB13B7802AB4C9250923857D0270D03AB5C4D42A55E5601BB5BA8F1E0FAA7`, server `46,810,624`/`F075208F4AE1834E70A223D8003DD35A944E6DEAC40F49C877E368567AABBF85`, and doctor `27,194,368`/`24F81F784CD30007B80F996A8695F4385BFAAC96372183A0BF95BB156B824B31`. They were not installed from macOS; Windows UI and fresh-client acceptance remain required.

## 2026-08-17 - clear false reconnect state before first-character selection

- A fresh empty selector can arm the current `op2/31` channel-reconnect probe before the real three-byte `GET_USERINFO op8` reaches the shared roster bootstrap. The existing roster fix marked `rosterRequested` but retained an already-armed `channelReconnect`, so the following first-character `op4` could still enter the reconnect route and reproduce the obsolete endpoint/scene lifecycle around `op24`.
- The authoritative roster bootstrap now clears only an unbound reconnect guess: no character may be selected and neither a town nor dungeon scene may exist. An already-bound channel reconnect remains unchanged. Regressions cover both the empty-selector correction and the bound-reconnect guard, then require the ordinary selection stream to contain no reconnect resident notice.
- Replaced the release-oriented root README with the concise developer startup flow: install the five client files, start/configure/status/login, register, and enter with the first character. The old second-character workaround is no longer documented as expected behavior.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the focused first-character/reconnect tests, the complete `dnfbridge` suite excluding the macOS-unavailable `127.0.0.2` bind test, the control suite excluding its two Windows-path-semantics tests, and the required `go vet` set. A Windows/amd64 candidate built successfully at `46,913,536` bytes with SHA256 `732C6B25ABD2473BB8B08BCB0B4FAEB822F13D0BA23AD92BCB81D71DB8BAF020`; it was not installed. No runtime executable, database, PVF, generated configuration, or client DLL changed. Fresh Windows-client acceptance is still required.

## 2026-08-06 - merge damage-font protocol and dungeon entry-level repair

- Selectively merged the supplied damage-font source without replacing newer inventory, party, scene, or repository work. The current client `op515` now validates the exact 19-byte action-162 request against the runtime PVF `[add damage font skin]` definition, consumes one source stack and persists character-scoped ownership in one character-asset transaction. It supports PVF `[period]`, `[date]`, and `[unlimit]` expiration contracts; failed validation or persistence leaves the source stack unchanged.
- Added `op1288` selection against durable, unexpired ownership and class-0 `NOTI 1239` projection (`selected`, count, then font-index/expiry entries). The visible sequence after a successful unlock is class-1 `op515` ACK, repository-backed source `op14` refresh, then NOTI 1239. The selected scene tail also projects the current damage-font state on login/reconnect after the actor-owned stores are ready.
- Merged the `op16` level gate without regressing the current party preflight or layered-dungeon runtime: a `dnfdungeon.ErrEntryLevelTooLow` result now sends only the proved class-1 failure body `{0,14,0}` and creates no runtime or scene-bootstrap state.
- Added protocol, atomic rollback, expiry, PVF, live `Script.pvf`, NOTI-layout, and insufficient-level entry regressions. `gofmt`, `go list -buildvcs=false ./...`, all `internal/modules/dnf/...` tests, the complete `dnfbridge` suite, controller tests, required `go vet`, and the real-PVF damage-font audit passed. Built but did not install `runtime/tmp/DNF90Server-damage-font-6f581375-9a63-4370-a4e7-48e0fa43a334.exe` (46,904,832 bytes). The `control.bat check --skip-database` preflight deliberately refused while the existing DNF90 PID `23188` is running; no runtime configuration, live executable, service process, database state, or PVF asset was changed.

## 2026-08-06 - force restart with merged linked-quest repair

- On request, forcibly terminated the running `DNF90Server.exe` PID `10092`, then rebuilt and installed the current source solely through `deploy/windows/control.bat start --rebuild`. The new server is READY as PID `23188`; portable MySQL remained READY as PID `34772` throughout.
- Post-start status confirms the 90CN profile assets and configuration are valid, the local database is reachable with its schema ready, and all 120 configured listener probes accept connections. The installed `runtime/bin/DNF90Server.exe` SHA256 is `C2A5D1645AC7975ECD2653618E9DBE609ED2C34FAD018AC01416F214773DA30E`.
- This deployment applies the linked-objective atomic completion and read-only `op356`/town-prerequisite repairs recorded below. No PVF asset, database gameplay state, or handwritten generated runtime configuration was changed.

## 2026-08-05 - merge linked-objective completion and read-only clear-quest fixes

- Merged the linked-task repair from the supplied `op33/op34` archive without regressing current quest replay, expert-job, or bulk `op1429` behavior. A no-reward linked child callback now atomically completes the child and recomputes its parent, returning the current-client sequences `op34 -> op574 -> op356` for a child callback and `op33 -> op574 -> op356` for a parent callback. These linked child completions grant neither character experience nor invented reward items.
- Merged the `op356` repair as projection-only behavior. Clear-quest snapshots, persisted-town login transitions, and town transport no longer create or complete PVF prerequisite quests. A town destination with an unmet prerequisite is blocked with no response packet, location mutation, or quest write; routes without prerequisites remain available.
- Added regression coverage for linked-objective protocol order and atomic state, clear-quest read-only projection, unmet teleport/map-station/cross-town prerequisites, and persisted-town login bitmap output. `gofmt`, package listing, all `internal/modules/dnf/...` tests, complete `dnfbridge` tests, controller tests, and required `go vet` passed. A candidate server binary was built under `runtime/tmp`; deployment preflight was intentionally blocked by its running-service safety guard, so no runtime configuration, live binary, database state, or service process changed.

## 2026-08-05 - grant gold cube fragments to the account crystal warehouse

- On request, granted `100,000` PVF-validated gold cube fragments (`3262`, `stackable/material/cubepiece_gold.stk`) to the active local account's account-shared crystal warehouse fixed slot `0:359`. The row carries the current 119-byte item shape and `[material]` metadata; the account inventory timestamp was updated in the same database transaction.
- Pre-write inspection proved that slot `0:359` was empty. Post-commit readback confirms item `3262`, count `100000`, raw length `119`, and the expected PVF path/type metadata. No source code, PVF, generated runtime configuration, client asset, or server executable changed; the running service stayed available.

## 2026-08-05 - make random-reward firecrackers use the ordinary consumable path

- The Spring Celebration Firecracker and Knight Joust Firecracker were sent through the current client's ordinary right-click `op44` path, but the inventory owner admitted only `[waste]` templates. Their PVF type is `[random reward item]`, so the bridge correctly avoided a fabricated ACK but the player saw no decrement or result.
- `op44` now resolves this one consumable family from the active `Script.pvf` `[chn random image percent]` table. The two Spring definitions (`490007317` and `490701730`) consume one firecracker for a 99,000/100,000 visual-only outcome or a 1,000/100,000 grant of `490701733`; Knight Joust Firecracker `490005593` uses its proved 80,000/100,000 visual-only outcome or 20,000/100,000 grant of `490005595`.
- The response remains the current-client native sequence `op44 ACK -> class-0/op14 source row -> class-0/op14 reward row when present`; no random-box result-window protocol is introduced. A reward is placed with its PVF item metadata in the valid main-inventory range. If a positive outcome cannot be placed, the entire transaction rolls back, preserving the source firecracker rather than losing an earned result.
- Added unit coverage for PVF table parsing, source decrement plus reward placement, and full-inventory rollback. The real-PVF smoke test verifies all three live source definitions and their exact weights/reward IDs. Package listing, all DNF-module tests, the complete `dnfbridge` suite, controller tests, required `go vet`, and deployment preflight pass.
- Built and installed `runtime/bin/DNF90Server.exe` (SHA256 `CF11E8789A2F6A6A79D12E1B0EE3B8DC3EE5386DE2B41D79089DEB453EE0BFB6`) solely through `deploy/windows/control.bat`. A stale project `DNF90Launcher.exe` process initially held its identical old backup; after it was closed, the controller finalized the verified atomic install. DNF90Server is READY as PID `10092`, portable MySQL is READY as PID `34772`, and all 120 listener probes pass. No PVF, database item state, or handwritten generated runtime configuration was modified.

## 2026-08-05 - rehydrate the client item object after a pet-bead template replacement

- The transformed bead ID and prompt were correct, but one incremental `op14` result row only patched the existing client object. Its old PVF tooltip and local item-lock state survived, so the new attribute was not immediately visible and a second use could be rejected locally as locked.
- Successful limited-cube use now emits `op338 ACK -> class-0/op14 target-slot deletion -> repository-backed class-0/op14 result row`. The deletion uses the established `0xFFFFFFFF` item-ID sentinel, so the client discards the old object before instantiating the transformed bead. Ticket and material refreshes remain in the following normal incremental row set; no full `op13` inventory replay is sent.
- Added simulated and real-PVF bridge regressions that pin the deletion row, its packet order, and the subsequent transformed-bead row. The target's persistent lock metadata is still validated independently; this change only clears the obsolete client object cache.
- Validation passed `gofmt`, package listing, all DNF-module tests, the complete `dnfbridge` suite, controller tests, required `go vet`, and real-PVF pet-bead smoke coverage. Built and installed `runtime/bin/DNF90Server.exe` (SHA256 `640B032099D180F2968AA56295F366DAD6EFE7EF6BA688C4F2DEA3DAABB73F75`) and restarted only through `control.bat`. DNF90Server is READY as PID `31556`, portable MySQL is READY as PID `7472`, and all 120 listener probes pass. No PVF, generated runtime configuration, or database item state was modified during deployment.

## 2026-08-05 - show the transformed pet bead in the limited-cube result prompt

- The successful pet-bead transaction and inventory refresh were correct, but the live client prompt displayed the ticket name because the limited-cube `op338` response reused the crystal-contract source-item field and sent ticket ID `490701734`.
- The limited-cube response now sends the committed `ResultItemID` in that first `u32`. The current client resolves this field for the prompt, so its display now agrees with the transformed bead in the target slot. Crystal-contract `op338` remains unchanged.
- Added bridge and real-PVF regressions that require the prompt item ID to equal the persisted transformed bead ID. The item-lock rejection remains a separate authoritative path and no lock state is fabricated or cleared by this correction.
- Validation passed `gofmt`, package listing, all DNF-module tests, the complete `dnfbridge` suite, controller tests, required `go vet`, and the real-PVF prompt/result regression. Built and installed `runtime/bin/DNF90Server.exe` (SHA256 `A8FC89EFA6E5E2354BDC6B24085F00CA86483FEB4D2F983E1E23ACE99E6164E5`) and restarted only through `control.bat`. DNF90Server is READY as PID `32824`, portable MySQL is READY as PID `34536`, and all 120 listener probes pass. No PVF, generated runtime configuration, or database item state was modified during deployment.

## 2026-08-05 - require a pet-bead change ticket to select a different bead attribute

- The active PVF result pool for pet-bead ticket `490701734` contains its input bead IDs, including the observed dark-attribute bead `490007240`. The previous weighted selection could therefore return the original ID, consuming one ticket and `3037 x10` while leaving the displayed attribute unchanged.
- Limited-cube selection now excludes the selected input item ID before calculating the weighted result. All remaining PVF candidates preserve their original relative weights. If no different candidate remains, the transaction fails before ticket or material consumption.
- Added domain coverage for same-only result-pool rollback, bridge coverage requiring the simulated bead to change, and real-PVF coverage that `490007240` cannot remain unchanged after a successful use. Detailed evidence and the exact validation command are recorded in `docs/2026-08-05_pet-bead-attribute-change-result-report.md`.
- Validation passed `gofmt`, package listing, all DNF-module tests, the complete `dnfbridge` suite, controller tests, required `go vet`, and the real-PVF `op338` regression. Built and installed `runtime/bin/DNF90Server.exe` (SHA256 `98DC86E60C14943F9486AC16CCA7A802CF6F516A75D7FC778FBBE71D5E8390ED`) and restarted exclusively through `control.bat`. DNF90Server is READY as PID `7652`, portable MySQL is READY as PID `32008`, and all 120 listener probes pass. No PVF, generated runtime configuration, or database item state was modified during deployment.

## 2026-08-05 - consume pet-bead change materials from the account crystal and soul warehouse

- The pet-enchant bead change ticket previously inspected only character-owned list-0 stacks. The required `3037` clear cubes were therefore reported insufficient even when the visible crystal/soul warehouse held them.
- The limited-cube owner now uses the existing atomic account-and-character item transaction. It keeps the ticket and target bead in the character main bag, consumes unlocked ordinary bag materials first, then consumes unlocked account-owned crystal/soul slots `354..365` in stable slot order. Ticket decrement, bead replacement, both material owners, and rollback stay in one transaction.
- `op338` now includes every changed account-shared slot in the existing class-0 `op14` repository refresh. The refresh reads slots `354..365` from `AccountInventory`, so the client immediately receives the authoritative post-use clear-cube quantity instead of a stale character-owned view.
- Added focused domain and bridge regressions for crystal-warehouse-only materials, split character/account consumption precedence, locked shared stacks, `op14` projection, and the real PVF combination `490701734` + `490007240` + `3037 x10` when the PVF smoke path is configured.
- Validation passed package listing, all DNF-module tests, the full `dnfbridge` suite, controller tests, required `go vet`, and the real-PVF ticket/material smoke test. Built and installed `runtime/bin/DNF90Server.exe` (SHA256 `B91A8A93412A5B1F466BBDC6273E8EE53AE7FC94B699377D33AB1D3005B3EB75`), then restarted only through `control.bat`. DNF90Server is READY as PID `2732`, portable MySQL is READY as PID `25612`, and all 120 listener probes pass. No PVF, generated runtime configuration, or database item state was modified during deployment.
- Added `docs/2026-08-05_reverse-pet-bead-ticket-report.md` with the PVF, database, protocol, verification, and deployment evidence chain. The report records no plaintext runtime credentials.

## 2026-08-05 - deliver the item-lock snapshot on every selected-character container bootstrap

- The pet-enchant bead change ticket was still rejected locally because the active current-client selection flow could send all six `op13` containers without the separate `op251` equipment-lock model. The client retained its stale/default locked state and therefore blocked the right-click action before it could emit `op338`.
- `sendCurrentSelectInventoryBootstrap` now builds the durable lock snapshot with the same complete inventory plan, then sends class-0 `op251` immediately after the last `op13` and before full mode1. This includes the explicit empty `00 00` list for a character with no locks. The old town-only follow-up send was removed, making the ordering consistent for initial town, return-select, and actor-bound scene paths without duplicate snapshots.
- Added regressions for populated lock rows, empty lock-list delivery, the full return-select C# initialization path, and post-op29 actor placement. Each asserts `op13` containers precede `op251`, which precedes the equipment-bearing mode1 packet.
- Validation passed package listing, all DNF-module tests, the complete `dnfbridge` suite, controller tests, and required `go vet`. Built `runtime/tmp/DNF90Server-item-lock-bootstrap-20260805.exe` (SHA256 `CDA6798D92BD15EACC4BB9EFD7111396A92AC76586C34EF26E51697496561B00`).
- Stopped DNF90 through `control.bat`, preserved the previous executable (SHA256 `944662F48A76E3BB42F523D44D3F18F8DC4683850A14CFE8909C282DF8DB201F`) at `runtime/backups/server/20260805-181700-item-lock-bootstrap-predeploy/DNF90Server.exe`, installed the candidate, and restarted through `control.bat`. The new PID is `22992`; readiness and all 120 listener probes pass. No database data, PVF, generated runtime configuration, or client asset changed.

## 2026-08-05 - allow current guardian-gem replacement on an occupied medal socket

- Live packet evidence showed `op829` reaching the bridge for the selected worn medal, but the old empty-socket-only guard rejected it with `guardian gem target socket is already occupied: socket=2`. The current client consequently had no response after its explicit replacement confirmation.
- The guardian-gem mutation now overwrites the selected raw socket word when occupied, consumes only the newly selected list-38 gem, and does not return the displaced gem. This matches the current client confirmation that the equipped guardian gem cannot be recalled. The existing list-3 worn-medal update and class-0/list-38 page rebuild remain the client-visible success projection.
- Replaced the occupied-socket rejection regression with atomic replacement coverage: it verifies the new raw socket value, source consumption, and no old-gem recovery. `gofmt`, package listing, all DNF-module tests, complete `dnfbridge` suite, controller tests, required `go vet`, and whitespace checks pass. Built but did not install `runtime/tmp/DNF90Server-guardian-gem-replace-candidate.exe` (SHA256 `9E55B146CB17833F84CC548197B8EE788D4ED6FE2407F9F7316203A7EAFDB240`). The live executable, database, PVF, generated runtime configuration, and client files were not changed.

## 2026-08-05 - bootstrap the current mailbox indicator after first town login

- The selected-town scene tail now sends the current-client class-0 `op63` mailbox alarm after the actor and rental wallet are committed. It reads the durable mailbox through the existing mail owner and sends the exact one-WORD unread count, including explicit zero so an old character's mailbox indicator cannot persist across a selection or reconnect.
- The initialization path deliberately does not push class-0 `op97`: that full mailbox list remains client-requested by `op96`, matching the current EXE's mailbox UI ownership. The tail cursor records the new alarm stage, so a failed write resumes from that packet without replaying scene initialization.
- Added packet-order and exactly-once coverage for an initial scene with one unread message; online-recipient mailbox delivery reuses the same bridge projection.
- Validation passed `gofmt`, package listing, all DNF-module tests, the complete `dnfbridge` suite, controller tests, and required `go vet`. Built but did not install `runtime/tmp/DNF90Server-mail-login-bootstrap-candidate.exe` (SHA256 `A4AFFE32035D87CD318C5ED9EE41D3AE98ED24CE5D2992323857EB2D44589A77`); the live executable, database, PVF, and generated runtime configuration were not changed.

## 2026-08-05 - implement character HonorExpert mastery after level 90

- Implemented the current-client `HonorExpert {u32 level, u64 current-level EXP}` state using the existing `etc/honorlevel.etc` expert table. Level zero remains the challenger state; each PVF row is the experience required to enter that level, and the persisted character state holds only progress toward the next level. The final validated table row is capped rather than emitting an unproven next-level state.
- Accepted EXP after the proven DNF90 level-90 boundary now advances HonorExpert together with ordinary character EXP and the SP/TP ledger. This includes the overflow portion of a level-89 award that crosses into level 90. Monster kills, dungeon-clear settlement, and Adventure Group growth capsules all write the same durable character stats; missing legacy stats remain a valid 0/0 state.
- The bridge now projects the durable state through current mode-1 USERINFO, the town actor tail, and the current `op37` character state at the proved `{u32, u64}` offsets. Mode-1 and op37 wire regressions, actor-tail projection, at-cap monster award persistence, multi-level table arithmetic, settlement idempotency, and atomic stat-transition coverage were added.
- Validation passed `gofmt`, package listing, all DNF-module tests, the complete `dnfbridge` suite, controller tests, and required `go vet`. The real `runtime/data/dnf/Script.pvf` honor-table smoke test passed. Built `runtime/tmp/DNF90Server-honor-expert-deploy-20260805.exe` (SHA256 `944662F48A76E3BB42F523D44D3F18F8DC4683850A14CFE8909C282DF8DB201F`). On request, stopped DNF90 while preserving MySQL, backed up the prior executable (SHA256 `C89B2ADD7959922A79207E2361954BA7470217094522F9584E50D2CF19CAB67B`) at `runtime/backups/server/20260805-175929-honor-expert-predeploy/DNF90Server.exe`, installed the candidate at `runtime/bin/DNF90Server.exe`, and restarted through `control.bat`. The new PID is `28564`; BAT status confirms READY with MySQL PID `5092` still running and all 120 listener probes accepting connections. No database, PVF, generated runtime configuration, or client asset changed.

## 2026-08-05 - make mailbox select-all fill available slots without crashing the current client

- The live op95 reproduction claimed mail `58`, then sent a clear-first class0/op97 snapshot followed by five full class0/op13 container pages while the mailbox UI was still active. The client faulted in `DNF.exe` at `0x027082D3` (`0xC0000005`, read of `0x41`) before it consumed the following private op413 payload; DNF90Server remained live and only observed the remote socket close. Mail claims now retain the required op95 ACK and op97 mailbox reload, but project only the durable changed main-inventory rows through current class0/op14, so the unsupported post-claim full-container replay and its combat-power side effect are removed.
- Batch op95 extraction is now per-mail atomic instead of all-or-nothing for the entire select-all request. It fills compatible stacks first, then empty quick slots `3..8`, then the PVF-resolved item page. Every mail that fully fits is committed with its gold and attachments; a later full mail remains unclaimed with all of its assets intact. If none fit, the existing inventory-full error remains. This preserves atomicity for each individual letter and lets a single select-all request use every available quick-slot and backpack position.
- Added regression coverage for the live-safe incremental refresh boundary, quick-slot-first placement, partial select-all success through both quick slots and the item tab, retention of the first unplaceable mail (including gold), and the all-full rejection path. `gofmt`, package listing, all DNF-module tests, the complete bridge suite, controller tests, and required `go vet` pass. Built but did not install `runtime/tmp/DNF90Server-mail-select-all-candidate.exe` (SHA256 `3414B98F134A33F7411EF33033905586801027EA4EB41390E1E6EC981E3663CC`). `control.bat check --skip-database` correctly refused its mutating preflight while DNF90 was live; `control.bat status` confirms the existing service remains READY with all 120 listeners. No database, PVF, client asset, generated runtime configuration, or live executable was changed.

## 2026-08-05 - stage complete item-lock projection for pet-bead change tickets

- The pet-enchant bead change ticket path now has the missing current-client item-lock bootstrap: after the selected town actor's full `op13` container sequence, the bridge sends one class-0 `op251` complete lock-list projection. It derives rows only from durable `equipment_lock_state` metadata and sends the explicit empty `00 00` list when no items are locked, clearing the client-side stale/default lock state that prevented `op338` from being emitted.
- The lock-wire module now serializes active and pending-unlock rows deterministically across main, avatar, personal-cargo, and pet containers. The limited-cube owner also refuses locked tickets or target beads and never consumes locked material stacks, so client and authoritative transaction behavior agree.
- Added exact packet tests for empty and populated lock snapshots, town bootstrap ordering, and limited-cube lock rejection. Focused `itemlock`, `limitedcube`, and full `dnfbridge` suites pass; a server candidate was built at `runtime/tmp/DNF90Server-item-lock-snapshot-candidate.exe` (SHA256 `D19AA1C75AF9DE59C3E849690A1453E4D32C8CEED3AC5A12BF045995DCCF2AF3`).
- The candidate was intentionally not installed: the concurrently modified mailbox module still has unrelated claim-flow regressions in its full module test suite. The existing runtime binary was left intact and restarted through `control.bat`; portable MySQL data was preserved and the service returned READY. No database, PVF, client asset, generated runtime configuration, or plaintext credential was changed.

## 2026-08-05 - restore equipped guardian-gem hydration during town login

- The selected-town bootstrap created the worn guild-medal object through full mode1, but that create row does not carry the guardian-gem raw socket words at 0x65..0x6D. As a result, a persisted guardian gem appeared empty after login until a wear-state update happened to send a list-3 row.
- After the complete mode1 actor state is installed and before creature/scene initialization continues, the bridge now sends the already-proved list-3/op14 rehydration only for actor slot 32 when its durable raw guardian-gem socket state is nonempty. Generic equipment op14 remains excluded from initialization.
- The town-route order regression now permits only this guarded medal row alongside the existing creature row, and a return-select regression verifies its list-3 row, raw socket value, and mode1-before-op14 ordering.
- Validation passed `gofmt`, package listing, all DNF-module tests, the complete `dnfbridge` suite, controller tests, and required `go vet`. Built `runtime/tmp/DNF90Server-guardian-gem-login-candidate.exe` (SHA256 `C89B2ADD7959922A79207E2361954BA7470217094522F9584E50D2CF19CAB67B`). On request, stopped DNF90 while preserving MySQL, backed up the prior `DNF90Server.exe` (SHA256 `E5882E12C5FA4968B7771E29B26AD66A7A4D8904DB594CB2440EA4F8A1B90875`) at `runtime/backups/server/20260805-172627-guardian-gem-login-predeploy/DNF90Server.exe`, installed the candidate at `runtime/bin/DNF90Server.exe`, and restarted through `control.bat`. The new PID is `31748`; BAT status confirms READY with valid runtime/repository configuration, MySQL schema, and all 120 listener probes accepting connections. No database, PVF, generated runtime configuration, or client asset was changed.

## 2026-08-05 - implement PVF-backed pet-enchant bead change tickets

- Added a dedicated `limitedcube` domain transaction for the pet-bead change action. It verifies the ticket stack at the requested main-inventory slot, selects the first eligible A-condition bead in stable slot order, consumes one ticket plus the complete B-condition material requirement atomically, then replaces that bead with one cryptographically weighted PVF result. Binding and an existing item-instance deadline remain on the transformed bead; the new template metadata, item ID, stack count, and any PVF result deadline come from the selected result row.
- Current-client `op338` now dispatches by runtime PVF behavior: only stackables carrying `[stackable type] [upgrade limit cube]`, `[action type] [limited cube]`, and `[action usable place] [village]` enter the new path. It rejects active-dungeon use, sends the established op338 success envelope, and sends class-0 `op14` updates for the ticket, transformed bead, and every material stack changed. Crystal-contract cubes retain their existing op338 path and dungeon ownership rules.
- The active ticket definitions `490007246` and `490701734` are resolved without hardcoded bead IDs. A conditional real-PVF smoke test asserts their 91-member A/result pools and `3037 x10` material requirement when `DNFBRIDGE_REAL_PVF_SMOKE` is supplied; focused transaction and bridge tests cover successful mutation, split material consumption, invalid/missing inputs, village-only enforcement, and crystal-contract fallback.
- Validation passed `gofmt`, package listing, all DNF-module tests, the complete `dnfbridge` suite, controller tests, required `go vet`, and the real `runtime/data/dnf/Script.pvf` smoke test for both ticket IDs. Built `runtime/tmp/DNF90Server-limited-cube-candidate.exe` (SHA256 `E5882E12C5FA4968B7771E29B26AD66A7A4D8904DB594CB2440EA4F8A1B90875`). On request, stopped DNF90 while preserving MySQL, backed up the prior `DNF90Server.exe` (SHA256 `7491F8F2371A3CA361D60164C7DF5E4A55ECCD5D7DC3CA629A6280F28EB02211`) at `runtime/backups/server/20260805-1604-limited-cube-predeploy/DNF90Server.exe`, installed the candidate at `runtime/bin/DNF90Server.exe`, and restarted through `control.bat`. The new PID is `16436`; BAT status confirms READY with valid runtime/repository configuration and all 120 listener probes accepting connections. No database, PVF, or generated runtime configuration was changed.

## 2026-08-05 - prepare a clean peach-rune native-effect verification

- The first 桃花 application was committed while the prior compatibility DLL blocked opcode 951, so it could restore tooltip state later but could not run the native first-application visual initializer. With the corrected DLL installed, added exactly one PVF-backed 桃花武器特效符文 (`2682369`) to character `pouut`'s verified empty main-inventory slot `0:6` for a clean reapplication test.
- The grant uses a standard 119-byte current item row, count one, native right-click identity field, and PVF metadata `stackable/cash/chn_20120821_effect_system/equipment_effect_peach_flower.stk`. No weapon entry, prior effect state, PVF, generated configuration, or runtime binary was changed. The server stayed READY with all 120 listener checks passing.

## 2026-08-05 - remove the custom Seria-luck top-left HUD overlay

- Removed the DLL-owned Seria-luck HUD compatibility path instead of continuing to mimic the native top-left widget: `90CN.dll` no longer consumes class0/op413 type 0, no longer registers or paints the Seria-luck layered popup/tooltip, and no longer embeds the Seria-luck HUD bitmap resources in the production resource script. The remaining class0/op413 type 1 combat-power sidecar ABI is unchanged and now uses the neutral `client_assets` resource manifest.
- Stopped the bridge from sending the private Seria-luck gauge on login/HUD-ready boundaries or after magic-box post-actions. The account-level Seria luck value, double-trigger accounting, and native magic-box result flags remain intact; only the unsupported custom display/projection was removed while the real EXE-owned protocol is still pending evidence.
- Validation passed focused Seria/legacy boundary tests, package listing, all DNF-module tests, the full `dnfbridge` suite, control tests, required `go vet`, Release/Win32 `90CN.dll` rebuild, and a production server candidate build at `runtime/tmp/DNF90Server-no-custom-seria-hud-candidate.exe` (SHA256 `B9B10FB1730AC01FF5803BD1C7A9BD8494649099560AE67B320977652FAB8B2C`). The rebuilt DLL is `client-patch/Release/90CN.dll` (SHA256 `33BA46E898D592CACAD5B3774A682660BF1194DADB80332456C4773A781652CE`). The current source rebuild of `DNF90Server.exe` matched the already-installed runtime binary (SHA256 `7491F8F2371A3CA361D60164C7DF5E4A55ECCD5D7DC3CA629A6280F28EB02211`).
- Deployed the rebuilt DLL into `client-patch/bin/90CN.dll`, `runtime/idb-client/90CN.dll`, `runtime/idb-client/Patch/Release/90CN.dll`, and the configured game directory `D:/BaiduNetdiskDownload/23.4.15.0/地下城与勇士/90CN.dll`, and updated `deploy/assets/client-compatibility.json` to the same SHA256. Backup snapshots for the replaced runtime/client copies and current server binary were kept under `runtime/backups/client-server/20260805-1425-remove-custom-seria-hud-replace`. BAT `status` remains READY with all 120 listener probes passing; `check --client` was not forced because the controller correctly refuses to preflight runtime configuration while DNF90Server PID `33344` is running. No database, PVF, or generated runtime configuration was changed.

## 2026-08-05 - repair party authority and scene synchronization

- Fixed the ordinary invite path so a transient one-member `SET_PARTY_INFO` (`op12`) projection on the acceptor cannot take leadership from the inviter. `ResponsePeer` continues to join the invite target, while `EntryIntoParty` keeps its existing-party sender as the anchor.
- Town, selector, post-start-map, and position-reorder scene reconstruction now resend current `class0/op153` realtime member rows before the `op9` frame, but deliberately do not resend `op11` peer endpoints. This restores the current client's teammate HP and minimap slot cache without restarting its private-LAN UDP relay.
- Opening the dungeon selector is now a same-area party scene transition for any member, not only the leader. The canonical leader and stable slots remain unchanged while every other ready party member receives the full passive selector sequence.
- Added regressions for acceptor-side transient `op12` leadership theft, non-leader selector fan-out, and the no-`op11` `op153`-then-`op9` scene refresh order. No database, PVF, client DLL, generated runtime configuration, or client asset was changed.
- Validation passed `gofmt`, package listing, all DNF-module tests, the full `dnfbridge` and controller suites, required `go vet`, and a production candidate build at `runtime/tmp/party-scene-sync-20260805-1329/DNF90Server.exe` (SHA256 `B9B10FB1730AC01FF5803BD1C7A9BD8494649099560AE67B320977652FAB8B2C`). The live `control.bat status` remains READY with all 120 game listeners; the mutating `control.bat check --skip-database` was intentionally blocked by its running-service guard and was not forced.

## 2026-08-05 - rewrite online parties around one authoritative manager

- Reworked the DNF90 party lifecycle against the current 86JP reference commit `069ad4f1fb9a96ce4fd824d3412b5951f07bfe2f`, while keeping the existing Go composition boundaries and current-EXE packet layouts. The C# transport and packets were not copied: `internal/modules/dnf/party` now owns the central, process-local membership graph, and `dnfbridge` remains responsible for compatibility packets, session validation, scene/roster projection, directory, raid integration, and the directed UDP relay.
- Party IDs are allocated independently of character IDs; membership has four stable slots; a character is atomically detached from an old party before it joins another; and invitations bind both participants' live session generations. Leader leave or leader transfer retires the old party identity, clears the old client table, and rebuilds survivors under a fresh party ID with the successor in slot zero. Reconnect replaces only the session generation, retaining the party/slot and rejecting packets from the retired socket.
- Party directory, peer invitation/accept, leave/kick/reorder/leader transfer, raid-derived parties, and relay topology now all resolve through this one manager. Per-session party state is reduced to a current-EXE packet cache; the previous divergent pending-invite and session-owned membership helpers have been removed. The existing 86JP-style directed UDP relay remains private-LAN-only in this local profile and advertises the configured local game address rather than a public or wildcard endpoint.
- Added regressions for leader rebuild, stable slots, generation-bound invitations and reconnect, cross-party joining with old-party survivor refresh, four-member capacity, reordering, and idempotent first-lobby creation. `gofmt`, package listing, every DNF-module test, the complete `dnfbridge` and controller suites, required `go vet`, production server build, offline BAT preflight, and live readiness all passed.
- Per the standing force-update instruction, the prior server was preserved at `runtime/backups/server/20260805-1035-party-manager-rewrite-predeploy/DNF90Server.exe` with SHA256 `AB6068D8F7F0DA2BC7D3DA0FDD7CC7B61362729AEBB2E4E09DF6410F0C26604C`. The installed `runtime/bin/DNF90Server.exe` SHA256 is `96A0F0E8611B381D93BE588986149957595F08C6CFBB69F5EDA2910D8EA39A91`; portable MySQL and DNF90Server are READY and all 120 game listeners accept connections. No client DLL, PVF, generated configuration, or database row was changed.

## 2026-08-05 - harden the current-client party lifecycle against reconnect races

- Compared the local party implementation with 86JP main commit `77fd463ca6426489d22ef625642d155e4db6ed26` (2026-08-04, `complete party networking and social lookup`). The existing DNF90 bridge already has the relevant current-EXE wire layouts for four-member formation, party directory, invite/accept, leave/kick, leader rebuild, scene fan-out, dungeon follow, and dynamic `class0/op11` peer-port publication.
- Adopted the missing lifecycle protection without copying C# packets: every pending invite is now bound to both authoritative `gameSession` instances and their selected-character generations. A response after the inviter reconnects cannot attach the acceptor to the replacement session. All state-changing online-party commands are also rejected when they arrive from a retired connection, so stale buffered packets cannot join, leave, kick, delegate, reorder, or change a current party.
- A generation is now allocated before a session is entered into the online-character directory, including focused/reconnect paths that precede the event loop. The exact 22-byte `op11` endpoint layout remains unchanged, but remote member rows now use a server-owned directed UDP relay: every ordered pair gets its own port, learns only the first datagram whose IPv4 matches that member's authenticated TCP peer, and forwards through the reverse pair socket. A partial or stale room is never advertised; the existing direct endpoint form remains only as the safe fallback when a complete relay room cannot be installed.
- The relay binds only the concrete configured game-channel IPv4 (currently the auto-detected private LAN address), never a wildcard. `server.partyUdpRelayPortStart` and `server.partyUdpRelayPortCount` reserve a default range of `30000-30063`. A future public profile must advertise its public relay IPv4 and expose that same UDP range (with separate bind/advertise addresses when it is behind NAT); this local-only profile intentionally continues to advertise only a private LAN address. Added regressions for inviter replacement, stale direct-join suppression, directed relay forwarding/source authentication, and former-member leg removal.
- Validation passed `gofmt`, package listing, every DNF-module test, the complete `dnfbridge` and controller suites, required `go vet`, and the production build. Per the force-update instruction, DNF, launcher, and old server were stopped; the prior server/control binaries were preserved under `runtime/backups/server/20260805-093635-party-relay-predeploy`. `runtime/bin/DNF90Server.exe` is now SHA256 `AB6068D8F7F0DA2BC7D3DA0FDD7CC7B61362729AEBB2E4E09DF6410F0C26604C`; its startup confirms `party_udp_relay=true`, host `192.168.5.7`, range `30000-30063`, and all 120 current TCP listener probes pass. No client DLL, PVF, generated configuration, or database row was hand-edited.

## 2026-08-05 - correct the native organization-name wire alignment

- Live packet evidence proved the server delivered the prior name, and the installed DLL forwarded mode-0 packets unchanged. The remaining fault was in the server packet shape: the current native structure is `{guild_level:u8, guild_name:DSTR, chaos_point:u32}`. The preceding update had projected the DSTR but omitted its mandatory leading `guild_level`, so the EXE consumed the string-length byte as the level and never accepted the name for its native head label.
- The selected actor now sends level `1` only when its durable adventure-group name is nonempty, followed by that GB18030 name and the zero chaos-point value. Actors with no name retain a level of `0`; peer projections remain empty. This uses the native nameplate reader and adds no DLL overlay, guild record, or synthetic guild ID.
- Exact wire regressions cover the required level/name pair and peer isolation. Validation passed `gofmt`, package listing, all DNF-module tests, the full bridge/control suites, and required `go vet`. Per the user's force-update instruction, the game launcher and prior server were stopped, the prior server was preserved under `runtime/backups/server/20260805-0830-adventure-overhead-native-wire-predeploy` with SHA256 `7DAACAF1B2DA63D9C32292BD00FC6751E244CCD4891D3D73B122143E92BEBBCA`, and `runtime/bin/DNF90Server.exe` now has SHA256 `B11DD8C60C2DDDAA299BAD5D3D9D099EAFB22FAA16CCF1A19E25390AC74879D3`. BAT-only startup and all 120 listener probes pass. No client DLL, PVF, generated configuration, or database row was changed.

## 2026-08-05 - restore the native adventure-group line above the current character

- Re-examined the current EXE's typed mode-0 actor tail after the prior HUD-ready account-state replays did not render a second name line. The DSTR after equipped-creature state is the native organization-name field used by the nameplate path; its following u32 is the independent chaos-point value, not a guild identifier.
- The selected local actor now projects its already-persisted `represent_account_name` into that native organization-name field. This is display-only: no guild is created, no synthetic identifier is emitted, no social or character data is changed, and peer actors cannot inherit the viewer's account label.
- Added exact-wire regression coverage for the GB18030 name bytes and for the peer-isolation rule. Validation passed `gofmt`, package listing, all DNF-module tests, the full bridge/control suites, and required `go vet`. After the client was closed, the previous server was preserved under `runtime/backups/server/20260805-0323-adventure-overhead-native-label-predeploy` with SHA256 `1F18D1FBD6D62CF943E88CB7EF080C84B246225670A916863B46EA139E4204C1`; `runtime/bin/DNF90Server.exe` now has SHA256 `7DAACAF1B2DA63D9C32292BD00FC6751E244CCD4891D3D73B122143E92BEBBCA`. BAT-only startup and all 120 listener probes pass. No client DLL, PVF, generated configuration, or database row was changed.

## 2026-08-05 - give joust settlement mails a real item-instance deadline

- The active PVF defines the returned 骑士大晶体 as usable through 2028-08-16, but the joust settlement mail had only written an item ID and count. The current client interpreted that zero attachment-instance timestamp through the retired event path and rejected the reward as expired.
- New settlement mails now persist the resolved PVF expiration on the attachment. Before each mailbox open, the server transactionally repairs only unclaimed, non-deleted mails whose durable source is `joust_settlement_multiplier_return`; claimed or unrelated mail is not touched. The repaired attachment is then projected to the mailbox and retained through extraction into inventory.
- Regression coverage verifies both new payout mail and the idempotent repair of an existing pending settlement attachment. Validation passed `gofmt`, package listing, all DNF-module tests, the full bridge/control suites, and required `go vet`. Candidate only while DNF is open: `runtime/tmp/DNF90Server-joust-mail-expiration-candidate.exe`, SHA256 `AC9868F39BD7E924FD270F8DB0DB50B11CBBCBA594E12BA6DFAA2165C5F0FB2C`.

## 2026-08-05 - merge layered-story return repair and restore HUD-ready adventure-group label replay

- Compared all three supplied repair packages against the active source. The persistent dungeon-permission route and Queen Maya quest 3249 terminal-child settlement are already covered by newer implementations, so those packets and transactions were not duplicated or downgraded.
- The layered-story return path now permits the final same-coordinate `MoveKind=1` transition back to its cached base room even when that scripted layer emits no combat death report. A completed terminal layer may also leave through its explicit adjacent target; any pending successor layer remains blocked. Regressions cover the no-death return, terminal exit, and anti-skip rules.
- The latest live capture disproved the prior mode-0 DSTR projection: that field reaches a generic actor event and does not render an overhead name. It is restored to the empty sentinel. The known visible label route is instead replayed after the HUD-ready account model refresh (`op1340 -> op1346 -> op1371`) so every login and town re-entry receives the persisted adventure-group name without changing scene-object shape. Validation passed `gofmt`, package listing, all DNF-module tests, the full bridge/control suites, and required `go vet`. After the user exited DNF, the prior server was preserved under `runtime/backups/server/20260805-0215-layered-story-return-adventure-label-predeploy` with SHA256 `697B4EA88F67EFD2E908B410633814E7709687B6A20E488235C32BF3D01D42E4`; `runtime/bin/DNF90Server.exe` now has SHA256 `1F18D1FBD6D62CF943E88CB7EF080C84B246225670A916863B46EA139E4204C1`. BAT-only offline preflight, strict startup, MySQL schema check, and all 120 listener probes pass; MySQL PID `8492` and DNF90Server PID `16540` are READY. No client DLL, PVF, generated configuration, or database row was changed.

## 2026-08-05 - attempted account-state replay for the town overhead HUD (superseded)

- Confirmed the account already holds a nonempty durable `represent_account_name`; the prior `op1340 -> op1346` replay correctly refreshed the account model but did not create the missing second overhead line.
- The HUD-ready class-0 `op1371` replay was installed as an evidence-gathering attempt after `op1340 -> op1346`. A later mode-0 DSTR experiment replaced it, but the subsequent current-client capture disproved that replacement. The current correction restores this HUD-ready replay and keeps the actor packet unchanged.
- `gofmt`, package listing, all DNF-module tests, the complete bridge/control suites, required `go vet`, and the production build passed. After the user exited DNF, preserved the preceding server in `runtime/backups/server/20260805-0106-adventure-overhead-name-predeploy` (SHA256 `6BCA1D51AEEE54C6B8F9BC863A3193994D6A3EAC5E5B3767CA8A7426809299D3`) and installed `runtime/bin/DNF90Server.exe` SHA256 `AA5DD399360C091E57002A3D0183B2F06E899D10F852209020E2853B9D4A7EDD`. BAT-only preflight, strict startup, readiness, and all 120 listener probes pass; portable MySQL PID `13636` and DNF90Server PID `3772` are READY. No client DLL, PVF, generated configuration, or database row was changed.

## 2026-08-05 - render the eighth joust entrant as the native mystery knight until reveal

- Static inspection of the current `NoPack.exe` `op1241` reader confirms that byte `10` of each 11-byte rider record is a special-display status and the client branches on value `1`. The restored roster had correctly selected an eligible mystery identity (including Lv Bu) but incorrectly emitted status `0` for every rider, exposing the selected identity and portrait before the opening draw.
- The eighth entry now carries the proven `MysteryRiderStatus=1` flag only in the opening roster. Its real PVF rider ID remains unchanged in the server model, public pool, wagers, bracket results, and settlement, so it is revealed by the normal match progression and payouts still resolve to the actual knight.
- Added domain and exact-wire-layout regressions ensuring the seven original regular knights stay visible, while only the eighth rider is flagged hidden in `class0/op1241`.
- `gofmt`, package listing, all DNF-module tests, the complete bridge/control suites, required `go vet`, and the production build passed. After the user exited DNF, preserved the preceding server in `runtime/backups/server/20260805-0048-joust-mystery-hidden-predeploy` (SHA256 `1E057C46681066A9A3778360928DC781D6E714432216217E4A347EF14CE2CBBE`) and installed `runtime/bin/DNF90Server.exe` SHA256 `6BCA1D51AEEE54C6B8F9BC863A3193994D6A3EAC5E5B3767CA8A7426809299D3`. BAT-only preflight, strict startup, readiness, and all 120 listener probes pass; portable MySQL PID `7072` and DNF90Server PID `1544` are READY. No client DLL, PVF, generated configuration, or database row was changed.

## 2026-08-05 - keep legacy joust settlements from disconnecting the current client at login

- Traced the immediate post-update disconnect to the initial town activity sequence. The current EXE accepted the unchanged `class0/op108 -> op1206 -> op1240` prefix, then the server aborted before `op1241`: previously persisted `completed_rounds_v1` rows were generated under the retired bracket and their champions no longer matched the restored original roster. The new history projection treated that mismatch as a fatal error while constructing the roster, so the client saw a network interruption immediately after `op1240`.
- Legacy or malformed settlement rows are now retained for recovery but ignored when their champion cannot be verified against the current original bracket. They neither block the login/roster push nor appear in the native match-history response or rider win/loss counters. Only a durable settlement whose champion matches the active bracket is projected and paid.
- Added the incompatible-legacy-settlement regression. `gofmt`, package listing, all DNF-module tests, the complete `dnfbridge` suite, control suite, required `go vet`, and the production build passed. After the user closed DNF, preserved the preceding server in `runtime/backups/server/20260805-0031-joust-login-disconnect-predeploy` (SHA256 `0D19D69F0B1173ED419E1A841F38F459ACB05400D6172C94E47370BE4D205977`) and installed `runtime/bin/DNF90Server.exe` SHA256 `1E057C46681066A9A3778360928DC781D6E714432216217E4A347EF14CE2CBBE`. BAT-only preflight, strict startup, readiness, and all 120 listener probes pass; portable MySQL PID `14016` and DNF90Server PID `16036` are READY. No client DLL, PVF, generated configuration, or database row was changed.

## 2026-08-04 - restore original joust entrants, real history/mail settlement, and HUD-ready adventure-name replay

- Replaced the fabricated 500-round joust history with durable completed-round records only. A new activity now starts each visible rider at `0胜/0败`; counters advance only from the seven actual bracket matches of a recorded settlement. The history response keeps its current-EXE-required 500-row wire size, with unused rows zero-sentinel padded.
- The current runtime PVF was verified to map the 2017 roster exactly: the seven regular entrants are 理查德、爱德华、罗兰、贝奥武夫、莱奥、伊萨尔、席恩; the eighth slot is a deterministic-per-round mystery selection from 吉利特、无双飞将吕布、骷髅骑士、机械骑士格弗雷. 湖上骑士兰斯洛特 remains excluded as the activity-dungeon lord. Every semi-final now repeats the state transition before its stage snapshot so the native countdown, battle state, and horse-flash animation are retriggered rather than painting the result directly.
- Winning support is no longer placed in the inventory. The exact material used for that round is stored with the ledger and returned as a durable system-mail attachment at `floor(winning_support * final_multiplier)`, atomically with clearing the pending claim. Legacy wagers safely default to the original permanent crystal. A live winner receives only a mailbox-alarm refresh; no duplicate payout is possible if that notification fails.
- Added a one-shot, post-type1345 replay of the authoritative adventure account model (`op1340 -> op1346`) after the town actor/HUD is ready. This preserves the required pre-mode1 initialization pair while allowing the current EXE's overhead widget to bind the stored adventure-group name on every login, channel reconnect, town move, return, and re-entry.
- Validation passed: `gofmt`, package listing, every DNF-module test, the complete `dnfbridge` suite, control suite, required `go vet`, and live `Script.pvf` joust roster smoke. Candidate: `runtime/tmp/DNF90Server-joust-original-flow-overhead-candidate.exe`, `46416896` bytes, SHA256 `0D19D69F0B1173ED419E1A841F38F459ACB05400D6172C94E47370BE4D205977`.
- After the user closed DNF, preserved the preceding server under `runtime/backups/server`, installed the verified candidate as `runtime/bin/DNF90Server.exe` with the same SHA256, and restarted the local services. Portable MySQL is READY as PID `16164`; DNF90Server is READY as PID `14844`; all 120 listener probes pass. No client DLL, PVF, generated configuration, or database data was changed.

## 2026-08-04 - restore task-manual bulk main-quest completion

- Captured the current client task-manual confirmation: it sends legacy `op1429` (`CLEAR_QUEST_TICKET`) with a zero u32 selector, not a normal `op34` single-task reward settlement. The previous implementation wrongly required exactly one active eligible epic for that zero selector; the selected level-90 character has multiple active eligible main quests, so it returned a no-op 13-byte result and the UI appeared to do nothing.
- A zero-selector `op1429` now performs its native bulk action: atomically complete every currently active, PVF-validated `[epic]` quest below the level-89 cutoff. Each completion remains explicitly no-reward; side, event, profession, level-90-or-higher, inactive, and nonmatching-job quests are left unchanged. The existing nonzero selector still completes only its exact eligible main quest. The reply carries the highest completed quest ID and refreshes completed, active, and acceptable quest projections.
- Added zero-selector multi-main and no-eligible-main regressions plus the bridge projection regression. Pinned the unrelated spend-time/joust packet-order test to a deterministic betting-window clock so it no longer fails when tests run during later live joust phases. `gofmt`, package listing, all DNF-module tests, the full `dnfbridge` and control suites, `go vet`, and production build pass. Candidate only while the user remains logged in: `runtime/tmp/DNF90Server-quest-auto-complete-candidate.exe`, `46407680` bytes, SHA256 `24A780A96A1FE9BD24813CC006DB84F98A768437AE973CD5AA4C316609AD23EC`.

## 2026-08-04 - keep the native cumulative-online-reward hover widget initialization atomic

- Static current-EXE analysis in `NoPack.exe` confirms that event `2347` owns the original cumulative-online widget: `op108` installs the catalog/total-stage data and `op1206` enters `sub_E713E0 -> sub_F41760`, which binds the elapsed time and completed-stage count to the native HUD and its `ItemReward` hover controls. The missing hover page was therefore a packet-order regression, not a missing client UI asset.
- Restored the required contiguous `class0 op108 -> class0 op1206` initialization sequence. The independent joust bootstrap (`op1240`, `op1241`, `op1242`) now follows the completed reward-widget pair rather than splitting it. This preserves the existing joust state while allowing the current EXE to bind the original daily/accumulated reward hover window.
- Updated protocol regressions to pin adjacency and partial-write recovery (`op108, op1206, [joust]`), then ran `gofmt`, package listing, every DNF-module test, the complete `dnfbridge` suite, control tests, and required `go vet` successfully. The prior server was backed up at `runtime/backups/server/20260804-2147-spend-time-hover-predeploy`; installed `runtime/bin/DNF90Server.exe` and the retained candidate both have SHA256 `7BB4E39B64F285C02626DF771BE9C4D9B98940D7B8F930FF8C2F927CE30E2B45`. BAT-only offline preflight, strict startup, readiness, and all 120 listener probes pass. DNF90Server PID `18816` and portable MySQL PID `7920` are READY. No client DLL, PVF, generated configuration, or character data changed.

## 2026-08-04 - add an internal battle-power improvement guide and official four-row equipment cell

- Restored the original green `战力提升` control below profession/level and made only that exact button rectangle interactive. Clicking it toggles a no-activation, in-game `战力提升攻略` side panel; no browser or external URL is opened, the remainder of the combat-power overlay remains click-through, and the guide closes with the personal panel. The guide shows the current score/rank and equipment percentage plus concise 90-version advice covering complete equipment, title/avatar/aura, creature and all creature equipment, reinforcement/amplification/set/rarity, white damage stacking, ordinary yellow/critical conflict, additional yellow/critical stacking, and all-attack classification into three attacks.
- Restored the official visible order `白字 / 黄字 / 装备 / 爆伤` inside one continuous equipment cell. The new `装备` percentage removes the already-displayed white/yellow/critical multiplier score from Lua's equipment subtotal before dividing the independent equipment-derived score by the base score, so the four rows do not double-count one another. The selected-character V6 capture (`base=20996`, `equipment=110501`, `54/36/38%`) resolves to an affix score of `39688`, an independent equipment score of `70813`, and `装备 337.26%`.
- Regenerated the exact `124x389` skin with the gold selector arrows on the equipment row, extended the detail cell through the former auto-expand footer, retained the original bottom border, and flattened transparent white RGB edge pixels onto the panel's near-black background to remove the reported white fringe. Release/Win32 builds with zero errors and the eight pre-existing C4996 warnings. After the user exited DNF, the prior canonical/configured DLLs and compatibility manifest were preserved under `runtime/backups/client/20260804-1712-combat-power-guide-predeploy`; Release, canonical, and configured client `90CN.dll` now share `1112576` bytes/SHA256 `E1742E4FFACEC0A505597D72C106F4806F233135E9692DFFFE931E97508FFEEA`, which is pinned by `90cn-decode-bypass-v1`. BAT-only offline preflight passed, portable MySQL is READY as PID `16412`, DNF90Server is READY as PID `18104`, and all 120 listeners accept connections.
- Removed the last inherited horizontal separator between `装备` and `爆伤`: the continuous-cell texture now begins on the old separator scanline and covers its full framed width while preserving only the true bottom frame. The preceding installed DLL and manifest are recoverable under `runtime/backups/client/20260804-2206-combat-power-detail-separator-predeploy`. Release, canonical, and configured client `90CN.dll` are now `1112576` bytes/SHA256 `354B03AADA64F4C291EE4D28FBEA6F7EF6846F20E2FD13DABEAF5E01B64E108C`, pinned by the compatibility profile. BAT-only offline preflight and strict startup passed; portable MySQL is READY as PID `17364`, DNF90Server is READY as PID `29208`, and all 120 listeners accept connections.
- The follow-up in-game capture identified one brighter source scanline immediately above that covered band. The final resource starts its separator-free texture one pixel earlier, erasing the last visible dark/light rule between `装备` and `爆伤` while retaining the outer bottom border. The preceding client DLL and manifest are recoverable under `runtime/backups/client/20260804-2213-combat-power-remove-final-separator-predeploy`. Release, canonical, and configured client `90CN.dll` now share `1112576` bytes/SHA256 `18B22F72E2464ACFA99B1393B8E475A5A013E023CAAF9FC7CA46E6C64EEC9E27`, pinned by `90cn-decode-bypass-v1`; BAT-only offline preflight and strict startup passed, with portable MySQL READY as PID `29028`, DNF90Server READY as PID `9088`, and all 120 listeners accepting connections.
- Removed the black rectangular exterior at the bottom and around the original anti-aliased TGP frame without discarding the real blue/metal border: fully transparent source pixels now use a rare colour key in the layered combat-panel window, so they reveal the game scene; partially transparent edge pixels remain flattened dark to avoid the original white fringe. The prior client DLL/manifest are recoverable under `runtime/backups/client/20260804-2224-combat-power-transparent-frame-predeploy`. Release, canonical, and configured `90CN.dll` now share `1112576` bytes/SHA256 `0818A9A88C56FC08A181AE948D2A5773B0A77533B51D386543E246C45F71214B`, pinned by `90cn-decode-bypass-v1`. BAT-only offline preflight and strict startup passed; portable MySQL is READY as PID `10692`, DNF90Server is READY as PID `29612`, and all 120 listeners accept connections.
- The colour-key approach exposed anti-aliased source-frame gaps and made the scene look as if it overlapped the border. Replaced it with a single opaque `124x386` panel cropped at the real one-pixel metal bottom edge, removing the retired black footer tail without creating any transparent holes. Both `基础属性加成` and `装备加成` caption frames now extend three pixels farther left and right while retaining their original, centred, unscaled lettering. The preceding DLL/manifest are recoverable under `runtime/backups/client/20260804-2231-combat-power-solid-frame-wide-headings-predeploy`. Release, canonical, and configured `90CN.dll` now share `1112576` bytes/SHA256 `9AB7FEBBF179F10E4FC38C31601E96868577746023E616A66249020BF9452237`, pinned by `90cn-decode-bypass-v1`; BAT-only offline preflight and strict startup passed, with portable MySQL READY as PID `21940`, DNF90Server READY as PID `16032`, and all 120 listeners accepting connections.
- The subsequent capture showed that the widened headers still exceeded the untouched `基础属性加成` value cell. Expanded that cell to the exact common rail coordinates used by both caption frames and the equipment detail cell (`x=7..116`), so the three sections now share the same left/right frame columns. The preceding DLL/manifest are recoverable under `runtime/backups/client/20260804-2244-combat-power-align-score-cell-predeploy`. Release, canonical, and configured `90CN.dll` now share `1112576` bytes/SHA256 `49D68D06034EF207F20CA5D01D7681258069E5169F566BE377993C76A01BCEA5`, pinned by `90cn-decode-bypass-v1`. BAT-only offline preflight and strict startup passed; portable MySQL is READY as PID `21696`, DNF90Server is READY as PID `14400`, and all 120 listeners accept connections.
- The next capture showed that the actual white/yellow/equipment/critical detail frame still retained the old narrow rails. Expanded that full `白字 / 黄字 / 装备 / 爆伤` frame symmetrically to the common `x=7..116` rail pair while preserving the runtime text centre and the equipment-row arrows. The preceding DLL/manifest are recoverable under `runtime/backups/client/20260804-2301-combat-power-wide-detail-cell-predeploy`. Release, canonical, and configured `90CN.dll` now share `1112576` bytes/SHA256 `E492E1242F41206C5E86496109419C5BB452588919DD4265C2B461DC6579ECA2`, pinned by `90cn-decode-bypass-v1`. BAT-only offline preflight and strict startup passed; portable MySQL is READY as PID `25856`, DNF90Server is READY as PID `9604`, and all 120 listeners accept connections.

- The follow-up capture isolated the remaining left/right black bars as the original bitmap's three fully transparent source columns on each side, which GDI had flattened into the panel's opaque background. The regenerated `118x386` skin removes only those six columns, retaining the authentic inner metal rails; all badge, score, button, captions, value cells, detail rows, arrow controls, hit-tests, and overlay geometry shift left by three pixels to remain aligned. The preceding canonical/configured DLLs and compatibility manifest are recoverable under `runtime/backups/client/20260804-2318-combat-power-crop-black-side-padding-predeploy`. Release/Win32 built with zero errors and the eight pre-existing C4996 warnings; installed `90CN.dll` SHA256 is `B46F77E4F659F1413368E7161155ACEAE69C739491E1912091CAA248E2CB482B`, pinned by `90cn-decode-bypass-v1`. BAT-only compatibility preflight and strict startup passed; portable MySQL PID `25888`, DNF90Server PID `8248`, and all 120 listeners are READY.

## 2026-08-04 - allow one round's joust support to be split among all riders

- Replaced the incorrect one-rider-only joust ledger with durable per-rider support entries while retaining the current client's single `op1242` total as the aggregate amount. A character may now add support to any of the eight active riders during the betting period, up to the proven 10,000-crystal total for that character and round. The already-recorded legacy wager is migrated into its original rider entry upon the next add, so it is neither lost nor counted twice.
- Public support and odds now aggregate every per-rider stake across all account characters. Settlement pays only the portion placed on the champion, leaves losing split portions unpaid, and remains atomic/idempotent. Legacy single-rider records continue to load, display, and settle unchanged.
- Added regressions for split wagering, aggregate cap enforcement, multi-rider public-pool projection, legacy fallback, and settlement of only the winning portion. `gofmt`, package listing, all DNF-module tests, the complete bridge/control suites, `go vet`, and the production build pass. Candidate: `runtime/tmp/DNF90Server-joust-split-support-candidate.exe`, `46403584` bytes, SHA256 `4D2FD70DBE684661659E607396BA435A2221A5AB5CAB2617B901DB42AB91A2DA`.
- After the user closed the game, preserved the preceding server under `runtime/backups/server/20260804-joust-split-support-predeploy` and deployed that candidate as `runtime/bin/DNF90Server.exe` with the same SHA256. Rebuilt the stale BAT control helper, completed the BAT-only preflight, and restarted the local services: portable MySQL is READY as PID `13108`, DNF90Server is READY as PID `27336`, and all 120 listeners accept connections. No client DLL, PVF, generated configuration, or wager record was overwritten.

## 2026-08-04 - add PVF equipment-grade scoring to combat-power V6

- Closed the missing `装备加成` component that kept the user's fully equipped level-90 character at `92662`. The combat-power catalog now reads each actually worn item's authoritative `[minimum level]`, `[grade]`, `[rarity]`, and `[equipment type]` from the active `Script.pvf`; it never infers rarity or quality from an item ID. The bounded local V6 calibration is `minimum level * 20 + grade * 5 + rarity * 100`. It applies to every proved current-actor slot `0..32`, so avatars, aura/skin/name-tag/weapon-avatar, the equipped creature, and red/blue/green creature artifacts participate alongside weapon, title, armor, jewelry and special equipment. Existing set-threshold affixes remain PVF-derived and are not counted twice as another invented set constant.
- Runtime-PVF regression pins the selected character's exact 30 worn definitions: 12 level-90 epic combat items at PVF grade 96, 12 avatar-family entries, one title, one creature, three level-60 creature artifacts and one name tag. Their new PVF equipment-grade subtotal is `38835`; combined with the previously captured V5 total `92662`, the expected unchanged-loadout V6 total is approximately `131497` and therefore crosses the requested `武炼 >= 130000` boundary. The visible official-style layout remains unchanged and still shows only `基础属性加成`, `装备加成`, `白字`, `黄字`, and `爆伤` in their existing cells.
- Extended the DLL-private class0/op413 snapshot append-only from version 3/66-byte body to version 4/70-byte body with `pvfEquipmentScore`, a dedicated validity flag, and strict length/version checks. `DNF90DamageAffixSnapshot` grows append-only from 104 to 108 bytes; `90CNLua/1.12.0` exposes the field to Chinese-documented Lua formula V6 and adds it only to the equipment subtotal. Both Lua and native rank/icon/hover-table boundaries now use the user's exact `0/3100/8100/23000/52000/65000/130000` ranges.
- Validation passed the Lua V6 regression, focused and real-runtime-PVF combat-power tests, `gofmt`, package listing, every DNF module test, the full bridge/control suites, required `go vet`, both Release/Win32 DLL builds, production server build, BAT-only offline compatibility preflight, strict startup, readiness, and all 120 listener probes. Installed `DNF90Server.exe` is `46398464` bytes/SHA256 `515480AF3DC1001985F254C957ED154ED443DC7DF914C9C263CDDA52275B83BE`; installed/canonical `90CN.dll` is `1107456` bytes/SHA256 `292ED106FABC848F1551D6690974EA7CF6F93F124754A3C3BD2CB932B9BDE487`; installed `90CNLua.dll` is `331776` bytes/SHA256 `450C738888DDEE312C013D4D14EDB1F21E96DB29382CFA5C81DB4A9A7FDCD426`; installed `lua/init.lua` SHA256 is `E38B42036FD320897ACFEF4EC9BB74815B55F1EC8C0B5D090000F146DBC8A9AA`. A lingering target-client PID `25188`, which the user had already reported closed, was the exact Restart Manager owner of both DLL locks and was terminated; the unrelated DOVE client PID `22536` was untouched. The preceding client/server unit is preserved under `runtime/backups/client-server/20260804-1405-combat-power-v6-predeploy`. Portable MySQL is READY as PID `21104`, DNF90Server is READY as PID `17260`, and no PVF, database content, character state, item, or generated runtime configuration changed.

## 2026-08-04 - repair retired horse-joust shop materials and client time gate

- Repaired the complete nine-item 2017 horse-joust chain (`490005585` through `490005593`): the shop crystal, activity crystal, exchange shard, four conversion/reward boxes, and celebration firework all move from the retired `2017-11-02 06:00:00` PVF deadline to the existing `2028-08-16 06:00:00` pool entry. This uses a restricted nine-file candidate patch, leaving all unrelated event expiration strings untouched.
- Login item-expiration reconciliation now recognizes only that retired joust item range. When one of those static items carries an older persisted instance deadline, its PVF deadline wins and is written back to inventory, warehouse, account cargo, metadata, and the current item-row expiry field. Ordinary per-instance expiry, rentals, and every item outside the range retain their existing precedence. The regression covers both automatic repair of a 2017 shop stack and preservation of a non-joust per-instance expiry.
- Current NoPack evidence identifies `sub_F2D0A0+0x86` as the remaining historical weekend/10:00 client gate: it displays message `76736` and returns before the native joust-opening request whenever its type-73 schedule string is empty. The compatibility DLL verifies the original short conditional branch `75 34` and changes only its condition to the equivalent unconditional continuation `EB 34`; native state checks, op1291 request, UI construction, and all server authority remain intact.
- Validation passed: `gofmt`, package listing, all DNF module tests, the full `dnfbridge` and control suites, and required `go vet`; Release/Win32 `90CN.dll` rebuilt with zero errors (eight pre-existing C4996 warnings). After DNF exited, installed server SHA256 `3FB3C010BF21A6BCA082FD5EE4CC9DECD5594DFA58A18F3E17AE457082610A47`, PVF SHA256 `EC7050524DDD2CB5126F4BC9ABAF438B602360CCCDBC15D1BE6AB291A2DC204A`, and DLL SHA256 `091EF15D513D2BA53B6A8E816FC3FDA38891CC61C1022C27F28223F53D26AB90`; all three active PVF paths and canonical/configured DLL paths match. Both asset and client-compatibility manifests now pin those hashes. BAT-only offline preflight, strict startup, readiness, and all 120 listener checks pass; MySQL is READY as PID `2952` and DNF90Server is READY as PID `26584`.

## 2026-08-04 - center and enlarge the combat-rank nameplate text

- Corrected the live TGP sidecar's copper rank nameplate after the `英杰` capture showed the two-character label visually left of the plate center. The runtime text rectangle now follows the plate's optical center with a one-pixel rightward correction, and the rank font increases from 9 to 10 pixels; score, profession/level, base score, and all three affix rows are unchanged.
- Regenerated the exact-size design preview and rebuilt `90CN.dll` with zero errors and the transport's eight pre-existing C4996 warnings. After DNF PID `6908` exited, the preceding canonical/configured DLL and compatibility manifest were preserved under `runtime/backups/client/20260804-1254-combat-rank-text-predeploy`. Release, canonical, configured-game, and junction-resolved runtime copies now share `1107968` bytes and SHA256 `B9BB8D9E697AAC1C9F34A8E2B609589C5FA57776FC9F6CEAC2A273DC91C3D69A`; `90cn-decode-bypass-v1` pins the same hash. BAT-only offline compatibility preflight passed, strict startup accepted all five client files, portable MySQL is READY as PID `17972`, DNF90Server is READY as PID `18400`, and all 120 listeners accept connections. Current-EXE visual capture remains pending.

## 2026-08-04 - merge town-scene, quest-completion, selection-permission, and op33 trigger repairs

- Semantically merged the four supplied archives instead of overwriting the current bridge with their older snapshots. A normal current-client town transition now completes any transport-only PVF `NeedQuest` before character-location persistence, then emits `op22 -> op23 -> optional persisted op356 -> full op24`. The existing complete area roster, party synchronization, transport-select suppression, expert state, and actor ownership paths remain intact. A NeedQuest save failure now moves neither the character nor the scene and emits no town packets; `op22`, `op23`, and `op24` write failures retain location rollback.
- Normal and profession task completion project their transaction-owned `PostCommitQuest` completed bitmap as op356 after character-state refresh and before the established op574/op21 snapshots. Reconnect/durable finish receipts are now deduplicated by `{characterID, questID}` only after their ACK was written successfully, so they neither duplicate the current client's terminal receipt nor leak suppression across character selection; a failed ACK is retryable.
- Character-selection scene entry retains the proven `ACK -> persisted op5 permission snapshot -> op1378/scene` order. The active enter-select path no longer appends a late duplicate op5, while the existing follower permission path is retained. The ESC archive's declared settlement-return limitation remains unresolved and is not represented as fixed here.
- The op33 set-trigger merge treats current one-hot types `0x10`, `0x20`, and `0x40` as independent 9-bit packed channels, preserves reserved high bits, saturates each channel at `511`, and clears only the exact task's stale terminal-replay keys when a committed new accept reactivates it.
- Added regressions for the exact town sequence and failures, completed-quest-before-op24 projection, normal/profession completion ordering, selected-character permission ordering, op33 channel arithmetic, reconnect duplicate suppression, cross-character replay isolation, and ACK-write retry. `gofmt`, scoped diff checks, package listing, every DNF module test, the complete dnfbridge suite, control tests, and required `go vet` pass. Candidate only: `runtime/tmp/DNF90Server-town-quest-op33-candidate.exe`, `46396416` bytes, SHA256 `57EA50E3F26F057FC0BE4409FFF75982BAC0E89A257A8225DAA3F5F9F6F7ED4E`. It is not installed yet because the local DNF client remains running; no loaded client, runtime executable, PVF, database, or generated runtime configuration was modified.

## 2026-08-04 - fix negative joust remaining-support count

- Corrected the current-EXE op1242 field meaning using `sub_E72540` and its `sub_F297B0` consumer. The second `u32` after the round is the selected character's current-round support amount, which the client subtracts from PVF `[max betting] 10000`; it is not the public support total. Sending the generated public total `16692` produced the reported `10000 - 16692 = -6692` display.
- Op1242 now writes the selected character's durable current-round ledger amount (zero for no wager or another round), while its eight `{u8 knight,u32 support}` records continue to carry the account-wide public pool used for distinct live multipliers. Values above the PVF per-character limit fail closed. A three-crystal wager regression pins `3` in the personal field, and a separate boundary test rejects `10001`.
- `gofmt`, package listing, every DNF module test, the complete bridge/control suites, required `go vet`, the production build, BAT-only offline preflight, strict startup, readiness, and all 120 listener checks pass. The preceding server is preserved under `runtime/backups/server/20260804-joust-personal-support-predeploy`; MySQL data was preserved. DNF90Server PID `22456` is READY with SHA256 `525266A8B7875EA8020E0AACF15B9D6D1E514A8C9C7A815BD2E665AFC3734E88`. No client DLL, compatibility hash, or PVF changed.

## 2026-08-04 - complete the all-day knight joust tournament, settlement, and history flow

- Completed the current-EXE class-0 activity protocol from the native consumers instead of retaining the opening-only placeholder. Op1243 now uses the exact 31-byte `{u16 round,u8 stage,7*DWORD match}` projection proved by `sub_E72600`; each match DWORD is `{winner_id,winner_action,loser_id,loser_action}` as consumed by `sub_F34350`, ordered as four quarter-finals, two semi-finals, and one final. Op1293 now returns up to 500 exact seven-byte `{u16 round,u8 winner,f32 multiplier}` rows as proved by `sub_F29A80`, followed by the client's zero-sequence sentinel padding.
- Added a restart-stable seven-match tournament simulation over the active `event/chn_event/chn_joust.evt`. The eight-rider draw and unbiased winner decisions are deterministic per round because the PVF exposes no hidden strength/win-probability field; each winner/loser action is selected from the rider's real four-by-seven Win/Loss cumulative tables using the opponent's proved attack category `0/1/27/28`. Malformed attack categories or cumulative tables fail closed. Op1241 history counters are reconstructed from the preceding 500 deterministic tournaments.
- Implemented the original two-hour cadence as an all-day repeating activity: betting `0..90` minutes, quarter-final stage at 90, semi-final at 95, final stages at 100/105, settlement at 110, and the next opening at 120. The process gameplay queue schedules only those boundaries. Op1240 is emitted once at real state transitions `1/2/3/4`; 95- and 105-minute animation steps emit only op1243, preventing the previously observed op1291/start-notice feedback flood.
- The public pool now aggregates durable ledgers from all 32 character slots on the authenticated local account, including already-settled ledgers for the still-active round, so one character's settlement cannot change another character's multiplier. Betting remains limited to one rider and 10,000 crystals per character/round, closes after 90 minutes, awards one PVF participation firework per wagered crystal, refreshes the authoritative inventory, and republishes the changed op1241/op1242 pool to every online session for that account without replaying op1240.
- Added atomic, idempotent settlement through `CharacterAssets`. Winning claims grant `floor(wager * final_multiplier)` permanent竞猜硬币 item `490005585`; losing claims only close the ledger. The pending flag and inventory change commit together, so inventory-full failures preserve the claim for reconnect retry and a committed claim cannot pay twice. A previous pending round must settle before a new bet can overwrite it. Completed-round winner/multiplier overrides are persisted in account settings before settlement and merged into the deterministic 500-row history after restart.
- Reconnect/town bootstrap now sends the current state, roster/pool, and the appropriate progressive op1243 snapshot; it also retries any due account settlement and refreshes the winning character's main inventory before the event snapshot. The background scheduler settles all account characters when an online session crosses the 110-minute boundary and pushes the authoritative inventory before state 4.
- Synthetic protocol/domain regressions cover exact op1243 byte order, all phase boundaries, account-wide pool aggregation independent of `pending`, three-firework placement for a three-crystal wager, deterministic history float rows, winner payout, settlement rollback, history persistence, and duplicate-settlement prevention. The real runtime Script.pvf smoke validates all 12 rider IDs/attack types and completes a tournament. `gofmt`, package listing, every DNF module test, the complete bridge/control suites, and required `go vet` pass. With DNF closed, the old server was preserved under `runtime/backups/client-server/20260804-0840-joust-complete-predeploy`; BAT-only offline preflight passed with all 120 ports available. The installed server is `46383616` bytes with SHA256 `00412DC5B372E392B3E73DDD5BE85CC1DB5C45F24471CC93109251C200B4930A`. The compatible client DLL unit is unchanged, so its existing manifest hashes remain authoritative. Strict startup accepted all client/PVF assets, portable MySQL is READY as PID `10516`, DNF90Server is READY as PID `10296`, and all 120 listener checks pass.

## 2026-08-04 - restore per-round joust multipliers and support pools

- Corrected the current-EXE op1241 interpretation from an invented `u32 + 3*u16 + u8` row to the layout proved by `sub_F2CA10`, `sub_F2C990`, and `sub_F2BF70`: `{u8 knight, u8 attack_type, f32 multiplier, u16 wins, u16 losses, u8 status}`. The previous zero-filled bytes occupied the real float field and therefore rendered every rider multiplier as `0`; op1241 now writes a finite nonzero `float32` at exact offset `2` for each of its eight 11-byte records.
- Added a strict domain catalog for `event/chn_event/chn_joust.evt`. It validates the level, per-round limit, crystal/reward item IDs, all 12 unique rider IDs, their current-PVF attack types, names, and complete 28-value win/loss battle tables. Runtime roster data now comes from the active Script.pvf and malformed or incompatible event data fails closed.
- Added continuous two-hour all-day round identities and deterministic single-account baseline support. Each round selects eight distinct PVF riders, assigns eight distinct support buckets, and calculates the original pool-style multiplier as `total support / rider support`; the baseline is stable across restart and changes with the next round. The exact 46-byte op1242 projection now publishes `{u16 round,u32 total,8*{u8 knight,u32 support}}` after the roster.
- The captured op1292 request path now validates the selected rider against that active round, performs crystal debit, participation-firework placement, and durable round ledger mutation atomically, refreshes the authoritative inventory, then republishes op1241/op1242 so every affected multiplier changes immediately. It deliberately does not replay op1240: live evidence proved that doing so causes the current EXE to issue another op1291 indefinitely, flood the start notice, and freeze.
- The spam/freeze candidate was not retained in production. Runtime remains on the preceding stable server while DNF PID `22744` is active; replacement and the BAT-only offline preflight are deferred until that client exits normally. Online `status` still reports MySQL/server ready and all 120 listener checks passing.
- `gofmt`, package listing, every DNF-module test (including a real-current-PVF 12-rider check), the complete bridge/control suites, required `go vet`, scoped diff checks, and the production build pass. The prepared source-matched candidate is `runtime/tmp/DNF90Server-joust-multiplier-candidate.exe`, `46286336` bytes, SHA256 `91F4D435BE2372B491DE413635A2A9A2028724FC12422BF01CD61878610D718C`.

## 2026-08-04 - complete the 2017 joust NPC opening handshake

- Live memory after the reported no-op proved event `2365` was active, the selected character met the PVF level-50 gate, and owner `609` already held all eight opening knights, but only in its fallback vector. Current-EXE disassembly then isolated both missing conditions: `sub_F2D0A0` rejects the owner while the transplanted NPC conversation leaves the scene in a blocked interaction sub-state, and the same native gate requires the type-73 state at `+4` to be nonzero before it promotes and renders the window.
- The client compatibility DLL now overrides `sub_22645B0` only for the exact return address `sub_F2D0A0+0x47`; every other scene-state caller retains the original result. Owner `609` open attempts also log their caller, native result, and primary-vector state. The repair does not manually construct a window or edit an owner vector: the current EXE still performs op1291, event-state validation, native control creation, registration, and rendering.
- The server now sends the proved three-byte class-0 op1240 body `{round=0,state=1}` after op108 and before the existing class-0 op1241 eight-knight roster. The op1291 response repeats op1240 then op1241 so a reconnect or late query repairs both native state and roster. Tests pin the exact op1240 layout and the complete `108 -> 1240 -> 1241 -> 1206` ordering, including partial-write recovery without replaying the process-lifetime op108 descriptor.
- Release/Win32 rebuilt with zero errors and the eight pre-existing C4996 warnings. `gofmt`, package listing, every DNF-module test, the complete bridge/control suites, required `go vet`, production server build, BAT-only offline compatibility preflight, strict startup, readiness, and all 120 listener probes pass. The installed server is `46197248` bytes with SHA256 `F673B9C7910B0F316684712636825A61C6C9DA469BD6FB47C026DE89B1EB74F0`; Release, canonical, analysis, and configured-game `90CN.dll` are `1107968` bytes with SHA256 `0E5FCDF80A49ED959C1B6E5E92D6C84DB661A7E53C1F704E2A9C9402E1477329`, pinned by `90cn-decode-bypass-v1`. The preceding client/server unit is recoverable under `runtime/backups/client-server/20260804-0710-joust-window-state-predeploy`. Portable MySQL is READY as PID `25940`, DNF90Server is READY as PID `26868`, and native click acceptance remains pending after the user's next login.

## 2026-08-04 - restore the original TGP combat-power frame and compact official rows

- Statically extracted the original `124x389` combat-power panel bitmap from the user-supplied 2017 plugin archive and embedded that source frame directly. The title bar, help button, copper rank nameplate, gold score plate, deep-blue value cells, section headers, arrows, side rails, and bottom frame are no longer approximated. Only the obsolete green upgrade control and bottom auto-expand control are mechanically cleared. The exact-size candidate is `client-patch/assets/combat_power_panel_skin_v2_preview.png`; the original/candidate comparison is `runtime/diagnostics/combat-power-tgp-reference-vs-v5-preview.png`.
- Reduced the visible equipment section to the requested official-style rows `白字/黄字/爆伤`. Yellow-additional is folded into the displayed yellow value, critical-additional is folded into the displayed critical value, and all-attack is classified into the three-attack equipment calculation instead of becoming a fourth or fifth row. The panel now uses the original positions for total score, profession/level, base score, equipment header, and three equal value rows; no equipment subtotal, slot count, formula label, loading prose, upgrade button, or auto-expand row is drawn.
- Extended the private class0/op413 projection to type-1/version-3. Alongside the existing PVF affixes and all `0..32` worn slots (including creature and three creature artifacts), it now carries the selected repository character's real job, packed grow type, level, PVF-resolved profession name, and PVF character-plus-equipment physical/magical/independent attack values. Profession names are parsed from each character `.chr` `[growtype name]` and branch `[awakening name]`; the client no longer guesses a profession from a local static label table.
- Lua formula V5 adds the three real attack values to the base basis, applies all-attack only as a three-attack equipment bonus, and retains auditable white/yellow/critical multipliers. The append-only native snapshots/panel state now carry identity and three-attack validity flags plus the UTF-8 profession name. The updated offline Lua regression, synthetic and real-PVF profession-name checks, every DNF module test, full bridge/control tests, package listing, required `go vet`, both Release/Win32 client builds, and a production server candidate build pass.
- After the user closed DNF, the preceding client/server compatibility unit was preserved under `runtime/backups/client-server/20260804-061654-combat-power-tgp-v5-predeploy`. The production server was rebuilt from current source as `46195712` bytes with SHA256 `EC512AA27AE6E0F44274C56FD6C3E664B6178746A47C7A46C9581D4EC9B87036`; canonical and configured-client `90CN.dll` are `1106432` bytes with SHA256 `AEA6774CAAD99634694183C2713A962ABBBCA7C5486E7B24388CC8AAF97FD108`, configured `90CNLua.dll` is `331776` bytes with SHA256 `8152A4C46B44BBD647172CACA0860EAA69E879DE1BF4B2D6A0B994381E4CBD17`, and configured `lua/init.lua` is `46488` bytes with SHA256 `D902A823F10019D41D2A183AD9625265419C9525C2E40E527CA4727ECE754325`. BAT-only offline compatibility preflight and startup both pass; MySQL PID `22376`, DNF90Server PID `28344`, readiness, and all 120 listener probes are healthy. Native in-game capture remains pending and is not represented as completed.

## 2026-08-03 - place the 2017 joust event entrance in Hendon Myre

- Restored the PVF-backed 2017 `艾莉亚` joust entry without inventing a new NPC index: the indexed legacy event slot `20449` now owns the existing `npc/chn_masang_2017.npc` role, shop `101262`, field animation, faces, and dialogue. Removed only the stale `[open visible event]`/`[event open]` gates so the deliberately enabled local activity remains visible outside its retired live-event window.
- Moved NPC `20449` on `map/cataclysm/town/hendonmyre/new_hendon_main.map` to `(2183,156)`, the exact midpoint between `艾莎` `(1896,156)` and `大祭司` `(2470,156)`. The source joust NPC remains unchanged, the old lion role is absent from the indexed target, and reverse-reading the repacked archive verifies the joust role and shop.
- Synchronized the candidate to `runtime/data/dnf/Script.pvf`, `runtime/idb-client/Script.pvf`, and the configured game directory. All three copies are `111095213` bytes with SHA256 `473C5B8504DF19153EF386DA51ABB55C1CDDDE231D60AB9C947C29B55267156E`; `deploy/assets/manifest.json` pins the same compatibility-unit size/hash. The preceding three PVFs and manifest are recoverable under `runtime/backups/pvf/20260803-joust-hendon-midpoint-predeploy`.
- BAT-only offline preflight passed after gracefully stopping the server and portable MySQL; MySQL data was preserved. Strict startup validated the PVF and all five client compatibility files, restarted portable MySQL as PID `17512` and DNF90Server as PID `24360`, and passed readiness plus all 120 listener checks. Current-EXE live acceptance of the NPC click and the request-driven `1291/1292/1293` joust flow remains pending and is not represented as completed by this entrance-only deployment.

## 2026-08-03 - match the compact TGP combat-power panel layout

- Reordered the native sidecar to the supplied TGP visual hierarchy: title, rank badge, total score, rank name, centered `基础属性加成`, centered `装备加成`, and one vertical row each for 白字、黄字、爆伤、黄追、爆追、全攻. Percentages now use the screenshot's two-decimal presentation while retaining the server's authoritative 0.1% storage.
- Removed every non-reference presentation row: equipment subtotal, occupied-slot count, active-set count, score-composition heading, base/equipment share bars, formula version, status prose, and `自动展开`. The complete 33-slot/PVF calculation remains unchanged behind the UI, and the rank-range table remains hover-only.
- Release/Win32 builds with zero errors and the transport's eight pre-existing C4996 warnings. The exact-size layout preview is `runtime/diagnostics/combat-power-v4-layout-preview.png`; the reference comparison is `runtime/diagnostics/combat-power-v4-layout-comparison.png`. After DNF PID `21648` exited, the old installed/canonical DLL and compatibility manifest were saved under `runtime/backups/client/20260803-220610-combat-power-compact-layout-predeploy`. Release, canonical, and configured-game `90CN.dll` now share SHA256 `8FFA7D112229CCC4B1C4B30428F1DFDEF3C3511E9ACCF46F802E48ED39DDC550`, and `90cn-decode-bypass-v1` pins that value. BAT status passed all 120 listeners; strict launch accepted all five client files and started formal PID `21312`. Native GDI capture remains pending at the character selector.
- The first live capture `runtime/diagnostics/combat-power-v4-live-misaligned.png` exposed two geometry errors hidden by the isolated preview: the root-widget width omitted about five pixels of the native visible frame, welding both panel borders together, and the total-score row crowded the badge's lower ornament. The corrected build uses a six-pixel visible inter-panel gap and moves the total/rank rows below the badge; Release/Win32 again passes with SHA256 `522F6021110426070BD62F22EC2C1F3324DDA40ECE67B858F05C3DFD4183B783`. After PID `21312` exited, the first compact-layout DLL and manifest were saved under `runtime/backups/client/20260803-222417-combat-power-layout-alignment-predeploy`; Release, canonical, configured-game DLL, and compatibility manifest now share the corrected hash. Strict launch accepted all five client files. Live game PID `6280` consumed the complete 30-row equipment snapshot and PVF projection (`54/20/20/16/18/79%`), rendered total `117213` and base `17074`, and visibly passed both normal and hover-table acceptance in `runtime/diagnostics/combat-power-v4-live-alignment-fixed.png`, `combat-power-v4-live-reference-comparison.png`, and `combat-power-v4-live-hover-fixed.png`. Final BAT status reports DNF90Server PID `24360`, portable MySQL PID `17512`, and all 120 listeners ready.
- A subsequent live annotation identified the badge's empty lower nameplate as the intended rank owner. The candidate now draws `探索/开拓/无畏/征服/战绝/英杰/武炼` only inside that `34..94 x 96..114` plate and leaves the former post-score row empty; all score, section, affix, window, tooltip, and calculation coordinates remain unchanged. Release/Win32 passes with candidate SHA256 `1A09C8E45F249981F92F00F4CF0D3DCA21167AA59362B8E699CAD20629E7CF85`. Deployment/live capture are deferred because a newly started game PID `25820` owns the preceding installed DLL.

## 2026-08-03 - PVF-backed Lua combat-power V4 and complete 33-slot projection

- Added a runtime-PVF combat-power catalog and an exact DLL-private class0/op413 type-1/version-2 projection. It keeps white additional damage, ordinary yellow damage, ordinary critical damage, yellow additional, critical additional, and all-attack rate in separate 0.1% fields; ordinary conflict categories take their greatest equipped value, while additive categories and independently active set thresholds accumulate. Unknown or malformed PVF items fail the projection closed instead of inventing a score.
- Expanded the authoritative worn scan to the current EXE actor slots `0..32`. Ordinary equipment, avatars, aura/skin/name-tag/weapon-avatar, the equipped creature, guild equipment, and the red/blue/green pet artifacts all participate. Pet artifacts are read from `PetRecord.Artifacts`; a legacy `EquippedKey` pet is used only when no authoritative equipment record exists. Expired rows contribute nothing, and successful item/pet/artifact post-actions refresh the private projection after the durable commit and ACK.
- Extended the append-only native/Lua ABI with `DNF90DamageAffixSnapshot`, `Query90CNDamageAffixSnapshot`, six PVF percentage fields, equipped-slot/set counts, and formula V4. The Lua formula applies the six damage categories as four explicit multipliers over the auditable custom base score while retaining complete-slot reinforcement/amplification breakdown. Unsupported original-TGP attack-stat weights, ignore-defense divisor, armor-mastery, rarity, and suit coefficients remain deliberately unguessed.
- Updated the sidecar to display 白字、黄字、爆伤、黄追、爆追、所有攻击力 plus occupied slots and active sets, retained the screenshot-backed seven ranks and thresholds, and synchronized the transport/Lua compatibility hashes. Both Release/Win32 DLLs, the Lua V4 offline regression, `gofmt`, scoped diff checks, package listing, every DNF module test, the complete bridge/control suites, required `go vet`, and the production server candidate pass. The candidate is `runtime/tmp/DNF90Server-combat-power-v4.exe`, SHA256 `1FE71070CA34A5547BEBCDF6E8B7F397AC29E6C7E30007086AA7818443C8545A`; offline BAT preflight and live deployment remain deferred while two client processes and server PID `27224` are active.

## 2026-08-03 - merge the complete Layered and activity-Story dungeon flow

- Merged the source backup `剧情副本Layered与活动Story通用流程完整源码备份_20260803_161356.zip` by behavior instead of overwriting the newer tree. The world-map resolver now keeps explicit Layered maps out of ordinary coordinate/ownership pools, recognizes quest-connected repeated-BOSS-coordinate Story chains in PVF declaration order, and resolves each Story stage without randomizing the ordinary base room. Five-field zero-child special-passive-object rows are parsed before the ambiguous legacy four-field grammar.
- The current-client op29 owner now implements the proved operation/payload matrix: ordinary first visit `0/1`, ordinary revisit `0/0`, first Layered or Story stage `1/1`, later refreshed stage `1/2`, and cached base restore `2/0`. Layer chains retain their original scene, actor bindings, defeated state, seed, drop RNG, and monotonic object-key allocator; a completed final layer can restore the base without reopening the run, and the client's final same-coordinate confirmation is consumed without a duplicate op29.
- Activity Story boss deaths can carry the optional op38 next-room tail and commit remaining hostiles before emitting their visual deaths, so the source-boss response remains first. Intermediate stages cannot settle early. A hostile type-8 AI boss and blocking `[dummy][boss]` remain separate authoritative death owners; neither death fabricates the other. No-reward clear-map completion sends the active op574 projection followed by the acceptable-quest op21 refresh, with retry state that never repeats a successful op574.
- Added synthetic regressions for repeated-coordinate Story stages, multi-layer cached-base restoration/final ACK consumption, Layered clear-map ownership, optional op38 coordinates, and both AI/dummy boss death orders. Real `runtime/data/dnf/Script.pvf` evidence passes for quest 3154 base-vs-layer ownership, quest 3344 maps `76327 -> 76328 -> 76329`, quest 3352 special-object child ownership, quest 3367 dual-boss gating, and quest 3425 missing-friendly-AI skip behavior. No PVF, database, client DLL, generated runtime configuration, inventory, or character state was changed.
- `go list`, every DNF module test, the complete bridge and control suites, required `go vet`, focused real-PVF quest 3151/3154/3344/3352/3367/3425 checks, `gofmt`, and scoped diff checks pass. The validated candidate is `runtime/tmp/DNF90Server.story-layered-candidate.exe`, `46095360` bytes, SHA256 `AFCA09D5397B3C9B03987DC9CC90CEC5A83279B2068C8B092D30CD8764F0783C`. Online BAT status still passes all 120 listeners on the installed server; offline configuration preflight and replacement were deliberately deferred because DNF clients `23376` and `26768` and server PID `27224` are active.

## 2026-08-03 - add equipment scoring to the Lua combat-power panel

- Added an append-only fixed equipment-snapshot ABI and a bounded, fail-closed parser for the current mode1/mode3 character-create equipment rows. It accepts at most 33 unique actor slots and caches a snapshot only after the complete variable-length row set parses successfully. Lua receives only packet-proved item ID, actor slot, upgrade level, amplification type/value, durability, and quality seed; no rarity, set effect, white/yellow/additional damage, or PVF property is inferred from an item ID.
- Bumped the standalone worker to `90CNLua/1.9.0` and added Chinese-documented `get_equipment_snapshot` and `calculate_equipment_score` interfaces. Custom formula V2 keeps the V1 base score and adds only slots `12..23` plus earring slot `25`: `200` per occupied combat slot, `upgrade_level^2 * 10`, and `amplify_value * 20` when the proved amplification type is `1..4`. Avatar/aura slots `0..11`, pets, medals, gems, and slots `26+` are explicitly excluded. The side panel now displays the equipment subtotal and `自定义公式 V2`; it remains a custom local comparison value rather than an official DNF formula.
- Release/Win32 builds completed with zero errors and only the transport's eight pre-existing C4996 warnings. The Lua V2 regression passes, including deep-copy behavior, slot filtering, avatar/pet exclusion, exact score breakdown, base-only fallback, and panel arguments. `90CN.dll` is byte-identical at Release, `client-patch/bin`, and the configured client: `502272` bytes, SHA256 `E5F7A13EC8CDBEEEA7ACA5458B20AB19418B3DF22FB32377AA5F43D49E394064`. Release and configured `90CNLua.dll` are `328192` bytes with SHA256 `F5C2827A9DFFFAA3F19A044FDBA4B6F27D12C712B34494E3472A6FC4F5C05BD3`; source, Release, and configured `init.lua` are `39731` bytes with SHA256 `3293DC0E3D188240DD764607010DF7BFFFD8B8738B52943AA8D10BBA461DCC37`. The compatibility manifest pins both DLLs, and the preceding installed unit is recoverable from `runtime/backups/client/20260803-170147-combat-power-equipment-v2-predeploy`.
- BAT-only launch accepted all five pinned client files and started formal PID `23376` with game PID `26768`. Live mode1 evidence parsed `30` equipment rows with `13` combat items, `12` upgraded items, and `12` amplified items. For character `1`, Lua produced base score `17074`, equipment score `24560`, total `41634`, and rank `黄金`; the retained screenshot `runtime/diagnostics/combat-power-equipment-v2-accepted.png` confirms the populated panel beside native personal information. The existing runtime was started after status found the server stopped; final BAT status reports DNF90Server PID `27224` and portable MySQL PID `23596` READY with all 120 listeners accepting connections. No server binary, PVF, database content, generated runtime configuration, item, or character state changed.

## 2026-08-03 - restore the Seria-luck town HUD from persisted account state

- Current `NoPack.exe`/IDA evidence closed the protocol ambiguity: the only native opcode `413` registration is class `1` and owns the title-book payload, while the current executable contains no class `0` Seria-luck/RandomboxBooster constructor or setter that can be called safely. Added an explicit append-only compatibility ABI instead: class `0`, opcode `413`, exact two-byte body `{type=0, percent=0..100}`. `90CN.dll` consumes only the exact 18-byte framed packet before the current town dispatcher; every malformed, differently sized, differently typed, or out-of-range lookalike falls through to the original EXE unchanged.
- Reconstructed the visible HUD from the current client's own `sprite_Interface2_RandomboxBooster.NPK` rather than inventing a replacement. The Chinese base (`210x35`), blue and full-orange fills (`200x11`), and exact Chinese hover explanation (`266x62`) are stored as premultiplied-BGRA DLL resources. A click-through, non-activating layered popup follows the DNF client at the native top-left coordinates, retains the empty bar at `0%`, switches to the orange fill at `100%`, and uses `HWND_TOP` so the DirectX owner cannot cover it. The main-window subclass replays a packet received before UI setup, refreshes on move/resize/show, owns hover tracking, and destroys both popup windows at the confirmed window teardown boundary.
- The bridge now reloads `seria_luck` from the authenticated account repository and projects `clamp(value, 0..8) * 100 / 8`; no luck value is synthesized. The first proved type-`1345` town-HUD-ready callback sends once, failed writes remain retryable, magic-box post-actions reload the committed value, and selection/town/reconnect lifecycle resets re-arm the one-shot gate. If the first callback arrives while the scene tail or account repository is temporarily deferred, any later normal DPROTO/heartbeat scene-ready boundary completes the same gated send; another type-`1345`, magic-box action, or relog is not required. Tests cover all clamped progress values, the exact runtime `server16` 18-byte wire image, duplicate callbacks, repository deferral recovery, failed-wire recovery, and committed metadata reload after a magic-box action.
- `Release|Win32` rebuilt successfully with zero errors and only the transport's eight pre-existing C4996 warnings. The embedded-asset production DLL is `502272` bytes with SHA256 `E5F7A13EC8CDBEEEA7ACA5458B20AB19418B3DF22FB32377AA5F43D49E394064`, which is pinned by `90cn-decode-bypass-v1`. Independent review also closed two non-blocking UI edges before deployment: background clients no longer raise the popup over another foreground application, and the existing UI timer retries a missed update message using a stable rendered-revision gate. The prepared server candidate is `45982208` bytes with SHA256 `705A2F126D87CF9E6FE9B3204979B22314A5DC60DD92EBD1115097B4A8094714`. The DLL smoke executable, focused Seria-luck regressions, full bridge/control suites, every DNF-module test, package listing, and required `go vet` all pass.
- After the user closed both clients, the preceding client/server compatibility unit was saved under `runtime/backups/client-server/20260803-1545-seria-luck-predeploy`; no loaded client was terminated or overwritten. BAT-only controlled stop, offline `check --skip-database`, strict compatibility validation, start, status, and all 120 listener probes pass. The final DLL and server hashes above are installed; DNF90Server is READY as PID `27224` and portable MySQL as PID `23596`, with fresh server stderr empty. Live connection `game-000242` reloaded committed luck value `4`, sent percent `50`, and formal game PID `26768` logged one exact private projection consumption followed by `rendered=1`. The retained screen capture `runtime/probes/seria-luck-20260803/pid-26768-live.png` visibly confirms the native-art HUD at the upper-left with its half-filled gauge. No PVF, database content, generated runtime configuration, item, currency, or character state changed.

## 2026-08-03 - add the Lua-driven combat-power side panel

- Added an append-only client ABI for querying the current character's proved base-stat snapshot and publishing a Lua-computed combat-panel state. `90CNLua/1.8.0` exposes Chinese-documented `get_character_stats`, `calculate_combat_power`, `get_combat_power`, `get_combat_power_rank`, and `set_combat_panel_enabled` interfaces. Formula V1 is explicitly custom rather than an official DNF formula: `(力量 + 体力 + 智力 + 精神) * 8 + (HP + MP) / 20`; equipment score remains visibly `待接入` until a reliable current-client equipment snapshot is proved.
- Live current-EXE inspection corrected the UI owner assumption before acceptance: owner `0xD9` is personal information and owner `0xC9` is equipment/inventory. The sidecar now follows the personal panel's root-widget coordinates every 100 ms, uses foreground z-order so it remains visible above DirectX, hides when personal information closes or inventory opens, and moves to the left only when the native panel has no right-side space. Base stats are cached from the active town-compatible class0 dispatcher and accepted only when the packet object key resolves to the actual current actor.
- Release/Win32 builds with zero errors and only the transport's eight pre-existing C4996 warnings. PE verification retains the established exports and adds `Query90CNCharacterStatSnapshot` and `Update90CNCombatPanel`. The final `90CN.dll` is byte-identical at Release, `client-patch/bin`, and the configured client: `380416` bytes, SHA256 `467BE2F0345E13CCA06B9C5F18B27E122C9C393BAB1FC69EF80B6C9476BD61E7`. Release and configured `90CNLua.dll` are `326144` bytes with SHA256 `1A2FF8E80FF484946D9F4610C5151DE86DB4971D7CC8B92FFB91964B58DDE986`; source and configured `init.lua` are `33484` bytes with SHA256 `685FE7A71C96B0DE874951012D259BD858DA14790381B0BB336F9A59E29EBBC3`. The compatibility manifest pins both DLL hashes, and the preceding installed copies are recoverable from `runtime/backups/client/20260803-150407-combat-power-sidecar-owner-fix-predeploy`.
- BAT-only launch accepted all five pinned client files and started PID `25788`. Live transport evidence reported UI owner `217`, then captured current actor key `7` with HP `15350`, MP `14250`, strength/vitality `43/43`, and intelligence/spirit `41/41`. Lua produced base score `2824` and rank `新秀`; the user's screenshot and the retained diagnostic `runtime/diagnostics/combat-power-personal-panel-live.png` confirm that the populated panel is visible immediately beside native personal information. DNF90Server PID `9488` and portable MySQL PID `2320` remained READY; no server binary, PVF, database content, generated runtime configuration, item, or character state changed.

## 2026-08-03 - correct the granted loadout from dimensional stamina to strength

- The live current-client tooltip proved the original enum assumption wrong: amplification type `1` renders `异次元体力`, while the runtime-PVF `[physical attack]` option used for `异次元力量` is type `3`. The user's screenshot was treated as authoritative current-EXE evidence; no item, level, or stat label was guessed to hide the mismatch.
- After the DNF client exited, created a fresh full backup at `runtime/backups/database/20260803-150539-fix-amplify-strength-final/dnf_local.sql`. A serializable repair found exactly the original 16 batch-tagged amplified items despite later movement: four inventory rows and twelve equipped rows. It changed only raw byte `0x13` and the five durable type aliases from `1` to `3`; packed amplification level `13`, base value `7`, quality, durability, item identity, slot, and every other field were preserved.
- Post-commit verification reports all 16 rows at raw level/type/value `13/3/7`, all 80 type aliases (`16 x 5`) equal `3`, and zero batch aliases remaining at type `1`. The scratch grant utility now uses type `3` for future dimensional-strength grants and includes a fail-closed `-repair-strength` mode that requires the exact 16-item manifest and exact pre-repair raw state before it can commit.
- No server binary, client DLL, PVF, compatibility hash, generated runtime configuration, medal, guardian gem, title, or extra-slot entitlement changed.

## 2026-08-03 - grant the requested level-90 amplified loadout and open extra equipment slots

- With the DNF client confirmed closed, backed up the complete `dnf_local` database to `runtime/backups/database/20260803-142413-level90-loadout/dnf_local.sql`, then updated the only active level-90 character (`pouut`, character `1`) in one serializable transaction. The durable support/magic-stone/earring mask is now the current-client value `0x13`.
- Granted 17 runtime-PVF-verified main-inventory items: the five-piece Super Continent set, three-piece Ecstasy set, both requested support/magic-stone/earring alternatives, Savior katana and greatsword, and the Heavenly Chosen title. The 16 non-title equipment rows carried top-quality seed `999999998`, packed upgrade level `13`, runtime epic amplification base value `7`, and the initially misidentified type `1`; the correction entry above records the later authoritative conversion to dimensional-strength type `3`.
- Granted all 13 runtime-PVF legendary `[flag]` medals at reinforcement `+16` in the proved list-38 medal page slots `0..12`, plus ten each of all 13 artifact-grade `[flag gem]` guardian gems in the proved guardian-gem page slots `49..61`. No ordinary backpack slot was used for either family.
- Added source-only scratch utilities for concurrent runtime-PVF item lookup/exact archive-text inspection and for this fail-closed grant. The grant utility verifies every ID/name/path/type/rarity/grade against the active `Script.pvf`, requires exactly one active level-90 character, locks and rejects occupied destination slots, defaults to dry-run, and commits only with `-apply`.
- The initial post-commit SQL verification reported slot mask `19`, exactly `43` granted rows (`17` main and `26` list-38), the original `16` amplified rows at level/type/value `13/1/7`, all `13` medals at `+16`, and `13` guardian-gem stacks totaling `130` units with raw count `10`. The correction entry above supersedes only that amplification type, now `13/3/7`.

## 2026-08-03 - restore equipped aura sockets and emblems during login

- The reported state was durable rather than lost: wearing the same aura again made its open sockets and embedded emblems visible without another socket transaction. The mismatch was in the cold-login projection. Inventory and wear-time item rows persist and publish the canonical `avatar_socket_data`/`avatar_color_data` pair, while the full mode1 equipment creator's current-EXE type-`0..11` branch previously read only separate `mode1_raw_a_hex`/`mode1_raw_b_hex` aliases. An equipped aura in actor slot `9` was therefore created without either extension block until native op19 wear processing rebuilt it.
- Mode1 equipment projection now feeds those same canonical socket/color blobs into the two proved type-`0..11` extension readers when no explicit captured mode1 block exists. Explicit mode1 evidence retains precedence, ordinary equipment types `12+` remain byte-for-byte unchanged, and legacy universal socket words are normalized through the existing current-EXE `0xFFEF` compatibility rule. No database/PVF field, login op14, DLL hook, item mutation, or acquisition notification was added.
- Added a complete mode1 regression for equipped aura item `101590068` in actor slot `9`; it proves the outgoing create row carries the normalized 30-byte open/embed block, both embedded emblem IDs, and the second color block. Focused and complete bridge tests, every DNF-module test, control tests, package listing, required vet, `gofmt`, scoped diff checks, BAT-only offline preflight, restart, online preflight, and all 120 listener probes pass. After DNF clients `1712`/`6472` exited normally, the prepared `45970944`-byte candidate was installed as `runtime/bin/DNF90Server.exe` with SHA256 `8F5DBBC0F889945F69E7F621CBB48032801D572D091007A7F8E0C649D520B7F7`; the previous server and instance configuration are recoverable from `runtime/backups/server/20260803-aura-login-socket-predeploy`. DNF90Server is READY as PID `9488` and portable MySQL is READY as PID `2320`. No client DLL, PVF, database content, or compatibility hash was changed.

## 2026-08-03 - add the Lua dungeon timer, room count, and BOSS-room prompt

- Expanded `DNF90ClientEvent` append-only from its stable 20-byte prefix to a 60-byte record carrying only packet-proved dungeon context: flags, dungeon ID, current room coordinates/layer/map, and the previous room for a change event. The typed op28 owner now retains its nonzero dungeon ID and explicit BOSS coordinates; complete op29 parsing supplies current/previous room fields, and `BOSS_ROOM` is set only when the current coordinates equal that same run's op28 coordinates. No map ID is hard-coded and no last-room or actor-order guess is used. `Dequeue90CNClientEvent` returns the 20-byte prefix to an old consumer and all 60 bytes to the current one; `90CNLua` accepts either size, so old/new transport-worker combinations remain bidirectionally compatible without invented zero context becoming valid data.
- Bumped the worker to `90CNLua/1.7.0` and added the Chinese-documented event fields `dungeon_id`, `room_x`, `room_y`, `room_layer_flag`, `map_id`, `is_boss_room`, plus previous-room fields where valid. The starter now provides a real dungeon tracker: entering starts its monotonic clock and counts the initial room, every deduplicated change increments the visit count, the first BOSS-coordinate room shows current time, and leaving shows total time and rooms before the following town event. `dnf90.set_dungeon_tracker_enabled(enabled)` controls only visible notices, while `dnf90.get_dungeon_run()` always returns a copied live/recent snapshot. Hot reload keeps only scalar active-run state and registers fresh functions; it does not retain stale callbacks or create a periodic demo timer.
- Release/Win32 rebuilds pass with zero Lua warnings and only the transport's eight pre-existing C4996 warnings. Lupa/Lua offline regression proves exact entry/BOSS/exit text and elapsed times, disabled-notice tracking, active-run hot reload, one listener set after reload, and legacy four-argument dispatch. The previously captured dungeon `6` op28/op29 stream parses as six complete rooms, BOSS `5:0`, with exactly its final `5:0`/map `76146` classified as BOSS. PE inspection retains exactly `A`, `Queue90CNClientNotice`, and `Dequeue90CNClientEvent` on the transport plus `Install90CNLua` and `Request90CNLuaReload` on the worker; the worker still imports only `KERNEL32.dll`.
- The final combined transport also contains the separately recorded equipped-pet enchant relog/swap compatibility and is byte-identical at Release, `client-patch/bin`, and the configured client: `369664` bytes, SHA256 `9632690CFB3A8720E80A2C4E81217142E24015EF7B3F20BEF52BC5DB85BF2A6C`. Release and configured `90CNLua.dll` are `323072` bytes with SHA256 `A56816E38C2E3BBD25EC8AA500BFE66A383641912A3AFD6E7EEB902AF22530C2`. Source, Release, and configured scripts are byte-identical at `25083` bytes with SHA256 `2E974F5C8F1F21E39BF1E8AE87BBFAFF2A746186D36FA6CFE926E50E4A30CE7D`; `90cn-decode-bypass-v1` pins both DLL hashes. The preceding configured unit and manifest are recoverable from `runtime/backups/client/20260803-132300-lua-dungeon-tracker-predeploy`.
- BAT-only launch accepted all five pinned client files. Formal PID `1712`, DNF UI thread `26224`, and Lua worker `26376` emitted `enter_dungeon` sequence `6` for dungeon `9`, room `0:1`, map `76151`; five distinct room events `7..11`; BOSS context `0xF` only at `3:1`, map `76156`; `leave_dungeon` `12` with retained context `0xB`; and context-free `enter_town` `13`. The entry, BOSS, and exit notices were each queued exactly once; elapsed wall time was about `01:53` across six visited rooms. Server interval `779987..780679` independently records connection `game-000478`, channel `251`, character `7`, exactly one op28 with BOSS `3:1`, one initial room, five identical committed moves, and one completed town send/commit. The second formal PID `6472` remained in town and was not modified or terminated. Evidence is retained under `runtime/probes/lua-dungeon-tracker-20260803`; the read-only DBWIN listener was stopped after capture. Final BAT status reports DNF90Server PID `26356` and portable MySQL PID `28228` READY with all 120 listeners accepting connections; this client-only deployment did not restart either service.

## 2026-08-03 - restore unlocked aura animation-skin placement

- Live current-client op19 evidence isolated the reported no-op to the server placement gate. The ordinary aura swap into worn slot `9` succeeded, then the same avatar panel sent the replaced `[aurora avatar]` from list `1` slot `21` to worn slot `11`; the bridge rejected it because the generic PVF mapping still required every aura to target slot `9`. The client had already restored `aura_flag=1`, so this was neither a locked-slot/DLL problem nor missing persistence.
- Equipment placement now retains slot `9` as the ordinary stat-bearing aura target and admits `[aurora avatar]` in slot `11` only when the selected character has the durable op863 `aura_flag` entitlement. Locked characters still fail closed, all other avatar categories retain their exact target, and the existing subtype0/mode0 appearance projection continues to publish the persisted slot-`11` item as the replacement animation.
- Added open/closed-slot placement regressions and pinned runtime item `101590023` against the real `Script.pvf` as valid for both aura targets `9` and `11`. `gofmt`, package listing, every DNF-module test, the complete bridge/control suites, required `go vet`, real-PVF smoke, production build, BAT-only offline/online preflight, and all 120 listener probes pass. The deployed `45969920`-byte `DNF90Server.exe` has SHA256 `B70F6400156B6AF6F7BD4F3EB29E2887CA566C9E7BCAF60ABEB053DFAE65DB66`; the previous server, first candidate, and instance configuration are recoverable from `runtime/backups/server/20260803-aura-skin-animation-slot-predeploy`. DNF90Server is READY as PID `26356` and portable MySQL is READY as PID `28228`. No client DLL, compatibility manifest, PVF, or database data was changed.

## 2026-08-03 - restore equipped pet enchants after relog and pet swaps

- Proved the reported missing enchant was a client-object refresh defect rather than data loss. Character `1` still had equipped creature item `400990168`, serial `41`, and enchant card `10008705`; the alternate creature also retained its own card. Immediate enchanting already used class0/op14 and the existing list-7 dynamic-state hook, while cold login created the equipped creature through full mode1 and pet swaps refreshed list `7`/creature state without ever replaying the equipped list-3 object. The server now emits exactly one class0/op14 row for list `3`, slot `26`, after full mode1 and after a successful slot-26 pet move, before op105/op102. Generic list-3 equipment refresh remains suppressed.
- Extended the signature-guarded current-EXE hook at RVA `0x019739B9` only from list `7` to either list `7` or the exact list `3`/slot `26` pair. Binary disassembly proves both guards, the native vtable `+0x158` dynamic-state setter, displaced `movsx`, and indirect native resume; no other equipment slot, object replacement, database field, PVF value, export, or import was added. The final `367616`-byte DLL is byte-identical at Release, `client-patch/bin`, and the configured client with SHA256 `3080BBC05C627DF4BC2E027B63BCF4B34427D28F61F4EF05868420AA008EEEAD`; `90cn-decode-bypass-v1` pins the same hash.
- Live acceptance exposed and closed a second legacy-row hazard before completion. Copying the old 47-byte equipped-creature row as a 119-byte op14 base made the card byte `0x98` look like upgrade count `152` and the repeated serial at legacy `+24` look like about 32 days of remaining time. The dedicated slot-26 refresh row is now built from zero and maps only proved fields: slot, item, serial, flags/durability, card, upgrade/amplify block, and authoritative expiration. Permanent pets explicitly carry remaining seconds `0` and expiration `0`; no legacy padding can enter either time field. Regression rows reproduce card bytes at legacy `+16`, zero upgrade at `+20`, and serial `41` at `+24`, then prove the outgoing row keeps card `10008705` while both time fields stay zero.
- `gofmt`, package listing, every DNF-module test, the complete bridge/control suites, required `go vet`, scoped diff checks, Release/Win32 DLL rebuild, production server build, BAT-only offline/online preflight, and all 120 listener probes pass. The deployed `45969408`-byte `DNF90Server.exe` has SHA256 `ECAFA9F48428114DF87B8DEB00C9034A55BC89D3B303FD4EC7944840CEE84899`. Live connection `game-000123` sent one 122-byte list-3/slot-26 op14 row for item `400990168`, serial `41`, card `10008705`, upgrade `0`, remaining seconds `0`, and expiration `0`; the user confirmed the enchant is visible and the false remaining-time line is gone. The preceding client/server compatibility unit and intermediate server are recoverable from `runtime/backups/client-server/20260803-pet-enchant-relogin-predeploy` and `runtime/backups/server/20260803-pet-enchant-sanitized-row-predeploy`.

## 2026-08-03 - complete the Lua dungeon room and exit lifecycle

- Extended the fixed client-event ABI with `dungeon_room_changed` type `5` and `leave_dungeon` type `6` without changing `DNF90ClientEvent`. The class0/op29 parser now reuses the strictly validated current-EXE counted layout after dungeon activation, tracks the proved room identity `(x, y, layer flag, map ID)`, suppresses identical repeats, and publishes one room event only when that complete identity changes. An accepted class0/op24 town transition from the active dungeon stage publishes `leave_dungeon`, clears all dungeon/room state, and then publishes the existing `enter_town`, preserving deterministic exit-before-town ordering. No room, dungeon, character, packet, PVF, database, or persistence field was added to the public event record or guessed.
- Bumped the standalone worker to `90CNLua/1.6.0`, mapped both native event types, admitted both names through `dnf90.on`/`dnf90.once`, and added the Chinese-commented `dnf90.on_dungeon_room_changed(event)` and `dnf90.on_leave_dungeon(event)` interfaces. Documentation now covers all six lifecycle names and the same-packet `leave_dungeon` then `enter_town` rule. Offline Lua validation proves generic/listener/once/specific ordering, two consecutive room events, exit-before-town ordering, invalid-name diagnostics, reload cleanup with monotonic listener IDs, and zero automatic notices; the six previously captured op29 bodies also pass the exact production parser as one initial room plus five distinct moves with no duplicate.
- Release/Win32 builds pass with zero Lua warnings and only the transport's eight pre-existing C4996 warnings. The final combined transport, including the separately recorded pet-enchant display compatibility, is byte-identical at Release, `client-patch/bin`, and the configured client: `367616` bytes, SHA256 `55950D0153D9B3BA91EB0C91B1295490FEB5CCC4995C3E115CF39820B4576CFE`. `90CNLua.dll` is byte-identical at Release and the configured client: `323072` bytes, SHA256 `A9A73E99E61E35350C309736A2D64372C2173B28B2EDABE134BB8C5DF4607479`. Source, Release, and configured scripts are byte-identical at `16615` bytes with SHA256 `9C07AD22E1895DA08B53696F4C4378847E0033200AED6B5FFF8343D6B92D9CA1`; `90cn-decode-bypass-v1` pins both final DLLs. The main DLL retains exactly its three established exports, while the Lua DLL retains two exports and imports only `KERNEL32.dll`. The preceding installed unit and manifest are recoverable from `runtime/backups/client/20260803-122900-lua-dungeon-lifecycle-predeploy`.
- BAT-only strict launch accepted all five client files. In formal PID `1244`, DNF UI thread `27868` and Lua worker `17964` produced the exact sequence `enter_dungeon` `6`, five `dungeon_room_changed` events `7..11`, `leave_dungeon` `12`, and `enter_town` `13`; the exit and town callbacks were one millisecond apart and retained the required order. The bounded server-log interval `765124..765984` independently records one dungeon start in room `0:0` map `76141`, five committed moves through maps `76142..76146`, and one completed return-to-town send/commit for connection `game-000240`, channel `251`, character `7`, dungeon `6`. Evidence is retained under `runtime/probes/lua-dungeon-lifecycle-20260803`; the read-only DBWIN listener was stopped after capture. A separately launched formal PID `10000` remained idle and was not modified or terminated. Final BAT status reports DNF90Server PID `28048` and portable MySQL PID `5192` READY with all 120 listeners accepting connections; this client-only change did not restart either service.

## 2026-08-03 - restore Dream Platinum selection and personal-cargo stack counts

- Traced the Dream Platinum Emblem Box `490701743` op160 failure to the runtime-PVF selection parser, not to backpack capacity. The real job/grow catalog contains legal empty placeholders for unavailable fifth advancements; the parser rejected the entire box when Go map iteration encountered any empty category and returned client error `4` (displayed as inventory space insufficient) even though selected category `1` validly contains emblem `10095199`. Empty unselected placeholders are now retained, while an explicit forged selection of one still fails closed. A synthetic regression and a real `Script.pvf` smoke both pass.
- Proved that the reported Abyss Party Invitation `3330` warehouse move did not lose data: the authoritative row had already merged `20470 + 5850` to `26320`. Its 119-byte raw row still encoded `20470` and both legacy amount aliases still encoded `17970`, so the list-2 refresh redrew an old count. Every move/merge/split now synchronizes the authoritative count into the raw `+0x06` dword and any existing amount aliases. Personal-cargo projection also prefers `ItemStack.Count` for PVF-proven stackables, which immediately makes this historical row display `26320`; its next ordinary mutation persists all mirrors in canonical form.
- Formatting, package listing, every DNF-module test, the complete bridge/control suites, required `go vet`, scoped `git diff --check`, real-PVF selection validation, production build, controlled BAT restart, and online preflight all pass. MySQL PID `5192` and its data remained running throughout. The installed `45955072`-byte `DNF90Server.exe` has SHA256 `BD5AB71EB5A0039F70604A46469A47F51744B6D68B2239DE8E4D5B15C1373E91`; the preceding image and instance configuration are recoverable from `runtime/backups/server/20260803-dream-platinum-warehouse-stack-predeploy`. DNF90Server is READY as PID `28048`, all 120 listeners accept connections, fresh stderr is empty, and the BAT launcher started the accepted client process as PID `1244` (game connection PID `10000`). No PVF, client DLL, compatibility hash, or reward item was changed.

## 2026-08-03 - add the proved Lua dungeon-entry lifecycle event

- Extended the fixed client-event ABI with `enter_dungeon` type `4` without changing `DNF90ClientEvent`. The production class0 wrapper now arms only on a typed op28 body with a nonzero dungeon ID, strictly parses the complete counted structure of the first initial-state op29 body, and confirms only the exact four-byte op3 state `1 / current object key / state 1` after native dispatch when that key resolves to the current actor. A thread-local idle/await-op29/await-op3/active gate makes the event one-shot; an accepted op24 town transition resets it. No dungeon, map, room, character, PVF, database, or persistence field is exposed or guessed.
- Bumped the standalone worker to `90CNLua/1.5.0`, mapped type `4` to `enter_dungeon`, added the Chinese-commented `dnf90.on_enter_dungeon(event)` callback, and admitted the name through `dnf90.on`/`dnf90.once`. The source and README document every public name and the worker/UI-thread boundary. Offline Lua validation proves generic/listener/once/specific ordering, event metadata, invalid-name rejection, hot-reload listener removal with monotonic IDs, and zero automatic notices. Release/Win32 builds pass with zero Lua warnings and only the transport's eight pre-existing C4996 warnings; the main DLL retains exactly its three established exports, while the Lua DLL retains two exports and only `KERNEL32.dll` as an import.
- The final combined transport also contains the separately recorded pet-enchant display compatibility and is byte-identical at Release, `client-patch/bin`, and the configured client: `367104` bytes, SHA256 `A4E74CCC1789DEB1A4CF223D575B51BD45FDA005DF563F459C47343D6677D422`. `90CNLua.dll` is `322560` bytes with SHA256 `D3B795A5D4F7773A4F80FD62F083902D038B384C870915788677CC0F66BAEF73`. Source, Release, and configured scripts are byte-identical at `15181` bytes with SHA256 `6ACD7F14CDC99023CAB582F6695EB71B0A99788B7A9C41BA32164635DE3B7D43`; the `90cn-decode-bypass-v1` manifest pins both final DLLs. The preceding configured unit and manifest are recoverable from `runtime/backups/client/20260803-120400-lua-enter-dungeon-predeploy`.
- BAT-only strict launch accepted all five client files and started formal PID `22000`. On connection `game-000962`, the server emitted exactly one 36-byte op28 for dungeon `3`, one 44-byte initial op29 for map `76131`, and one four-byte op3 for current object key `7`. DNF UI thread `26536` queued type `4` sequence `5`; Lua worker `9576` dispatched `enter_dungeon` exactly once. Five later room-change op29 packets produced zero repeats. The completed route then emitted one typed op24; the DLL reset active dungeon stage `3`, queued `enter_town` sequence `6`, and Lua dispatched it once. Evidence is retained under `runtime/probes/lua-enter-dungeon-20260803`; the read-only DBWIN listener was stopped after capture. DNF90Server PID `13648` and portable MySQL PID `5192` remain READY with all 120 listeners accepting connections, and the accepted client remains running.

## 2026-08-03 - refresh repeated pet-enchant state in the existing client object

- Proved that the repeated pet enchant is already durable rather than lost: character `1` pet serial `41` and inventory list `7` slot `39` both persist card `10008705`, and the canonical 47-byte row contains that card at `+0x0E` with upgrade byte at `+0x12`. Cold-login and incremental writers use the same layout as 86JP, so no database, PVF, packet-offset, or server transaction change was made.
- Current-EXE analysis isolated the stale display to `sub_1D73120`: class0/op14 parses the complete incoming row, but the same-template fast path updates only serial/count and skips the native vtable `+0x158` dynamic-state setter used by its replacement-object path. Added one signature-guarded production `90CN.dll` hook at RVA `0x019739B9` that invokes that existing setter only for list `7`, preserves the pet pointer, restores the displaced instruction, and then resumes native slot refresh. This covers repeated replacement and clearing without deleting/recreating the pet or emitting acquisition behavior.
- Release/Win32 builds with zero errors and the eight pre-existing C4996 warnings. Binary disassembly proves the exact list-7 guard, native `+0x158` call, displaced `movsx`, and indirect resume; the DLL retains only its three established exports and adds no imports. Package listing, all DNF-module tests, the complete bridge/control suites, required `go vet`, and server reproducibility pass; the generated server is byte-identical to the running image at SHA256 `20EAB79B8A6859BF28251A0BFFA00233EC4395286ACD54546FDA29916F7DCECB`. After the configured DNF client exited normally, the final combined `367104`-byte DLL was installed byte-identically at Release, canonical package, analysis, and configured-game paths with SHA256 `A4E74CCC1789DEB1A4CF223D575B51BD45FDA005DF563F459C47343D6677D422`; `90cn-decode-bypass-v1` pins that same hash. The preceding installed copies and compatibility manifest are recoverable from `runtime/backups/client/20260803-120900-pet-enchant-display-predeploy`. BAT-only launch accepted all five compatibility files and started the formal client as PID `22000`; the transport trace installed the pet hook immediately at `0x01D739B9` with the exact list-7 scope and result `1`. DNF90Server PID `13648` and portable MySQL PID `5192` remain READY with all 120 listeners accepting connections; the server was not restarted.

## 2026-08-03 - add the first proved Lua game-lifecycle event

- Extended the fixed `90CN.dll` client-event ABI with `enter_town` type `3` without changing the record layout. The production town co-presence wrapper now scopes the two already-audited current-EXE op24 gates to each typed class0/op24 dispatch, records any native early return across all gate calls, and publishes the event only after the bounded final roster row resolves to the exact current actor. This is one event per accepted final town transition packet, including login entry, town movement, and dungeon return; rejected or malformed packets publish nothing, and no character name, channel, map, scene, PVF, database, or persistence value is guessed.
- Bumped the standalone worker to `90CNLua/1.4.0`, mapped the native event to `enter_town`, added the Chinese-commented `dnf90.on_enter_town(event)` callback, and admitted the name through the reload-safe `dnf90.on`/`dnf90.once` listener API. Dispatch remains generic callback, registered listeners in order, then the direct specific callback on the owned Lua thread. The starter logs the event but never shows an automatic notice. Offline Lua validation proves ordering, one-shot removal, listener-error isolation, invalid-name rejection, hot-reload cleanup with monotonic listener IDs, and zero notices.
- Release/Win32 rebuilds pass: the transport build retains only its eight pre-existing deprecated CRT warnings and `90CNLua.dll` has zero warnings. The final combined transport DLL also contains the separately recorded creature-rename null guard; it is `364544` bytes with SHA256 `76C52ED336D1FB4C60632E3DB1DE9EBF33A162286F994EC91A9C259B5F4F67F1` and still exports only `A`, `Queue90CNClientNotice`, and `Dequeue90CNClientEvent`. The Lua DLL is `322560` bytes with SHA256 `1A2FF29035857B07D2979680A748AD94F3654B4CC113F9CC07192B2BD37E19F5`, imports only `KERNEL32.dll`, and retains exactly its two public exports. Source, Release, and configured-client scripts are byte-identical at `14469` bytes with SHA256 `3BF96F9EB23BB54D13718E06F300009FEE915F77D68FEAC9C017D16C4AAA97A9`; the compatibility manifest pins the final combined transport and Lua DLL hashes. The pre-lifecycle unit is recoverable from `runtime/backups/client/20260803-113215-lua-enter-town-predeploy`.
- BAT-only launch validation accepted all five client files and started formal PID `27852` on that final combined DLL. Its DNF UI thread `28564` evaluated the op24 scene/loading gates without rejection, accepted a 28-byte one-row final transition whose selected key `7` resolved to the current actor, and queued type `3` as sequence `2`. `90CNLua` loaded the final script on owned state `0x09352670`; worker `18320` dispatched `enter_town` exactly once with the same producer UI thread, while an eight-second duplicate check remained at one native confirmation and one Lua callback. Evidence is retained under `runtime/probes/lua-enter-town-20260803`; the read-only DBWIN listener was stopped after capture. DNF90Server PID `13648` and portable MySQL PID `5192` remain READY with all 120 listeners accepting connections; unrelated DOVE client PID `18100` was not modified, and the accepted formal client remains running.

## 2026-08-03 - restore the production creature-rename null guard

- Live packet and exception evidence isolated the reported rename crash. Class1/op100 committed the GB18030 name and consumed exactly one rename card, class0/op101 then reached current `NoPack.exe` VA `0x034DE96E` and faulted while reading `this=0x14`; this is the already-proved optional op105-map-copy null dereference, not a bad creature serial, failed database transaction, or malformed rename request. The durable pet name and remaining card count were preserved despite the client exit.
- The narrow `ProxyCreatureRenameMapNullCheck` implementation and verified RVA/signature still existed, but the newer production-only transport installer no longer installed it after the historical all-in-one installer was retired. Added a dedicated production `InstallCreatureRenameCompatibility`: it waits for exact bytes at RVA `0x0198515F`, resumes the unchanged native map copy when the lookup succeeds, and skips only that final optional copy when the lookup returns null. The two earlier native actor/live-creature name updates and all server-owned rename persistence remain unchanged.
- Release/Win32 rebuilt with only the eight pre-existing deprecated-CRT warnings and zero errors. The complete DNF-module and `dnfbridge` suites, control tests, package listing, required `go vet`, the current production server reproducibility build, and scoped `git diff --check` pass. The running server image is byte-identical to current source at SHA256 `20EAB79B8A6859BF28251A0BFFA00233EC4395286ACD54546FDA29916F7DCECB`; it was not restarted. BAT offline check correctly refused while the managed server was active, and BAT status then passed with DNF90Server PID `13648`, portable MySQL PID `5192`, and all 120 listeners accepting connections.
- Deployed the new `364544`-byte `90CN.dll` to the canonical and configured-client paths with SHA256 `76C52ED336D1FB4C60632E3DB1DE9EBF33A162286F994EC91A9C259B5F4F67F1`, and advanced the `90cn-decode-bypass-v1` compatibility manifest to the same hash. The configured client was not running; the unrelated DOVE client was not touched. The preceding canonical/installed DLLs and manifest are recoverable from `runtime/backups/client/20260803-113500-pet-rename-nullguard-predeploy`.

## 2026-08-03 - add reload-safe Lua event listener registration

- Added the pure-Lua `dnf90.on(event_name, callback)`, `dnf90.once(event_name, callback)`, and `dnf90.off(listener_id)` interfaces on top of the existing `90CNLua/1.3.0` native lifecycle queue. The first revision accepts only the proved `ui_ready` and `ui_closed` names, assigns process-local positive listener IDs, and preserves the existing direct `dnf90.on_event`, `dnf90.on_ui_ready`, and `dnf90.on_ui_closed` callbacks. Dispatch order is generic callback, registered listeners in registration order, then the direct specific callback.
- Listener dispatch uses a stable snapshot: callbacks registered while an event is running begin with the next event, cancellation can suppress a later listener in the current snapshot, and `once` removes itself before invocation so callback re-entry cannot run it twice. Each listener has an independent protected call, so one error cannot stop remaining listeners or the direct callback. Hot reload replaces the listener table and therefore discards every old function while retaining a monotonic next ID, preventing a stale ID from cancelling a newly registered listener.
- Offline Lua validation proves registration order, one-shot removal, double-cancel results, listener-error isolation, deferred registration, invalid-name rejection, hot-reload cleanup, monotonic IDs `1..8`, zero default timers, and zero automatic notices. Source, Release, and configured-client `init.lua` copies are byte-identical at `13836` bytes with SHA256 `57015E7F3C1FD67E7F4E3696667726D168BD544109F9748ABCD6EC422EBA70D0`. The DLL binaries and compatibility manifest are unchanged; the preceding script is recoverable from `runtime/backups/client/20260803-111038-lua-listeners-predeploy`, and no DNF client was running during deployment. An initial BAT launch correctly refused while DNF90Server and MySQL were externally stopped; after that external workflow restored them as PIDs `13648/5192`, all 120 listeners passed preflight. Formal client PID `4252` then loaded the final script in the current EXE's Lua 5.1.5 state `0x093A2890` on worker `10096` and completed `ui_ready` sequence `1` from DNF UI thread `28596`; it remains running.

## 2026-08-03 - restore the missing spend-time activity HUD catalog row

- Corrected the earlier active-only op108 conclusion with current `NoPack.exe` IDA and live-memory evidence. The client had already consumed event 2347 parameters and op1206 progress (`10800` seconds, four rewards, a constructed fixed UI), but its base-event table remained empty; `sub_2041F70(2347)` therefore returned false and the one-time `sub_F3EDF0` HUD gate kept the activity entry hidden. The DLL route was already correct. Release, canonical `client-patch/bin`, and configured-client `90CN.dll` copies are now byte-identical at `361472` bytes with the compatibility-manifest SHA256 `B3EDBC0540AE618FBD5844EC4515C2AD52E918F9A14507CED8C8D5DEBB21C4BD`.
- The protected op108 descriptor now begins with one 15-byte base-catalog row for event 2347: the value `1` observed on every row in the real 1798-byte legacy op108 fixture, two parser-safe empty DSTR fields, and `has_extended=0`. It then carries the existing active row and 12 parameters, for an exact 68-byte plain body. The client reads a retired-count field only when the installed base table contains special key 9; this one-event catalog does not, so no retired-count byte is emitted. This replaces the incomplete 53/54-byte active-only interpretation without inventing event rewards, inventory state, or client resources.
- Exact-field builder coverage, protected-zlib decoding, partial-write ownership, the complete bridge and DNF-module suites, control tests, `go list`, required `go vet`, production build, `git diff --check`, and BAT-only offline/online preflight pass. A fresh client connection produced a 50-byte compressed op108 whose decoded body is exactly 68 bytes and reads `base_count=1, base_event=2347, value=1, empty title/description, has_extended=0, active_count=1, active_event=2347`. Deployed `DNF90Server.exe` is `45954048` bytes with SHA256 `20EAB79B8A6859BF28251A0BFFA00233EC4395286ACD54546FDA29916F7DCECB`; the preceding server and instance configuration are recoverable from `runtime/backups/server/20260803-1110-spend-time-base-catalog-predeploy`. DNF90Server PID `13648` and portable MySQL PID `5192` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty.

## 2026-08-03 - add safe Lua client-window lifecycle events

- Added a separate 32-entry lifecycle-event queue to `90CN.dll` and the undecorated `Dequeue90CNClientEvent` export. Each fixed ABI record carries its structure size, `ui_ready` or `ui_closed` type, process-local sequence, client process ID, and proved DNF UI thread ID. Main-window installation publishes `ui_ready` only after the existing window procedure and notice bridge are ready; `WM_DESTROY` publishes `ui_closed`, with `WM_NCDESTROY` as an idempotent fallback and bridge publication cleared at final destruction. A cancellable `WM_CLOSE` request is deliberately not treated as a closed window.
- `90CNLua/1.3.0` polls that queue every 250 ms only after a successfully loaded script exposes the internal dispatcher. It never executes Lua on the DNF window thread. Every valid event is converted to a Lua table containing `name`, `sequence`, `process_id`, and `ui_thread_id`, then invokes `dnf90.on_event(event)` followed by `dnf90.on_ui_ready(event)` or `dnf90.on_ui_closed(event)` on the owned worker; protected calls isolate callback errors. `ui_closed` remains documented as best-effort when a process is terminated abruptly. The first event set deliberately excludes character, channel, map, scene, packet, PVF, database, and persistence guesses.
- Release/Win32 rebuilds pass: `90CNLua.dll` has zero warnings and `90CN.dll` retains only its eight pre-existing deprecated CRT warnings. Offline Lua validation proves zero default timers/notices, generic-before-specific ordering, exact event metadata, unknown-event rejection, and callback-error isolation. Formal client PID `13264` loaded the `9957`-byte script on state `0x09492670` and Lua worker `19188`; DNF UI thread `28080` produced `ui_ready` sequence `1` and later `ui_closed` sequence `2`, and both callbacks completed on worker `19188`. The client then exited normally.
- The deployed `90CN.dll` is `361472` bytes with SHA256 `B3EDBC0540AE618FBD5844EC4515C2AD52E918F9A14507CED8C8D5DEBB21C4BD` and exports `A`, `Queue90CNClientNotice`, and `Dequeue90CNClientEvent`. The deployed `90CNLua.dll` is `322560` bytes with SHA256 `12633FE3DAFB9675654AEEBB385E7C22D9BAFB864728D01118DB4484C41F07C6`, imports only `KERNEL32.dll`, and retains exactly `Install90CNLua` and `Request90CNLuaReload`. Source, Release, and configured-client `init.lua` copies use SHA256 `0C3DAB30109CAD702744FD8262D8C8EDD277528B5D87B212165957DEEB22554E`; the compatibility manifest pins both DLL hashes. The preceding DLL/script/manifest unit is recoverable from `runtime/backups/client/20260803-105300-lua-ui-events-predeploy`.

## 2026-08-03 - retire the automatic Lua notice demonstration

- Removed the starter script's five-second demonstration timer and automatic startup notice. `lua\init.lua` now creates no business timer and never calls `dnf90.notify` by itself; the Chinese comments and README direct business scripts to call `dnf90.notify("提示内容")` explicitly and decide whether to retry a `false` result. The notification queue, main-thread renderer, timer API, hot reload, and DLL binaries are unchanged.
- Offline Lua validation proves load counts `1/2`, zero default timers, zero automatic notices after timer dispatch attempts, and a successful explicit notice call. Source, Release, and configured-client script copies are byte-identical at `7413` bytes with SHA256 `24BC91E47F683BE9B0F4FEAC6081D005B5ED3AD24BE02FD913532FFEE5AA4421`. Formal client PID `9820` hot-reloaded that script as load count `3` on state `0x09421D00` and worker thread `25416`; the following observation window contained no demonstration timer or notice output. The preceding demonstration script is recoverable from `runtime/backups/client/20260803-101500-lua-notify-demo-removal`. No DLL, compatibility hash, PVF, database, or persistent game state changed.

## 2026-08-03 - add a safe Lua-to-main-thread client notice interface

- Added the public `dnf90.notify(text)` interface to `90CNLua/1.2.0`. The isolated Lua worker converts UTF-8 to UTF-16 and calls the undecorated `Queue90CNClientNotice` export in `90CN.dll`; a fixed 32-entry queue accepts at most 511 UTF-16 characters per notice and carries no caller-owned pointer across threads. The existing DNF main-window procedure drains the queue and performs every USER32/GDI operation on the current client's UI thread. All public interfaces, parameters, return values, limits, and thread constraints retain Chinese source comments and README documentation.
- Current-EXE evidence was used to reject two unsafe or ineffective display paths. The verified native candidate at RVA `0x01E6F9C0` accepted its established callers, but downstream `sub_34D1950` hashes the supplied text into a registered sound/resource table and live testing did not render arbitrary text; that address was therefore not exposed as a Lua UI API. A layered child window also failed live creation. The accepted renderer is instead a DNF-owned, non-topmost, click-through `WS_POPUP` with a dark panel, gold border, centered Chinese text, and a four-second lifetime; bridge publication between the loader/UI thread and Lua worker uses interlocked reads and writes.
- The UTF-8 starter script demonstrates three five-second timer callbacks so the notice appears after the client's startup black-screen phase. It retries if the bridge is not ready, does not mutate gameplay, inventory, PVF, database, or persistent state, and remains hot-reloadable. Offline Lua validation proves load counts `1/2`, old-timer replacement, exact 5000 ms scheduling, three ticks, and `false`/`true` notice retry. Formal client PID `3212` hot-reloaded the `7959`-byte script on owned Lua state `0x08F42270` and worker thread `2052`; `90CN.dll` logged the queue on that worker and dispatched it on DNF UI thread `28444` with renderer `owned-popup-overlay`. The standalone rendered-notice proof is retained at `runtime/probes/lua-notify-visual-20260803-0932/notify-final-5s-overlay.png`; an accidental whole-desktop capture was discarded immediately.
- Release/Win32 builds completed successfully before deployment. `90CN.dll` is `360448` bytes with SHA256 `1102C4B86982E5714CCA0EEF0F4033DE2477CFE105008E2E12B58CBCB6EDDD51` and exports `A` plus `Queue90CNClientNotice`; `90CNLua.dll` is `321024` bytes with SHA256 `496B2E5A093939E4060C63A4E7734089A0F57F266393EFCE6F52B19F1891883A`, imports only `KERNEL32.dll`, and exports exactly `Install90CNLua` and `Request90CNLuaReload`. Source, Release, and configured-client copies of `init.lua` are byte-identical with SHA256 `5FBDCEAF70761147024D69F6921A0E2BB739873D7BFB63640E918DD358A5588B`; the compatibility manifest pins both DLL hashes. The preceding formal client unit is recoverable from `runtime/backups/client/20260803-093000-lua-notify-predeploy`. BAT status validation reports DNF90Server PID `19036`, portable MySQL PID `26748`, and all 120 listeners ready; formal client PID `3212` remains running.

## 2026-08-03 - add the first safe owned-Lua timer and client-info interfaces

- Extended the owned Lua worker without adding a game hook or an unproved current-EXE address. Two private native helpers expose only `GetTickCount64` and reliable process/plugin metadata through the already-verified Lua push-string path; the UTF-8 bootstrap builds the public `dnf90.set_interval`, `dnf90.cancel_timer`, and `dnf90.get_client_info` interfaces on top. Client information is deliberately limited to process ID, the dedicated Lua worker thread ID, game image base, compatibility profile, plugin version, and timer resolution; no character, channel, inventory, scene, packet, PVF, database, or persistence value is guessed.
- The DLL dispatches `dnf90._dispatch_timers` every 250 ms on the same thread that owns the Lua state. Repeating callbacks start after their requested 250 ms-to-24 hour interval, may return `false` to stop, and are removed with one Chinese diagnostic if they raise an error. Every successful initial load or hot reload invokes the public `dnf90.on_reload` callback; reloading replaces the timer state so functions retained from the preceding script cannot continue running. The starter demonstrates three one-second callbacks and all public/internal names, arguments, return values, limits, error behavior, and thread constraints have Chinese comments; the README documents the same boundary.
- Release/Win32 rebuild passes with zero warnings and errors. The PE32 candidate still imports only `KERNEL32.dll` and exports exactly `Install90CNLua` and `Request90CNLuaReload`; it is `320512` bytes with SHA256 `1C6BE1F91537709B99F22E4168A57CE8870062A9E147699AE88AAEB0AC311BE3`. The source and Release UTF-8 scripts are byte-identical at `7209` bytes with SHA256 `4E602098A90C7777E7A251B8DC9E7D2811B50CE851396C42B253B006FB7271DF`. An offline Lua smoke proves typed client metadata, timer timing, explicit cancel, `false` auto-stop, error isolation, load counts `1/2`, and old-timer removal.
- A separate current-EXE probe PID `8384` provided native Lua 5.1.5 acceptance: owned state `0x093F1EE0` on thread `12236` reported version `90CNLua/1.1.0`, ran initial reload callback count `1`, executed timer `1` exactly three times, then a timestamp-only save preserved the script hash, ran reload callback count `2`, and executed replacement timer `2` exactly three times on the same state/thread. The probe was stopped after validation. The configured game directory remains on the preceding plugin/script hashes `2E37383897C24BB9F4D56F191E7BF1C918051DF175BE9566E2B61D869D235C8C` and `CE857F11B04B4D40F7A25FB0B12CAF04E70C6E9357B6A057D8A55EE58BB0B581` because supported client PID `24036` is running; the compatibility manifest was not advanced. DNF90Server PID `19036` and portable MySQL PID `26748` remain READY with all 120 listeners accepting connections.
- After PID `24036` exited, the final DLL and script were deployed to the configured game directory; the prior DLL, script, and manifest are recoverable from `runtime/backups/client/20260803-085655-lua-timer-api-predeploy`. `runtime/idb-client` remains the same game-directory Junction, and the compatibility unit now pins plugin SHA256 `1C6BE1F91537709B99F22E4168A57CE8870062A9E147699AE88AAEB0AC311BE3` while retaining the current transport DLL hash. BAT launch validation passed all five required binaries. Formal client PID `10996` then loaded the `7209`-byte script as version `90CNLua/1.1.0` on owned state `0x09332890` and thread `16612`, invoked reload count `1`, and executed timer `1` exactly three times. A timestamp-only save preserved script SHA256 `4E602098A90C7777E7A251B8DC9E7D2811B50CE851396C42B253B006FB7271DF`, invoked reload count `2`, and executed replacement timer `2` exactly three times on the same state/thread. The accepted formal client remains running; DNF90Server PID `19036` and portable MySQL PID `26748` remained READY.

## 2026-08-03 - restore the persisted aura-appearance slot during cold town entry

- The reported aura-appearance slot was not relocked in persistence: character `1` still had the durable `aura_flag=1`. Real packet evidence from the failing cold-login lifecycle showed no class1/op863 `01 AURA` restore before disconnect, while older working lifecycles emitted it only after the client first sent op36 for a town-area move. The defect was therefore the server restore boundary, not the expansion ticket transaction, database flag, avatar socket state, or DLL hash.
- The shared cold-login/return-select/channel-reconnect player-state finalizer now emits the marked op863 restore after all six op13 inventory managers and the op898 crystal state, but before spend-time, adventure-group, full mode1, and the final op24 town transition. The existing `90cn-decode-bypass-v1` DLL safely caches that marker until the avatar-panel owner exists; the per-town lifecycle gate retains op36 as an idempotent fallback and prevents duplicate restores. Closed slots still emit nothing and no expansion ticket is consumed.
- Added a full initial-town regression with a persisted open slot. It proves exactly one `01 AURA` packet is present without any op36 progress, after the last container packet and before op1340. Focused aura/cold-login tests, the complete bridge and DNF-module suites, control tests, `go list`, required `go vet`, `gofmt`, `git diff --check`, production build, BAT-only offline preflight, and all 120 listener probes pass.
- Deployed `DNF90Server.exe` is `45951488` bytes with SHA256 `881752F2C23D0349E2706B74654A2F05CC05756B8C095FB53C0B2C68D9D0E708`; the preceding `62A02DADB14632AAC9CDE8C2F10658C085C6FC86A77B9C2A9B0A5847A57D4CFB` image and its instance configuration are recoverable from `runtime/backups/server/20260803-aura-skin-cold-login-predeploy`. DNF90Server PID `19036` and portable MySQL PID `26748` are READY, fresh stdout/stderr logs are empty, no DNF client process remains, and PVF, database contents, and client DLLs were not modified.

## 2026-08-03 - restore the current-client cumulative online-time activity

- Current `NoPack.exe` IDA evidence closed the two activity layouts instead of reusing the old enum names. The process-first class0/op108 body is exactly `u16 base_count=0 + u8 active_count=1 + event 2347 + 12*u32` (53 bytes); only a later op108 in the same client process has the extra retired-count byte. Event 2347 uses the four reward item IDs in params 0..3, `0xFFFFFFFF` in unused params 4..9, reward count `4` in param 10, and total duration `10800` in param 11. Its class0/op1206 state is exactly `u32 event_id + u8 narrow_flag=0 + raw[0x20]`, with elapsed seconds and completed reward count in the first two dwords. The descriptor now uses the current protected fixed-16/marker-1 zlib transport and is immediately followed by ordinary op1206; event 2348 remains disabled because its separate three-slot passive UI is not the four-stage PVF attendance catalog.
- The bridge projects the real `event/chn_event/chn_attendanceevent.evt` durations `1800/1800/3600/3600` and items `490006574..490006577` into a server-owned 30-second clock. Login publishes authoritative progress after all inventory managers exist, thresholds grant through the existing PVF item allocator and atomic account/character/inventory claim ledger, and only fresh same-character receipts emit op14 rows. Progress and claims survive reconnects and reset at Asia/Shanghai 06:00; exact-boundary intervals are split so the preceding day is claimed before reset. A PID registry sends the 53-byte grammar only after launcher authentication, keeps pre-session POST retries idempotent, resets for each newly registered process lifecycle (including PID reuse), preserves the gate across channel sessions, and uses progress-only when TCP owner PID is unknown or a running client reconnects after a server restart. A proved replacement reconnect settles before its new op13 bootstrap without allowing auxiliary same-character probes to stop the resident clock.
- Exact 53/54-byte builders, protected-zlib transport decode, op108-before-op1206 ordering, partial-write recovery, PID lifecycle, replay isolation, automatic two-service-day claiming, exact 06:00 half-open intervals, and auxiliary/reconnect ownership regressions pass. `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, scoped diff checks, production build, BAT-only offline preflight, controlled restart, and all 120 listener probes pass; the independent final audit found no P0/P1 blocker. Deployed `DNF90Server.exe` is `45950464` bytes with SHA256 `62A02DADB14632AAC9CDE8C2F10658C085C6FC86A77B9C2A9B0A5847A57D4CFB`; the preceding `50063A7B1B52FC4DDF8A7B68D2A32F96B2E93E773349D903A617DA680A0A4396` image is recoverable from `runtime/backups/server/20260803-spend-time-predeploy-0649`. DNF90Server PID `17352` and portable MySQL PID `17372` are READY, fresh stderr logs are empty, and the PVF, database contents, client DLLs, and active client PID `14584` were not modified. That already-running client is intentionally progress-only after the server restart; a full launcher-driven client restart is required once so the new-process registration can safely authorize its single 53-byte activity descriptor.

## 2026-08-03 - replace inactive game-Lua callbacks with an owned embedded Lua state

- Live acceptance rejected both callback-dependent loader revisions. The original three verified `lua_tinker` wrappers installed correctly but never ran after character/scene switching; a follow-up `lua_pcall` hook also installed but received no call. Process timing and read-only current-EXE evidence explained the result: the `ijl15` proxy loads `90CN.dll` about 18 seconds after process creation, while the optional native Lua manager is already absent (`0x051818EC == 0`) and its only three direct wrapper call sites cannot provide a live `lua_State*`. No additional hook was guessed from this negative evidence.
- Read-only live-memory disassembly proved the embedded Lua 5.1.5 construction path instead: `luaL_newstate` is at RVA `0x2A40` with entry bytes `E8 2B E9 FF FF 85 C0`, and `luaL_openlibs` is at RVA `0x1530` with entry bytes `55 8B EC 83 EC 08 83 3D`. `90CNLua.dll` now calls those verified routines without patching EXE code, owns the resulting state on its dedicated worker thread, loads `init.lua` immediately, and polls for timestamp/reload-generation changes on that same thread. Native `dnf90` calls remain the only explicit path from this isolated VM into the DLL; no gameplay, packet, PVF, database, or persistence interface was added.
- A lightweight probe client under `runtime/probes/lua-pcall-client-20260803-003339` kept its DLLs independent while sharing immutable client assets through hard links/Junctions. DBWIN captured owned state `0x093E25E0`, the Chinese `init.lua` success line, version `90CNLua/1.0.0`, and `script loaded` on thread `2176`; repeated timestamp-only saves advanced the cumulative count through `4`. The final UTF-8 script is `2101` bytes with SHA256 `CE857F11B04B4D40F7A25FB0B12CAF04E70C6E9357B6A057D8A55EE58BB0B581`. The probe process was closed normally; its link directory was retained because safe cleanup correctly refused a potentially recursive Junction deletion.
- Release/Win32 rebuild passes with zero warnings. The PE32 plugin remains dependent only on `KERNEL32.dll` and exports exactly `Install90CNLua` and `Request90CNLuaReload`; it is `318976` bytes with SHA256 `2E37383897C24BB9F4D56F191E7BF1C918051DF175BE9566E2B61D869D235C8C`. A separate Lua runtime smoke loads the final Chinese-commented script twice and proves counts `1/2`, two logs, and the version. The candidate is not yet installed because the configured client PID `14584` still holds the preceding plugin image open; the formal game path, compatibility manifest, and unrelated DOVE PID `25264` remain unchanged. The preceding plugin/manifest unit is backed up under `runtime/backups/client/20260803-003158-lua-pcall-probe`.
- After the supported client exited, the final plugin and script were deployed to the configured game directory; `runtime/idb-client` resolves to that same directory through its existing Junction, so no duplicate file was written. The immediately preceding DLL, script, and manifest are recoverable from `runtime/backups/client/20260803-083812-owned-lua-final-predeploy`. The compatibility unit now pins the final `90CNLua.dll` SHA256 above while retaining the current `90CN.dll` SHA256 `B8362890B84C45F4A658BB1D50F5991C491376DE9564E5C748B0FAD54D5F2689`; BAT launch validation passed all five required client files. Formal current-client PID `24208` then emitted DBWIN evidence for owned state `0x09432890`, the `2101`-byte UTF-8 script, and Chinese load count `1`; a timestamp-only save preserved script SHA256 `CE857F11B04B4D40F7A25FB0B12CAF04E70C6E9357B6A057D8A55EE58BB0B581` and reloaded on the same state/thread with count `2`. DNF90Server PID `17352` and portable MySQL PID `17372` remained READY, and no unrelated DNF client process was present or modified during deployment.

## 2026-08-03 - correct National Day aura universal-socket wire type

- Traced the reported 2017 National Day aura through the live booster commit and relational inventory row. Source box `490701319` correctly granted aura `101590068` into avatar slot `1:21`, and the row contained two 30-byte socket records, so the package selection, PVF default-socket detection, grant transaction, and list-1 refresh all ran. The actual defect was the socket discriminator: the bridge persisted each PVF `[M socket]` as ushort `0x00EF`, while the current client requires its special universal-socket value `0xFFEF`; it therefore rendered both otherwise-present records as closed.
- Avatar socket creation now writes `0xFFEF` for the `M` family while retaining the low-byte family value used by emblem compatibility checks. List-1 full and incremental projections also canonicalize historical `0x00EF` rows to `0xFFEF`, so the already-granted aura becomes visible after a character reload without reopening the box or consuming socket material; a later socket mutation persists the canonical form normally. Other socket families remain unchanged, and avatars without socket metadata still emit the established empty optional blob.
- Added exact special-wire-type and historical-row repair regressions. The synthetic National Day grant coverage, real production-PVF resolution for both auras `101590068/101590069`, and the complete `dnfbridge` suite pass.
- Formatting, package listing, every DNF-module test, the control suite, complete `dnfbridge` tests, required `go vet`, production build, controlled BAT restart, strict installed-client validation, and online preflight pass. MySQL PID `14936` and its data were preserved; DNF90Server SHA256 is `50063A7B1B52FC4DDF8A7B68D2A32F96B2E93E773349D903A617DA680A0A4396`, READY as PID `24908`, all 120 listeners accept connections, and fresh server stderr is empty. The preceding server image is recoverable from `runtime/backups/server/20260803-national-aura-ffef-predeploy`.

## 2026-08-03 - add protocol-independent daily online-event ownership

- Added the source-only `onlineevent` domain for future current-EXE-proved activity projections. It records server-observed half-open presence intervals as a compact per-service-day union in account metadata, so retries, overlapping sessions, reordered ticks, and multi-session overlap cannot double-credit time. Following the 86JP `ClockService` evidence, the default boundary is Asia/Shanghai 06:00 rather than civil midnight; each proved event may explicitly configure another local wall-clock boundary. Corrupt/future state, impossible thresholds, invalid rewards, and account/character ownership mismatches fail closed.
- Reused the existing character-settlement unit of work to commit the account-scoped claim receipt, selected character gold, and caller-projected inventory rows atomically. Stage replay returns the persisted receipt without invoking the item allocator again, while any allocator or transaction failure rolls back the reward and ledger together. Item IDs, stack rules, raw rows, event dates, thresholds, and rewards remain caller/PVF owned; this module invents none of them.
- Added a strict read-only PVF catalog for the real `event/chn_event/chn_attendanceevent.evt`. It preserves the four raw process durations `1800/1800/3600/3600`, the source sum thresholds `5/10/15/25`, raw sum-activity value `0`, and the eight exact reward item/count pairs `490006574..490006581`; every reward must resolve uniquely through `stackable.lst` to a readable definition. The catalog deliberately does not convert those values into cumulative stages or infer an activity ID, start/end date, enabled state, reset rule, or packet field.
- Added coverage for interval overlap/replay/gaps, continuity through 00:00, default 06:00 reset and cross-boundary clipping, an explicitly configured midnight boundary, claim/service-date validation, status initialization, threshold locking, corrupt/stale state, strict PVF section/list/path validation, exact claim replay, and transaction rollback. Synthetic, production-PVF, focused/race, focused `go vet`, and every DNF-module test pass. No bridge handler, opcode, packet body, activation descriptor, composition-root registration, runtime configuration, PVF mutation, database schema/state, client file, or deployed executable was changed because the current EXE's activity request/response layouts still require authoritative evidence.

## 2026-08-03 - withdraw the unsupported op413 Seria-luck projection

- Rechecked every current `NoPack.exe` class0/class1 registration instead of trusting the legacy server enum or an old fixture. The current client registers no handler for opcode `413`; `TitleBookGet` is only the repository's legacy name and the previous eight-byte fixture is not current-handler evidence. The old two-byte `{type, percent}` BOOSTER_GAGE projection therefore cannot initialize the Seria-luck HUD and is unsafe on the current upper stream.
- Removed that speculative projection from cold town initialization, the type1345 deferred tail, and post-magic-box refresh while preserving the durable account-level `seria_luck_value` transaction. These paths now log one explicit withheld-projection reason and send no guessed replacement opcode. The HUD remains intentionally unavailable until its real current-client registration/body is proved by IDA or live traffic.
- Regressions pin that both the type1345 lifecycle and successful magic-box post-action emit no op413 luck packet. The protocol-independent online-event domain and its raw PVF catalog remain disconnected from transport; no activity activation descriptor, packet, reward grant, PVF row, client file, database state, or runtime executable was invented or deployed.

## 2026-08-03 - deploy and live-load the separate Lua bridge

- Promoted the already-validated Release/Win32 pair as one `90cn-decode-bypass-v1` compatibility unit: `90CN.dll` is `354304` bytes with SHA256 `B8362890B84C45F4A658BB1D50F5991C491376DE9564E5C748B0FAD54D5F2689`, and `90CNLua.dll` is `319488` bytes with SHA256 `BF1C4A2BDCB97C57B8719ABAB28252784996522052DBAAED07F332FF4B910C3F`. Byte-identical copies now exist in the configured `23.4.15.0` game directory and `runtime/idb-client`; canonical `client-patch/bin/90CN.dll` was synchronized as well. The UTF-8 `lua/init.lua` in both runtime targets remains a hot-editable external script and has SHA256 `D004B5DEC08E1481203B69C5B6894DBD6D509A26C2034294B4F2B9F09D4F2BBE`.
- Updated `deploy/assets/client-compatibility.json` to pin the new transport DLL and require the separate Lua DLL as the fifth client binary. The generic control regression fixture now validates the same five-file shape. The three preceding `90CN.dll` copies and compatibility manifest are recoverable from `runtime/backups/client/20260803-001812-lua-plugin-predeploy`; there were no preceding Lua DLL or script files to overwrite.
- BAT controller launch strictly validated `DNF.exe`, `ijl15.dll`, `ijl15_real.dll`, `90CN.dll`, and `90CNLua.dll` before starting a supported client. `90CN_trace.log` proves `LoadOptionalLuaPlugin` returned success in supported PIDs `14584` and `18656`; a DBWIN capture from a clean probe PID `21740` independently observed `[90CNLua] installed game_base=0x00400000 wait_ms=0`. The probe instance was closed normally after validation, while the earlier supported client PID `14584` and unrelated DOVE PID `25264` were left running. Script execution remains pending the next proved native game-Lua callback (normally entering a character or another Lua-driven scene); no `init.lua` success line appeared during the bounded 30-second channel-stage capture.
- `go list`, the complete `dnfbridge` and control suites, and required `go vet` pass. The broader DNF-module suite is blocked by an unrelated pre-existing dirty-tree compile error in `internal/modules/dnf/onlineevent/owner.go` (`time.Time.Ceil` does not exist); that file was not changed. Runtime `status` passes with DNF90Server PID `10172`, MySQL PID `14936`, and all 120 listeners accepting connections. Offline `check` correctly refused to rewrite/preflight configuration while that managed server is running, so live client launch supplied the compatibility validation instead.

## 2026-08-03 - register the minimal native `dnf90` Lua API

- Extended the separate `90CNLua.dll` bridge with four deliberately non-gameplay Lua 5.1 functions: `dnf90.log(text)` writes bounded UTF-8 text through wide debugger output, `dnf90.version()` returns `90CNLua/1.0.0`, `dnf90.reload()` schedules work for the next proved native Lua callback, and `dnf90.is_ready()` returns a real Lua boolean. The valid `dnf90` identifier deliberately replaces the impossible `90cn.*` spelling because Lua identifiers cannot begin with a digit. Registration uses the current EXE's verified `lua_pushcclosure`, `lua_createtable`, `lua_settable`, and boolean-push entrypoints, preserves any unrelated fields already present in the `dnf90` table, and restores the original stack before compiling the external script.
- Expanded the UTF-8 starter `init.lua` with Chinese interface comments covering names, arguments, return values, and thread behavior. Each load now calls `is_ready`, `version`, and `log`, producing one Chinese success line with the cumulative load count; it still performs no packet, character, inventory, PVF, database, or persistence mutation. The source and Release script copies are byte-identical.
- Release/Win32 rebuild passes with zero warnings. Offline current-client validation pins the native prologues at RVAs `0x3030`, `0x31E0`, `0x3360`, and `0x200E0`; a separate Lua runtime smoke executes the UTF-8 starter twice and proves counts `1/2`, two Chinese log messages, the version result, and callback return values. The resulting PE32 plugin is `319488` bytes with SHA256 `BF1C4A2BDCB97C57B8719ABAB28252784996522052DBAAED07F332FF4B910C3F`. No DLL, script, compatibility manifest, or running client was changed; live execution remains pending on a fresh supported `23.4.15.0` client rather than the unrelated active DOVE process.

## 2026-08-02 - add a separate current-EXE Lua script loader DLL

- Added the independent Release/Win32 `client-lua/90CNLua.dll` project. It does not ship another Lua runtime or depend on `lua51.dll`; it validates the pinned 2018 current-EXE PE profile, waits for the packed image to restore three proved `lua_tinker` call specializations, captures their `lua_State*`, and loads external Lua 5.1 source only on the originating game Lua thread. Every attempt preserves the pre-call stack, uses protected calls for compilation/execution, bounds scripts to 1 MiB, rejects embedded NUL data, and catches loader faults without suppressing native game exceptions.
- The default script is the sibling `lua/init.lua`; `DNF_LUA_SCRIPT` can select an absolute or plugin-relative path. File timestamp polling provides hot reload at the next native Lua callback, and the undecorated `Request90CNLuaReload` export schedules the same action without running Lua on its caller. The starter script is intentionally non-gameplay and only proves repeatable loading. Diagnostics use `OutputDebugString`, so the plugin creates no game-directory log or configuration state.
- Production `90CN.dll` now optionally discovers the sibling plugin by an exact module-relative path and calls its undecorated `Install90CNLua` export. Absence is a no-op, `DNF_LUA_PLUGIN=0` disables discovery, and no Lua code was merged into the transport DLL. Release/Win32 builds pass: `90CNLua.dll` has zero warnings, is PE32/x86, imports only `KERNEL32.dll`, exports exactly the two documented entrypoints, is `317440` bytes with SHA256 `3E36BB9C6EDB3FB34FC3C3C4262754B21303C31A1CD353D37A06BFB74C5D4524`; the candidate `90CN.dll` builds with only its eight pre-existing C4996 warnings and SHA256 `B8362890B84C45F4A658BB1D50F5991C491376DE9564E5C748B0FAD54D5F2689`. Neither candidate was copied to `client-patch/bin`, the configured game directory, or the compatibility manifest, so the installed `90cn-decode-bypass-v1` unit and any running client remain untouched.

## 2026-08-02 - restore purchased crystal-contract selection during login

- Runtime account logs proved that premium type `97` was active and that the durable crystal selection was still `4`; neither the purchase nor its persistence had been lost. Current `NoPack.exe` IDA instead located op898 at `0x4081CEA` in the class0 notification table registered through `sub_225C940`. The previous class1 body `{0, selection}` was consumed as a failed response plus error code and never reached the crystal-contract handler.
- The bridge now keeps the client-authored op535 selection and op338 cube-use ACKs on class1, but sends the authoritative op898 restore as raw class0 `{0, selection}`. Cold login, character reselection, and return-to-town publish it once after all six op13 inventory containers and before op1340/full mode1/op24; the later client-authored op36 path remains an idempotent fallback and is re-armed only for a new town lifecycle.
- Added exact classification, one-shot/re-arm, and initial-town ordering regressions. Focused tests, the complete bridge suite, all DNF-module tests, control tests, package listing, required `go vet`, and an independent read-only review pass. BAT-only offline preflight and controlled restart pass with all 120 listeners accepting connections. Deployed `DNF90Server.exe` SHA256 is `200E280E49CE013D9EB546CABA45D3BDC4C7FC6FD82F1C77421C81A1D0895E65`; the preceding `992143D69B4424617CE7CE8C292B375B9CBAFCD2DEB2E064D25F9B891E6B2C41` image is recoverable from `runtime/backups/server/20260802-crystal-contract-class0-predeploy-234251`. Server PID `10172` and portable MySQL PID `14936` are READY, and both fresh stderr logs are empty.
- Offline preflight also exposed the already-pending client compatibility deployment: the manifest pinned `FD6DF70F4EED8A7D4922F3D8D61D786417707F0F1AF676DCF494093F22AFAD2F` while the configured client and IDB copy still contained `604BC9AC21EA30C1D1C3B205C37A5070A0AA7FD56FEFA8B4A8BA9EA741292026`. After gracefully closing only the configured `23.4.15.0` client, synchronized the already-built manifest-matching DLL to that client and `runtime/idb-client`; the unrelated DOVE client was not touched. The old DLLs and manifest are backed up under `runtime/backups/client/20260802-client-fd6-sync-235213`.

## 2026-08-02 - audit the missing luck, calendar, and online-event UI

- Verified that the current client already contains the HUD luck-gauge, RandomboxBooster, event-calendar, on-time-event, 2017 honey-time, and weekly-attendance NPK resources. Their absence is therefore not a missing DLL/NPK problem. The activity calendar/National Day basket, staged booster reward, active-event icon descriptor, and accumulated-online reward have only legacy command enums or resources; there is no current handler, owner, event catalog, daily timer/reset, idempotent claim ledger, or atomic reward grant in the server.
- A later exhaustive current-client registration scan corrected this audit's preliminary op413 interpretation: current `NoPack.exe` registers no class0/class1 op413 handler at all. The repository's `TitleBookGet` label and old eight-byte fixture are legacy metadata, not a current handler. The existing two-byte class0/op413 `{type, percent}` luck-gauge sender is therefore not valid for this client, which explains why packet logs showed it sent while the UI remained absent. The account-level `seria_luck_value` mechanic is durable, but the current luck-gauge opcode/body still requires authoritative capture/decompilation before it can be exposed safely.
- Item-grade adjustment itself is already implemented through op81 with atomic material consumption, quality mutation, and op14 refresh. What is absent from the screenshots is the online-event reward source that grants the adjustment box. No speculative event packets, fabricated rewards, PVF changes, or database rows were introduced by this audit.

## 2026-08-02 - correct false pet remaining-time display from legacy equipped rows

- Live database and current-client evidence identified the screenshot's exact value rather than treating the 2018 Spring Festival pet as a timed PVF item. `火狐艾芙` item `400990168` has no `[expiration date]` or `[usable period]`, and its durable inventory expiry is null. Its recoverable 47-byte equipped-pet row stores creature serial `41` at byte `0x18`; the generic `raw_entry_hex` padding path carried those bytes into a `0x77` list-7 row, where the current EXE reads dword `+0x16` as pet remaining seconds. The resulting `0x00290000` is `2,686,976` seconds (31.099 days), which the UI rounds up to the reported 32 days.
- Both list-7 encoders now authoritatively own dword `+0x16`: permanent pets write zero, while genuinely expiring pet instances write the positive seconds remaining until their persisted/PVF-derived absolute expiry. This preserves the creature serial only at row `+0x06`, keeps the independent absolute expiry at `+0x38`, clears stale padded legacy bytes, and covers full-list login, repository refresh, hatch/read, and incremental pet update paths without changing PVF or database state.
- Added an exact serial-41/47-byte regression plus permanent and real-expiration coverage. Focused inventory/bridge pet, creature, and expiration suites, all DNF-module tests, package listing, required `go vet`, and the production build pass. The complete bridge suite is presently blocked by 13 pre-existing crystal-contract/channel-reconnect op898 failures in unrelated dirty-tree work; those files were not changed. Candidate `runtime/bin/DNF90Server.pet-duration-fix.exe` is `45754880` bytes with SHA256 `23CF041E6665DFFC4BFE325324C5AD8339DC40389F7E1FC99978A2C215914B75`. No live file was replaced: BAT preflight correctly refused while server PID `6724` is running, and the visible configured client remains active as PID `17928`.

## 2026-08-02 - restore expert-job menu state, quest abandonment, and 2018 pet-bead use

- The gray expert-job menu was a client projection race rather than a quest or database value: the authoritative class0/op205 update wrote expert-job type `3`, then a later current-actor class0/op2 mode-0 row rebuilt the actor and cleared `actor+0x364`. The compatibility DLL now caches op205 only for the exact local actor/object key and reapplies that type after native mode-0 parsing only when the same actor is proved; op6 removal clears the cache so a different character or remote actor cannot inherit it.
- Implemented the current EXE's class1/op32 quest-abandon contract (`u16 opcode + u16 quest id`, success `u8 1 + u16 quest id`) with DB-before-ACK ownership, same-session replay suppression, and accept/abandon replay lifecycle clearing. The PVF owner rejects `[cant giveup]` quests and quests whose give/reward item rollback still needs a typed asset transaction; the four real expert-job terminal quests `10021/11009/11015/11018` are proved no-asset and can now be removed durably instead of being swallowed by the generic handler.
- Ported 86JP's missing list-7 creature target for class1/op272 enchant-by-bead into the Go character-pet transaction. A successful pet bead now writes card ID/raw `+0x0E` and upgrade byte `+0x12` to both the list-7 inventory row and its typed `PetEntry`, consumes the list-0 bead, sends the two current op14 changed rows, then returns the op272 ACK. Runtime PVF remains authoritative: the resolver now reads the real `[bead limited usable item]` field (falling back only to legacy `[target item id]`). The screenshot's account-bound dark bead `490007596` resolves card `10008663` and permits only pets `400990167/400990168/400990170/400990171`; the currently equipped `400990006` remains correctly rejected.
- Package listing, all DNF-module tests, the complete dnfbridge/control suites, required `go vet`, focused owner/wire tests, and real-PVF quest/pet-bead regressions pass. Release/Win32 `90CN.dll` built with zero errors and the eight pre-existing C4996 warnings; canonical/package size is `353280` bytes with SHA256 `FD6DF70F4EED8A7D4922F3D8D61D786417707F0F1AF676DCF494093F22AFAD2F`, pinned by `deploy/assets/client-compatibility.json`. Candidate server `runtime/bin/DNF90Server.expert-job-gray-giveup-pet-enchant.exe` is `45748736` bytes with SHA256 `C2C743B8B4D571E1FB57F455149CA9B7AA07E4A9F9F3090249415D13381E282F`. Deployment is pending because the visible main client still holds the old DLL open as PID `17928`; no live file has been overwritten yet.

## 2026-08-02 - correct historical Cera-package commodity IDs and displayed prices

- Live client evidence rejected the initial `950008..950235` allocation: the package cards omitted their Cera prices and checkout fell back to each stackable definition's unrelated `[price]` value (for example `131340`) instead of the intended shop row. The 11-token `[package]` row and price offset were correct; the allocator had incorrectly started after the maximum commodity across every Cera category rather than remaining in the package commodity domain.
- `cmd/scratch/pvfshopgift` now starts after the real `[package]` maximum `108513` and skips every positive integer already present anywhere in the original `etc/cerashop.etc`, including occupied value `108514`. The 228 new rows therefore use the collision-free package-domain range `108515..108742`; their exact prices remain National Day `19900/33800`, Spring Festival `19900/39900`, and ordinary/true SAO `23900/25900`. The real-PVF regression pins this range in addition to resolving every package, price, reward list, avatar choice, and live expiration.
- The corrected PVF is `111094706` bytes with SHA256 `F3717725DB34D8ACE2BF3E3CCA408692E734EE52365EFF69E39408D31E546603`, and `deploy/assets/manifest.json` pins the same compatibility-unit size/hash. It was rebuilt from the untouched preimage rather than modifying the rejected output. Package listing, all DNF-module tests, the complete bridge/control suites with the corrected PVF, focused real-PVF validation, and required `go vet` pass. After the client exited normally, byte-identical copies were installed at `runtime/data/dnf/Script.pvf`, `runtime/idb-client/Script.pvf`, and the configured game directory. The rejected PVF and its manifest are recoverable from `runtime/backups/pvf/20260802-historical-gifts-price-fix-predeploy`. Strict BAT preflight validated the PVF, channel asset, all four client compatibility files, generated configuration, and 120 available ports; controlled startup then loaded the corrected PVF. DNF90Server PID `6724` and MySQL PID `19028` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty.

## 2026-08-02 - create National Day auras with their PVF default emblem sockets open

- Traced the real 2017 National Day package chain instead of matching display names or package IDs in the grant path: the published packages contain `490701319` 国庆节光环徽章装扮礼盒, whose two selectable rewards are aura items `101590068` 冷峻的海军指挥 and `101590069` 愤怒的海军指挥. Both runtime-PVF definitions are `[aurora avatar]`, declare `[avatar emblem socket num] 2`, and carry two `[M socket]` rows under `[emblem socket default]`.
- The shared Cera/booster avatar-grant projection now initializes those PVF-declared default sockets when an aura is created, so opening the National Day aura box yields two already-open M sockets without consuming an avatar socket-opening material. `[avatar type select]` remains manual-opening metadata, and ordinary avatar parts are never pre-opened even if they contain a default-socket declaration.
- Added synthetic isolation tests for default aura sockets, manual-only aura socket choices, and ordinary avatars, plus a real production-PVF regression that resolves `490701319`, proves both exact aura choices, grants them through the same booster path, and verifies their persisted two-socket data. Formatting, package listing, full `go test ./...`, all DNF-module/bridge/control suites, required `go vet`, the focused real-PVF regression, and BAT preflight pass. Candidate `runtime/bin/DNF90Server.national-aura-default-sockets.exe` is `45702656` bytes with SHA256 `992143D69B4424617CE7CE8C292B375B9CBAFCD2DEB2E064D25F9B891E6B2C41`; the installed `DNF90Server.exe`, PVF, database, and client DLL were not changed or started.
- On explicit operator deployment, stopped only the server while preserving the running portable MySQL, backed up the preceding `DNF90Server.exe` SHA256 `CD7B2EE6E491D545A4667C8BBEB125492CB712E5CF0B29EE71AE7ED67078C5AE` under `runtime/backups/server/20260802-national-aura-default-sockets-predeploy`, and installed the candidate as the production executable with its exact `992143D69B4424617CE7CE8C292B375B9CBAFCD2DEB2E064D25F9B891E6B2C41` hash. The game-directory and `runtime/idb-client` copies of `90CN.dll` were synchronized from canonical `client-patch/bin/90CN.dll`; all three are `352768` bytes with SHA256 `604BC9AC21EA30C1D1C3B205C37A5070A0AA7FD56FEFA8B4A8BA9EA741292026`, matching the already-pinned `90cn-decode-bypass-v1` compatibility hash. The two preceding DLL copies and manifest are recoverable from `runtime/backups/client/20260802-client-compat-604bc9-predeploy`. Strict BAT startup validated all client files, PVF, database, and 120 listeners; DNF90Server PID `3684` and MySQL PID `25688` are READY, and the fresh server stderr log is empty.

## 2026-08-02 - publish 2017 National Day, 2018 Spring Festival, and SAO Cera packages from PVF

- Added the reproducible `cmd/scratch/pvfshopgift` operator tool. It reads the real `stackable.lst`, item definitions, localized name tokens, package contents, suitable-job tags, and the current summer catalog's job-code mapping before appending rows to `etc/cerashop.etc`; it does not create item definitions, expand the shared string pool, or overwrite its input archive. The catalog adds 228 openable products: 104 海军的梦想/浪漫 packages at 19,900/33,800 Cera, 104 兽人指引者/守护神 packages at 19,900/39,900 Cera, and the PVF's 10 ordinary plus 10 真·SAO packages at 23,900/25,900 Cera. The pre-sale gunblader item `490701980` is deliberately excluded because its real package data has no direct avatar choice and the current op518 contract rejects it with `avatar_choices=0`; listing it would create a product that can be bought but not opened.
- Repointed only 111 expired script references needed by these products to the already-existing `2028-08-16 06:00:00` pool value: all 104 National Day source packages and seven immediate SAO reward boxes. No broad string-pool replacement or unrelated event expiry changed. The real-PVF regression resolves every listed product through `newPVFCeraShopCatalog` and `resolveCurrentCeraPackageDefinition`, verifies prices/counts, requires at least one real avatar choice, and rejects expired SAO direct rewards.
- The production compatibility-unit PVF is `111094679` bytes with SHA256 `94E102FB153E4232C34A069D69B3F37B1CBDFB8273899D0633C80D045F83E216`; `deploy/assets/manifest.json` pins the same size/hash. Byte-identical copies are installed at `runtime/data/dnf/Script.pvf`, `runtime/idb-client/Script.pvf`, and the configured game directory. The preceding PVF and server image are recoverable from `runtime/backups/pvf/20260802-historical-gifts-predeploy`; the task-only production build completed at SHA256 `8BF590676B093DD725F0371BAF6A15C894CA71DA55E2D7ACC9D1B91FA26F8353`, then the previously deployed server image `F4752E8FFF2B5A35F647BC04896B4225A31616A7EC8A5625F1DCB3179A541DEE` was restored because this PVF task does not authorize deploying unrelated dirty-tree server changes. Package listing, all DNF-module tests, the complete bridge/control suites with the candidate PVF, required `go vet`, and focused real-PVF validation pass. BAT preflight validates both runtime assets, then correctly blocks on a pre-existing client compatibility mismatch: the configured game directory contains `90CN.dll` SHA256 `5BF751ECB5CD6D6F244C0B83043A99586A6065EBF178CDEE29FDC5CCA3D78567`, while the current pending compatibility manifest requires `604BC9AC21EA30C1D1C3B205C37A5070A0AA7FD56FEFA8B4A8BA9EA741292026`. No DLL or compatibility manifest was changed and the controller therefore did not restart the stopped server/MySQL; operator direction is required before choosing between the installed DLL unit and the pending manifest unit.

## 2026-08-02 - auto-activate PVF contract items before inventory projection

- The screenshots identified the four three-day premiumlist items now present as ordinary bag rows: 霸王契约 item `31` / premium type `22`, 达人契约 item `44` / type `27`, 成长契约 item `2660409` / type `84`, and 晶之契约 item `10000388` / type `97`. Kept `etc/premiumlist_new.etc` as the only authority for every contract item's type and term, so the same rule also covers the runtime PVF's one-day, seven-day, fifteen-day, minute-unit, and additional contract families without name or item-ID guesses.
- Added a deterministic premium-domain inventory activation planner and a `RentalAssets` transaction that removes every proved list-0 contract stack and extends the account expiry by `PVF duration * stack count` from `max(now,current expiry)`. Duration overflow or missing PVF/account persistence leaves the recoverable item untouched. Unrelated list-0 items, avatar/pet containers, warehouse rows, PVF, and database schema are not changed.
- Added both acquisition projections: a class0/op14 grant containing a contract is converted to account time before its first client-visible update and that slot is sent as a deletion row; full op13 list-0 snapshots reconcile before reading their rows. Character selection performs the same migration before building the typed premium section, so existing contract items disappear on the next selection and their complete quantities become days without emitting a premature pre-ACK notification. Runtime activation sends the current class0/op66 remaining-time notification once per resulting premium type and refreshes crystal-contract state for type `97`.
- Added planner/overflow, two-stack duration, selection-migration, exact op66, and no-bag op14 regressions. The real production PVF verifies the four screenshot item IDs and all other premium families. Package listing, full `go test ./...`, all DNF-module tests, the complete bridge/control suites, and required `go vet` pass. Candidate `runtime/bin/DNF90Server.contract-auto-use.exe` SHA256 is `8BF590676B093DD725F0371BAF6A15C894CA71DA55E2D7ACC9D1B91FA26F8353`. No live database, PVF, server executable, or client DLL has been changed while the visible client remains active.

## 2026-08-02 - merge the complete expert-job abandonment and current-store contracts

- Audited `副职业完整体系源码备份_20260802_191614.zip` (SHA256 `1C304B6DD6255645C1D3FB34C6F2412E12A49D239B5960FD940E4975BB57C93D`) capability-by-capability instead of overwriting the newer repository layout. The current tree already owned all four expert jobs, recipe learning, compound/card compound, equipment extraction, disjointer/enchanter stores, machine repair/upgrade, and two-character atomic store mutations. The confirmed missing domain was current class1/op239 abandonment plus its quest-chain reset; the existing store projection also still used the older kind/notification layouts rejected by the supplied current-client evidence.
- Added PVF-backed give-up costs for all four jobs and implemented op239 as one character-settlement transaction spanning the list-0 gold wallet, character expert-job stats, learned recipes, machine fields, and quest state. Success now clears type/EXP/machine/recipes, advances the bounded give-up cost tier, removes only PVF quests marked `job change quest = 20`, and returns the exact `success + finalGold + giveUpState + messageFlag` body. Login quest-list construction repairs the corresponding legacy abandoned-terminal state without removing unrelated quests or an actively restarted expert-job transition.
- Replaced the old expert-store wire projection with the supplied current-EXE contract: unified op598 kind `0/3`, owner ACK before owner-only NOTI544, peer-only NOTI538 with the full town/u32-area/attach-mode body, and NOTI539 close for owner and same-area peers. op235 now includes the store owner in its success body; op601 returns final expert EXP, uses owner NOTI533, refreshes qualification changes through NOTI544, and closes zero-endurance stores. The type-zero op205 projection is mirrored into the current actor by the compatibility DLL so menu/self-click state clears after abandonment.
- Added deterministic planner, atomic commit/rollback, legacy reconciliation, exact packet-body/notification, route-registration, and real-PVF regressions. The production PVF proves costs `1000/10000/100000/1000000` for every job, 15 transition quests, and terminal quests `10021/11009/11015/11018`. `go test ./...`, package listing, all DNF-module tests, the complete bridge/control/release suites, required `go vet`, and both real-PVF smoke tests pass. Release/Win32 `90CN.dll` rebuilt with zero errors and the eight pre-existing C4996 warnings; Release/package SHA256 is `604BC9AC21EA30C1D1C3B205C37A5070A0AA7FD56FEFA8B4A8BA9EA741292026`. Candidate server `runtime/bin/DNF90Server.expert-job-merge.exe` SHA256 is `95269CF34E0DA953464C7705F88358C25E100A8034862790EEF8625818A8957A`. No PVF or database state changed. Deployment is intentionally deferred while the visible client remains active as PID `4520`; the existing server/MySQL remain READY as PIDs `26260/24604`, and all 120 listeners pass status preflight.

## 2026-08-02 - restore one native inventory manager for crystal and soul warehouse counters

- Direct IDA and live-process inspection proved that the server rows were already correct. The current main list-0 manager contained the persisted quantities for slots `354..364`, while the warehouse UI alias pointed at a different 366-slot manager whose same rows were empty. The production DLL trace identified the split: its hook on generic actor-container getter `sub_24A1BC0` created a replacement even during town actor installation because town also reports transition state `3`. Native character switching then rebound `dword_51B2768`, but the UI alias `dword_51B2790` retained the old empty manager.
- Restored the current EXE's ownership boundary. `sub_24A1BC0` is again side-effect free and is no longer hooked in production; missing-container repair now exists only at the single real manual-pickup path `sub_24B2840`, after a nonempty pickup-object list has been proved. The unrelated production hook on generic item-row parser `sub_21792A0` was also removed; premium-contract right-click fallback continues to resolve items through the native main-inventory manager.
- Removed the DLL's crystal-contract op898 cache and deferred direct call to `sub_1E7F810`. IDA proved that routine is a register-dependent `__userpurge` path using native `EDI`, not the declared `__thiscall`; calling it from the scene UI hook could silently corrupt panel state or crash. The bridge no longer sends op898 from the pre-panel scene tail and retains the proved first client-authored op36/UI-ready replay, so the native op898 handler applies durable selection only after inventory owner `0xBE` exists. No database, persisted warehouse row, PVF, or grant-notification mutation was introduced.
- Release/Win32 `90CN.dll` rebuilt successfully with only the eight pre-existing C4996 warnings. `Release/90CN.dll`, `bin/90CN.dll`, and the live game-directory DLL SHA256 are all `5BF751ECB5CD6D6F244C0B83043A99586A6065EBF178CDEE29FDC5CCA3D78567`, and the compatibility manifest was updated as the same unit. Binary inspection confirms the generic-getter, generic-item-row-cache, and ABI-unsafe deferred-apply hooks are absent while the manual-pickup repair remains. Package listing, all DNF-module tests, the complete `dnfbridge` and control suites, and required `go vet` passed. Deployed `DNF90Server.exe` SHA256 is `F4752E8FFF2B5A35F647BC04896B4225A31616A7EC8A5625F1DCB3179A541DEE`; the preceding server image is backed up under `runtime/backups/server/warehouse-client-state-predeploy-20260802-2052`, and the preceding client DLL is backed up under `runtime/backups/client/warehouse-counter-getter-predeploy-20260802-2025`. `control.bat status` reports server PID `26260`, portable MySQL PID `24604`, valid runtime/database state, and all 120 listeners accepting connections; both fresh stderr logs are empty. The persistent local launcher immediately restarted the server after the offline-stop attempt, so its safety guard refused `check --skip-database` rather than killing that user-visible launcher. Fresh-client visual acceptance remains pending.

## 2026-08-02 - restore the confirmed native list-0 crystal and soul warehouse rows

- The fresh-client screenshot rejected the zero-tail warehouse experiment even though the latest login packet contained all nine persisted nonzero rows at slots `354..364`. Packet-log reconstruction confirmed the selected-character order was already exactly `list0 -> list1 -> list2 -> list7 -> list12 -> list38`, with no list36 and no op14, and decoded every warehouse row as correct slot/item/count followed by an all-zero `0x77` tail. The missing values were therefore not a database or packet-count failure.
- Correlated the last visibly working screenshot (`2026-07-31 15:49:45`) with `server-20260731-154353.stdout.log` and the recoverable server image SHA256 `8C43C1F8AC654024F3DFA787C453671A92B8CA930F737DADF29E9F3B7B36868C`. That runtime used the same six class0/op13 list order, but projected slots `354..365` through the ordinary list-0 stack encoder and preserved each persisted native `0x77` row. The supplied `internal.zip` contains that same projection. Read-only current-EXE analysis independently confirmed that the warehouse UI resolves those slots from the main list-0 manager and reads quantity from the resulting native item object; itemspace 36 only protects already-created objects and does not create them.
- Restored only the account-shared list-0 projection to `currentItemListEntriesFromMap`, so authoritative slot, item ID, and count are patched while the persisted `RawEntry`/native identity tail survives. No list36/op14 workaround, item grant notification, DLL change, database rewrite, PVF change, or unrelated party/inventory rollback was introduced. Regressions now require the native crystal and soul row tails to survive both direct list construction and selected-character bootstrap, including migrated legacy account slots.
- Formatting, package listing, all DNF-module tests, the complete `dnfbridge` and control suites, required `go vet`, exact production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `04C85DBB7020F47F50A88A88A3F4CC20C7BB27B1F4AB4F5061C0ADCD1BAEFE93`; the rejected zero-tail image SHA256 `DEDD08B6C558130C9B2BCF15975B10871225684BBE6D9B9B09236B51C6C8FBB6` is recoverable from `runtime/backups/server/20260802-warehouse-native-row-predeploy`. DNF90Server PID `25684` and portable MySQL PID `21728` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty. Fresh-client visual acceptance remains pending.

## 2026-08-02 - install crystal and soul warehouses as silent list-0 virtual counters (rejected by subsequent live evidence)

- The live screenshot conclusively rejected the initialization `op14` path: it left every warehouse value at zero and routed each row through the ordinary acquired-item notification UI. Read-only decompilation of the open current `NoPack.exe` identified the separate overwrite trap. `sub_2339B30(36)` sets a preservation flag; the following list-0 rebuild stages slots `354..365`, and `sub_2336050` restores the old fixed objects after parsing, overwriting newly parsed quantities with their previous zero state. Selected-character initialization therefore sends neither itemspace 36 nor a trailing op14.
- Verified the corresponding latest 86JP implementation in `ItemListPacketBuilder.BuildMainItemListBody` and `ItemListProtocolWriter.WriteVirtualCountEntry84`: fixed crystal/soul counters are ordinary entries of the single class0/op13 list-0 snapshot, and each row carries only slot, fixed item ID, and authoritative count with every remaining byte zero. Adapted that semantic contract to the current client's proved 0x77 row width. The account-owned slots remain persisted unchanged, but their `RawEntry` and ordinary item-instance metadata are no longer copied into the login wire rows.
- Added regressions for direct list-0 placement, zero tails despite nonzero persisted raw tails, migrated account-shared residue, exact six-list bootstrap order, and absence of any trailing initialization packet. Formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `DEDD08B6C558130C9B2BCF15975B10871225684BBE6D9B9B09236B51C6C8FBB6`; the preceding stable `DED86A32DECD6B3B21C788CF7EC1C77B688824D4F5CAA4B15802058C5F3364D3` image is recoverable from `runtime/backups/server/20260802-warehouse-list0-zero-tail-predeploy`. DNF90Server PID `20264` and portable MySQL PID `9744` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty. Fresh-client visual acceptance remains pending.

## 2026-08-02 - correct expert-job menu unlock through the current op205 client boundary

- Fresh relog screenshots and direct read-only inspection of the running current client disproved the preceding op754 experiment: class0/op205 correctly held disjointer type `3`, but the current actor's native `expert_job_type` field at actor-state `+0x34C` remained zero. Re-reading `sub_200C720` established why: both op754 records resolve only the composite remote-actor maps through `sub_17B6AA0`; they cannot select the local actor returned by `sub_269A050`, and a missing first actor also leaves the reader before that record's remaining bytes. The system-menu and self-click gates compare the local actor field, so op754 was removed from login/reconnect and task-completion flows rather than retained as a silent refresh.
- The matching `90cn-decode-bypass-v1` DLL now reuses its already-installed class0 dispatcher boundary. After the native op205 handler has updated the auxiliary-profession manager, the compatibility layer accepts only a bounded op205 body whose second byte is a real type `1..4`, resolves the existing current actor through the EXE's native object-manager/current-actor function, and mirrors that server-owned type into the actor's 32-bit field at actor `+0x364`. This opens neither op2/mode3 personal information nor any other UI, creates no actor, and does not grant, persist, or infer character, inventory, database, or PVF state. Login still sends op205 after full mode1, and expert-job settlement remains `op34 ACK -> op205 -> committed character/skill/quest snapshots`.
- Updated the four-job production-PVF regression to require the exact op205 state for enchanter `2702`, alchemist `2708`, disjointer `2710`, and doll controller `2712` while rejecting both legacy op2 and op754 actor carriers. Release/Win32 rebuilt with zero errors and the existing eight deprecation warnings. Release, packaged, and installed DLL copies are byte-identical at SHA256 `C2B2728AC753B1FACB78AB288DAEAA1E47A6992D1A5F89FF9CE7496CCC23556A`, and the strict client manifest pins that hash; the preceding DLL is recoverable from `runtime/backups/client-patch/20260802-expert-job-op205-predeploy` and `runtime/backups/expert-job-op205-client-sync-20260802`. Formatting, package listing, every DNF-module test, complete `dnfbridge` and control suites, required `go vet`, the real-PVF four-job regression, release-package test, exact server build, BAT-only offline preflight, strict installed-client validation, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `3087886B6DCDB90ACCF9868FA90634234B62C75F0A14678E41666BEB633C4AE3`; the preceding `00E3620E2D386A9DC452FAB5697A2890353BC31346F04A575D824703023FACA9` image is recoverable from `runtime/backups/expert-job-op205-client-sync-20260802`. DNF90Server PID `18916` and portable MySQL PID `27820` are READY, all 120 listeners accept connections, and the fresh stderr log is empty. Fresh-client visual acceptance remains pending.

## 2026-08-02 - reject ordinary-item op14 as a crystal/soul warehouse carrier

- Live acceptance disproved the combined empty-list36/list0/op14 experiment. The warehouse counters remained zero, while every op14 row entered the client's ordinary acquired-item notification path and displayed one `acquired` line for each crystal/soul type. This proves that the current client did not retain fixed warehouse objects addressable by `sub_1D73120` at that lifecycle point; continuing to send op14 would be both visually wrong and unsafe as an initialization strategy.
- The experimental executable was stopped immediately and replaced with the preceding stable list-0-only image. Its matching source and regressions were restored so a later production build cannot reintroduce the op14 notifications. The failed packets did not mutate repository rows, account quantities, gold, database schema/state, PVF, or client files. Warehouse rendering now remains an explicitly client-side initialization/mapping investigation rather than another server-side item grant experiment.
- The restored `DNF90Server.exe` SHA256 is `DED86A32DECD6B3B21C788CF7EC1C77B688824D4F5CAA4B15802058C5F3364D3`; the `runtime/backups/server/20260802-virtual-counter-three-phase-predeploy` directory contains that preceding stable image, not the rejected experiment. DNF90Server PID `23276` and portable MySQL PID `24628` were READY, all 120 listeners accepted connections, and the focused restored-source warehouse/bootstrap regressions passed.

## 2026-08-02 - refresh expert-job actor state without opening personal information (superseded by later live evidence)

- Live completion of disjointer task `2710` proved that the database settlement, class1/op34 chain-20 receipt, and class0/op205 machine state were already correct, but the system-menu expert-job icon and the self-click disjoint-store action remained disabled. Current `NoPack.exe` analysis isolated the split ownership: op205 updates the expert-job manager, while those controls read `expert_job_type` from the town actor state written by `sub_2005520`. 86JP refreshes that field through its legacy op2 subtype0 body, which is incompatible with this client's current op2 transport; the current op2 mode3 alternative also opens the personal-information panel.
- Expert-job settlement now sends `op34 ACK -> op205 -> class0/op754` before the existing character/skill/quest snapshots. The current EXE registers op754 to `sub_200C720`, whose two records reuse the proved current mode0 actor entry and mode3 stat entry and reach `sub_2005520` without entering op2's panel-owning dispatch. Initial-town login and reconnect send the same silent actor projection immediately after mode1 and op205 whenever the persisted character already owns an expert job, so a character that completed the task before this repair does not need to repeat it. Both records are built from the committed/persisted character, repository equipment/creature state, PVF stats, current scene key, and actual town owner channel; no character, inventory, database, PVF, or client value is invented or rewritten.
- Added exact envelope/owner validation and production-PVF completion regressions for enchanter `2702`, alchemist `2708`, disjointer `2710`, and doll controller `2712`. The regressions require `op34 -> op205 -> op754`, compare the complete silent actor refresh body, retain the prohibition on legacy op2, and verify the committed job type; the no-job login path remains a no-op. Formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, the real-PVF four-job test, production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `0C166349DB6CF6CFE7AB78E7F2BD70EF28BC69A845D5755B040A4B72B3EC1F6D`; the preceding `FF0308E40AE60774D45C840B2824D2EB75C427E15896681AAF758FC7DEED2381` image is recoverable from `runtime/backups/server/20260802-expert-job-actor-ui-predeploy`. DNF90Server PID `23468` and portable MySQL PID `8164` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty.

## 2026-08-02 - restore current-client party P2P endpoints, selector mapping, and account warehouses

- Fresh current-client traffic proved that class0/op2 is the per-session UDP endpoint registration, not a disposable reconnect probe: its 31-byte body contains the client LAN/outer IPv4 values, the client-selected UDP port in big-endian form, MTU, and token. The bridge now retains that endpoint and advertises each member's real dynamic port in the initial class0/op11 party exchange, falling back to port 10000 only before registration. This replaces the incorrect hard-coded 10000 advertisement that left both clients in `connecting`, prevented teammate minimap projection, and stopped the passive member from following the leader's dungeon selector state.
- Party formation still sends the current-EXE `op153 -> op11 -> op9` sequence, but scene reconstruction no longer restarts the P2P handshake. Passive selector entry installs the authoritative op9 roster after its op23/mode0/mode1/op3 actor transition and before op15/fatigue/op27; town-area movement refreshes the mover and same-area party peers with op9 only after actor installation. Leader delegation clears and rebuilds only the op9 slot table, preserving the established realtime/P2P transport. The authoritative leader remains normalized into slot zero for every roster and dungeon projection.
- Itemspace 36 is account-owned in the current client. It now loads `AccountInventory`, merges the durable shared rows, and emits slots `354..365` with the same virtual-counter semantics as list 0 before the complete list-0 snapshot. This corrects the previous empty list36 body that armed the panel but left crystal and soul warehouse counters at zero; no inventory row, gold value, database schema, PVF, or client file was rewritten.
- Added exact regressions for dynamic op2 port capture and big-endian op11 projection, op9-only town/selector/leader-transfer rebuilds, passive-member selector ordering, same-area party refresh, and account-owned itemspace-36 crystal/soul rows. Formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `FF0308E40AE60774D45C840B2824D2EB75C427E15896681AAF758FC7DEED2381`; the prior live `ED0BF4BF5AAA2C6F6DA9457335A8EEFD219A4635F13F0C05DF195F428BDEFC3F` image and the intermediate expert-job build `605916D2EC1363F25039CFFBFF2D65EA7E30CA8DFF3CFCFC4CC50EABD6B62D20` are recoverable from `runtime/backups/server/20260802-party-p2p-selector-warehouse-predeploy`. DNF90Server PID `6008` and portable MySQL PID `9872` are READY, all 120 listeners accept connections, and the fresh server stderr is empty.

## 2026-08-02 - publish expert-job state immediately after quest completion

- Current `NoPack.exe` decompilation confirms that class1/op34 chain 20 carries only the selected expert-job type plus the two zero-length skill-tree placeholders, while the complete expert-job state is a separate class0/op205 packet. Successful expert-job quest settlement now sends the atomic op34 success receipt first, immediately follows it with a forced current-character op205 projection, and only then publishes the ordinary post-commit character, active-quest, and acceptable-quest snapshots. The incompatible legacy 86JP class0/op2 follow-up was deliberately not copied because that command is current-object transport in this EXE.
- Added production-PVF regressions for the complete reachable expert-job quest set: enchanter task `2702` selects type 1, alchemist task `2708` selects type 2, disjointer task `2710` selects type 3, and doll-controller task `2712` selects type 4. The bridge integration test completes each real task through the atomic repository owner, verifies the committed `expert_job_type`, pins exact ACK/op205 ordering, rejects any op2 follow-up, and validates every type-specific op205 recipe, qualification, level, machine-grade, and endurance field against the runtime expert-job catalog.
- Formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, both real-PVF expert-job quest tests, and the production build pass. The rebuilt `runtime/bin/DNF90Server.exe` SHA256 is `605916D2EC1363F25039CFFBFF2D65EA7E30CA8DFF3CFCFC4CC50EABD6B62D20`; it was not restarted, so live PID `26328` continues to serve the preceding `ED0BF4BF5AAA2C6F6DA9457335A8EEFD219A4635F13F0C05DF195F428BDEFC3F` image with all 120 listeners present and an empty stderr log. BAT preflight and status correctly refused to manage the live image after Windows renamed the still-loaded old executable to `DNF90Server.exe~` during replacement; no runtime configuration, PVF, database schema/state, client, or deployment script changed.

## 2026-08-02 - synchronize passive party selector and restore virtual warehouse counters

- Fresh two-client traffic isolated the blank passive-member dungeon selector: the leader received the current class1/op15 success body `01 01 00`, while the server moved the follower through op23, mode0/mode1, op3, fatigue, and op27 without installing that selector acknowledgement. The follower transition now emits the same current-EXE op15 success after its actor/user-state transition and before fatigue/context, and records the acknowledgement gate so later leader op16 admission does not replay it. This keeps the passive member on the same selector lifecycle and allows the current client to display the leader-owned dungeon and difficulty state.
- Corrected the preceding warehouse experiments with the combined current `NoPack.exe` contract. Itemspace 36 must immediately precede list 0 to arm preservation of the twelve pre-created fixed objects at slots `354..365`; the following list-0 rows must also be virtual-counter rows containing only authoritative slot, item ID, and quantity, with their ordinary item-instance tail zeroed. Either list 0 alone or preserved raw 0x77 instance tails leaves the panel at zero. The selected-character and ordinary item-list plans now use `36 -> 0`, keep backpack and gold in the complete list-0 owner, and do not rewrite account inventory or database quantities.
- Added exact regressions for the passive follower's op23/mode0/mode1/op3/op15/fatigue/op27 order, one-shot acknowledgement state, itemspace-36 preservation ordering, complete list-0 ownership, and zero-tail crystal/soul counter rows. Formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `ED0BF4BF5AAA2C6F6DA9457335A8EEFD219A4635F13F0C05DF195F428BDEFC3F`; the preceding binary SHA256 `119292B8AE50749622079E8821F00A054B4EDEC4EF710217CFE29EC92331EDEC` is recoverable from `runtime/backups/server/20260802-party-selector-virtual-warehouse-combined`. DNF90Server PID `26328` and portable MySQL PID `28288` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty.

## 2026-08-02 - preserve party roster across town areas and restore proven list-0 warehouses

- Fresh two-client traffic identified the remaining town-transition split without changing the authoritative server party: class1/op36 sent op6 to the stationary old-area client, and the current EXE removed the departed town actor from its scene and its local party manager at the same time. Town area changes now distinguish ordinary peers from authoritative party peers. Ordinary peers still receive op6; party peers retain the existing actor identity and receive only the native op23 town/area/position update. The mover continues to rebuild its own scene-owned party table after entering the destination. No cross-area actor or full party-table replay is sent to the stationary client, avoiding the client-generated empty class1/op13 `LEAVE_PARTY` seen with the previous rebuild attempt.
- Compared the supplied `internal.zip` implementation directly with the current bridge. The previously working warehouse path sends the persisted account crystal/soul fixed slots `354..365` as part of the complete list-0 class0/op13 snapshot; it does not create a separate itemspace-36 snapshot. Restored that exact container ownership and ordering while retaining repository-authoritative slot, item, count, and raw 0x77 identity. The database was not rewritten, no op14 increment/acquired-message workaround was reintroduced, and backpack, gold, character containers, account cargo, and crystal-contract state remain on their existing owners.
- Added regressions for complete list-0 account warehouse rows, migrated account-shared residue, six-container bootstrap ordering, and same-old-area party movement. The movement regression requires the stationary party client to receive exactly one op23 area update and rejects op6, actor recreation, or party-table replay. Focused and complete `dnfbridge` suites, package listing, every DNF-module test, control tests, required `go vet`, production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `119292B8AE50749622079E8821F00A054B4EDEC4EF710217CFE29EC92331EDEC`; the preceding binary SHA256 `1205234763836C5D5D0FB81B68C43E00FD77AB4917102981C7E4023948E6BE48` is recoverable from `runtime/backups/server/20260802-town-party-area-update-list0-warehouse`. DNF90Server PID `9088` and portable MySQL PID `23904` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty.

## 2026-08-02 - port remaining 86JP enchanter and disjointer expert jobs

- Ported the remaining two expert jobs from 86JP commits `0c78604` and `82a09e0` into the current Go/PVF ownership model: enchanter (`expert_job_type=1`) and disjointer (`expert_job_type=3`). The unified startup catalog now reads enchanter levels, ordinary recipes, card qualifications, monster-card recipes, bead mappings, card success/experience tables, extractors, extraction results, store limits, endurance/repair rules, plus disjointer result/additional/big-win tables, self-service rewards, machine durability, repair costs, upgrade costs, and character-level limits directly from the configured runtime PVF. The production `Script.pvf` smoke test resolves 11 enchanter recipes, 6 card recipes, 356 cards, 382 bead mappings, 6 extractors/48 extraction rules, 128 disjointer result rows, and 11 repair levels for each machine family.
- Extended class1/op238 so enchanters can craft ordinary products and convert a selected monster card into its PVF bead, consuming the selected card and exact recipe materials on both success and failure while preserving the card's dynamic enchant-upgrade value on a successful bead. Class1/op416 now accepts enchanter extractors with the same locked-item, equipment-state, inventory-capacity, and transaction guarantees already used by the other production jobs. Recipe-scroll learning and class0/op205 expert-job projection now include enchanter card recipes, qualifications, level/endurance, and the disjointer machine grade/endurance layout.
- Added the complete player-store lifecycle for both professions using the current command numbers and the 86JP-proved bodies: create, area notification/replay, enter, visitor disjoint/enchant use, close, disconnect/character-switch/town-move cleanup, machine repair, and disjointer upgrade. Cross-player services commit requester inventory/gold and owner inventory/gold, expert experience, and machine endurance through the existing two-character MySQL transaction in stable character-ID order; a callback or persistence failure rolls both characters back. Enchant success applies only runtime-PVF-proved card/equipment combinations, consumes the card even on failure, and never invents an item or enchant table.
- Added deterministic enchanter/disjointer planning, partial repair/upgrade, exact store/compound/extraction wire, card-craft, and two-character commit/rollback regressions. Formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, the real-PVF smoke test, and the exact production build pass; focused domain/bridge race tests also pass. The rebuilt `runtime/bin/DNF90Server.exe` SHA256 is `1205234763836C5D5D0FB81B68C43E00FD77AB4917102981C7E4023948E6BE48`; it was not restarted. BAT preflight correctly refused to rewrite runtime configuration while live PID `8048` was running, then `STATUS.bat` passed service/repository/PVF/MySQL checks and all 120 listener probes. No client, PVF, database schema, generated runtime configuration, or deployment script changed.

## 2026-08-02 - preserve party state across town movement and install account warehouses through itemspace 36

- Live two-client packet evidence isolated the town-transition collapse: after one member submitted class1/op36 for another area, the bridge rebuilt that member locally and then incorrectly replayed a cross-area mode0/mode1/op9/op153 party sequence into the stationary client. The stationary current client immediately answered with an empty class1/op13 `LEAVE_PARTY`, and the server disbanded both sessions. Town movement now sends the ordinary leave notification to old-area peers and rebuilds only the mover's local party owner; it no longer reinserts a cross-area actor or replays a full party table into stationary clients.
- Ordinary-party rosters now normalize the authoritative leader into slot zero before storage and before every realtime, endpoint, scene, directory, and dungeon projection. This corrects the invite flow that appended the accepter before the inviter and displayed the leader in the second UI position. Current live traffic also proved that this client delegates leadership with class1/op121 `CHANGE_HOST` and a one-byte selected slot. The bridge now handles that command, clears the old client-side table, moves the new leader to slot zero, updates every member's authoritative leader/party identity, and rebuilds the complete op153/op11/op9 sequence. The legacy op335 result was also corrected from an invalid three-byte body to the current EXE's exact two-byte position/result body.
- Corrected the preceding post-list0 op14 warehouse conclusion. Current `NoPack.exe` decompilation proves that `sub_226FA70(36)` resolves the same fixed-object manager, `sub_2339B30(36)` arms preservation of slots `354..365`, and the following list-0 rebuild restores those objects. Selected-character initialization now sends a silent class0/op13 itemspace-36 snapshot first, followed by list 0 and the remaining containers. Crystal/soul rows retain their persisted 0x77 identity while repository slot/item/count fields remain authoritative. The erroneous op14 increments and their visible `acquired` messages were removed; database quantities were not rewritten.
- Added regressions for itemspace-36-before-list0 ordering, raw crystal/soul identity and counts, absence of a trailing op14 update, inviter-as-slot-zero formation, op121 leader transfer/rebuild, exact op335 result width, and stationary-peer isolation during town movement. Formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, the exact production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `1205234763836C5D5D0FB81B68C43E00FD77AB4917102981C7E4023948E6BE48`; the preceding binary SHA256 `F4571C43E08FB4CF2424428ED9953049970F20755DCC21DC789233A131462DE7` is recoverable from `runtime/backups/server/20260802-party-town-itemspace36-changehost`. DNF90Server PID `8048` and portable MySQL PID `5688` are READY, all 120 listeners accept connections, the fresh server stderr is empty, and MySQL reached normal readiness.

## 2026-08-02 - use authoritative party-menu marker and post-rebuild virtual warehouse updates

- Current `NoPack.exe` decompilation corrected both reported regressions. In class0/op7 mode 15, `sub_1D82CF0` reads the trailing `u16` as a party-presence marker rather than an opaque request echo: `0xffff` clears the selected target's party flag and every other value sets it before `sub_325FA70` opens the interaction menu. The click path now derives that marker from the target's live runtime party state, so a partyless target advertises `invite party` and an active target advertises `join party`.
- The same EXE proved why valid crystal/soul counts embedded in list-0 op13 stayed visually zero. `sub_2339B30` removes and preserves the twelve pre-created virtual objects at slots `354..365` before the full rebuild; after incoming 0x77 rows are parsed, `sub_2336050` restores those old objects over any temporary rows at the fixed slots. Account crystal/soul rows are therefore excluded from op13 and are emitted immediately afterward through one class0/op14 update. `sub_1D73120` finds the restored fixed object, verifies its authoritative item ID, and applies the row's `+0x06` quantity in place. The op14 rows contain only slot, current-EXE item ID, and repository count, so stale item-instance tails cannot affect the virtual counters. No account inventory or database values were rewritten.
- Added exact regressions for the `ffff`/`0000` menu marker, op13 exclusion of fixed virtual counters, post-op13 op14 ordering, crystal and soul quantities, and zero instance tails. Formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, the exact production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `F4571C43E08FB4CF2424428ED9953049970F20755DCC21DC789233A131462DE7`; the preceding binary SHA256 `F58111B65631367A278CED4BAACC3492B2E9EFA95B7C1AFF0A78C108FE2EA33D` is recoverable from `runtime/backups/server/20260802-op7-party-marker-warehouse-op14`. DNF90Server PID `26900` and portable MySQL PID `26532` are READY, all 120 listeners accept connections, the fresh server stderr is empty, and the MySQL stderr contains only normal startup/readiness lines.

## 2026-08-02 - port 86JP alchemist and doll-controller expert jobs

- Ported the two expert-job implementations from 86JP commit `7f426881` into the current Go ownership model: alchemist (`expert_job_type=2`) and doll controller (`expert_job_type=4`). Startup now parses their real `character/expertjob/*.exj` tables, recipe stackables, extraction tools, compound success/experience rates, equipment rarity/level rules, additional results, and big-win results directly from the configured runtime PVF. The production `Script.pvf` smoke test resolves 71 alchemist recipes plus 6 extractors/48 extraction rules, and 53 doll-controller recipes plus 6 extractors/48 extraction rules.
- Added the current-client command flows for class1/op238 compound and class1/op416 extraction, using the request and response layouts verified against the open current `NoPack.exe` in IDA. Compound consumes the exact PVF materials/gold for every attempt, grants only successful output, and advances experience only for successes. Extraction validates the selected expert tool and equipment state, rejects locked, disjoint-forbidden, and trade-delete equipment, removes the target, grants the PVF result rows, and advances expert-job experience.
- Recipe-scroll use on class1/op44 now learns configured recipes up to the PVF level-plus-two limit. Learned recipe IDs are stored as typed character extra-stat rows, so recipe consumption, learned state, materials/target removal, rewards, and `expert_job_exp` all share the existing character-settlement transaction and roll back together. Login, recipe learning, and level changes publish the current class0/op205 expert-job recipe list after the mode1 actor exists; auto-learned recipes remain derived from PVF and manually learned recipes are persisted without adding a new schema or runtime file.
- Added deterministic compound/extraction planning tests, atomic owner commit/rollback coverage, exact op238/op416 wire tests, typed learned-recipe projection coverage, and an opt-in real-PVF catalog smoke test. Formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, and the production build pass. The rebuilt `runtime/bin/DNF90Server.exe` SHA256 is `F4571C43E08FB4CF2424428ED9953049970F20755DCC21DC789233A131462DE7`; it was not restarted. The BAT-only offline `check --skip-database` correctly refused to rewrite/preflight while live PID `26900` was running; `STATUS.bat` then passed the live service/repository/PVF/MySQL checks and all 120 listener probes. No client, PVF, database schema, generated runtime configuration, or deployment script was changed.

## 2026-08-02 - restore crystal/soul raw rows and resolve party menu state at click time

- Live login evidence proved that the account warehouse was neither empty nor omitted: character `1` merged nine account-owned slots into its initial list-0 snapshot, and the production database still held the correct fixed crystal/soul item IDs, counts, and 119-byte raw rows. The immediately preceding zero-tail compatibility attempt was therefore disproved for the current EXE. Crystal and soul rows now preserve their real 0x77 raw identity/state (including the repeated item identity at `+0x0E`) while patching only the authoritative slot, item ID, and count. No inventory or account data was rewritten.
- Town-player interaction menus now resolve the target's authoritative runtime party state on every mode-15 click. After the request ACK and before the class0/op7 selection notice opens the menu, the bridge sends a receiver-specific class0/op9 projection for the target: kind 3 when the target has no party, or the complete kind-0 member table when the target has an active party. This makes the native client offer `invite party` for a partyless target and `join party` only for a target that actually owns a party, while also repairing stale client-side party links.
- Added regressions that retain current raw identity/state for both crystal and soul warehouse rows and verify the exact click sequence for both partyless and active targets. Required formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, exact production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `F58111B65631367A278CED4BAACC3492B2E9EFA95B7C1AFF0A78C108FE2EA33D`; the preceding binary SHA256 `F3645A0905397B15E75DC0826882AA9F16F90A9E2BB1CD1096759717D9AC7E1C` is recoverable from `runtime/backups/server/20260802-dynamic-party-menu-warehouse-raw77`. DNF90Server PID `21328` and portable MySQL PID `28384` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty.

## 2026-08-02 - complete ordinary-party peer transport and preserve account inventory

- Compared the current bridge with the supplied 2026-08-01 86JP party implementation and ported only the missing current-client-compatible transport step. Every active ordinary-party snapshot now uses the native order class0/op153 realtime rows, class0/op11 `PARTY_IP_INFO`, then class0/op9 member frame. The op11 body contains one exact 22-byte endpoint row per online member with the observed private IPv4, network-order game peer port, character/account identity, NAT type, MTU, and character attribute. This supplies the client party peer manager that clears the persistent `connecting` state and drives teammate/minimap synchronization without emitting raid-party packets.
- Kept partyless menu state authoritative. A partyless actor still receives class0/op9 kind 3; an active solo party receives its real kind-0 frame. Packet evidence for the reported `join party` label showed the target character explicitly submitted class1/op12 `SET_PARTY_INFO`, creating a solo party, so the label was correct for that live state rather than a translated invite label. The controlled restart clears transient solo parties so two characters that have not created a party can re-test the ordinary invite path from a clean state.
- Removed the unsafe account-only class0/op13 list-0 town refresh. That packet replaced the client's complete main-inventory owner with only account-shared rows, making backpack contents and gold appear empty even though repository data remained intact. Town initialization now retains the ordinary complete inventory projection and replays only the separately owned persisted crystal-contract selection state; the unused unsafe refresh helper was deleted.
- Account-shared crystal and soul slots `354..365` are now serialized as virtual counter rows in the current 0x77-byte inventory format: only slot, item, and count are populated and the item-instance tail is zero. This prevents stale raw-entry bytes from invalidating the side-panel count while preserving the repository-backed quantity.
- Added exact op11 layout, party snapshot ordering, cross-area replay, virtual crystal/soul row, and non-destructive town initialization regressions. Required formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, exact production build, BAT-only preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `F3645A0905397B15E75DC0826882AA9F16F90A9E2BB1CD1096759717D9AC7E1C`; the preceding binary SHA256 `3DFFB86403E148ACA8777A69BF642BCEFD5529E75C8547BFC45F277A778A9E57` is recoverable from `runtime/backups/server/20260802-party-op11-crystal-virtual-count`. DNF90Server PID `27544` and portable MySQL PID `25840` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty.

## 2026-08-02 - merge owner-ffff ordinary-death and NPC type-4 start-map fixes

- Merged the two supplied 2026-08-01 Mermaid Kingdom fixes into the current split dungeon runtime without overwriting the newer session/runtime refactor. A strictly validated variable op39 death with `owner=0xffff` may now enter the ordinary monster death path only when its object is the active room's announced, still-live PVF monster with an exact runtime/expected-hostile binding. It retires only that monster, emits the normal op38 death notification, and does not promote the target to a Boss or force-retire neighboring hostiles; tutorial, AI/APC, passive-object, stale, cross-room, malformed, and already-defeated cases remain fail-closed.
- The current op29 start-map actor projection now maps PVF rank `[NPC]` to the current EXE's proved actor type `4` and always writes the reverse-polarity room-clear skip byte `1`. `[NPC]` remains excluded from the ordinary `0..3` reward classifier, while other unknown ranks continue to fail instead of falling back to a fabricated normal-monster type.
- Added focused owner-sentinel state/packet coverage, a synthetic NPC start-map regression, and an opt-in real-PVF regression for dungeon `162`, maze `3`, room `4:2`, monster `62531`. Focused tests, the real `runtime/data/dnf/Script.pvf` regression, package listing, every DNF-module test, the bridge suite excluding the two independently failing town/crystal-warehouse tests, the control suite, required `go vet`, and the production build pass. The rebuilt `runtime/bin/DNF90Server.exe` SHA256 is `3DFFB86403E148ACA8777A69BF642BCEFD5529E75C8547BFC45F277A778A9E57`; it was not restarted. Full bridge validation remains blocked by `TestTownAreaReadyReplaysPersistedCrystalSelectionAfterPanelOwnerExists` and `TestHandleTownSetUserAreaRefreshesOnlyOccupiedAccountSharedWarehouseRows`, which also fail in isolation with short-packet assertions and do not touch this fix. BAT preflight stops because live PID `6156` is executing `DNF90Server.exe~`, which the controller does not recognize as the managed server. No database, PVF, client DLL, generated runtime configuration, or deployment script changed.

## 2026-08-02 - port complete ordinary-party selector and shared dungeon flow

- Compared the local 2026-08-01 86JP source against the current-client bridge and ported its transaction ordering rather than its obsolete packet layouts. A leader selector request now drives each passive same-area member through the complete current `NoPack.exe` transition (`op23 area=0xFF`, op2 mode 0/mode 1 owner binding, op3 user state, fatigue, optional permission snapshot, and op27) without fabricating a class1 request ACK. The transition is idempotent, so the later leader op16 does not replay or corrupt the passive member's selector owner.
- Party dungeon entry now freezes the leader's exact maze, every resolved room-map choice, boss coordinate, and seed. Every follower is repository/PVF validated and has its entry packet plan built before any runtime commits; follower runtimes are committed first as a rollbackable batch and the leader is committed only after all followers succeed. A low-level, exhausted-fatigue, stale-party, missing-session, topology, or packet-plan failure therefore leaves the whole party in town. Successful leader room changes are serialized onto every follower session with the same target and leader-owned room seed.
- Town transport op15 scene finalizers are now consumed without opening or fanning out the dungeon selector. This separates ordinary portal/teleport transitions from genuine dungeon-gate intent. After any party member crosses a town-area boundary, every other town-ready party session is also rebuilt with that member's mode0/mode1 actor, op9 party frame, real op23 area, and op153 realtime rows, so both the mover and stationary peers retain the two-member HUD and minimap projection.
- Corrected the crystal/soul warehouse town refresh to the current EXE's proved fixed16 class0/op13 reader with only occupied account-shared rows. It no longer replays the character backpack into the side-panel owner and no longer sends a twelve-row op14 packet that the panel can misinterpret.
- Added regressions for the complete passive selector packet order, transport-finalizer suppression, bidirectional cross-area peer rebuilding, all-or-nothing party admission, frozen per-room map choices and entry seed, synchronized room movement/seed, and occupied-only account warehouse refresh.
- Required formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, the exact production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `9FA5F46BA488FE423169DC5D0C988A5C728C4A9EB145036B1AF6D943813E3365`; the preceding binary SHA256 `70D751F54880F7B04CBB0B5A3DB5C19A289F3421E99EA477CE4A97E056702CCF` is recoverable from `runtime/backups/server/20260802-party-shared-dungeon-flow`. DNF90Server PID `6156` and portable MySQL PID `25372` are READY, all 120 listeners accept connections, and both fresh stderr logs are empty.

## 2026-08-01 - remove duplicate party actor crash and restore cross-area party state

- The first current-client access violation in `90CN_trace.log` occurred immediately after the same local scene object received two consecutive class0/op9 kind-0 party frames. The post-start-map route now occupies the native player-placement op9 position with exactly one party-aware frame and appends only the proved op153 realtime table; the later duplicate local snapshot was removed. IDA 9.3 review of current `NoPack.exe` `sub_1D64CA0` also proved that inline member identity is consumed only when the outer actor owner differs from the client's owner, so the bridge does not force extra fields into a same-owner row.
- A town-area transition can rebuild the client's actor manager while a teammate remains in another area. Before replaying the receiver's local party frame, the bridge now resolves each online cross-area member from the authoritative online index, installs that actor through the current mode0 -> mode1 -> actor-op9 order, and sends op23 with the member's real town, area, position, direction, and state. This keeps the same party/member references and provides real location input for the HUD/minimap without falsely claiming town co-presence.
- Party dungeon entry now performs a complete member preflight before creating or committing any member runtime. If any online member is outside the same resident area, not selector-ready, or fails the selected dungeon's level/fatigue/party validation, no member is moved; this prevents the leader entering alone while a lower-level follower remains in town. The leader's genuine op15 selector entry now prepares same-area party followers with the native prerequisite packets and no fabricated class1 ACK, allowing the follower to observe the leader-owned dungeon/difficulty selection.
- Post-op24 account storage recovery now rebuilds the complete fixed16 list-0 inventory owner with class0/op13 instead of sending only incremental op14 rows. Persisted crystal/soul warehouse slots therefore return together with the normal bag owner after every accepted town transition.
- Focused regressions, the complete `dnfbridge` suite, `go list -buildvcs=false ./...`, every DNF-module test, the control suite, required `go vet`, the exact production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. Deployed `DNF90Server.exe` SHA256 is `70D751F54880F7B04CBB0B5A3DB5C19A289F3421E99EA477CE4A97E056702CCF`; the preceding binary SHA256 `9A9AC77553615FD8AF63CA07E20BEAAF5C99703C3FD506E880A0CD42FF6B8026` is recoverable from `runtime/backups/server/20260801-214354-party-town-crash-sync`. DNF90Server PID `22096` and portable MySQL PID `20504` are READY, all listeners accept connections, and the fresh server/MySQL stderr logs are empty.

## 2026-08-01 - enable persisted current-client alternate skill-tree switching

- Ported the proved current `NoPack.exe` class1/op260 flow into the existing `skillcmd` owner instead of adopting the reference server's in-memory session mutation. The request decoder now requires the observed five-byte body (`u8 current_tree` plus a four-byte transport-owned tail), retains the tail only for diagnostics, and rejects an out-of-range current index.
- The skill owner now serializes op260 with character skill/SP/TP mutations through the character-progression transaction, revalidates selected-character ownership, requires the persisted `skill_tree_index` ownership marker, rejects stale/replayed current-tree values, and commits the opposite `0/1` index before exposing success. The response matches the current reader exactly: success is `01 + target`; malformed, unavailable, stale, owner-mismatched, or persistence-failed requests return bounded failure `00 13` without changing state.
- This change opens only the proved switch. The current two-tree op19 projection remains the existing identical initial-plan model, while op28/op29 continue to allow only the separately proved tree-zero mutation path; independent second-tree learning is not invented from the reference implementation. Domain, response, handler, entitlement, replay, malformed-body, legacy-tail, aligned-enum, and focused bridge regressions pass.

## 2026-08-01 - synchronize party dungeon entry, restore town warehouses, and restore trade confirmation

- Current `NoPack.exe` decompilation (`sub_1D800C0`) proved that class0/op153 rows are `user_id, hp_percent, helper, party_slot`. Realtime party snapshots now send the real HP percentage and stable `0..3` member slot, and the unproved unsolicited class0/op7 mode-10 status rows were removed from create/join, town replay, dungeon replay, and call-realtime paths. This prevents the duplicate ordinary-party/raid-party notification chain while keeping member identity and HUD status authoritative.
- Opening the party leader's selector with class1/op15 now affects only the leader and sends no packet to followers, so walking through an ordinary town/dungeon-gate transition cannot pull a teammate into the selector. Only after the leader submits class1/op16 with an exact dungeon and difficulty do same-area followers receive the native prerequisite sequence (`op36`, optional `op5`, `op27`) followed by the leader-owned `op28/op29` runtime. Followers receive no fabricated class1/op15 or class1/op16 request ACK, and every member uses the leader's dungeon, difficulty, maze, map, topology, and seed.
- Live town-transition evidence showed that class0/op24 preserves the ordinary backpack but rebuilds the side-panel owner for the account-shared crystal and soul warehouses. After each accepted class1/op36 town-area transition, the bridge now sends one incremental class0/op14 list-0 update containing only persisted slots `354..365`; it does not replay or rebuild the complete inventory container family. Regression coverage verifies both crystal and soul rows, exact counts, and no unrelated container packets.
- Live trade traffic showed that the current client emits class1/op24 state `2` after an offer projection. That state is now treated as an ACK-only compatibility message: it receives success without changing the shared trade state or broadcasting class0/op17, eliminating the one-sided `交易错误` dialog while preserving the two-party final-confirmation transaction.
- Live current-client traffic proved that item-trade `添加完成` submits class1/op24 value `5`, while choosing `是` in the final resource-727 dialog submits value `3`. The online trade owner projects each initial `5` as class0/op17 state `1`, which is the exact state consumed by `sub_2340B10` to open `是否确认交易`. The first final `3` is acknowledged and retained only on the server because the submitting client already moves itself to `正在等待对方最终确认`; broadcasting op17 state `3` would close the unanswered peer's resource-727 dialog. Only both final `3` values commit the existing atomic gold/item exchange. A second `5` remains a bounded legacy spelling of final consent but cannot skip the shared state-1 boundary.
- Live two-client town transition traces showed that the runtime party remained authoritative (`party_id=1`, two members), but `SET_USER_AREA` rebuilt the client's scene-owned party manager and only peer-actor op9 projections were replayed. After installing the new-area roster and peer actors, every active member now receives its own authoritative op9 frame followed by the corrected op153 realtime rows. This preserves the same leader, member slots, and two-member HUD regardless of which client crosses a town boundary first.
- Ordinary party dungeon entry is now delivered as one leader-owned batch to every online member. Each member receives an independently owned validated runtime with the leader's proved dungeon/maze/start-room/map/boss topology and random seed, the current op29 party-member index is `0..3` instead of the solo `0xFF` sentinel, and a local op9/op153 party snapshot is replayed after player placement so the dungeon HUD retains every member. Every successfully entered member is removed from town presence without disbanding the party.
- The current client opens short-lived same-character game connections for channel/raid-directory probes. Those auxiliary transports no longer replace the resident town session, remove its online actor, or detach its trade/party state when they close; a real town-area commit explicitly promotes the new resident session. Regressions cover auxiliary disconnect isolation, two-phase trade confirmation, leader/follower op29 indices, identical party entry seed, removal of both town presences, and two-member realtime/status projections.
- Required formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, exact production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. The replacement `DNF90Server.exe` SHA256 is `9A9AC77553615FD8AF63CA07E20BEAAF5C99703C3FD506E880A0CD42FF6B8026`; the preceding binary SHA256 was `55440846F548452457F42F65907E130D08D9231A7384030744BBAE3716ADFBD2` and is recoverable from `runtime/backups/server/20260801-town-warehouse-party-op16`. DNF90Server PID `18916` and portable MySQL PID `21228` are READY, all 120 listeners accept connections, and the fresh server/MySQL stderr logs are empty.

## 2026-08-01 - keep traded items in visible bag pages and repair the hidden Bakar orb

- Current `NoPack.exe` evidence established that the item-trade pseudo-container has twelve native rows: `0..2` are currency rows and the nine visible item rows are `3..11`. Ordinary item offers now allocate only `3..11`; both the offering client's op19 success ACK and the peer's class0/op15 item projection use that same server-assigned row. This removes the two observed failures where a count-one item rendered as `+1` gold in row 0 or disappeared behind the non-item pseudo rows 1/2.
- The atomic exchange no longer copies the sender's exact main-inventory slot into the recipient. It classifies the source slot into the current client's equipment, consumable, material, quest, profession, emblem, or special page and allocates the first free visible cell on that same page. Avatar transfers retain their separate list. A regression covers the reported consumable source slot `0:110` landing at the recipient's visible `0:65` instead of inheriting an expansion-dependent high cell.
- Database inspection confirmed the reported `使徒巴卡尔 宝珠` was not deleted: character `7` (`发发发好`) retained item `490702514`, count `1`, at hidden slot `0:110`, including all eight extra attributes. With both clients offline, the item and its extras were moved transactionally to the verified-empty visible consumable slot `0:65`. The pre-repair inventory rows and previous server binary are recoverable from `runtime/backups/server-db/20260801-183307-trade-visible-slot`.
- Required formatting, package listing, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, exact production build, BAT-only offline preflight, controlled restart, and all 120 listener checks passed. The replacement `DNF90Server.exe` SHA256 is `6EC09D04CE3A520B5AF2AA8D3836BA517595E1382A656F599E1EC884D3B8F53F`; the preceding binary SHA256 was `90BB539215878AFA7758297292B8E5479A4A8296AFE9074AF84794C1559F129B`. DNF90Server PID `24032` and portable MySQL PID `16608` are READY; the fresh stderr log is empty, and post-restart SQL verification finds the repaired orb only at `0:65` with all eight extras.

## 2026-08-01 - restore production dungeon pickup compatibility after installer refactor

- Live dungeon evidence showed that pressing the pickup key emitted no class1/op43 request even though the ground object was present and selectable. The already audited controlled-actor pickup repair remained in source, but the production transport-worker refactor had left both native hook installations inside the unreachable historical `#if 0` installer. The repair is now a dedicated production compatibility module: it validates the current EXE's complete getter/manual-pickup prologues, hooks only `sub_24A1BC0` and `sub_24B2840`, and lazily constructs the missing 260-byte kind-366 container only for the controlled actor while dungeon state is exactly 3. Native eligibility checks, request construction, inventory ownership, and the server pickup transaction remain unchanged.
- Release/Win32 rebuilt with zero errors and the existing eight deprecation warnings. Release, packaged, IDA-test, and installed DLL copies are byte-identical at SHA256 `D441D53624C9177CD3F846D3AFF2BD1CC63FFCE1493F19A0C41FD0D51EFF9AFA`, and the strict `90cn-decode-bypass-v1` manifest pins that hash. The preceding DLL and manifest are recoverable from `runtime/backups/client-patch/20260801-1739-pickup-hook-production-install`. Fresh client PID `17960` passed strict launch validation and logged both hook installations plus `dungeon_pickup=1`; the running DNF90Server PID `20300` and portable MySQL were not restarted.

## 2026-08-01 - bind new-character preview before op359 and correct live trade/party projections

- Fresh current-client crash captures identified the remaining new-character failure at `NoPack.exe+0x1D8CCD8` inside `sub_1D8C0C0`: the preview mode-0 actor existed and the current-actor cache had been repaired, but `dword_52B5BB0` was still null when the next handler dereferenced it. The select-preview route now sends the exact binding-only class0/op2 mode-1 body immediately after mode 0 and before op359. This invokes the native `sub_2008010 -> sub_2002FC0 -> sub_2693E90` descriptor-binding path with zero equipment rows; the later full equipment-bearing mode 1 remains authoritative and unchanged. Regression coverage asserts the mode0/mode1/op359 order, owner, object key, base wire size, and zero create/update counts.
- Live class1/op19 bytes showed that allocating item offers in internal slots `1..9` was necessary but insufficient: the success ACK still echoed the request's destination slot `0`, so the offering client rendered item count `1` in the gold row while the peer correctly received item slot 1. Item deposits now rewrite only the ACK destination slot to the server-assigned trade row. Gold continues to use slot 0. A cancellation regression verifies the exact current-EXE class0/op16 return map (`count=1, trade_slot=1, source_list=0, source_slot=97`) and confirms both trade registrations are cleared; this also prevents the native cancel handler from looking up a row the ACK never created.
- Fresh two-client traces disproved the preceding `actor+0x498` menu-field hypothesis: the field was already zero after each partyless peer's kind-3 packet. The actual stale state came from the local actor's initial-town path, which unconditionally sent a kind-0 zero-member party record on every partyless login. Initial town now projects an authoritative kind-3 record when runtime `PartyID` is absent and reserves kind 0 for explicitly active parties. The unproved DLL field write and its op9 parsing branch were removed; remote actor visibility and the bounded mode0 current-actor compatibility repair remain unchanged. Route, return-select, reconnect, and exact kind-3 body regressions cover the new invariant.
- Required `gofmt`, `go list -buildvcs=false ./...`, all DNF-module tests, the complete `dnfbridge` and control suites, required `go vet`, exact production server build, and Release/Win32 client build passed. BAT-only offline and live preflights passed after replacement. Deployed `DNF90Server.exe` SHA256 is `90BB539215878AFA7758297292B8E5479A4A8296AFE9074AF84794C1559F129B`; Release, packaged, IDA-test, and installed `90CN.dll` copies are byte-identical at SHA256 `DB36056594BE0509F21DB9ADFE0493A7D07AAFB36854F79AAF5940B69FBAF4E5`, and the strict compatibility manifest pins that hash. Previous binaries are recoverable from `runtime/backups/client-server/20260801-preview-binding-trade-ack-partyless-local`. DNF90Server PID `20300` and portable MySQL PID `9912` are READY with all 120 listeners accepting connections.

## 2026-08-01 - complete new-character actor binding, partyless peer menu, and trade item slots

- Fresh current-client exception evidence disproved the preceding mode-0 packet-byte rewrite. The new character's mode-0 row completed with a context actor but left `sub_269A050`'s object-manager current-actor cache empty; the next native state handler reached `sub_34DBE70` through `sub_1DECD90` and raised `0xC0000005` while reading null `+0x2C`. The compatibility DLL no longer edits the packet descriptor. It applies the already-audited current-EXE mode-0 owner comparison only to the established context-zero current-scene sentinel, then verifies and fills the exact native current-actor cache from the created global/context-zero actor after op2 completes. Nonzero remote contexts are unchanged.
- A partyless peer now has its native member-side party owner cleared after the authoritative class0/op9 kind-3 record. Current `sub_26A14D0 -> sub_26A13A0` normally performs that `actor+0x498 = 0` assignment while enumerating a matching party object; a newly created partyless actor has no such object to enumerate, which left the peer menu on “join party”. The repair is bounded to kind 3, the exact object key/owner lookup, and the current-EXE field used by the menu; active kind-0 party records are untouched.
- Fixed the item-trade pseudo-container collision observed live. Slot `0` is reserved by the current client for the character-gold row, but the server allocated the first ordinary item offer there, so a count-one item rendered as gold `+1` and no item. Ordinary offers now allocate slots `1..9`; gold remains slot `0`, removal and final transfer mappings preserve those distinct slots, and the atomic repository transaction is unchanged.
- Focused and complete DNF bridge/module/control tests, `go list`, required `go vet`, scoped formatting/diff checks, the exact production server build, and Release/Win32 client build pass. Deployed `DNF90Server.exe` SHA256 is `B49897B71C239AF41DAEF61AB399F4F01AD7137959672DBB344655D84B0B4B57`; Release, packaged, IDA-test, and installed `90CN.dll` copies are byte-identical at SHA256 `8E9C8B934538A7F6EB0DCA010E41AE6292C97045D2CC6657D193C5E395983559`, and the strict `90cn-decode-bypass-v1` manifest pins the same DLL hash. The previous server and DLL copies are recoverable from `runtime/backups/client-server/20260801-current-actor-party-trade-slot`; database-free and live deployment preflights passed, and the replacement server started ready as PID 22188.

## 2026-08-01 - clear partyless town state, complete item-trade handshake, and fix new-character town-entry crash

- Rechecked current `NoPack.exe` class0/op9 in IDA instead of copying 86JP behavior. A kind-0 record always creates or retains the current client's party-display object even when its slot count is zero, which leaves the target marked as joinable. A genuinely partyless same-area actor now receives the exact kind-3 record in its receiver-specific owner context. Current-EXE `sub_1D64CA0 -> sub_26A14D0` removes only that party-manager record and clears its member-to-party links; the separately owned town actor remains present. Active one-person and multi-person parties continue to receive the repository-backed kind-0 slot table.
- Corrected the remaining current-client item-trade rejection. The third op19 source field is an opaque live item-object value and is not a durable stack amount, so it is retained for diagnostics but no longer compared with repository quantity. Source list/slot, item existence, count, binding, expiry, and account-shared restrictions remain authoritative. Server-side offer rows now allocate the first free pseudo-container slot while the raw requester ACK preserves the client's destination slot zero contract, preventing later offers from replacing the first row.
- Completed the native two-stage confirmation handshake. Both participants' op24 state `1` (adding complete) is relayed as class0/op17 to both clients, allowing the current EXE to open its confirmation dialog; only both state `5` (confirmed ready) execute the existing atomic two-character inventory/gold transaction. Invalid states fail closed and cancellation/disconnect retains rollback behavior. Regressions cover mismatched opaque instance values, visible item offers, both adding-complete notifications, and final atomic commit.
- Fixed a production client-patch regression that crashed a newly created character on its first town mode-0 packet. The captured trace showed wire owner zero, no current actor, then access violation `0xC0000005` at `NoPack.exe+0x34DBE73` through `sub_1DECD90`. The scoped class0 bridge now temporarily replaces owner zero with the current EXE's native local owner only for the first current-scene mode-0 create, calls the native dispatcher, and immediately restores the packet byte; nonzero remote contexts and all server-authored character data remain untouched.
- Release/Win32 `90CN.dll` rebuilt with zero errors (the existing eight deprecation warnings remain). Release, packaged, and installed DLLs are byte-identical at SHA256 `B1871E319966069F1A1D75F452A974682D825B4E53F9858FCC2807BC10570653`; the `90cn-decode-bypass-v1` compatibility manifest pins the same hash. The previous installed and packaged DLL (`E565A4A29ED2DA726E5D2CB0475BB6CCAA2D3D34D3CB1BEA37AED96220F62068`) is recoverable from `runtime/backups/client-patch/20260801-party-trade-new-character-entry`.
- `gofmt`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, BAT-only offline preflight, strict client-profile verification, exact production build, controlled restart, and all 120 listener checks pass. Deployed `DNF90Server.exe` SHA256 is `81BCF386E71121276C1835D1F292CA8131496EB9468F4B951C909B25400464F8`; the previous binary (`5C9DB655019E2F9988E1E86D2D5CB2CBEFD71928C53827952C6B64D856784285`) and a verified copy of the new build are recoverable from `runtime/backups/server/20260801-party-trade-new-character-entry`. DNF90Server PID `24520` and portable MySQL PID `22688` are READY; fresh stdout/stderr logs are empty. The client must be fully restarted to load the new DLL before two-client and new-character acceptance.

## 2026-08-01 - atomic trade gold/item projection and authoritative town party-menu state

- Corrected the current 28-byte class1/op19 item-trade interpretation from live 23.4.15.0 traffic. List0/slot0 with item instance zero and destination list4/slot0 is the real character-gold pseudo row, so it is now projected to the peer as the current 0x77-byte class0/op15 slot0/item0/count row instead of being rejected as a missing inventory item. Gold from both participants and both inventories commit through one two-character `CharacterTrade` transaction; insufficient balance and overflow fail closed, cancellation restores the virtual source mapping, completion refreshes each durable wallet, and neither side can commit independently.
- Corrected normal item offers in the same flow. The op19 source value is the current item-row instance/amount field rather than the PVF template ID; validation now compares it with the repository-backed 0x77 row value. This removes the false rejection that left the peer trade list blank while retaining binding, expiration, account-shared-slot, stack-count, and atomic destination-capacity checks.
- Made town party menu state actor-authoritative. Initial remote actor creation now sends that actor's real runtime op9 party-slot state rather than an unconditional empty display, and every create/join/leave/kick snapshot republishes the changed actor state to all same-area peers. Explicit op12 one-person parties stay active and therefore show “join party”; a multi-member party reduced to one member is fully disbanded for both affected actors, clearing the stale pointer that made a partyless target show “join party” instead of “invite to party”. No raid command or raid state is emitted by the ordinary-party path.
- Added domain and bridge regressions for item-instance projection, the observed 100000-gold wire row, atomic gold-plus-item exchange, insufficient-gold rollback, remote actor active/empty party projections, and residual-singleton disband on leave/kick. `gofmt -l cmd internal pkg`, `go list -buildvcs=false ./...`, `go test -buildvcs=false -count=1 ./...`, the required scoped DNF/bridge/control tests, required `go vet`, exact production build, strict client-profile preflight, controlled restart, and all 120 listener checks pass.
- Deployed `DNF90Server.exe` SHA256 is `5C9DB655019E2F9988E1E86D2D5CB2CBEFD71928C53827952C6B64D856784285`; the previous binary (`A820082BF7E1A3980B3EF063B30B3020E2803B33B7653DDFFFFB06E681EC2453`) and a verified copy of the new build are recoverable from `runtime/backups/server/20260801-trade-gold-party-menu-semantics`. DNF90Server PID `13792` and portable MySQL PID `3568` are READY, startup stdout/stderr are empty, and fresh two-client menu/trade acceptance remains pending.

## 2026-08-01 - complete repository/session refactor and current-client peer trade fixes

- Completed the interrupted framework refactor without restoring the former repository monolith. Repository contracts and clone/transaction boundaries remain in `internal/modules/dnf/repository`; the in-memory and MySQL adapters, schemas, migrations, component lifecycle, and their backend-specific tests now live under `repository/memory` and `repository/mysql`. All owner/bridge tests use the explicit memory adapter, production assembly imports the MySQL adapter only from `internal/services/logic/dnf`, and the grouped `gameSession` lifecycle state compiles and passes the complete suite. No database schema/data, generated runtime configuration, PVF, DLL, or client compatibility profile changed.
- Rechecked the open 23.4.15.0 `NoPack.exe` in IDA 9.3 and implemented the current-client item-trade state machine from its handlers instead of copying old 86JP layouts. Class1/op19 moves between the real inventory and pseudo-container 4; class0/op15 publishes the exact 0x77 item row (or `item_id=0xffffffff` removal) to the peer; op24 state 5 publishes class0/op17; both ready states commit one atomic two-character inventory transaction and class0/op18 maps trade slots to destination slots. Cancel/disconnect uses class0/op16 return mappings. Bound, expired, account-shared, missing, and insufficient stacks fail closed.
- Corrected ordinary-party acceptance so class1/op11 no longer emits the unrelated class1/op705 raid-entry command after the normal response. Trade-mode peer acceptance now starts the server-owned trade session. Corrected current mode-3 peer information projection using the fields consumed by `sub_2005520`: durable PvP grade/rank plus `expert_job_type`, fixing the profession row without synthesizing character data.
- Validation passed `gofmt -l cmd internal pkg`, `go list -buildvcs=false ./...`, `go test -buildvcs=false -count=1 ./...` (including all DNF modules, complete `dnfbridge`, control, repository memory/MySQL, and architecture boundary suites), required `go vet`, exact production build, strict client-profile verification, BAT-only offline preflight, controlled restart, and online listener verification. Deployed `DNF90Server.exe` SHA256 is `A820082BF7E1A3980B3EF063B30B3020E2803B33B7653DDFFFFB06E681EC2453`; the previous binary (`E58361140D58E300EB691EC2ED160169855DBC358371A70496D7572B528A9CDC`) is recoverable from `runtime/backups/server/20260801-trade-party-repository-refactor/DNF90Server.exe`. DNF90Server PID `22480` and portable MySQL PID `5172` are READY, and all 120 listeners accept connections. Two-client party/trade/user-info acceptance remains pending.

## 2026-08-01 - framework hardening phase 2: group gameSession lifecycle state

- Extracted six cohesive field groups from the flat `gameSession` struct (dnfbridge) into named sub-structs, keeping every field type, initial value, reset point, and lock discipline byte-identical: `dungeon dungeonSessionState` (mu/runtime/runToken/deathTower), `petGrowth petGrowthClockState`, `lottery lotteryState`, `questReplay questReplayState` (accept/trigger/finish replay dedup with their lazy map initialization), `party partySessionState`, and `townPostTransition townPostTransitionState`. All 708 selector references across 72 files were rewritten via gofmt AST rules plus manual multi-key literal merges; selector counts match the pre-refactor baseline exactly.
- Lock ownership was verified group by group: `session.dungeon.mu` keeps guarding the same critical sections (combat handlers, card flow manual unlocks, character bind/unbind, death-return and elevator timer callbacks), `party.mu` still guards `Route` without nesting wireMu, and no mutex address ever escapes. Thirteen comments referencing the old lock names were updated.
- The selector/scene-bootstrap gate cluster and the initialTown cluster were deliberately left flat for a later phase.
- No database schema/data, PVF, DLL, generated runtime configuration, channel type, protocol byte, packet order, or gameplay behavior changed.
- Validation passed `gofmt -l cmd internal pkg` (clean), `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, and the exact production build. Deployed `DNF90Server.exe` SHA256 is `E58361140D58E300EB691EC2ED160169855DBC358371A70496D7572B528A9CDC`; the previous binary (`E3B9A03D9C6172745D5816EB646816560599BE798ACC90E292C687C28B9E05C6`) was superseded in place without an archive copy because this is a byte-neutral refactor. DNF90Server PID `21664` and portable MySQL are READY, all 120 listeners accept connections, and online runtime preflight passes.

## 2026-08-01 - framework hardening phase 1: PVF patch toolchain, bridge cleanup, entry dedup, boundary tests

- Extracted the PVF script-string patch capability from the supplied `new_dfo_server_Patch_source_20260730` reference into `internal/platform/pvf/patch_string.go`. `Archive.PatchScriptString` rewrites a shared ANSI string-pool reference in selected type-1 scripts without growing the pool, recompresses and re-encrypts each touched chunk (`bODy`/`grpi`/`hEAd` + guard), rebuilds the group table and header, and re-opens the archive to verify old-value absence and new-value presence. The implementation reuses the existing private `repack.go` helpers instead of duplicating crypto. Unit tests cover token rewriting, pool location, a synthetic NKPI round trip, and error branches; a `DNFBRIDGE_REAL_PVF_SMOKE`-gated real-PVF round trip ran against a `t.TempDir()` copy of the pinned Script.pvf (original untouched, SHA256 unchanged before and after). A scratch CLI lives at `cmd/scratch/pvfpatch` and never writes in place. No runtime behavior changed; the tool produces patched PVF only as explicit operator action, and any future patched asset must follow the compatibility-unit hash evidence rule.
- Converged `dnfbridge` structure without touching a single protocol byte. Deleted the dead `sendUpperGetUserInfoRosterBootstrapFixed15` variant (zero call sites; the three live variants keep byte-identical envelopes) and replaced the eleven copied PVF-preload blocks in `Service.Start` with a registration table plus a uniform loop that preserves preload order, per-catalog error text, and the exact cleanup set on failure. The full dnfbridge suite stays green.
- Removed the scaffold duplicate entry `cmd/server/dnfbridge` (byte-identical to `cmd/server/dnf90`, zero references); `cmd/server/dnf90` remains the sole authoritative game-server entry and the `dnfbridge` service name is unchanged.
- Isolated the demo game framework from the production binary: `Components`/`ComponentsWithConfig`/`GameManifest` moved from `internal/services/logic/dnf` to the new subpackage `internal/services/logic/dnf/game`, cutting the `go list -deps ./cmd/server/dnf90` closure from 539 to 519 packages. `services/logic`, `reference/player`, `runtime/{leaderboard,moderation,notice,redeem}`, `modules/samples/world`, `adminops`, and `asyncpayload` no longer link into `DNF90Server.exe`. No code was deleted.
- Added `internal/architecture/dependency_test.go`: offline AST-based boundary tests asserting `modules/dnf/**` never imports `services/**`, `services/dnfbridge` never imports `internal/platform/db` (persistence only via `dnfrepo`), and `services/logic/dnf` is imported only from the composition root tree plus an explicit allow-list (`cmd/server/doctor`, documented in-test). Stale allow-list entries fail the test; a temporary violation probe confirmed the assertions fire.
- No database schema/data, PVF, DLL, generated runtime configuration, channel type, protocol byte, or gameplay behavior changed.
- Validation passed `gofmt -l cmd internal pkg` (clean), `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge`, control, architecture, and platform/pvf suites, required `go vet`, and the exact production build. Deployed `DNF90Server.exe` SHA256 is `E3B9A03D9C6172745D5816EB646816560599BE798ACC90E292C687C28B9E05C6`; the previous binary (`7F65E333448D5BEF55B5EF91FEEFEC962EF3E1B79B1243B0CF58C7D2A86DFB88`) was superseded in place without an archive copy because the changes are byte-neutral refactors over the same source tree. DNF90Server PID `324` and portable MySQL are READY, all 120 listeners accept connections, and online runtime preflight passes.

## 2026-08-01 - merge quest-3249 terminal subquest settlement and suspicious-village elevator recognition

- Merged the supplied `梅娅女王任务3249末端子任务结算修复（没问题的不需要）_20260729_182653` package into the current quest module without taking its older baseline. `finishQuestClearTrigger` now receives the mutable quest record and commit time and returns the changed quest fields: when a quest-clear parent's remaining trigger is nonzero only because unresolved PVF children each belong to that parent, are durably active, and sit at exactly zero progress, the same settlement transaction completes those terminal children (`auto_completed`, `auto_complete_reason=quest_clear_parent_terminal_zero_trigger`, `auto_completed_by_parent`) and lets the parent finish; any child outside that proved shape keeps the settlement rejected. The existing growth-contract experience bonus, profession grants, and previous-skill clone logic introduced after the package's baseline are unchanged. Added unit regressions for terminal-child completion and nonzero-progress rejection, plus the real-PVF quest-3249 graph test (`DNFBRIDGE_REAL_PVF_SMOKE`-gated).
- Merged the supplied `疑惑之村电梯被动对象通用识别（可参考）_20260731_013750` package into the split dungeon runtime without restoring the archived `dungeon_runtime.go` monolith or `game_select_scene.go`. The suspicious-village elevator encounter is recognized purely by PVF content semantics (four event-monster positions, scroll 1111, controller 1113, summon 1112 spawning monster 56716, and the in-room special-monster actor), never by dungeon, maze, coordinate, or map ID. Once FINISH_LOADING completes inside the scoped room, the server schedules four 15-second op254 ticks (`u8 counter, u8 state`); the control monster retiring before op38 completes the encounter with state 1 (counter <= 3) or state 2, and an incomplete encounter blocks room movement with `errDungeonElevatorIncomplete`. Timer delivery follows the current death-return convention (`gameplayTimers` + session event-loop post with lifecycleToken/generation guards) instead of the package's direct timer-goroutine send.
- Layered-map navigation now handles exhausted explicit layers: `ErrLayeredIndexInvalid` at an active layer falls back to a cached-base-room return plan (`LayerReturn`, LayeredRoomFlag 1, actor rows suppressed to avoid duplicate object keys), followed by a short same-room `LayerBaseAck` revisit; new `worldmap.DungeonSession.ValidateLayeredBase`/`CommitLayeredBase` fail closed on any cached-base mismatch. `runtimeDungeonState` gained `layeredMapConsumed`, `layeredBaseAckPending`, and `suspiciousVillageElevator`; the op38 special-monster retirement path completes the elevator through `completeCurrentSuspiciousVillageElevatorLocked`. Added scope/state-machine regressions, layered-base worldmap tests, and the real-PVF elevator scope test (`DNF_WORLDMAP_REAL_PVF_SMOKE`-gated).
- No database schema/data, PVF, DLL, generated runtime configuration, channel type, or invented character/item data changed.
- Validation passed `gofmt -l cmd internal pkg` (clean), `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, and the exact production build. Deployed `DNF90Server.exe` SHA256 is `7F65E333448D5BEF55B5EF91FEEFEC962EF3E1B79B1243B0CF58C7D2A86DFB88`; the previous binary (`6DC322454AC8ADD6ADC87526E2B0B730FFA699283D7FBEC47D4621729B5C079A`) is recoverable from `runtime/backups/server/20260801-quest3249-elevator-merge/DNF90Server.exe`. DNF90Server PID `3916` and portable MySQL are READY, all 120 listeners accept connections. Quest-3249 in-game settlement and suspicious-village elevator behavior with a live client remain pending acceptance; the two real-PVF tests are gated on their smoke environment variables and were not run in this environment.

## 2026-07-31 - complete current-client peer actions and reapply name tags after equipment creation

- Completed the current-EXE class1/op10 and op11 peer-request state machine instead of treating every menu choice as a selection-only click. The bridge now decodes the exact mode-specific request lengths, sends the requester ACK, records and validates the pending inviter/mode pair, forwards the exact class0/op7 prompt to the same-area target, and returns the class0/op8 response to the original requester. Ordinary party modes update the server-owned runtime party and publish snapshots; trade mode forwards only the proved current-client protocol and does not invent a trade transaction.
- Added exact current-client handlers for remote role information and adventure information. Legacy op8 mode 3 now resolves a real same-area online target and returns that character's current mode-3 projection; op1403's two-byte target form loads the target character, account, and adventure aggregate while retaining the existing 16-byte self form. Missing, offline, or cross-area targets fail closed.
- Restored the server-owned portions of the visible peer menu: op17 private/1v1 chat acknowledges the requester and notifies the target, op290 adds an online friend row, and op118/op119 apply the client's blacklist-state mutation response. Bodies use exact bounded DSTR parsing and real repository/session identities. No new friend, blacklist, mail, or trade persistence schema was invented.
- Rechecked `NoPack.exe` name-decoration initialization. `sub_2008D80` accepts the mode0 item/expiry pair only after equipment endpoint 30 already exists with matching state. Startup previously sent mode0 before the full mode1 equipment creation, so the durable decoration was present in the repository and mode1 row but missed its one binding opportunity. Initial town state now replays the typed mode0 name-tag projection immediately after mode1; a Cera purchase sends a one-row class0/op14 endpoint-30 equipment refresh before the same mode0 projection. The server does not synthesize an item, expiry, slot, or appearance.
- Added exact regressions for mode-specific peer request/response packets, live trade forwarding, pending-state validation, remote role/adventure data, chat/friend/blacklist actions, and name-tag endpoint-30-before-mode0 ordering. `gofmt`, scoped regressions, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, exact production build, BAT-only offline preflight, controlled restart, and online listener verification pass.
- Deployed `DNF90Server.exe` SHA256 is `6DC322454AC8ADD6ADC87526E2B0B730FFA699283D7FBEC47D4621729B5C079A`; the previous binary (`1C7AF3B952BD65F62BF67DE405E2B992A2F2AC587130106487A046AC5CE9A3A3`) is recoverable from `runtime/backups/server/20260731-peer-actions-name-tag-order/DNF90Server.exe`. DNF90Server PID `8464` is READY, all 120 listeners are present, and the fresh stdout/stderr logs are empty. Fresh two-client menu and name-decoration acceptance remains pending.

## 2026-07-31 - restore normal town peers, interaction menus, and name-tag initialization

- Completed the current-EXE town co-presence ownership correction. Remote actors now use the receiving client's committed resident-town owner, so they remain native global player objects with ordinary white names rather than auxiliary/party-blue objects. The installed compatibility DLL only supplies the bounded missing visual-component/scene-slot lifecycle for those already-created actors; character identity, area membership, position, equipment, and party state remain server/repository-owned.
- Traced the current `NoPack.exe` peer-click path through class1/op10 `sub_1D11A80` and class0/op7 `sub_1D82CF0 -> sub_325FA70`. A valid same-area mode-15 click now receives its class1/op10 success marker followed by the exact nine-byte class0/op7 selection notice (`u16 target, u8 mode, u32 value0, u16 value1`) on the requesting session, allowing the native invite/info/trade menu to resolve the existing global actor.
- Replaced the obsolete post-purchase name-decoration `class0/op2` legacy subtype0/subtype1 pair. After the class1/op64 Cera-shop success ACK, the bridge now sends the current typed mode0 actor body: `sub_2008D80` receives the durable item/expiry pair for worn endpoint 30, and the same projection carries appearance slot 28 for the town name decoration. The appearance-only mode0 refresh is also sent to other registered players in the same concrete town area. Cold-login mode0 continues to load the same active equipment/character aggregate; no item, expiry, character, or PVF data is synthesized.
- Added exact packet/body regressions for peer selection and dual-client name-tag refresh, including endpoint 30, appearance slot 28, owner context, current mode0 framing, and absence of the legacy subtype packets. `gofmt`, scoped diff checks, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, exact production build, strict client-profile verification, BAT-only offline preflight, controlled restart, and online runtime preflight pass.
- Deployed `DNF90Server.exe` SHA256 is `1C7AF3B952BD65F62BF67DE405E2B992A2F2AC587130106487A046AC5CE9A3A3`; the previous binary (`7A7F8DAE77869088979C377BEE053F7D51BB981E177104C190CDA8139AD29078`) is recoverable from `runtime/backups/server/20260731-peer-menu-name-tag-current-mode0/DNF90Server.exe`. The installed compatibility DLL remains SHA256 `E565A4A29ED2DA726E5D2CB0475BB6CCAA2D3D34D3CB1BEA37AED96220F62068`. DNF90Server PID `22400` and portable MySQL PID `21424` are READY, all 120 listeners accept connections, server stderr is empty, and MySQL reports ready with only its expected startup warnings. The online listener probe produced the expected short accepted/aborted probe connections in server stdout. Existing client PIDs `24240` and `9632` remained open across the controlled restart; authenticated relog acceptance is pending.

## 2026-07-31 - register current-EXE remote town actors in native scene slots

- Live two-client object-table inspection disproved the key-only lookup bridge as the final co-presence fix. Each remote mode0 actor existed in the current EXE's owner-context map and was reused by mode1/op9/op22/op23, but only the local actor occupied one of `dword_51B2738`'s eight scene slots. Both remote actors had a null visual component at `actor+0x4C4`, so no later movement or party packet could render them.
- Current `NoPack.exe` IDA evidence identified the missing native lifecycle step. `sub_26A2980(sceneActorManager, slot, actor)` is the audited scene-slot writer, and for a non-current actor it calls `sub_2694890(actor, 0)`, which constructs the exact `actor+0x4C4` component absent in live memory. After a successful remote mode0 context create, the compatibility bridge now assigns that existing actor to the first genuinely empty native slot; it synthesizes no actor, packet field, position, character, inventory, or repository data. Class0/op6 clears only the matching assigned slot before discarding the transient owner mapping.
- Release/Win32 rebuilt with zero errors and the existing eight unsafe-CRT warnings. `go list`, every DNF-module test, the complete `dnfbridge`, control and release suites, required `go vet`, online server preflight, and strict client validation pass. Release, packaged, and installed DLLs are byte-identical at SHA256 `F03F4EEF123F04496F2906BCC4CAFD3D540B6867E252735FFE6F0DA26C50CB37`; the strict `90cn-decode-bypass-v1` manifest pins the same hash. The preceding lookup-only DLL is recoverable from `runtime/backups/client-patch/20260731-1841_town-native-scene-slot/90CN.dll`. Fresh clients PID `23288` and `8804` loaded the new scoped bridge; authenticated two-client visual acceptance is pending.

## 2026-07-31 - bridge current-EXE remote town actor lookup contexts

- Fresh two-client native-table evidence isolated the remaining co-presence defect after complete server delivery. Typed mode0 successfully created each remote actor through `sub_17B6AA0(objectKey, ownerContext)`, and matching mode1/op9 retained it, but `sub_2699A30(objectKey)` remained null. Direct current-EXE disassembly confirms both class0/op23 area state and op22 movement start with the key-only lookup and skip their update when it fails; the server cannot supply the missing owner because neither packet layout contains one.
- Replaced the read-only diagnostic with a bounded current-client compatibility bridge. A successful remote mode0 records its exact wire owner for the uint16 object key. Only while native class0/op22 or op23 handles that same key, a failed `sub_2699A30` call falls back to the existing native `sub_17B6AA0(key, owner)` object. Local/global lookups, unrelated opcodes, server-owned coordinates, actor creation, roster membership, and repository data remain unchanged; class0/op6 clears the transient mapping.
- Release/Win32 rebuilt with zero errors. The packaged and installed client DLLs are byte-identical at SHA256 `9E6B0A3E9C890083D4786E4494B5368ABEF5F4A692D7618AF37FD9D7335ADC18`, and the `90cn-decode-bypass-v1` compatibility manifest pins the same hash. The preceding diagnostic DLL is recoverable from the client-root `_codex_backups/20260731_1824_town_actor_context_lookup/90CN.dll`. Control/release tests and online runtime preflight pass; both fresh client processes report successful installation of the scoped lookup and class0 bridge. Two-client visual acceptance is pending.

## 2026-07-31 - trace current-EXE town peer object creation after complete wire delivery

- Fresh two-client acceptance still rendered neither peer after the complete `mode0 -> mode1 -> same-owner op9 -> op23` sequence. Packet evidence confirms characters `1` and `5` were both in channel `42`, town `38`, area `0`; each direction carried remote owner `1` or `5`, the full repository-backed mode bodies, a matching-owner kind-0 op9 body, op23, and continuing op22 movement. The remaining fault is inside the current EXE's actor/object lifecycle rather than bridge presence, packet order, or delivery.
- Added a bounded read-only class0 diagnostic to production `90CN.dll`. For typed mode0/mode1, kind-0 op9, op23, and op24 only, it records the native before/after results of `sub_2699A30(objectKey)`, `sub_17B6AA0(objectKey, ownerContext)`, and `sub_26A2000(sceneActorManager, ownerContext, objectKey)` plus the current actor pointer. It never creates, removes, rebinds, or edits an actor.
- Release/Win32 rebuilt with zero errors. The packaged and installed client DLLs are byte-identical at SHA256 `668072F76DBE8C5239E2A5A7B6EE292FC45413AF68536BDB361109E1DC1AF6F4`, and the `90cn-decode-bypass-v1` compatibility manifest pins the same hash. The preceding installed DLL is recoverable from the client-root `_codex_backups/20260731_1815_town_copresence_readonly_diag/90CN.dll`; control and release tests pass. Both old DNF processes were closed for installation; fresh two-client reproduction is required to collect the native object-table evidence.

## 2026-07-31 - complete remote town actor creation with same-owner op9 registration

- Fresh two-client acceptance disproved `mode0 -> mode1 -> op23` plus a complete op24 roster as sufficient: both sessions were repository-bound to characters `1` and `5` in channel `42`, town `38`, area `0`; the server sent both actor projections and all state/movement notifications in both directions, but neither client rendered the peer.
- Rechecked the current `DNF.exe` directly. Class0/op9 `sub_1D64CA0` requires an existing current actor, then resolves its kind-0 target through `sub_26A2000(sceneActorManager, ownerContext, objectKey)` and skips the record when that lookup is empty. Op9 is therefore display registration for an already-created actor, not an alternative actor creator. The previous op9-only attempt could never work, and its channel-42 owner could not resolve a mode0 remote object stored under owner `1` or `5`.
- Town co-presence now sends one identity-consistent sequence in both directions: typed mode0 creates the remote object; equipment-bearing mode1 binds it; kind-0 op9 registers that exact object using the same non-local owner and full uint16 object key; op23 then commits its area position. The sole full-area op24 and selected-self-last camera row remain unchanged.
- Extended the bidirectional regression to require exact `mode0 -> mode1 -> same-owner op9 -> op23` ordering, matching owner/object identity, and no duplicate trailing transition.
- Validation passed `gofmt`, focused ordering/identity regressions, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, exact production build, strict client-profile check, BAT-only offline preflight, controlled restart, and online runtime preflight. Deployed `DNF90Server.exe` SHA256 is `C57DE2964FC6EC1CC67CFEBEE76BBF385351976C264BE385F8C0F01ED2C2E977`; the previous binary is recoverable from `runtime/backups/server/20260731-same-owner-op9-copresence/DNF90Server.exe`. DNF90Server PID `22596` and portable MySQL PID `23852` are READY, all 120 listeners accept connections, and fresh stdout/stderr logs are empty. Two-client visual acceptance remains pending.

## 2026-07-31 - adapt 86JP's complete town co-presence bootstrap to the current client

- Compared the current 86JP `TownHandler.BroadcastAreaRosterAsync` flow against live 90CN packet evidence. 86JP creates each peer with legacy USERINFO subtype 0, follows with full subtype 1, sends op23 area state, then finishes the newcomer's snapshot with one full op24 area roster; op22 movement alone never creates a visible actor.
- Replaced the disproved class0/op9-only peer display with the current EXE's typed equivalents: repository-backed class0/op2 mode0 appearance, equipment-bearing mode1 binding, then class0/op23 state. Remote mode0/mode1 owner bytes now derive deterministically from the full character ID, never use reserved zero, and never equal the receiving session's selected-actor owner, so `90CN.dll` takes its audited remote-actor branch instead of redirecting the packet to the local actor.
- The accepted op36 transition remains the sole op24 scene transition, but now contains the complete sorted same-area roster with the selected character as the final row. Full uint16 character/object IDs remain authoritative across mode0, mode1, op23, op24, movement, party, and leave notifications; no character or channel data is synthesized.
- Added regressions for non-local remote-owner projection, bidirectional `mode0 -> mode1 -> op23` ordering and identity, the full-area op24 roster, selected-actor-last camera binding, solo suppression, and absence of a duplicate transition.
- Validation passed `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, exact production build, strict client-profile check, BAT-only offline preflight, controlled restart, and online runtime preflight. Deployed `DNF90Server.exe` SHA256 is `49CE75B7CB859BC7A38DC97302EC6A85FB16063654473199D942B84EB54832B0`; the previous binary is recoverable from `runtime/backups/server/20260731-86jp-copresence/DNF90Server.exe`. DNF90Server PID `23332` and portable MySQL PID `24108` are READY, all 120 listeners accept connections, and fresh stdout/stderr logs are empty. Two-client visual acceptance remains pending.

## 2026-07-31 - restore native op9 town co-presence and enforce Gold Card x2 grants

- Live two-client evidence disproved the attempted remote mode0/mode1 actor creation: the server delivered mode0, mode1, and op23 in both directions on channel 42, but the current client rejected the remote actors and both characters remained mutually invisible. Restored the known working current-EXE path: load the real character record, send class0/op9 kind0 with the displayed actor's committed town-channel owner and object key, then send that actor's class0/op23 area state in deterministic order to the newcomer and existing peers.
- The co-presence path still does not replay op24. The selected actor and camera owner therefore remain controlled by the existing town transition, while missing repository records fail closed instead of synthesizing a player. Party and movement state continue to bind to the same server-owned online-player entries.
- Fixed Gold Card results whose client card face showed `x2` but whose committed paid reward contained only one equipment instance. An available daily Gold Card use now doubles the frozen paid item reward: stackables use a checked doubled count, while equipment is cloned as two independent count-one instances so inventory equipment never becomes a stack. Op35/op71 presentation aggregates identical physical instances back into one item tuple with quantity two, matching the card face without weakening durable delivery semantics.
- Added regressions for bidirectional op9-before-op23 display, actor-owner/object-key identity, absence of trailing town transitions, solo suppression, stackable and equipment multiplication, two-slot equipment delivery, independent raw item data, and op35/op71 `x2` presentation. No database schema/data, PVF, DLL, generated runtime configuration, channel type, or invented character/item data changed.
- `gofmt`, scoped `git diff --check`, focused regressions, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, exact production build, strict client-profile check, BAT-only offline preflight, controlled restart, and online runtime preflight pass. Deployed `DNF90Server.exe` SHA256 is `977A94AB69798E8C393C6F04C59AD7A9ED0361768EBFB619724379C9117A4E8C`; the previous server is recoverable from `runtime/backups/gold-card-double-reward-20260731-1712/DNF90Server.exe`. DNF90Server PID `21696` and portable MySQL PID `8820` are READY, all 120 listeners accept connections, and fresh stderr is empty. Launcher PID `4708` opened a fresh client PID `7232`; two-client co-presence and Gold Card acceptance remain pending.

## 2026-07-31 - restore town co-presence, party-directory refresh, and early Crystal state

- Fixed the missing same-area remote actors that left two online characters mutually invisible. The online-player manager previously sent only the incremental class0/op23 area state for a peer that did not yet exist in the receiving scene. It now loads the real character/equipment aggregate, sends current-EXE class0/op2 mode0 and equipment-bearing mode1 with the remote owner context, then sends op23 in deterministic character order to both the newcomer and existing peers. It does not replay op24, so the selected actor and camera owner remain unchanged; a missing repository record or uncommitted local owner fails closed.
- Fixed the full party window opening without requesting its contents. After the patched ordinary-channel controller successfully creates current-EXE UI owner `9`, the compatibility DLL emits the exact audited native writer sequence `u16(98) -> u8(0) -> flush`. The server's existing request-owned op98 handler remains authoritative for the class0/op87 directory snapshot; the DLL does not construct party rows or send an unsolicited response into a closed UI.
- Fixed the persisted Crystal Contract selection still displaying `X` immediately after login. The durable op898 state now has a one-shot scene-tail stage after actor commit and before the inventory panel can open; the later client-authored op36 path is retained as an idempotent fallback. This supplies the existing DLL's deferred owner-`0xBE` native apply path early enough to render the stored selection. Account contract expiry, `premium_crystal_selection`, selected account crystal stack, op535 selection transaction, and op338 consumption remain repository-owned.
- Added regressions for bidirectional repository-backed remote actor creation and ordering, solo suppression, early/one-shot Crystal state, retry-safe deferred-tail delivery, and the updated confirmed-town-return order. No database schema/data, PVF, generated runtime configuration, channel type, or invented character/party data changed.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, Release/Win32 DLL rebuild, exact production server build, strict client-profile check, BAT-only offline preflight, controlled restart, and online runtime preflight pass. Deployed `DNF90Server.exe` SHA256 is `2E776F980E026FBE75C6D72D818408B07227C837C2F741D1243B6504C1F7E9DA`; Release/package/installed `90CN.dll` SHA256 is `2EB9020531293954A86177ED0C7A6C786EEFBDA35E33E474A03A9FAC86249AB7`. The previous server and DLLs are recoverable under `runtime/backups/copresence-party-crystal-20260731-1655`. DNF90Server PID `8124` and portable MySQL PID `21264` are READY, all 120 listeners accept connections, and fresh stdout/stderr logs are empty. Fresh-client acceptance remains pending.

## 2026-07-31 - restore premium-contract use and durable Crystal/Aura UI state

- Live packet and relational evidence separated persistence from client dispatch. Character `1` still had `aura_flag=1` and `premium_crystal_selection=4`; account premium types `22`, `27`, and `97` were active; op535 committed selection `4` and sent op898, but right-clicking the reported premium-contract items produced no client op44. This matches the previously audited current-EXE gap that had been removed when the production DLL was reduced to transport-only behavior.
- Restored the current-client contract fallback as a narrow compatibility boundary. The complete native right-click path runs first; only when it emits no op44 does the DLL resolve an exact main-bag item from the pinned runtime-PVF premium-contract allow-list and use the native scalar writers for the proved 15-byte op44 request. The op44 counter now runs independently from optional packet-body tracing, preventing a native request and fallback request from being sent in the same click cycle. The Go inventory/premium transaction remains the sole authority for validation, item consumption, expiry extension, ACK, notification, and item refresh.
- Restored durable Crystal Contract UI initialization by caching exact class1/op898 state before its inventory panel exists, then applying that selection through the current client's native `sub_1E7F810` refresh branch after owner `0xBE` is created. Live relog disproved the earlier global-only assumption: `sub_1E7FDF0` calls the same function with its apply flag clear, which restores internal state but leaves the visible “契约中” icon empty. The relational `premium_crystal_selection` column, account contract expiry, selected account crystal stack, op535 transaction, and op898 body remain server-owned and unchanged.
- Restored the already-open Aura skin slot through the matching compatibility marker. Once per town UI lifecycle, a character with durable `aura_flag=1` receives marked body `01 AURA` on class1/op863. The DLL consumes only that exact marker and defers the native unlock handler until avatar-panel owner `0xC9` exists, so login does not force-open the panel. Ordinary client-authored expansion-ticket success remains unmarked and follows the native result path.
- Added bridge regressions for the marked Aura restore, closed-slot suppression, and one-shot behavior. No database schema/data, PVF, generated runtime configuration, or gameplay grant rule changed.
- Validation passed scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge`, control, and release suites, required `go vet`, two Release/Win32 DLL rebuilds with zero errors, strict client-profile verification, production server build, BAT-only offline preflight, controlled restart, and online runtime preflight. The pre-panel-apply DLL is recoverable from `runtime/backups/client/crystal-panel-apply-20260731-162249/90CN.dll`; the previous server is recoverable from `runtime/backups/server/premium-contract-state-20260731-1604/DNF90Server.exe`. Installed/package/runtime DLL SHA256 is `5DB7ADE487AF1B51C8D8D5C5D388D1C655B9EBE2D05057355641CC7325C5408E`; deployed server SHA256 remains `B67B541518DFE06F4C8066A480291B4516A5753A2F05A0E41DD674A6808A3D6F`. Fresh client PID `15072` installed the `op898-deferred-panel-apply-and-marked-op863` and contract-use hooks; DNF90Server PID `7912` and MySQL PID `4680` remain READY, and all 120 listeners accept connections. Authenticated first-open Crystal panel acceptance remains pending.

## 2026-07-31 - settle empty boss rooms with PVF destroy-object conditions

- Merged `destroy_object空房结算修复_20260731_150019` into the split dungeon-combat owner without restoring the archived `dungeon_runtime.go` monolith.
- A variable class1/op39 may complete an already clear Boss room only when the active room, map, Boss coordinate, and maze all match; the current PVF places exactly one passive object with that ID; the maze declares exactly the same one-object `[destroy object]` clear condition; and the object ID exceeds op38's u16 target range.
- The accepted path treats the passive object as client-owned and already destroyed, sends no truncated class0/op38, and reuses the existing ordinary final-room transition so the proved `op115 -> op73` settlement order and pending-town-return cancellation remain intact.
- Added end-to-end and ownership-boundary regressions covering the real out-of-range ID `69292`, settlement entry, absence of op38, and rejection for u16 IDs, uncleared/non-Boss rooms, mismatched maps, absent/duplicate objects, and unsupported clear counts.
- No database, PVF, DLL, generated runtime configuration, or deployment script changed.

## 2026-07-31 - merge compatible inventory, progression, dungeon, and notice fixes

- Merged the missing-non-hostile-AI Green City behavior into the split extended-actor planner: an absent non-hostile AI definition is diagnosed and skipped, while missing hostile actors and malformed faction data still fail closed.
- Changed pickup placement so stackable main-inventory items merge across quick slots and their PVF bag range before a new quick-slot stack is allocated. The repository-owned pickup transaction and raw-entry finalization remain authoritative.
- Corrected the class0/op86 equipment-upgrade announcement to carry the selected character ID used by the current client's town-actor lookup; removed the stale zero ID and unproved trailing byte.
- Added PVF level-stat values to the same optimistic character-progression transaction that commits EXP, level, SP, and TP. Historical characters are repaired from the full stored Stats map when selected, without replacing unrelated fields.
- Stopped class1/op160 booster-selection packages from consuming PVF `[need material]` exchange rows. Explicit random-booster material rules remain owned by the booster definition.
- Confirmed the current split layered-map implementation already contains the supplied base-room actor-return fix, so the archived monolith was not restored.
- Added and updated focused regressions for cross-range pickup merging, progression/stat synchronization, historical stat repair, upgrade notices, and booster material handling. No generated runtime configuration, database schema, PVF, or DLL changed.

## 2026-07-30 - restore the proven single-packet town transition boundary

- A fresh cross-town return to Seria room reproduced the known current-client null-owner crash exactly: `0xC0000005` at `0x0262EBC0`, reading `0x000004C8` with `ECX=0`. The accepted op36 request targeted town `38`, area `1`, but the server followed typed op24 with a complete `mode0 -> mode1 -> op105 -> op102 -> op37 -> op30 -> op19 -> op120` player-state replay. That replay reactivated the same unbound type-3013 scene object previously identified in the current EXE.
- Restored the 27 July boundary for ordinary op36 moves, including cross-town Seria portals, and op1425 previous-village returns: persist the validated target location and send exactly one class0/op24 transition. These paths no longer send op22/op23 before op24 or rebuild the actor, creature, finish-loading, skill, and HUD owners after op24. Cold-login initialization, the working native op19 skill/equipment route, dungeon rules, and repository transactions are unchanged.
- Completed dungeon settlement returns now rebuild the selected actor with equipment-bearing mode1 rows. The reproduced op72 return previously sent `create_count=0`, leaving native equipped-item objects absent after returning to town even though mode0 still projected the character appearance; the same regression now requires the persisted equipped item to be present in mode1. Direct, death, and tutorial returns retain their existing equipment-bearing path.
- Runtime PVF and real packet evidence confirm the Hendon Myre `39/0` to Elvenguard `38/3 -> 38/0` route, its reverse border-map topology, and completed gate quests `3155/3156`. The generic cross-town handler accepts both directions; no server rejection or missing Go route was found, so no DLL, PVF, or synthetic client transition was introduced.
- Regression coverage requires town `40` to town `38`/area `1`, ordinary room moves, and previous-village returns to end after the single op24 with no trailing packet; op24 write failures still restore the previous durable location. `gofmt`, scoped diff checks, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, the production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass.
- A final combined regression covers completed dungeon return equipment reconstruction, cross-town Seria entry, ordinary cross-town movement, and the real-PVF gate-state initialization in one deployed source state; the focused suite and the complete `dnfbridge` suite pass.
- No DLL, database schema/data, PVF, or generated runtime configuration changed. The previous server is recoverable from `runtime/backups/server/seria-room-transition-20260730-100034/DNF90Server.exe` (SHA256 `35CC4B480BB0CF7BF102B86DFAEDA340FE45A78A851863DBBFB56AB9409C2D66`). Deployed `DNF90Server.exe` SHA256 `A6213D6AFA9E7353F110DD8B03FCD8285CB5958B840B0C73C3ED5F579421CB75` is READY as PID `24948`; portable MySQL is READY as PID `20516`, all 120 listeners accept connections, and fresh server stderr is empty. Fresh-client acceptance remains pending.

## 2026-07-30 - merge NPC shop op715 and equipment material exchange fixes

- Merged `NPC商店op715与装备材料兑换修复_20260730_020900` into the current split NPC-shop gameplay module without restoring the removed `npc_shop.go` monolith. Both legacy/plain-clone and upper routes now own class-1 opcode `715`, normalize only its proved four-byte transport boundary, and return the current-EXE success body containing four empty purchase-count lists.
- Current-EXE equipment buys may encode one item with wire count `0`; item-kind validation now normalizes only equipment `0/1` to one, while stackables still require a positive count and equipment counts above one fail closed.
- PVF `[need material]` exchange is now supported for equipment as well as stackables. The existing NPC-shop domain owner keeps the material debit and item grant atomic across character/account inventory, preserves fixed account cube-fragment slots, emits incremental item updates, and leaves gold untouched.
- Added protocol, route-ownership, zero-count equipment, successful/insufficient equipment-exchange, and rollback regressions. Runtime `Script.pvf` confirms observed shop equipment `116010117` requires item `10158124 x46`. No database, generated runtime configuration, or DLL behavior changed.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, the exact production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass. The previous server is recoverable from `runtime/backups/server/npc-shop-op715-20260730-0942/DNF90Server.exe` (SHA256 `2EBAAFA4B98FA4E9B4908960DB298045B2D078C234780D78C46BD26E1E8E0800`). Deployed `DNF90Server.exe` SHA256 `35CC4B480BB0CF7BF102B86DFAEDA340FE45A78A851863DBBFB56AB9409C2D66` is READY as PID `23612`; portable MySQL is READY as PID `25144`, and all 120 listeners accept connections. Server stderr is empty and stdout contains no ERROR/FATAL/panic; its WARN rows are the expected aborted socket probes from BAT listener checks. The database was stopped normally and preserved.

## 2026-07-30 - restore native op19 routing for skills and equipment moves

- The current transport DLL's bounded scene-opcode allowlist had regressed relative to every audited 25/27 July working source: opcode `19` was missing while `18`, `20`, and `105` were still admitted. That single omission blocked both class0/op19 full skill initialization and the class1/op19 `MOVE_ITEMSPACE` ACK from reaching their existing native client handlers.
- Restored only `case 19` in `IsSceneRouteOpcodeAllowed`. Skill-tree construction, quick-slot installation, item relocation, equipment/fashion/creature state, and repository transactions remain owned by the native client and Go server; the DLL still performs transport and current-EXE compatibility only.
- Removed the rejected server-side workarounds. Wear responses no longer append class0/op14 list-3 rows: the latest crash trace proved those fixed 119-byte rows lacked the native extended item blob and caused the client to consume the following row header as a huge copy length. The class1/op2 reconnect probe also no longer triggers a duplicate skill packet. The accepted 27 July server order remains authoritative: one full pre-op24 skill projection and native op19 move ACK handling.
- Release/Win32 rebuilt with zero errors. The final packaged, runtime-linked, and installed `90CN.dll` SHA256 is `5ECFF6D0E14E95F08B6831A80F9E1A367726EDD53A8074D37781CFFBA6E29609`; the strict `90cn-decode-bypass-v1` compatibility manifest pins the same value. The previous client DLL is recoverable from `runtime/backups/client/op19-route-20260730-014603/90CN.dll`.
- `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, Release/Win32 DLL build, BAT startup preflight, and online runtime preflight pass. Deployed `DNF90Server.exe` SHA256 is `2EBAAFA4B98FA4E9B4908960DB298045B2D078C234780D78C46BD26E1E8E0800`, READY as PID `15100`; portable MySQL is READY as PID `16612`. The previous server is recoverable from `runtime/backups/server/op19-route-20260730-014603/DNF90Server.exe`. A transient launch attempt occurred while the final DLL and manifest were being synchronized and was rejected before DNF.exe started; all four client files now match, and the login UI was restarted cleanly as PID `5952`. No database or generated runtime configuration changed; fresh-client acceptance remains pending.

## 2026-07-30 - open the native full party directory on ordinary channels

- Current `NoPack.exe` evidence separates two native party-directory UIs. Ordinary channels open owner `0x17B`, a six-row regional summary whose open path sends no directory request; the complete window is owner `9`, whose refresh emits class1/op98 mode `0`. Changing the channel type would incorrectly move ordinary gameplay into a PVP/competition channel class, so the channel ETC, channel type, and area mapping remain unchanged.
- The bridge now accepts only the two observed one-byte op98 requests: mode `0` for the full directory and mode `1` for the regional summary. Both produce the same request-bound, deterministic class0/op87 snapshot from active server party state; empty, multi-byte, unknown-mode, inactive-scene, and dungeon requests still fail closed. Directory join remains server-owned and merges the authoritative party state before projecting it to online members.
- Production `90CN.dll` does not hook the generic scene opener and does not build rows or own party state. One current-EXE-specific, same-length patch set changes only the ordinary controller's four owner IDs—its owner-379 existence guard, open, and two matching close paths—to owner `9`. Existing mode/group instructions stay byte-for-byte unchanged; owner 9's current initializer does not read either parameter. All four complete instruction contexts are validated before one protected write span is touched, so a client mismatch installs none of the UI patch.
- Release/Win32 rebuilt with zero errors after preserving the concurrently restored op19 route. The packaged DLL, strict `90cn-decode-bypass-v1` manifest, and installed game-root DLL all use SHA256 `5ECFF6D0E14E95F08B6831A80F9E1A367726EDD53A8074D37781CFFBA6E29609`; the immediately preceding op19-capable DLL is recoverable from `runtime/backups/client/party-directory-full-20260730-0151/90CN.dll`, and the earlier baseline from `runtime/backups/client/party-directory-full-20260730-0140/90CN.dll`.
- `gofmt`, focused party-directory tests, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, the exact production build, BAT-only offline `check --skip-database`, controlled restart, and online status/preflight pass. The database was stopped normally and preserved. Deployed `DNF90Server.exe` SHA256 is `2EBAAFA4B98FA4E9B4908960DB298045B2D078C234780D78C46BD26E1E8E0800`, READY as PID `15100`; portable MySQL is READY as PID `16612`; all 120 listeners accept connections and fresh stderr logs are empty. The previous server is recoverable from `runtime/backups/server/party-directory-full-20260730-0142/DNF90Server.exe`. Fresh-client UI and create/join acceptance remains pending.

## 2026-07-30 - restore the accepted 27 July wear and skill refresh paths

- Reverted the rejected ACK-only wear path and restored the 27 July server behavior directly. Changed equipment, fashion, and endpoint-17 moves now return the committed op19 ACK, rebuild the authoritative item containers, and publish the selected actor appearance. Pet inventory/equipment changes rebuild the pet container; equipped-creature slot 26 additionally publishes creature state and actor appearance. Redirected unequip ACKs still report the real empty destination chosen by the repository transaction.
- Restored the 27 July skill initialization call directly after the final full mode1 and creature state, before op21, op574, and typed op24. The legacy type1345 callback again owns only the deferred scene tail and Seria gauge; it no longer retains or sends a second skill projection.
- No DLL, database field, migration, generated runtime configuration, or client behavior was added. The installed DLL remains transport-only.
- `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, production build, controlled BAT restart, and online runtime preflight pass. The previous server is preserved at `runtime/backups/server/backup-direct-port-20260730-011201/DNF90Server.exe`. Deployed `DNF90Server.exe` SHA256 `2EBAAFA4B98FA4E9B4908960DB298045B2D078C234780D78C46BD26E1E8E0800` is READY as PID `7356`; portable MySQL remains READY as PID `15336`.

## 2026-07-29 - stop the legacy town-ready callback from reactivating an unbound scene object

- Fresh client crashes at 18:31, 19:17, and 22:09 shared exception `0xC0000005` at `0x0262EBC0`, with `ECX=0` and read address `0x000004C8`. Current `NoPack.exe` IDA traced the exact chain `sub_23BE010 -> vtbl+0x314 sub_23B2E60 -> vtbl+0x244 sub_2F7DB90 -> sub_262EBC0`: the type-3013 scene object was constructed with a null owner at `this+0x590`, then the legacy type1345 callback's repeated actor/finish/HUD chain allowed its update tick to dereference `owner+0x4C8`. The surviving comparison session received only the existing deferred tail and Seria gauge after type1345.
- Legacy type1345/u32(2) now accepts only the already-owned deferred select-scene tail plus the independent op413 gauge. It cannot replay mode0, mode1, op105/op102, op37, op30, op19, or op120. Initial skills are installed exactly once at the ordering preserved by all four 2026-07-27 source backups: after final full mode1 and creature state, before op21, op574, and the first typed op24. A separate accepted gate prevents an earlier heartbeat from releasing the deferred tail in the same call that commits op24; repeated or malformed type1345 packets are idempotent and silent.
- Focused lifecycle/order/retry tests, the complete bridge suite, targeted race tests, `go list -buildvcs=false ./...`, every DNF-module test, the control suite, required `go vet`, scoped `git diff --check`, and the production build pass. The BAT-controlled deployment preserved the previous server at `runtime/backups/server/seven-fixes-20260729-225251/DNF90Server.exe` (SHA256 `E412BF1670A7BC1E575B6628FBB2F36A55F4FE66FD5AC8F2AE97AF71D3C33EFC`). Deployed and independently rebuilt candidate SHA256 are both `26800DE8DD700EFCFD0D88DA8735B069CA79BEFAED5A92C6FA4D821390D9445C`; the server is READY as PID `1672`, MySQL is READY as PID `20728`, and all 120 listeners accept connections. No DLL, database schema/data, or generated runtime behavior belongs to this fix. Fresh-client acceptance remains pending.

## 2026-07-29 - merge the seven inventory, quest, production, and dungeon-experience fix bundles

- Merged `普通装备分解本次获得数量修复_20260729_2155` into the split disjoint owner/protocol without restoring the removed monolith. The durable warehouse stack remains cumulative, while op26 now reports `Granted` so the popup shows only this operation's cube/soul amount.
- Merged `任务enemy_reward_item杀怪掉落修复_20260729_191207`: active quests now include current-PVF type-1 `[enemy reward item]` rows alongside `[monster reward item]`, with the same dungeon/difficulty wildcards, bounds, roll, atomic grant, and incremental item refresh.
- Merged `任务奖励体验装备两天期限修复_20260729_2208`: `[usable period]` is resolved for equipment as well as stackables, quest-finish grants instantiate the relative period exactly once, and the persisted/raw item row carries the absolute two-day expiration.
- Merged `史诗制作机催化剂批量产量修复_20260729_160646`: catalyst output remains `completed units * per-unit make count`; `[max make item count]` no longer truncates an entire multi-output request. The existing material/carry transaction, overflow checks, and current-client ACK remain unchanged.
- Merged `技能无色小晶块账号晶体背包消耗修复_20260729_172706`: op18 accepts only the observed one-byte skill-state trailer, identifies fixed account crystal slots, and consumes them through the account+character item transaction instead of writing slot 354..359 into character inventory.
- Used `诺伊佩拉任务修复（仅供参考）_20260729_2215` only as behavioral evidence. A first op33 that observes an already persisted active-zero trigger now receives one transition success ACK without another op574 snapshot; same-session replays remain silent.
- Merged `副本杀怪与通关经验结算_成长契约即时播报与双槽位_整合最终版_20260729_222432` into the newer split dungeon runtime. Monster EXP is still committed once per accepted death, its Growth Contract bonus is shown immediately in the proved op37 slot, and op35 separately carries clear/monster Growth Contract bonuses plus monster base EXP in its proved tail field. Unproved Score fields stay zero, and the current runtime-PVF premium percentage plus durable settlement receipt remain authoritative.
- Added focused regressions for every imported behavior and synchronized stale town/deferred-tail tests with the current type1345 and one-shot skill lifecycle. No archive monolith, database schema, generated runtime configuration, or DLL behavior was introduced.
- `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, all DNF-module tests, the complete `dnfbridge` and control suites, the required `go vet` set, the production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass. The portable database was preserved. The previous server is recoverable from `runtime/backups/server/seven-fixes-20260729-225251/DNF90Server.exe` (SHA256 `E412BF1670A7BC1E575B6628FBB2F36A55F4FE66FD5AC8F2AE97AF71D3C33EFC`). Deployed `DNF90Server.exe` SHA256 `26800DE8DD700EFCFD0D88DA8735B069CA79BEFAED5A92C6FA4D821390D9445C` is READY as PID `8120`; portable MySQL is READY as PID `7136`, and all 120 listeners accept connections. Server stderr is empty and stdout contains no ERROR/FATAL/panic; the online port probe produced only expected short-connection WARN rows. MySQL reached `ready for connections` with its existing self-signed-CA and deprecated `skip-symbolic-links` warnings.

## 2026-07-29 - preserve the receiving member's actual op9 party slot

- Fresh ordinary-channel `game-000240` and special-channel `game-000483` traffic both carried one party member in slot `0` but ended the class0/op9 actor record with `00 01 00 00`. Current `NoPack.exe` IDA at `sub_1D64CA0` proved that the second post-slot byte is not a generic active flag: it is stored as the selected party-member slot and passed to `sub_26A0300`. The stale value `1` therefore selected an empty slot and contaminated the later replacement/removal path that emits `组队信息出现异常`.
- The bridge now derives that byte independently for every receiving session by locating its real character ID in the bounded, deduplicated member projection. A one-member party writes slot `0`; a second member receives slot `1`; an active projection that does not contain its receiver, including an empty active member list, fails closed without writing a fabricated frame. The outer op9 owner remains the committed town owner: IDA proved that an owner equal to the native current context resolves the global/current actor, while changing it to `0` would force the context-object path and risk a duplicate actor.
- Every projected receiver/member is captured together with its selected-character generation under the online-session registry and revalidated immediately before the write. A concurrent character switch or unbind cancels the complete `op9 -> op153 -> op7` projection rather than leaking a stale op9 or a suffix-only half sequence into the replacement character session.
- Unit and online-session regressions pin tails `00 00 00 00` and `00 01 00 00` for the leader and second member respectively, verify receiver-relative slots `0/2/1` after a three-member reorder, and require missing, empty, or unbound receiver paths to write no bytes. Focused race tests, the complete bridge suite, and scoped `git diff --check` pass. No unsolicited op87 snapshot was restored, no database or generated configuration changed, and the DLL remains transport-only.
- `gofmt`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, focused race tests, required `go vet`, the exact production build, and BAT-only offline/online preflights pass. Portable MySQL was stopped normally for the offline preflight and restarted with its data preserved. Deployed `DNF90Server.exe` SHA256 is `26800DE8DD700EFCFD0D88DA8735B069CA79BEFAED5A92C6FA4D821390D9445C`, READY as PID `1672`; portable MySQL is READY as PID `20728`, and all 120 listeners accept connections. Fresh server stderr is empty and stdout contains no `ERROR`, fatal, or panic entry; its WARN entries are the expected aborted socket probes emitted by BAT listener checks. MySQL reached `ready for connections` with only its existing self-signed-CA and deprecated `skip-symbolic-links` warnings. The pre-restart binary is preserved at `runtime/backups/server/party-selected-slot-20260729-225528/DNF90Server.exe` with the same deterministic SHA256. No DNF client process was running during deployment.

## 2026-07-29 - make unequip placement and town actor recovery authoritative

- Current-client wear requests may repeat the same apparently empty inventory endpoint when the preceding unequip has not yet redrawn the open bag. The equipment owner now resolves that stale endpoint inside the character item transaction: when the nominated target is already durably occupied and the client still reports an empty instance, it scans forward and then wraps within the same current-EXE page, commits the first real empty slot, and returns that actual slot in the op19 ACK. Page ownership is kept strict: main equipment `9..64`, avatars `0..209`, creature bodies `0..139`, creature artifacts `140..188`, and guild medals `0..48`; a full page fails closed and never spills into quick, consumable, material, guardian-gem, or another pet page.
- A committed wear mutation now prebuilds all five repository-backed container projections before writing any op13 packet, then publishes the native-owner mode0 wear state only while the selected town actor generation is ready. A failed repository read cannot leak a partial list set or a stale mode0. Equipped-creature changes retain their dedicated op105 and optional op102 suffix. Domain and bridge regressions cover consecutive ordinary-equipment, avatar, and creature unequips that repeat the first bag slot, require the second ACK to carry the next real slot, and verify inventory, equipment, pet, list, and appearance state.
- Every typed op24 town generation now owns the ordered `mode0 -> mode1 -> op105 -> optional op102 -> op37 -> op30 -> op19 -> op120` cursor. Direct dungeon/selector returns, op15, op35, and ACK-only op37 consume the same confirmed generation; missing character/equipment/creature/skill data and wire failures advance only after a successful stage. op35 resumes a confirmed return even after its dungeon runtime was detached, while op37, ordinary op36, and previous-village transitions propagate an incomplete suffix instead of silently leaving the client falsely ready. The deferred select-scene tail has the same per-write suffix cursor.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, targeted race tests, and required `go vet` pass. Candidate `DNF90Server.next.exe` SHA256 is `E412BF1670A7BC1E575B6628FBB2F36A55F4FE66FD5AC8F2AE97AF71D3C33EFC`. No database field, schema migration, generated runtime configuration, or DLL behavior was added; the DLL remains transport-only.
- BAT-only offline and online preflights plus the controlled restart pass. Deployed `DNF90Server.exe` SHA256 is `E412BF1670A7BC1E575B6628FBB2F36A55F4FE66FD5AC8F2AE97AF71D3C33EFC`, READY as PID `1984`; portable MySQL is READY as PID `17796`, and all 120 listeners accept connections. Fresh server stderr is empty and its stdout has no error/fatal/panic entry; MySQL reached `ready for connections` with only its existing self-signed-CA and deprecated `skip-symbolic-links` warnings. The preserved previous server is recoverable from `runtime/backups/server/unequip-town-recovery-20260729-215707/DNF90Server.exe` (SHA256 `1C3D949D0FC3D4BAF25FE6B847023CE74738D3A0E09CFD44FF9EAE3995CB69D2`). The database and running DNF client process were preserved.

## 2026-07-29 - make ordinary-party directory snapshots request-bound

- Fresh create-party acceptance moved past the prior op87 directory-type failure, then reproduced a new client access violation at `0x034457EF`. The exact wire sequence was class1/op12, class0/op9, class0/op153, class0/op7, and an unsolicited class0/op87; the client closed only after op87. Current `NoPack.exe` IDA traced the crash through `sub_217E5A0 -> sub_3445AF0 -> sub_3445690`: the directory notification ran with event `6` while the optional party-directory UI resource returned by virtual slot `+96` was null, and the unchecked downstream lookup dereferenced `0x0000063C`. The corrected ordinary discriminator `2` was present, so this is a separate lifecycle defect rather than a row-layout regression.
- Party creation, directory join, invite acceptance, direct entry, member movement/removal, leave, and disconnect no longer broadcast op87 into inactive clients. These mutations continue to send only their authoritative ACK and current-party frame/realtime/member projections. Absolute op87 snapshots remain server-owned and are emitted only by the existing client directory refresh request path while the town session is ready. No party business was moved into the DLL.
- Regressions require create and directory-join flows to end without unsolicited directory bytes while preserving the explicit refresh snapshot and exact current-party state sequence. The database and generated runtime configuration are unchanged.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass. The database was preserved. Deployed `DNF90Server.exe` SHA256 is `19B39454089E7A5DA5758C15E1C98ED9A3683F12AAC919DEBEBEEA468530A32E`, READY as PID `68308`; portable MySQL is READY as PID `64500`, and all 120 listeners accept connections. Fresh-client create-party acceptance remains pending.

## 2026-07-29 - install first-town skills at the live HUD boundary and preserve the native pet slot

- Fresh `game-000359` traffic proved that the repository-backed class0/op19 body was not empty: it carried 23 learned skills, quick slots `[1, 8, 190, 27, 18, 99]`, and the same 420 bytes as the historical capture. Current `NoPack.exe` IDA then showed that typed op24 destroys HUD owner `668`, while class0/op37 requires the selected actor and manager to exist and class0/op19 rebuilds that HUD skill owner. The only client-accepted lifecycle evidence remains `game-000478`: class0/op37 character state, class0/op30 completion, the same class0/op19 skill body, class0/op120, followed by repeated client op38 skill-use requests.
- Initial-town setup now prepares and validates the real skill projection before op24 but does not send it there. The client-authored type1345 HUD-ready boundary sends `class0/op37 -> class0/op30 -> class0/op19 -> class0/op120`, without fabricating a class1/op37 ACK. Each successful write commits an independent gate, so a failed op30/op19/op120/gauge write resumes only the missing suffix; repeated type1345 packets cannot reinstall the skill manager. Regression tests pin the historical 420-byte body and SHA256 `167DF43EF14DF2019D676EF10B2032D0581355C309FBCA2FAF34057B52C51AF6`, forbid pre-op24 op19, require the HUD-ready order, and cover every segmented write failure.
- The latest pet swap had already committed the correct `dnf_pets.equipped_key`, equipment slot 26, returned old pet, and town appearance. IDA of the class1/op19 success handler showed that it owns the native endpoint relocation and UI refresh. Slot-26 equip, swap, and unequip therefore keep `ACK -> mode0 -> op105 -> optional op102` but no longer replay item-list op13 for lists `0/1/2/7/38`, which rebuilt the list-7 manager and discarded the visible equipped-slot object. Pet artifacts in slots 27-29 retain their container refresh. No op14/list3 double-install, database field, schema migration, or DLL behavior was added.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, and required `go vet` pass. BAT-only offline and online preflights pass; the database was preserved. The previous party-directory build is recoverable from `runtime/backups/server/skill-hud-pet-slot-20260729-1815/DNF90Server.exe` (SHA256 `D5A4D966F7FA6A1328589096D2D50988DBCC568E57C8739287D62111B5431028`). Deployed `DNF90Server.exe` SHA256 is `19B39454089E7A5DA5758C15E1C98ED9A3683F12AAC919DEBEBEEA468530A32E`, READY as PID `37208`; portable MySQL is READY as PID `43180`; all 120 listeners accept connections. Fresh-client skill-bar and pet-slot acceptance remains pending.

## 2026-07-29 - stop ordinary-party directory snapshots from indexing past the current client container

- Fresh create-party traffic reproduced the client access violation at `0x00FD5C89` immediately after class0/op87. The 30-byte row carried `00 0C 00 FF` after the party ID, and the crash register held `EBX=0x0A`. Current `NoPack.exe` IDA proved that `sub_1D3F040` treats the second byte in that group as a directory discriminator, decodes a protected logical base value `2`, subtracts it, and passes the result to `sub_26C57F0` as an unchecked map-array index. The prior service incorrectly wrote resident channel `12`, producing exact invalid index `12-2=10`; changing only the following `specialState` byte could not prevent this crash.
- The op87 builder now owns the current ordinary-directory discriminator and always writes `2` (container index zero). `DirectoryRecord` no longer exposes the field as `ChannelID`, and the bridge no longer suppresses otherwise valid parties merely because their resident channel cannot fit in an unrelated `u8`. Party IDs, selection IDs, six member slots, the `specialState=FF` ordinary sentinel, and the `FF/FFFF` empty-slot sentinels remain unchanged. The transport-only DLL was not changed.
- Exact-body regression requires `directoryType=2`; bridge regressions prove directory projection is independent from leader/member resident channel IDs, including a channel above `255`. `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, and required `go vet` pass.
- The database was preserved. BAT-only offline/online preflights and controlled restart pass; all 120 listeners accept connections. The previous server is recoverable from `runtime/backups/server/party-directory-type-20260729-1741/DNF90Server.exe` (SHA256 `5A1D0AF8099CA93951C6E022A4CDF35BA3A1CF490CE927F293FA56DAD62E4538`). Deployed `DNF90Server.exe` SHA256 is `D5A4D966F7FA6A1328589096D2D50988DBCC568E57C8739287D62111B5431028`, READY as PID `52012`; portable MySQL is READY as PID `57048`. Fresh-client create/list acceptance remains pending.

## 2026-07-29 - restore first-town skills and bind the equipped pet after mode0

- Fresh `game-000409` first showed that cold login cannot wait for op36 because this client may never emit one. The first attempted correction moved the prepared 420-byte class0/op19 projection to type1345 after the initial op24. Fresh acceptance `game-000240` then falsified that boundary: packet 102 successfully wrote all 23 learned skills and quick slots `[1, 8, 190, 27, 18, 99]`, no later op24/mode/skill packet overwrote it, yet the client did not apply it and later crashed with `C0000005`. The exact 420-byte body is byte-identical to the same-character historical success; only the lifecycle differed. The four 2026-07-27 snapshots and the successful capture install op19 after final full mode1 and creature op105/op102, before op21/op574 and the first typed op24. Cold login now restores that proved server-owned order. Type1345 owns only the remaining scene tail, rental op985, and Seria-luck op413; later successful op36 routes retain their already-initialized-manager refresh.
- The same live session showed correct durable creature state but the wrong client-object order. MySQL had slot 26 item `400990199`, serial/creature key `11`, and `pets.equipped_key=11`; the server nevertheless sent `op105 -> op102 -> mode0`. Current op102 carries only level/mode/experience and therefore depends on mode0 having already created target 26, while sending mode0 last detaches the newly rebuilt target from the creature table. Runtime slot-26 changes now use `op19 ACK -> item lists 0/1/2/7/38 -> mode0 -> op105 -> optional op102`. Unequip sends an empty mode0 followed by op105 and no op102. Pet artifacts and ordinary equipment/fashion paths remain unchanged.
- Added bridge and domain regressions that require exactly one cold-login skill packet in the order `full mode1 < op19 < op21 < typed op24`, forbid a duplicate skill packet at type1345, retain later op36 compatibility, and lock pet equip/unequip ordering, equipped mode0 creature identity, and absence of growth op102 after unequip. No database field, schema migration, client DLL behavior, mode1 actor rebind, op14 row, or op357 redraw was added.
- `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline/online preflights, and controlled restart pass. The database was preserved. The previous server is recoverable from `runtime/backups/server/skill-preop24-20260729-172215/DNF90Server.exe`; deployed `DNF90Server.exe` SHA256 is `5A1D0AF8099CA93951C6E022A4CDF35BA3A1CF490CE927F293FA56DAD62E4538`, READY as PID `64340`; portable MySQL is READY as PID `61328`; all 120 listeners accept connections and the fresh server/MySQL stdout/stderr logs are empty. The installed transport-only `90CN.dll` was not changed. Fresh-client acceptance remains pending.

## 2026-07-29 - restore audited multi-client startup compatibility

- The launcher already opted each client into multi-client mode with `DNF_MULTI_CLIENT=1`, and the current-EXE `CreateMutexW` / `FindWindowW` compatibility hooks remained implemented, but the production transport worker no longer installed them. The first client therefore remained online while the second PID exited during the launcher's five-second startup check, before opening a game connection.
- `InstallTransportWorkerInner` now installs only those two audited single-instance hooks. Normal launches remain a no-op, and the production DLL still does not install UI, inventory, party, character, scene, camera, or other game-business hooks. The service and database were not restarted or changed.
- Release/Win32 rebuilt with zero errors. `client-patch/bin/90CN.dll`, the strict `90cn-decode-bypass-v1` compatibility manifest, and the installed game-root DLL now use SHA256 `3539703314BD28E05EA842004225CCFCFE1F2A34D5F551398E7E4E0CA4FF6F25`; the displaced DLL is recoverable from `runtime/backups/client/multi-client-startup-20260729-163845/90CN.dll`.
- Launcher, control, and release tests pass; the manifest parses, scoped `git diff --check` passes, and online `STATUS.bat` reports MySQL PID `21808`, server PID `66648`, valid configuration, and all 120 listeners accepting connections. Live account-1/account-2 launch produced stable DNF PIDs `63912` and `58468`; both connected to LAN game port `10012`, and each trace records `mutex=1`, `window=1`, both isolated audited branches, and `multi-client startup compatibility result=1`.

## 2026-07-29 - stop ordinary-party op87 from entering the optional complete-display path

- Fresh `game-000240` traffic isolated the create-party exit to the first server-owned class0/op87 directory snapshot: class1/op12, class0/op9, class0/op153, and class0/op7 all completed, then the client closed immediately after the 30-byte op87 row. The server and portable database remained healthy.
- Current `NoPack.exe` IDA at `sub_1D3F040` proved that the fourth byte after the party/channel identifiers is a signed special-state field. `FF` is the ordinary-party sentinel; the previous neutral `00` entered the optional complete-display subsystem and caused the client crash. The following `u32`, stored at the party projection's `+88`, is the op12 `selectionID`, not `targetDungeonID`.
- The service-owned directory builder now emits `specialState=FF`, preserves the exact occupied and `FF/FFFF` empty member-slot layout, and projects the leader's `SelectionID`. Exact-body tests pin the `FF` sentinel and field order; leader, refresh, create, join, invite, movement, leave, and disconnect bridge regressions pass. The transport-only DLL and `client-patch` were not changed.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, candidate build, BAT-only controlled restart, and online runtime preflight pass. The database was preserved. The previous server is recoverable from `runtime/backups/server/party-op87-crash-20260729-1629/DNF90Server.exe`; deployed DNF90Server SHA256 is `19B467F6F31434EA06BC0747934848731849A667856DAD388BB1F54A8D939FEA`, READY as PID `66648`, and portable MySQL is READY as PID `21808`. All 120 listeners accept connections and fresh server/MySQL stderr logs are empty. The installed transport-only `90CN.dll` remains SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`.

## 2026-07-29 - restore the proven post-op19 wear-status projection

- Fresh `game-000240` traffic, durable MySQL state, and current `NoPack.exe` IDA proved that the latest weapon swap, fashion equip, and creature unequip all committed correctly and received the exact 11-byte class1/op19 success body. The regression was the preceding ACK-only change: `sub_232BE10` applies the local move only while the selected container manager still has a matching pending move, so a missing/stale local object graph leaves the persisted server state invisible and makes the next wear click unusable.
- All four `releases/DNF90-source-oneclick-20260727*.zip` snapshots contain the same known-working server sequence, and historical packet logs show the client continuing to issue reverse op19 moves after it: `op19 ACK -> class0/op13 list 0/1/2/7/38 -> class0/op2 mode0`. Creature slot 26 uses `ACK -> the same five containers -> op105 -> optional op102 -> mode0`; pet artifacts refresh the authoritative containers without an actor redraw. The exact ACK body, durable equipment owner, endpoint-17 normalization, codec, database schema, and transport-only DLL remain unchanged.
- `PostActionRefreshSelectedActorAppearance` now has a dedicated wear path that emits one repository-backed mode0 current-scene object. It no longer substitutes op357 and does not append full mode1. Generic list-3/op14 equipment rows remain suppressed. Existing full mode0+mode1 helpers are retained only for callers that explicitly own a full actor rebind.
- Regressions cover ordinary equipment, fashion, alternate worn endpoint 17, creature slot 26, and artifact slots 27 through 29 across equip/unequip/re-equip, plus exact bridge ordering and absence of op14, op357, full mode1, and skill replay. `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production build, BAT-only offline/online preflights, and controlled restart pass. The database was preserved. The previous server is recoverable from `runtime/backups/server/wear-status-projection-20260729-161909/DNF90Server.exe`; deployed `DNF90Server.exe` SHA256 is `4CB2805040E530F185EC3E931269088E54847C3F0A8E6FA24B8AE68118B730C7`, READY as PID `17304`; portable MySQL is READY as PID `67316`; all 120 listeners accept connections and fresh stdout/stderr logs are empty. The installed transport-only DLL was neither rebuilt nor replaced and remains SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`. Fresh-client acceptance remains pending.

## 2026-07-29 - restore native wear ownership and rebuild skills after every town transition

- Fresh `game-000240` traffic and current `NoPack.exe` IDA corrected the preceding wear workaround. The class1/op19 success handler already moves or swaps the live inventory/equipment objects, attaches or detaches the worn component, and refreshes the open UI. A following class0/op14 replaced the same row a second time, which left an equipped item visible in the backpack or hid an unequipped item and made the next click unusable. Class0/op357 is a user-state application rather than an appearance-only refresh: even with unchanged bits it detaches and reattaches display components 0 through 8, duplicating the lifecycle already completed by op19.
- Ordinary equipment, fashion, alternate worn endpoint 17, and pet artifacts now stop at the exact 11-byte class1/op19 ACK. Equipped creature slot 26 uses `op19 ACK -> op105 -> optional op102`; it no longer receives an op14 row or op357. The ACK preserves the current client's positional contract (`A = non-worn container`, `B = worn endpoint 3/17`) and reports any owner-selected real empty A slot. No complete container, mode0/mode1 actor replacement, database field, or DLL-side business hook was added.
- IDA also proved that the 420-byte length-prefixed skill protobuf is already correct and that the timing was wrong. Every class0/op24 handler calls the scene teardown that removes HUD/skill owner 668, so the former type1345 op19 was erased by the client's later op36 transitions. Type1345 now keeps only its deferred scene tail and Seria-luck op413. Every successful `op36 -> op22 -> op23 -> op24` immediately rebuilds and sends the selected character's repository/PVF-backed class0/op19; consecutive town-area transitions each close with a fresh skill projection. A post-op24 build/write failure is logged without rolling back a location and op24 that already reached the client. The existing one-shot crystal-contract gate remains unchanged, so this does not restore the duplicate contract initialization.
- Added same-handler, same-repository `wear -> unwear -> wear` regressions for ordinary equipment, permanent fashion, creature slot 26, and all three pet-artifact slots 27 through 29. Every step requires the exact ACK, zero item-row refreshes, zero op357, and consistent inventory/equipment/pet persistence. Bridge regressions require wear ACK-only behavior, the creature-state-only extension, type1345 with no skill op19, two consecutive `op22/op23/op24/op19` sequences, and no rollback after a fourth-packet skill write failure.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass. The portable database was preserved. Deployed `DNF90Server.exe` SHA256 is `7C1F6D44F3E88987ECBAE8A432DC3E65698D23A5928413EC3B3D38FBD1D1FBE7`, READY as PID `64084`; portable MySQL is READY as PID `62808`; all 120 listeners accept connections; fresh stderr is empty and there are no critical server log entries. The deployed image is recoverable from `runtime/backups/server/wear-native-skill-final-20260729-153644/DNF90Server.exe`. The transport-only installed `90CN.dll` was neither rebuilt nor replaced and remains SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`.

## 2026-07-29 - restore the server-owned public party directory on ordinary channels

- Current `NoPack.exe` evidence corrected the ordinary-channel create parser: class1/op12 is exactly `mode:u8, namePreset:u8, optional DSTR name, memberCode:u8, selectionID:u32, selectionCode:u8, selectionValue:u16, recruitFlag:u8, targetMode:u8, targetDungeonID:u16`. The server now rejects legacy/trailing shapes instead of shifting the target and member fields, and active party IDs use the selected leader's real `u16` character ID rather than the historical `100000+` values that truncated in the client's directory and join packets.
- The Go bridge now owns the complete runtime directory lifecycle. Successful create/update publishes the current client's absolute class0/op87 snapshot (`u16 count` plus fixed 28-byte rows), op98 refresh rebuilds that snapshot from online server party state, and op90 directory entry resolves and merges the selected server-owned party before refreshing member frames. Create, invite/entry, member movement, leave/kick, character switch, and disconnect all republish or remove the directory row deterministically; stale peer writes cannot roll back an already accepted mutation.
- Ordinary and special channels now use the same service-side party state and packet projection. Directory rows use the leader's current channel and target dungeon, neutral occupied-slot state, and the client's exact `FF/FFFF` empty-slot sentinels. Channels that cannot be represented by the current op87 `u8` field are suppressed instead of silently truncated.
- Added exact-body, create-sequence, refresh, leader-projection, and cross-session directory-join regressions. The transport-only DLL and `client-patch` were not changed; connection bootstrap, channel jump, packet codec, DPROTO, inbound routing, and read-only diagnostics remain its only responsibilities.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge suite including three consecutive uncached passes, the control suite, required `go vet`, candidate build, BAT-only offline/online preflights, and controlled restart pass. The database was preserved. The previous server is recoverable from `runtime/backups/server/party-directory-20260729-152338/DNF90Server.exe`; deployed DNF90Server SHA256 `84CC7F62CCC9762D3E6A30858D888A5938B5BFAEE2B00CE60368ABEB6085DB12` is READY as PID `65440`, portable MySQL is READY as PID `57284`, all 120 listeners accept connections, and fresh server/MySQL stdout/stderr logs are empty. Both source and installed transport-only DLLs remain SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`.

## 2026-07-29 - restore the top-left Seria luck HUD and live progress refresh

- **Superseded by the 2026-08-03 current-EXE registration audit:** the cited op413 fixture came from legacy/old-client evidence. Current `NoPack.exe` has no class0/class1 op413 handler, so the projection described below has been withdrawn and must not be restored without new current-client evidence.

- The missing icon is the top-left `sprite_interface2_hud_luck_gauge` / `hud_luck_icon` display for “赛丽亚的幸运,” not the rental lucky-star wallet. The local 86JP initialization capture stores class0/op413 (`0x019D`, `BOOSTER_GAGE`) as the exact two-byte body `00 19`: gauge type `0`, progress `25`. Combined with the existing account-domain maximum of `8`, this proves the current EXE expects a `0..100` percentage in the second byte; for example persisted luck `4` projects as `00 32` (50%).
- Initial town entry now reloads the authenticated account's persisted `seria_luck_value` and sends the absolute gauge at the exact legacy type1345/u32(2) HUD-ready boundary, after the prepared skill projection. Missing repositories/accounts and invalid metadata remain write-silent instead of inventing state, while normal absence of the metadata key projects the domain default `0`.
- Successful Seria item `2682272` opens schedule a bridge-owned absolute gauge refresh after their native ACK and committed inventory refresh. This applies to both op208 single-open and op1128 batch-open, reloads the post-transaction account value, and does not run for ordinary boxes or failed mutations. The domain still owns the `0..8` accumulation/doubling rule; the bridge alone owns the current-EXE class0/op413 percentage wire format.
- Added regressions for percentage/clamp mapping, the persisted value `4 -> 50%` login packet and duplicate type1345 suppression, post-action reload of a committed value, and Seria-only scheduling for both single and batch opens.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass. The portable database was preserved. The previous server is recoverable from `runtime/backups/server/seria-luck-hud-20260729-141842/DNF90Server.previous.exe`; deployed DNF90Server SHA256 `E8A3FAF3EC21D87876DB18A5F6A882F501811617F9A7A4542EFA99717C62C3CB` is READY as PID `32640`, portable MySQL is READY as PID `24156`, all 120 listeners accept connections, and the fresh server stdout/stderr logs are empty.

## 2026-07-29 - keep wear mutations in place and flush skills at the real HUD-ready boundary

- Fresh `game-000240` evidence proved that weapon/fashion equip and unequip already committed correctly: every class1/op19 ACK was sent, equipment slot 3 persisted, weapon slot 12 cleared, and weapon `101000069` returned to main slot 31. The client nevertheless retained an equipped source row or hid an unequipped destination row, then stopped emitting later op19 requests. The missing piece was the changed non-worn item row; the following mode0/full-mode1 actor bootstrap was also replacing the live inventory/equipment managers while their window remained open.
- Changed equipment, fashion, and pet moves now keep the current EXE's native op19 mutation and publish only the committed non-worn rows through class0/op14 after the ACK: equip sends the emptied source row, unequip sends the persisted destination row, swaps cover both rows, and duplicate/no-op endpoints are suppressed. The durable owner normalizes wire endpoint 17 to equipment list 3 internally, while the ACK retains the request's raw actor-worn endpoint 3 or 17 and uses the owner's committed slot; this preserves the distinct branches in local IDA `sub_1D2DE80`. No database field or full-container replay was added.
- Ordinary visible worn slots now finish as `op19 ACK -> exact op14 row(s) -> op357 same-actor redraw`. They no longer send post-wear mode0, full mode1, or a duplicate skill projection, so the camera-bound actor and open inventory/skill managers remain intact. Equipped creature slot 26 uses `ACK -> exact list-7 op14 row -> op105 -> optional op102 -> op357`; pet artifacts keep their narrower endpoint path. Optional post-commit creature/redraw failures are logged without turning a durable successful move into a disconnect.
- Fresh PID `66380` sessions prepared a valid 420-byte skill body with 23 learned skills and quick slots `[1, 8, 190, 27, 18, 99]`, but never sent class0/op19: after typed op24 the client authored legacy type1345 body `02 00 00 00`, which was previously discarded as unhandled. That exact proved body now owns the common scene/HUD-ready boundary. Ordinary selection completes its deferred actor-binding tail first; channel reconnect, whose tail is already complete, flushes only the retained skill projection. DPROTO/check-user readiness may complete the actor tail but cannot consume or send the prepared skill body, eliminating the early-send race. The transition-stage gate and successful-write clear make the type1345 send exactly once; malformed, early, unprepared, and repeated type1345 packets do not send or consume it.
- Added domain and bridge regressions for equipment/fashion/pet equip and unequip, committed redirected slots, raw endpoint 17 preservation, list-0/list-1/list-7 op14 rows, avatar optional blobs, pet state ordering, same-actor redraw, absence of post-wear mode0/mode1/skill packets, type1345 stage gating, malformed-body rejection, and duplicate suppression. `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, the required `go vet` set, BAT-only offline preflight, controlled restart, and online runtime preflight pass. The candidate at `runtime/bin/DNF90Server.wear-skill-candidate.exe` and deployed `runtime/bin/DNF90Server.exe` both have SHA256 `E8A3FAF3EC21D87876DB18A5F6A882F501811617F9A7A4542EFA99717C62C3CB`; the prior executable is recoverable from `runtime/backups/server/wear-skill-20260729-142131/DNF90Server.exe`. The deployed server is READY as PID `55832`, portable MySQL is READY as PID `64336`, all 120 listeners accept connections, and fresh-client acceptance remains pending. The transport-only DLL was neither rebuilt nor changed; the source and installed `90CN.dll` both remain SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`.

## 2026-07-29 - make the native channel commit and town actor owner atomic

- The fresh-client exit was a client-side `0xC0000005` at NoPack.exe RVA `0x030DBE73`, where op124 dereferenced a null selected actor. Packet and current-EXE evidence isolated the prerequisite mismatch: CHANNELINFO field 8 committed native scene channel `253`, while the following town mode0, mode1, and op9 packets still used owner `0`. The client therefore classified the selected actor as remote and never created its local actor. The equipped avatar row, full mode1 equipment state, skill payload, transport DLL, and database data were not the crash source.
- Game bootstrap now separates launcher discovery probes from the selected account-bound connection. Only an authenticated connection whose channel fits the current EXE's `u8` town-owner field sends one class0/op1 CHANNELINFO and records that same byte as both connection and current town owner. Unbound all-port probes, plus channel IDs above `255`, retain the proven proactive class1/op1 endpoint success and owner `0`; IDs `256` and `501` are explicitly covered so they cannot be truncated. The requested class1/op1 response remains one-shot after CHANNELINFO.
- Cold login, return-select re-entry, channel reconnect, completed-dungeon return, ordinary equipment/avatar refresh, party op9, runtime mode1/mode3 refresh, and quest slot-expansion mode1 now use the committed town owner consistently. Generic builders still default to owner `0`, and select preview plus pre-op27/post-op29 dungeon mode0/mode1/op9 remain owner `0`. The op9 outer owner follows the active scene while its inline-identity discriminator remains `0`.
- Regressions exercise channel IDs `253`, `255`, `256`, and `501`; require the cold-login and reconnect mode0, both mode1 packets, and op9 outer owner to agree before op124; restore `253` on dungeon return; retain `0` inside the dungeon; and keep equipment, fashion, weapon, and quest-completion refreshes on the active town actor. `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass.
- The database was preserved. The previous server is recoverable from `runtime/backups/server/channel-owner-crash-fix-20260729-131140/DNF90Server.exe`; deployed DNF90Server SHA256 `AEC2BEEA30DEFDAF92A339483781C63C66FF097D5A562C1FDDC315D395B49AC2` is READY as PID `1968`, portable MySQL is READY as PID `66312`, and all 120 listeners accept connections. Fresh server stderr is empty. The installed transport-only DLL was neither rebuilt nor replaced and remains SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`. Fresh authenticated client acceptance remains pending.

## 2026-07-29 - merge identical global and independent dungeon drops on pickup

- Merged the behavioral fix from `全局与独立掉落拾取同类叠加修复_20260729_1130` into the current DNF dungeon pickup owner instead of overwriting the newer bridge with the shared pre-extraction transaction implementation. Global-table and independent-table drops already converge on this owner, so both paths now receive the same atomic placement and persistence rule.
- PVF `StackLimit = 0` is treated as an unbounded stack rather than as “stacking disabled.” Pickup still prefers an existing same-item, unbound stack in the applicable quick/category range before allocating an empty slot; positive PVF limits and `int64` overflow remain enforced, while equipment and bound stacks remain non-mergeable.
- Added domain and current-bridge regressions proving that a zero-limit same-item drop increments the existing row and does not occupy a second slot. Existing positive-limit, quick-slot priority, category fallback, equipment, expiration projection, and rollback coverage remain active.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production build, BAT-only offline/online preflights, and the controlled restart pass. The account-bound channel endpoint fixture was updated to register its synthetic client PID, matching the current launcher/session isolation rule without changing runtime channel behavior. The database was preserved; the previous server is recoverable from `runtime/backups/server/drop-identical-stack-20260729-1259/DNF90Server.exe`; deployed DNF90Server SHA256 `A786E89258AAEC9DBE8A200F04B9CCBBFC05D341521BC86B9E1A74045A863BCD` runs as PID `63672`, portable MySQL runs as PID `65472`, and all 120 listeners accept connections. Fresh server stderr is empty; MySQL reached `ready for connections`, with only its existing self-signed-CA and deprecated `skip-symbolic-links` warnings.

## 2026-07-29 - merge the complete current-EXE epic production machine workflow

- Merged the shared `史诗制作机完整修复_op1417-op1421_20260729_0056` implementation into the current module-oriented bridge without overwriting the later split dispatch or initial-town lifecycle. The epic-production gameplay module now owns both legacy and current-upper routes for op1417 start, op1418 target change, op1419 process, op1420 material compound, and op1421 ability option, with the current default-class gate on every upper command.
- The runtime PVF is authoritative for production targets, job restrictions, catalysts, change costs, material contribution/carry, weekly limits, and ability recipes. Requests reject forged targets, duplicate slots/groups, wrong shared slots, type-only recipes that are not yet safely resolvable, locked items, invalid counts, and incomplete PVF definitions instead of inventing a result.
- Target changes, production processing, catalyst compounds, and ability application mutate character state, character inventory, and account-shared materials inside the account/character asset transaction. Success publishes bounded op44 item deltas; failures preserve all durable balances. Ability selections persist in the existing item raw `0x72` tail, while op1355 now carries target, charge, remaining weekly count, and both material carry values.
- Focused op1417-op1421 regressions and the real runtime `Script.pvf` smoke pass, including shared epic-soul consumption, compound carry, ability recipe consumption, replay/idempotency, response bodies, and initial actor-bound op1355 projection.
- `gofmt`, scoped `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass. The database was preserved. The previous server is recoverable from `runtime/backups/server/epic-production-complete-20260729-1250/DNF90Server.exe`; deployed DNF90Server SHA256 `FCF7EAC27BC6E771156ABC09E2F543675ADCAA9E59B2E77A34E4C30E5F80B186` runs as PID `53984`, portable MySQL runs as PID `64372`, all 120 listeners accept connections, and the new server/MySQL stdout and stderr logs are empty.

## 2026-07-29 - finish the channel handshake and restore post-wear actor managers

- A game TCP session now publishes one current-EXE class0/op1 `PB_ENUM_NOTIPACKET_CHANNELINFO` before accepting the client's 590/598-byte class1/op1 endpoint request, then returns one class1/op1 success. CHANNELINFO field 7 is the configured advertised server index, field 8 is the connected channel ID, and field 9 is the proven three-entry local-controller index `0`; raw directory types 22/23 remain only in the requested endpoint success. Per-session gates suppress duplicate notices, repeated endpoint probes, and the late class0/op1 sequence that previously left `ch00-未知` or armed the 600-second Error2 watchdog.
- Current-EXE analysis showed that a worn-item class1/op19 ACK followed only by existing-actor mode0 redraws the model but tears down the actor's attached equipment manager, leaving later equip/unequip clicks unusable. Successful ordinary equipment/avatar changes and equipped-creature slot 26 now use `op19 ACK -> current-context mode0 -> complete repository-backed mode1`; full mode1 reconciles all 33 worn endpoints and finalizes the same actor's interaction state. Slot 26 then publishes its dedicated op105/op102 creature state, while pet-artifact endpoints keep their narrower path.
- The repository/PVF-backed current skill protobuf was already valid, but mode0/full-mode1 actor rebinding can replace the native skill manager. The bridge therefore sends the selected character's current class0/op19 skill projection immediately after the final mode1 on a worn appearance mutation. The login projection remains one-shot in its proven post-op24 position. No skill, appearance, channel, or inventory business behavior was added to the transport-only DLL.
- `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, and the required `go vet` set pass. The production build and BAT-only offline/online preflights pass; the database was preserved, all 120 listeners accept connections, and the fresh server/MySQL stdout and stderr logs are empty. The displaced server is recoverable from `runtime/backups/server/channel-appearance-skill-20260729-123914/DNF90Server.exe`. Deployed DNF90Server SHA256 `6B7ED026CC4083E52F1277D3F52F70BAA58C4406C48EEF563F65278E5B8C2768` runs as PID `43824`; portable MySQL runs as PID `61400`. The installed transport-only DLL was neither rebuilt nor replaced and remains SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`.

## 2026-07-29 - restore the current-EXE one-shot channel endpoint handshake

- Game TCP open now sends exactly one class0/op1 `PB_ENUM_NOTIPACKET_CHANNELINFO` and waits for the current client's native 590/598-byte class1/op1 request before sending exactly one class1/op1 endpoint success. Per-session gates suppress duplicate CHANNELINFO, repeated endpoint probes, legacy-shaped op1, and any later town/reconnect replay, preserving the first-success watchdog-clear path.
- CHANNELINFO field 7 remains the configured advertised server index, field 8 is the connected channel ID, and field 9 is the proven local-controller index `0` instead of the unrestricted `channel_info.etc` type. Raw types such as 22/23 remain accepted and are carried only by the requested class1/op1 endpoint success. Focused regressions cover packet order, both request sizes, wrong-shape silence, one-shot behavior, and a real type-23 catalog endpoint.

## 2026-07-29 - refresh worn models on the bound town actor and initialize skills after HUD commit

- Fresh `game-000364` acceptance separated durable inventory movement from rendering. Weapon item `101000069` moved from worn slot 12 to main slot 31 and fashion item `412500008` moved from worn slot 3 to avatar slot 2; both transactions committed and each received one valid class1/op19 ACK. The following class0/op342 projections dropped from 18 occupied rows to 17 and then 16, but contained no explicit clear row for the removed slots. Live client state retained both old model objects, disproving the earlier assumption that an omitted group-0 row reliably clears a worn slot when the active and mirror equipment groups share an object.
- Ordinary equipment and fashion moves now finish as `class1/op19 ACK -> class0/op2 mode1 -> class0/op357`. The ACK reports both committed positional endpoints, including a server-redirected empty inventory slot. Mode1 targets the existing selected actor in local scene context `0` and supplies the complete repository-backed 0..32 presence projection; same-value op357 then invokes the current client's native in-place component redraw. The sequence is gated to a ready town actor, never sends mode0, and therefore does not replace the camera-bound actor. Dungeon moves, creature slot 26 (`ACK -> op105 -> op102`), and pet-artifact slots 27..29 retain their dedicated paths. The transport-only DLL is unchanged.
- The same live session proved skill data was present rather than empty: the server built 23 learned skills and six active quick slots, but sent class0/op19 before typed op24. Current-EXE `sub_1D6E240` fully replaces the selected actor's skill trees and quick-slot UI, while the later `sub_1D901D0 -> sub_2FE6260` scene transition destroys layer 14 / UI owner 668 and rebuilds the HUD without restoring that payload. Initial-town login now prepares the repository/PVF skill packet before the fallible transition boundary and sends that exact body once, immediately after op24 succeeds. Route resets discard any stale prepared body, and reconnect/deferred completion retain the same one-shot ordering.
- Added regressions for committed redirected ACK endpoints, `ACK -> mode1(context 0, actor key) -> op357`, nonfatal redraw failure after a committed ACK, one post-op24 skill initialization, zero pre-op24 skill packets, and duplicate-route suppression.
- `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, the required `go vet` set, the production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass. The database was preserved. The previous server is recoverable from `runtime/backups/server/appearance-skill-post-op24-20260729-1035/DNF90Server.exe`; the deployed DNF90Server SHA256 is `9F348686ED7B98878FCE865C795CE5AE8618AF6277E2530944C9E6303B108F36` and runs as PID `62980`; portable MySQL is READY as PID `62288`; all 120 listeners accept connections; new server/MySQL stdout and stderr are empty. The installed transport-only DLL was not rebuilt or replaced and remains SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`.

## 2026-07-29 - fix transport-only login crash by restoring the native local scene owner

- Fresh transport-only acceptance isolated the login exit to the client, not the server. The current process raised `0xC0000005` at `0x034DBE73` while class0/op124 finalized the town state; the server stayed healthy and only observed the later client TCP reset. Current-EXE disassembly pinned the prerequisite failure: mode0 body `00 01 00 00 CE ...` stored owner `206` in `byte_5199C49`, while the native scene-channel global remained `0`, so `sub_2009160` took the remote-object branch instead of `sub_20036C0`, never created the selected local actor, and left op124 with a null current actor.
- The physical channel ID is again connection metadata rather than an implicit local scene commit. Cold login, return-select, completed-dungeon return, and channel reconnect now carry current scene context `0` consistently through mode0, actor-binding mode1, complete mode1, mode3, and the op9 outer record. The mode0 head is therefore `00 01 00 00 00`, which satisfies the current EXE's native `0 == 0` local-owner comparison without any DLL remap or memory write. Session initialization and later party projections retain the same selected-actor owner.
- Class0/op1 was deliberately not restored as a workaround. Although CHANNELINFO field 8 is the native writer for the scene-channel global, this profile's visible channel `206` has raw/catalog type `11`, while the handler's field-9 controller table has only three entries; writing `11` would index out of bounds. More importantly, existing live evidence proves that a class0/op1 transition arms the current client's 600-second Error2 watchdog. The established one-shot class1/op1 endpoint success and write-silent pre-bootstrap remain unchanged.
- Updated cold-entry, return-select, dungeon-return, reconnect, heartbeat-resume, preview, mode0/mode1, and op9 regressions to require local context `0` independently of the connected channel. `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF-module test, the complete `dnfbridge` and control suites, the requested `go vet` set, the production build, BAT-only offline preflight, controlled restart, and online runtime preflight pass.
- The relational database was preserved. The displaced server is recoverable from `runtime/backups/server/login-crash-local-actor-context-20260729-0945/DNF90Server.exe` (SHA256 `EE064CC4DC9F746E97ACEEEC39220A86D697E2F27C3D0DF4E95427D044DB1C42`). Portable MySQL is READY as PID `57656`; DNF90Server SHA256 `2B8238A32F35CD9BC1191FD68560C2E4E6511FC6632DEC200DD37923A2EF19B4` is READY as PID `61512`; all 120 listeners accept connections and fresh server stderr is empty. The installed transport-only DLL remains unchanged at SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`.

## 2026-07-29 - make the DLL transport-only and move town actor/appearance ownership to Go

- Production `90CN.dll` now installs only direct-launch TCLS, channel-directory/bootstrap-port, upper-body encode/decode, outbound DPROTO, bounded inbound routing, and read-only exception/transport diagnostics. The historical business installer is unreachable, `debug-compat` is an independent explicitly invoked test DLL, and packet-body logging is disabled unless `DNF_PROTOCOL_TRACE=1`. Release/Win32 links 49 functions, depends only on `KERNEL32.dll`, contains no party/aura/contract/inventory/appearance/pet/UI business strings, and the packaged DLL plus strict compatibility manifest use SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`.
- Town actor identity no longer relies on the removed client-side owner remap. Cold login, return-select, dungeon return, and channel reconnect carry the committed resident channel through mode0, actor-binding mode1, full mode1, mode3, and the op9 outer record. The op9 nested byte remains `0` because current `sub_1D64CA0` uses it as the inline-DSTR discriminator; writing the channel there made the client skip the name and misread every following field. Dungeon-only context-0 camera packets remain unchanged.
- Current `NoPack.exe` class0/op342 (`EXPAND_EQUIPSLOT_INFO`, `sub_1D7A3B0`) is now the ordinary equipment/avatar appearance owner after a successful op19 mutation. The server sends `class1/op19 ACK -> class0/op342` with the selected actor key, complete group-0 `sub_1D77560` rows, zero final state, and two `0xFF` skipped groups. It updates and clears slots on the existing actor without class0/op2 reconstruction, so the camera-bound actor is preserved. Runtime base slots `0..13` map directly; only untouched `source=pvf_create_equipment_list` rows use the legacy worn-slot conversion. Proven legacy slot-26 creature rows are preserved from their equipped-slot and creature-key metadata. The sender fails closed instead of publishing a partial authoritative group, and any still-unmapped pet row defers only the optional refresh without invalidating the already-committed op19 ACK or disconnecting the client.
- Equipped creature slot 26 keeps the current-EXE-native `op19 ACK -> op105 -> op102` sequence; creature unequip omits op102, and pet-artifact slots 27..29 stop after the op19 ACK. No mode0/mode1/op342 pet workaround or database field was added. Silent DLL aura replay was removed; durable `aura_flag` remains server-owned in selected-user subtype0.
- `gofmt`, complete DNF-module and `dnfbridge` suites, `go list -buildvcs=false ./...`, the control suite, required `go vet`, Go candidate build, and Release/Win32 DLL rebuild pass. After the client exited, the transport-only DLL and server candidate were deployed with recoverable backups at `runtime/backups/client/transport-only-20260729-090725/90CN.dll` and `runtime/backups/server/transport-only-20260729-090850/DNF90Server.exe`. BAT-only offline `check --skip-database`, controlled startup, and online status pass: installed `90CN.dll` uses SHA256 `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`, running `DNF90Server.exe` uses SHA256 `EE064CC4DC9F746E97ACEEEC39220A86D697E2F27C3D0DF4E95427D044DB1C42`, MySQL is ready as PID `59072`, the server is ready as PID `62792`, and all 120 listeners accept connections.

## 2026-07-29 - correct the town actor state wire and refresh worn models in place

- Fresh acceptance disproved the preceding ACK-only/single-op24 hypothesis. The deployed client had actually received initial op23 bytes ending in `03 05`, and ordinary op36 received no op22/op23 at all. Current `NoPack.exe` `sub_1D89590 -> sub_23DBA50` reads those fields as `direction` then `state_bits`, not the reverse: the old packet therefore selected direction 3, cleared the appearance-visible bit in state 5, and entered the state-bit-2 event path. The bridge now emits the EXE-authoritative tail `05 03` and uses `op22 -> op23(direction,state) -> one op24` for initial entry, ordinary town movement, and previous-village return. The existing dungeon camera path is unchanged.
- Successful op19 replies now preserve the request-side source endpoint and publish the durable destination endpoint. No complete op13 container reconstruction or class0/op2 actor replacement follows an equipment, avatar, or pet move. Same-value class0/op357 reapplies the repository-backed `user_state_bits` after login mode1 and after slots 0..8 change, using the current EXE's existing-actor redraw path without replacing the camera-bound actor.
- Current `NoPack.exe` can still skip its own visible-model branch in the affected town scene. The matching compatibility DLL therefore runs the exact live-actor worn-slot lookup and vtable apply/clear calls after the original class1/op19 handler. It refreshes every worn endpoint touched by the ACK, so equip, unequip, worn-slot swaps, ordinary equipment, fashion, weapon avatar, weapon, title, pet, and pet-artifact slots share one native in-place boundary. Aura keeps its already-working native path.
- Equipped pet slot 26 now finishes as `op19 ACK -> native worn-slot apply/clear -> op105 -> op102`. It no longer sends broad list-7 op13, mode0, or mode1 rebuilds; artifact slots 27..29 stop after the ACK/native slot apply. The existing persisted `user_state_bits` value is sufficient, so no database field or migration was added.
- Release/Win32 rebuilt with zero errors. Packaged, manifest, and installed `90CN.dll` all use SHA256 `2D3238399837D497BECB88677A16FAE8919910408F3B8465E011AA58196F2C02`; the displaced DLL is recoverable from `runtime/backups/client/appearance-town-20260729-0748/90CN.dll`. Final visual acceptance remains explicit: test one ordinary fashion slot, weapon/weapon-avatar, pet equip/unequip, and town movement after a fresh client start.
- `gofmt`, scoped `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production Go build, the Win32 client build, BAT-only offline preflight, controlled restart, and online runtime preflight pass. Database data was preserved; portable MySQL is READY as PID `53276`, DNF90Server SHA256 `014CEEE15B9AFCAB279B39D508A88F5E7F0DEA713653F8156F159A5ABE75494B` is READY as PID `61088`, and all 120 listeners accept connections. Fresh logs contain no server error/fatal/panic and no MySQL startup error; the server stderr is empty and MySQL reports READY. The preceding server is recoverable from `runtime/backups/server/DNF90Server.before-town-wire-native-appearance-20260729-0750.exe`.

## 2026-07-29 - restore actor-bound inventory, single town transition, and native appearance refresh

- Fresh `game-000359` traffic proved the inventory repository and wire bodies were populated: list 0 carried 105 rows, list 2 carried 41 rows, and list 7 carried 34 rows. The complete op13 family was sent immediately after the op4 select ACK, roughly 230 ms before the local actor was created. Current `NoPack.exe` resolves each destination container manager before installing op13 rows, so that early snapshot was discarded even though it contained the real items.
- The selected-character container lifecycle now has one owner after actor binding and cargo-pad reset. Town and tutorial/dungeon entry install the complete ordered `0/1/2/7/12/38` family immediately before the final equipment-bearing mode1; the select ACK and scene-ready tail no longer publish or replay those containers. Dungeon scene and camera packets are unchanged.
- The same live trace showed an ordinary op36 sending two byte-identical self op24 packets (`26 00 01 00 01 00 E2 02 F8 00 05 03`). Current-EXE `sub_1D901D0` treats op24 as a full scene transition: it clears position state, runs the selected-actor and camera callbacks, then closes the global scene transition. Co-presence therefore no longer sends a second self-containing op24. The authoritative town-move op24 remains, while multiplayer synchronization keeps only peer op23, leave op6, and movement op22.
- Current-EXE `sub_1D2DE80` consumes the successful op19 ACK endpoints and performs the live container, equipment/avatar model, and UI refresh itself. Ordinary equipment and avatar moves now stop at that exact ACK instead of following it with full op13 reconstruction and a second actor projection that replaces the objects the native handler just updated. Aura and equipped-creature lifecycles retain their dedicated projections.
- Premium initialization is single-owned as well: op4 retains its embedded premium entries, explicit op903 requests still return op903, and the first accepted town-UI op36 remains the crystal op898 owner. The deferred scene tail no longer pushes duplicate op903/op898 state.
- Regressions now require one authoritative op24 per accepted town transition, peer-only co-presence, all six op13 lists after actor/cargo binding and before the final mode1, zero post-op24 item-list replay, ACK-only ordinary equipment/avatar refresh, and no scene-tail op903/op898. The complete DNF-module and bridge suites pass.
- `gofmt`, scoped `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, controlled restart, and online preflight pass. The restart preserved the portable database; MySQL is READY as PID `61364`, DNF90Server SHA256 `7407937CB7D9048A5F1AAE049B1D8017354B4E38232D6B2C6F060819B5DD45B0` is READY as PID `61304`, all 120 listeners accept connections, and fresh server/MySQL stdout/stderr are empty. The previous server is recoverable from `runtime/backups/server/DNF90Server.before-town-inventory-native-refresh-20260729-064936.exe`.

## 2026-07-29 - restore live actor binding, mode1 appearance refresh, and one-shot contract UI replay

- Fresh current-client trace corrected the preceding cold-login conclusion. After the single complete mode1, every post-op19 mode0 logged a newly resolved `current_actor` while `controlled_actor` remained a different pointer; mode0 therefore hit the existing-object branch without updating the live controlled actor. The reported two login refreshes were duplicate crystal-contract initialization, not evidence that the prerequisite actor-binding mode1 was redundant.
- Cold completed-character login and return-select re-entry again send the established light actor-binding mode1 before the repository-backed complete mode1 and typed op24. Ordinary equipment/avatar op19 post-actions now use the existing-actor mode1 updater, including complete empty-slot removal state. Aura and equipped-pet lifecycles retain their dedicated projections.
- Repeated client op36 requests in one town UI lifecycle now emit only one UI-ready op898 crystal-contract replay. The gate uses its own session mutex, rolls back after a failed send, and is rearmed whenever a new initial-town route is reset or armed, so character selection and dungeon return are not suppressed. Explicit contract selection, consumption, and activation notifications remain unaffected.
- Added regressions for op19 ACK followed by repository-backed mode1 equipment state, empty mode1 unequip state, two-stage cold/return-select actor binding, exactly one op36 UI-ready contract replay, and replay rearming for a new town route. `gofmt`, scoped `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, and the required `go vet` set pass.
- The database was preserved across the controlled BAT restart. Portable MySQL is READY as PID `42164`; DNF90Server SHA256 `786E48081D96DE733206BA430DEA602EAAED633CE498FB915ED29391D866C019` is READY as PID `62028`; all 120 listeners accept connections; fresh server stdout/stderr are empty. The previous image is recoverable from `runtime/backups/server/DNF90Server.before-actor-appearance-camera-contract-20260729-0553.exe`. The exact offline check remains blocked only by the already-running client's recorded `F15D...` DLL differing from the staged source manifest's not-yet-installed `D516...` compatibility unit; startup validated the installed unit without overwriting the client, then restored the source manifest unchanged.

## 2026-07-29 - collapse cold town entry to one actor-binding mode1

- Fresh server/client evidence disproved the earlier op24/op36 hypothesis: the affected login received one typed op24 and no client op36, while class0/op2 carried one mode0 object create followed by a light mode1 and a second complete mode1. The first mode1 changed the controlled-actor pointer and the later mode1 rewrote the same actor state, matching the reported two visible initialization rounds.
- Cold completed-character login and return-select re-entry now keep the required mode0 actor creation but skip the preliminary light mode1. The existing complete equipment-bearing mode1 remains the single actor-binding/state application before op24. Channel reconnect and completed-dungeon return retain their separate established routes so this acceptance experiment does not broaden beyond the reported path.
- Regression coverage now requires exactly one mode1 on both cold login and return-select, while preserving the full player-state sequence, creature state, op9 display state, actor scene snapshot, and final typed op24.
- `gofmt`, scoped `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, restart, and online runtime preflight pass. Database data was preserved; portable MySQL is READY as PID `59764`, DNF90Server SHA256 `A6135052521835522AA380CD8BB550A42FD56B7D2AF60337FA8DC2A47D9CF860` is READY as PID `57168`, and all 120 listeners accept connections.

## 2026-07-29 - restore the bounded op9 party-member identity block

- Fresh acceptance disproved the local-context omission: the 45-byte class0/op9 body crashed immediately after party creation. The current client still selects the extended member branch for the owner-context-0 record in channel 206 and therefore consumes `job/level/grow/flag/name` before the three member-state bytes.
- Restored the previously accepted 58-byte single-member layout by comparing the record owner byte with the selected numeric channel for this compatibility profile. The rejected local-context regression was removed; the bounded cross-channel identity regression remains authoritative.
- The missing visible party frame is a separate native actor-binding defect and must not be worked around by shortening the wire body. The compatibility DLL now walks the bounded op9 member table after native dispatch and repairs the exact `sub_269EE50` invariant (`memberActor+0x498 = recordActor`) only for the one-shot successful op12 path where the member key resolves to the current actor, the same key is present in a valid slot, and the pointer is still null. The outer `recordActor` is resolved exactly as the current EXE does at `sub_1D64CA0`, through `sub_26A2000(sceneActorManager, ownerContext, objectKey)`; it is not substituted with the member actor. The native HUD refresh then runs after the repaired binding.
- The first binding build made the party frame visible but displayed `组队信息出现异常`, proving that assigning the member actor as its own party owner was structurally wrong. Release/Win32 was rebuilt with zero errors using the exact outer-record resolver; that intermediate DLL had SHA256 `D516F42837694642E1A507A554810D24D9D134FBD3030B0F87BF224C1DCC46E6` and is recoverable from `runtime/backups/client/party-outer-record-20260729-062131`.
- Fresh IDA evidence then closed the remaining member-target ambiguity. `sub_269EE50` compares the op9 record context with the selected channel and, for this profile's context `0` versus channel `206`, resolves the member through `sub_17B6AA0(objectKey, ownerContext)` before writing `sub_2690120(member)+0x14` (effective member `+0x498`). It does not write only the `sub_2699A30` current-actor object. The compatibility repair now restores that exact context-member binding and keeps the separate current-actor binding solely for the existing HUD state-1 compatibility path. The rebuilt package, installed client, and strict manifest all use SHA256 `394F14253A71521C91AB61DB8C65C9E904F3ACD35AE94448FBA4536D7CC47FA3`; the displaced intermediate DLL is recoverable from `runtime/backups/client/party-context-member-20260729-064853`. Visual acceptance remains pending.
- The restored production server is SHA256 `E8738DBBC34A01DFEE9EF2630D36B62E14EB4B221CC077F75957796492B7DE96`. `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, BAT-only offline preflight, restart, online preflight, all strict client hashes for the previously installed compatibility unit, and all 120 listeners passed. Database data was preserved.

## 2026-07-29 - remove duplicate selected-container rebuilds from scene-ready

- Audited the selected-character path from the initial ACK through typed town transition and the deferred/runtime scene-ready gates. The ACK already installs the complete repository-backed op13 family; both later gates replayed the same list `0/1/2/7/38` after op24, and the full-list helper also published op985 before the explicit post-actor op985. Those duplicate writers could rebuild client objects after the selected actor was already bound.
- Removed the two scene-ready op13 replays. Premium services, aura-slot state, crystal contracts, creature state, and the remaining scene projections retain their dedicated packets. Rental points now have one explicit owner: a single absolute op985 after the scene actor is committed.
- Preserved the newer current-EXE op9 cross-channel identity implementation rather than reverting it to the earlier incomplete slot body. Regressions now walk both bounded identity blocks and require zero post-op24 op13 packets plus exactly one op985 in the fully provisioned scene-tail fixture.
- Focused regressions, `go list -buildvcs=false ./...`, every DNF-module test, the complete bridge/control suites, and the required `go vet` set pass. The simultaneously completed party-HUD compatibility DLL was installed as the matching strict-profile unit with SHA256 `28D6237D279174C59D2D6E1049F1E8447F34C05B336E50C10C41A939C1790599`. Database data was preserved; portable MySQL remains READY as PID `58456`, DNF90Server SHA256 `E12BC77B4074D81226B414E1EF7714BF4007B14F950C89268BC18890CD6B09A9` is READY as PID `60576`, all 120 listeners accept connections, and fresh stderr is empty. The previous server is recoverable from `runtime/backups/server/DNF90Server.before-select-bootstrap-dedup-20260729-0418.exe`.

## 2026-07-29 - close the current-client party HUD refresh boundary

- Fresh acceptance proved the completed op9 identity branch fixed the create-party crash: class1/op12 returned success, the 58-byte class0/op9 member projection completed both `before` and `after` dispatch traces, and class0/op153 plus op7 followed without disconnect. The remaining defect was specifically that the visible party HUD did not refresh.
- Current `NoPack.exe` `sub_1D64CA0` populates the global party slots from op9, but calls the native `sub_27310B0` HUD state-1 refresh only when its owner/object lookup is pointer-equal to the native current actor. The bridge's established owner-context-0 local-scene remap keeps the actor valid but can miss that final equality branch.
- The compatibility DLL now arms a one-shot refresh only after a successful class1/op12 `SET_PARTY_INFO` ACK and consumes it only on the immediately following nonempty scene-9999 class0/op9 kind-0 actor projection. After the original op9 handler has populated the slots, it invokes the exact native `sub_181E750 -> sub_27310B0(1)` path. Ordinary scene op9 traffic, rejected op12 requests, and unrelated messages are unchanged.
- Release/Win32 rebuilt with zero errors. The packaged compatibility DLL and strict manifest now use SHA256 `28D6237D279174C59D2D6E1049F1E8447F34C05B336E50C10C41A939C1790599`. Installation and fresh-client visual acceptance require the currently running DNF process to exit.
- After the client exited, the rebuilt DLL was installed into the configured game directory and verified byte-for-byte against the packaged copy. The previous on-disk DLL was retained under `runtime/backups/client/party_hud_20260729_042517`; fresh-client visual acceptance remains pending.

## 2026-07-28 - complete the current op9 party-slot identity branch

- Fresh create-party acceptance disproved the first slot-table projection: the class1/op12 ACK succeeded, but the following 45-byte class0/op9 actor update crashed the client before op153/op7. `90CN_trace.log` pinned the first fault to the `upper_pkt_read_raw` call returning into `sub_1D64CA0+0x719` (`0x1D653C9`), rather than to the slot-registration call itself.
- Re-reading the full current `NoPack.exe` branch showed that every slot whose actor owner context differs from the selected game channel consumes an additional identity block before its three slot-state bytes: `u8 job`, `u8 level`, `u8 grow`, `u8 flag`, then `u32 nameLength + raw name`. The incomplete packet omitted that block, so its post-slot active bytes `01 00 00 00` were misread as `nameLength=1` and the raw reader stepped past the packet boundary.
- The bridge now compares the actor owner context with the actual session channel, emits repository-backed job/level/grow/name identity for every cross-channel slot, and keeps the no-identity path only when both channel bytes match. The slot state byte now carries the member state instead of the previously misassigned HP percentage; HP/MP remain owned by the verified op153/op7 packets.
- Exact-wire regression coverage walks both complete identity blocks, bounds each raw name inside the packet, pins member state and final flags, and keeps the original zero-slot actor record byte-for-byte. Single-member creation, online join, leave, kick, and position-change sequences pass the bridge suite.
- `gofmt`, scoped `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, restart, and online runtime preflight pass. Database data was preserved; portable MySQL is READY as PID `58456`, DNF90Server SHA256 `E48ABD3D0AFDC2672BD6C346E69540B550E9351F99769D30AC9A105F934C18D1` is READY as PID `52380`, all 120 listeners accept connections, and fresh server stdout/stderr are empty. The crashing image is recoverable from `runtime/backups/server/DNF90Server.before-party-identity-20260728-2215.exe`.

## 2026-07-28 - restore the live town actor bind and existing-actor appearance updates

- Fresh acceptance disproved both the ACK-only appearance boundary and the one-packet ordinary-town transition. In the latest authenticated trace, cold town entry bound the selected character with class0 notifications `op22 -> op23 -> op24`, while a later accepted client-authored op36 emitted only op24; after that transition, the character could move beyond the camera. The same session committed equipment/avatar moves, but the live town actor received no existing-object appearance update.
- Ordinary op36 town movement and previous-village return now replay the exact current-client local binding prefix `op22 -> op23` before the authoritative op24 area roster. `op23` owns current town/area/position state and the controlled-scene-actor rebind; all three writes remain inside the existing durable-location rollback boundary.
- Ordinary equipment and avatar endpoint moves now finish their class1/op19 ACK with one repository-backed class0/op2 `mode1` projection. Current `NoPack.exe` resolves that mode against the existing selected actor and replaces its complete worn projection, including a zero-row projection after unequip. No full item-container replay or create-mode actor replacement was added.
- Equipped-pet slot 26 remains a separate lifecycle: its post-action first rebuilds the selected actor/creature association with the existing mode0 projection and then publishes the repository-backed op105 status table and op102 growth state.
- Focused regressions require the exact `op22 -> op23 -> op24` sequence and bodies for ordinary movement/previous-village return, require op19 ACK followed by one mode1 appearance projection for equipment/avatar moves, prove an empty mode1 projection clears removed equipment, and require pet ordering `mode0 -> op105 -> op102`.
- `gofmt`, scoped `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline/runtime preflight, and restart pass. Database data was preserved; portable MySQL is READY as PID `53716`, DNF90Server SHA256 `1BAC2CB3ADABA095270C3447C710F67668069E542FFE287EA45F9DDA39F5B35E` is READY as PID `58080`, all 120 listeners accept connections, and fresh server stderr is empty. The previous server is recoverable from `runtime/backups/server/DNF90Server.before-town-bind-mode1-20260728-210256.exe`.

## 2026-07-28 - rebuild the visible party frame from the current op9 actor slot table

- The preceding crash fix correctly removed the legacy party body from class0/op9, but it also left the current `sub_1D64CA0` kind0 actor record's own party-slot count at zero. Class0/op153 and class0/op7 only update member state after those scene-object slots exist, so party creation succeeded without ever constructing the visible party frame.
- Re-read the complete current-EXE kind0 record. After the established minimal actor fields it consumes `u8 slotCount`, then six bytes per slot (`u8 slot`, `u16 scene object key`, and three state bytes), followed by the four existing post-slot flags. The bridge now reuses the same safe repository-backed actor record, writes only that verified slot table, and never restores the misaligned legacy party structure.
- Create/leave ACKs defer the current-client projection to the bridge. Create, accepted invite, direct entry, leave, kick, and position exchange now refresh the op9 slot mapping consistently; op153 and op7 follow the mapping for realtime state and HP/MP. Empty-party projection clears the slot table and active flag.
- Regression coverage preserves the original zero-slot actor body byte-for-byte, pins the exact six-byte member rows and active flag, and verifies create/join/leave/kick/position packet ordering.
- `gofmt`, scoped `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, and runtime preflight pass. Portable MySQL data was preserved and is READY as PID `53716`; DNF90Server SHA256 `1BAC2CB3ADABA095270C3447C710F67668069E542FFE287EA45F9DDA39F5B35E` is READY as PID `58080`, and all 120 listeners accept connections. The preceding image is recoverable from `runtime/backups/server/DNF90Server.before-party-frame-20260728-205950.exe`.

## 2026-07-28 - restore the known-good town display ownership boundary

- The shared town display regression was not caused by missing inventory, equipment, pet, or position data. Fresh traces showed the authoritative snapshots and op24 coordinates reaching the client, while the supplied known-good July source exposed three later lifecycle changes: equipment/avatar/pet refresh had been changed from the working class0/op2 `mode0` projection to `mode1`, an extra full actor projection had been inserted immediately after ordinary op24 movement, and multiplayer entry had begun creating remote actors with owner context `0`.
- Current `90CN.dll` deliberately remaps a `mode0` object with owner context `0` to the local actor branch because the selected scene channel is not zero. Reusing that context for remote actors can therefore replace or redirect the selected town actor. The result is one common ownership failure presenting as several symptoms: camera no longer follows movement, equipment/avatar/pet wear or removal is not reflected until relog, and later town refreshes target stale actor state.
- Restored the known-good boundaries: op19 mutations end with the repository-backed `mode0` selected-actor appearance projection; pet slot 26 publishes op105/op102 before that projection; ordinary town movement and previous-village return no longer append a speculative full actor refresh after op24; town co-presence returns to op17 notifications plus the op18 roster without context-zero remote actor creation. The current op19 envelope remains the ordinary configured upper transport after the fixed16 zero-prefix hypothesis was rejected.
- Regression coverage now locks the mode0 appearance rows (including explicit empty slots), pet `op105 -> op102 -> mode0` order, single op24 transition boundary, and absence of remote actor recreation during town entry.
- `gofmt`, scoped `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, and runtime preflight pass. Portable MySQL data was preserved and is READY as PID `15772`; DNF90Server SHA256 `3E24E286053178411F17B47F9707E18DB08092A558A1D1C8EF872AFFBF0B9A01` is READY as PID `6364`, all 120 listeners accept connections, and fresh stdout/stderr logs are empty. The preceding server is recoverable from `runtime/backups/server/DNF90Server.before-town-display-boundary-20260728-195739.exe`.

## 2026-07-28 - stop party creation from feeding legacy party bytes into the current scene-record handler

- Fresh production traffic isolated the create-party disconnect to the server response sequence for class1/op12: the success ACK was followed by a legacy class0/op9 party snapshot, then valid class0/op153 and class0/op7 state packets. The client closed immediately after receiving that sequence.
- Current `NoPack.exe` is authoritative: `sub_1D64CA0` parses class0/op9 as counted town-scene actor records, not as the old party-info structure. The legacy party bytes therefore drove the scene parser through an invalid record layout and crashed the client. `sub_1D800C0` confirms the existing class0/op153 member rows, `sub_1D82CF0` confirms the existing class0/op7 status-kind `0x0A` HP/MP rows, and the class1/op12 success path requires only the one-byte ACK.
- Removed the obsolete op9 party builder/checksum and all remaining sends from create, leave, accepted-invite, online-party, and raid-party synchronization paths. Current party flows now use only the verified ACK, realtime-member, and peer-status layouts; regression coverage explicitly fails if create-party emits class0/op9 again.
- Focused party and bridge regressions, `go list`, control tests, required `go vet`, production build, and BAT offline/startup preflight passed. The full DNF-module and bridge suites still contain pre-existing failures in concurrently edited pet/town actor-refresh expectations, outside this party packet change. Deployment preserved the database, started portable MySQL as PID `24160`, and started DNF90Server SHA256 `9A32B9B36C5CCCBB73F834F760557D1C9EB077EA1B4B291B9C6751EA6F274CE7` as PID `59344`; all 120 listeners accept connections.

## 2026-07-28 - restore the production training-room entry layout

- Live class0/op16 evidence isolated the apparent training-room freeze before any entry response: dungeon `5000` selected maze `0`, but layout construction rejected room `(0,0)` because it expected a boss map and the boss pool was empty. Since the rejected request produced no op16/op28/op29 continuation, the current client remained indefinitely on the dungeon-select screen.
- Production `Script.pvf` is authoritative and contains a complete one-room definition rather than missing data: `dungeon/PoongjinTrainingRoom/PoongjinTrainingRoom.dgn` marks `(0,0)` as both start and boss, and its sole owned map is `36250`, `map/PoongjinTrainingRoom/11100(0,0)start.map`. The resolver now accepts that coordinate-owned start map only after explicit specifications and exact boss matches fail, and only when the same room is both start and boss. Ordinary rooms and existing tower-style uncoordinated-map fallback behavior are unchanged.
- Added a synthetic layout regression, a production-PVF map ownership regression, and a full real-PVF runtime smoke that prepares a level-90 dungeon `5000` session and successfully builds the current-client dungeon-info/start-map packets. No database field or migration is required.
- The first post-deployment acceptance proved the layout fix worked and op16 now returned success, but exposed a second fail-closed boundary before op28: unlike every captured ordinary dungeon request, the current EXE writes training-room dungeon `5000` with `runtime_state=1` and `runtime_token=65535`. Entry validation now accepts only that exact evidence-backed training compatibility unit while continuing to reject state `1` for ordinary dungeons or a training request without the captured token. The regression decodes the exact live 21-byte op16 body `881300000000000001ffff00000000000000000000`, runs it through the production-PVF handler, and requires real op16, op28, and op29 network writes.
- After the captured runtime-state correction, `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, both production-PVF training-room regressions, production build, BAT-only offline preflight, restart, and online runtime preflight pass. Database data was preserved across the controlled restart; portable MySQL is READY as PID `15772`, DNF90Server SHA256 `3E24E286053178411F17B47F9707E18DB08092A558A1D1C8EF872AFFBF0B9A01` is READY as PID `6364`, all 120 listeners accept connections, and fresh stderr is empty. The immediately preceding image is recoverable from `runtime/backups/server/DNF90Server.before-training-entry-20260728-200012.exe`; the earlier pre-layout image remains at `runtime/backups/server/DNF90Server.before-training-room-20260728-194349.exe`.

## 2026-07-28 - reject the fixed16 zero-prefix op19 hypothesis

- Fresh live acceptance disproved the special class1/op19 fixed16 zero-prefix envelope: equipment, avatar, and pet operations still committed durably but did not refresh the town model, and additional live refresh behavior regressed. The special envelope and its zero-sequence test expectations were removed; op19 is again written through the ordinary configured upper transport.
- Client trace instead isolates the shared failure to the initial town actor lifecycle. Class0/op2 mode0 creates the visible selected actor, a following mode1 resolves a different live mutation target, and successful op24 processing does not reconcile the two generations. Later op19 ACKs and repository-backed projections reach the client but therefore update a different target from the object currently rendered in town.
- Investigation is now constrained to the current EXE's op24 self-row selection and actor-binding fields/order. No additional feature-specific refresh packets are being added while that common binding boundary remains unresolved.

## 2026-07-28 - restore the shared existing-town-actor refresh boundary

- Client-side `90CN_trace.log` proved the server's post-mutation class0/op2 mode0 packets reached `NoPack.exe`, but `sub_2009160` resolved the already-present actor key to the same object pointer and returned without replacing its worn projection. This explains the shared symptom across equipment, avatars, pets, and later town movement: the data was committed and delivered, but the create-mode packet could not update an existing actor.
- Equipment and avatar op19 completion now follows the authoritative container refresh with a full repository-backed class0/op2 mode1 existing-actor projection. Pet slot-26 moves use the same mode1 boundary first, then publish op105/op102 against that current creature generation.
- Ordinary op36 town-area movement and legacy previous-village return now send the same full mode1 actor rebind immediately after their accepted typed op24 transition. The final area roster remains responsible for peer rows and camera selection; the disproved standalone post-op24 op120 experiment remains removed.
- Focused regressions require mode1 rather than mode0 after equipment/avatar mutation, require pet ordering `mode1 -> op105 -> op102`, require every accepted town transition to include the selected actor's post-op24 mode1 rebind, and require database location rollback when either transition write fails.

## 2026-07-28 - reject the post-op24 camera-placement hypothesis

- Fresh authenticated acceptance proved the server emitted exact op24 position/roster bodies followed by class0/op120 actor-slot placement, yet camera behavior was unchanged. The extra op120 hypothesis is therefore disproved and its code/test expectation was removed instead of retaining an ineffective compatibility packet.
- Investigation moves upstream to the local actor creation/scene binding lifecycle and compares that complete initialization sequence with the supplied known-good source; the valid op36 body and durable town-position transaction remain unchanged.

## 2026-07-28 - restore the known-good final town bind and committed appearance projection

- Fresh live acceptance disproved the preceding assumption that an op19 ACK and the first single-row op24 were sufficient. The latest connection committed both equipment moves and sent valid class1/op19 ACKs plus repository-backed item lists, yet the town actor remained visually stale; ordinary op36 also sent only the first single-row op24 and the controlled actor could still leave the camera.
- Compared the exact failing sequence with the supplied July 5 working server. Its town-area lifecycle finishes remote publications with a class0/op24 area roster that includes the selected character as the final row. Current `NoPack.exe` `sub_1D901D0` uses that matching row for its selected-actor callbacks, so the self roster is restored even for a solo area as the final camera bind. Current remote mode0/mode1 actor creation remains intact.
- Restored the working equipment/avatar boundary `op19 ACK -> authoritative item containers -> repository-backed class0/op2 mode0`. The ACK still owns endpoint relocation, while mode0 closes the town-world appearance state and writes explicit empty appearance rows after unequip. Pet slot 26 similarly keeps op105/op102 state and then republishes the committed actor object.
- Protocol regressions now require the two op24 phases for a town-area change, require the final self row to preserve exact actor key/coordinates/direction/state, and require ordinary and pet op19 mutations to end with the committed mode0 actor projection.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, and BAT-only offline/runtime preflight pass. Database data was preserved across the controlled restart; portable MySQL is READY as PID `58264`, DNF90Server SHA256 `5805E615F56E00F501475EEBB22FD12F50CD0F77666F8664661B15EEC36C99E5` is READY as PID `58328`, all 120 listeners accept connections, and fresh stderr is empty. Final client acceptance still requires a new authenticated op36 plus equipment/avatar op19 sequence after this restart.

## 2026-07-28 - complete the current-client adventure-group runtime

- Live character-collection acceptance exposed one additional request outside the original op1340/op1403 projection: selecting the `剑皇` card sent legacy op1467 body `0B 21`, but no owner handled it. Local `NoPack.exe` `sub_C79FE0`, `sub_26900E0`, and result handler `sub_C7CC60` prove the request is `u8 job + packed grow` and the success body is `u8 server-group count`, followed by each `u8 server-group + u8 character count + 52-byte rows`. A second live capture then proved that op1467 does not reuse the op1403 roster layout: `sub_9F2810`, `sub_9F3F70`, and `sub_9F43B0` read the duplicated server group at row `+0`, the displayed `u32` level at `+8`, the op1468 character ID at `+12`, and the GB18030 name at `+20`. Both transports now emit that dedicated layout, eliminating the false `Lv0`, keeping list/detail server labels consistent, and making representative selection return the real character ID.
- A follow-up live screenshot proved the dedicated op1467 representative list was correct at level 90 while the main op1403 collection card still displayed level 0. Current `NoPack.exe` `sub_C7BCC0` parses roster raw `+42` and raw `+48` as independent fields; `sub_A100B0`/`sub_A105F0` project raw `+48` into display-object `+0x50`, and `sub_9FDCF0` passes that exact dword to the card's `Lv%d` formatter. The op1403 writer now keeps the existing byte level at raw `+42` and also writes the repository-backed level as a `u32` at raw `+48`, with regression coverage for both fields and the separate op1467 layout.
- Re-read the local `NoPack.exe` handlers and captured live traffic before implementing the missing tabs. The current client sends expedition selection as legacy op441 (`u8 count` plus selector bytes), expedition dispatch as op442, reward preview/claim as op1407/op1408, shop purchase as op1414, and growth-capsule use as op1491. Both legacy and upper dispatch tables now route the client-authored forms; the former placeholder/deferred handlers were removed.
- `etc/adventurersystem/adventurersystem2018.etc` and `adventurerexpeditionsystem.etc` are now the sole runtime authorities for management limits, traits, character collection inputs, all three shop categories, item prices and limits, monthly reset, capsule thresholds/reward, expedition areas, durations, attributes, and reward rates. Startup fails closed when this production PVF data is incomplete instead of inventing substitute content.
- The 7,420-byte op1340/op1403 projection now includes persistent shop currency and purchase counters, growth-capsule experience, the current roster, corrected activity tuples, and active expedition state at the exact current-EXE offsets. Basic information, traits, and character collection continue to derive from the same account roster and PVF rather than maintaining conflicting copies.
- Expedition member selection, dispatch, state push, reward preview, and one-time reward claim are implemented with strict selector validation and replay-safe account persistence. Shop purchases validate the exact PVF product/category/limit, spend the matching adventure currency, grant the real PVF item inside the existing account-inventory transaction, and refresh both the item slot and adventure-group state. Growth-capsule use atomically consumes accumulated account experience, applies the PVF grant through the character progression tables, updates skill points, and refreshes the character and account views.
- Accepted level-90 dungeon settlements now divert the capped portion of earned experience into the account growth capsule within the existing settlement transaction and completion receipt, so retries cannot add it twice. Adventure runtime state uses the existing account-metadata repository; no database column or schema migration is required.
- Added focused PVF, state, wire-offset, expedition request/response, shop, capsule, maximum-level settlement, transaction, and replay regressions. Existing mail, inventory, package-item, and dungeon reward ownership remains unchanged.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, and BAT-only offline/runtime preflight pass after the dedicated op1467 row fix. Database data was preserved; portable MySQL is READY as PID `54588`, DNF90Server SHA256 `78F038BEDCE68710A6BC29D9EE6567B8189D3D202A013ABDE22E3B5A529A94D1` is READY as PID `21244`, all 120 listeners accept connections, and fresh stderr is empty. The immediately preceding server is recoverable from `runtime/backups/server/DNF90Server.before-adventure-op1467-layout-20260728-1830.exe`.
- The op1403 collection-card level correction passes `gofmt`, focused dual-layout regressions, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, and BAT-only offline/runtime preflight. Database data was preserved across the controlled restart; portable MySQL is READY as PID `12804`, DNF90Server SHA256 `C8F3AFF10D127EF4C693A6FE7C0A07824C93353B278FE4469E1984ECDA5A969E` is READY as PID `52724`, all 120 listeners accept connections, and fresh stdout/stderr logs are empty. The preceding server is recoverable from `runtime/backups/server/DNF90Server.before-adventure-card-level-20260728-192737.exe`.

## 2026-07-28 - restore the current-client profession-completion popup

- Live quest evidence isolated the missing popup to profession quest `4621` (`n_quest/job/ats_sword_01.qst`): the atomic owner correctly committed `grow_type 0 -> 1`, PVF rewards, skill state, and the op34 success result, but the bridge sent a full op19 skill snapshot immediately before op34. Current `NoPack.exe` op19 handler `sub_1D6E240` closes UI layers `0/1`; this removed the profession window before the finish handler could populate it.
- Rechecked current `NoPack.exe` op34 handler `sub_1D32120`. Profession chains `1/2` own two inline skill-tree lists, each encoded as `u8 count`, then `u8 absolute level + u16 skill ID + u8 learned flag`. The finish owner now persists the sorted positive pre/post skill delta in its durable receipt, and op34 writes that exact PVF-backed delta to both trees. The pre-ACK op19 is removed; the committed character, active-quest, and acceptable-quest snapshots remain after op34.
- The official skill window and the following armor-mastery introduction are one client-owned op34 chain, not two server packets. After consuming both inline trees, `sub_1D32120` resolves virtual skill `197`, changes its class-specific armor-mastery mapping, refreshes the skill manager, and continues the second window. The production PVF proves this prerequisite for all 57 ordinary class branches across jobs `0..8` and `11..15` (including female mage branch five); only the two external professions, Dark Knight `9` and Creator `10`, are excluded. Real-PVF and exact-wire regressions now lock skill `197` into both trees for the complete ordinary-profession matrix.
- Class change and awakening receipts may legitimately contain zero inline grants when the runtime PVF branch has no new free skills. Nonzero grants are range-checked, deterministic, replay-safe, and never reconstructed from invented skill data.
- Domain and transport regressions require durable grant replay, exact two-tree op34 bytes, op34 as the first response, and no op19 anywhere in the profession completion flow. The production `Script.pvf` smoke test passes with 16 jobs, 453 class grants, and 118 awakening grants.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, and BAT-only offline preflight pass. The fixed source is present in the currently running server image; final acceptance still requires completing another profession quest in the live client.

## 2026-07-28 - keep the op24-bound local actor alive across deferred state and op19 moves

- Live acceptance disproved the preceding town/appearance restoration. Repository commits, class1/op19 success ACKs, every authoritative item-container snapshot, and correctly cleared mode0 appearance rows were all present, yet the visible actor remained stale and later movement could leave the camera. The failure was therefore not missing equipment data or an incomplete op24 row.
- Re-read current `NoPack.exe` directly. Class1/op19 handler `sub_1D2DE80` applies the two move endpoints to the live local equipment/avatar objects and invokes the actor model refresh itself. Scene-transition handler `sub_1D901D0` selects the local actor and performs its camera/scene callbacks when the op24 row key matches the selected actor. A later full class0/op2 mode0 is not an incremental refresh: it replaces that already-selected actor object.
- Removed both post-bind replacement paths. Ordinary equipment/avatar moves now retain `op19 ACK -> authoritative container snapshots` and deliberately send no local mode0/mode1 actor snapshot afterwards. Pet slot-26 moves retain the op19-owned object mutation and publish only their repository-backed op105 status table plus op102 growth state. The deferred scene-ready creature fallback is now idempotent after the normal post-mode1 op105/op102 initialization and never rebuilds the actor after op24.
- Protocol regressions require no class0/op2 bytes after ordinary op19 appearance actions, require pet moves to end with op105/op102 without a local actor rebuild, and require the deferred scene-ready callback to write nothing once the creature state was already installed. Focused inventory and complete bridge tests pass.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production build, and BAT-only offline/runtime preflight pass. Database data was preserved; portable MySQL is READY as PID `33864`, DNF90Server SHA256 `AFF4FA90DDC92AC7E209218C8B019FF6B6D61DA479FA6CF30FE59084BE11F6F0` is READY as PID `55180`, all 120 listeners accept connections, and fresh stderr is empty. The previous server remains recoverable from `runtime/backups/server/DNF90Server.before-local-actor-lifecycle-20260728-1645.exe`.

## 2026-07-28 - restore the aura-skin expansion without opening the avatar panel

- Live relog acceptance disproved the previous op863 restoration boundary: the durable `aura_flag=1` was correct, but sending an ordinary unsolicited success result opened the avatar inventory panel instead of silently restoring its expansion.
- Re-read current `NoPack.exe` `sub_1D25590`. Its success branch first creates scene UI owner `0xC9`, switches that owner to the avatar page when necessary, and only then applies the `11/8` unlock event. An unmarked op863 response therefore remains correct only for the client-authored expansion-ticket request.
- Relog restoration now carries a compatibility-unit marker. The matching `90CN.dll` class1 hook consumes only that exact marked op863, caches the pending unlock, and does not run the native result handler while owner `0xC9` is absent. When the player later opens the avatar panel through native `sub_2FDD1A0`, the patch replays `sub_1D25590` after the panel is fully created, so the slot unlocks without the server opening any page. Ordinary ticket-use success remains unmarked and follows the original request/result path.
- Bridge regressions require the marked silent-restore body while preserving the one-byte ticket-use success body and the closed-slot no-push rule. The Win32 Release compatibility DLL rebuild succeeds with the current EXE prologue checks.
- `gofmt`, focused and complete bridge tests, `go list`, every DNF-module test, control/release tests, required `go vet`, the Win32 client build, BAT-only offline preflight, and online runtime preflight pass. Packaged and installed `90CN.dll` SHA256 is `380015CCDA3E7C4C1D89FB912EEF86A1A0578E5FB6527BDA3A0913B0292FF370`; production DNF90Server SHA256 is `6C8C6D5417464FF7040F642FD5AD3129691D21A95811F51FC7B9DC09F9CB2A42`. Live DNF PID `38924` logged the exact 21-byte op863 restore as `aura-skin silent restore cached` rather than entering the native UI handler. Database data was preserved; portable MySQL is READY as PID `42068`, DNF90Server is READY as PID `57804`, all 120 listeners accept connections, and fresh stdout/stderr logs are empty.

## 2026-07-28 - complete owned adventure-group activity fields

- Rechecked current `NoPack.exe` response reader `sub_C7F960` and basic-page presenter `sub_9F44D0`. The 7,420-byte state stores management level at raw `+0`, points at `+8`, consecutive-login days at `+20`, the 100-entry roster at `+60`, and twenty content counters at `+7348`. Each counter is `u16 count + u8 type`; the previous writer reversed that tuple and consequently exposed every row as the same content type.
- The full class0/op1340 and class1/op1403 projections now write the corrected counter tuples and the durable UTC+8 consecutive-login streak. The streak uses one key-scoped account-metadata value so date/count cannot be partially updated, preserves unrelated account data, handles same-day retries idempotently, resets after missed days, and saturates safely.
- Accepted ordinary dungeon settlements whose pre-settlement character level falls inside the runtime PVF `[recommended level]` range now increment the character's durable recommended-dungeon clear count inside the existing experience/SP transaction. The existing settlement receipt prevents packet retries from incrementing twice, and the account packet sums all roster characters with a `uint16` wire cap.
- No database column or migration is required: the login streak uses the existing account-metadata table and clear counts use the existing character-stat storage. PVP, Anton, Luke, Tower of Despair, Tower of Grief, highest-proficiency, collection, expedition, and shop state remain zero/unset until their authoritative gameplay owners exist; no synthetic progress was introduced.
- Focused adventure-group, dungeon-owner, and complete bridge regressions cover tuple ordering, login-day rollover, metadata isolation, PVF range validation, transaction replay, account aggregation, and the emitted wire offsets.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, and the production build pass. DNF90Server SHA256 `9D7EB46934572BA6BA48BBEC9A0F7AB5DCE345D5AB1F7A58C1CCFC77D6EB74A6` is deployed as PID `58112`; portable MySQL restarted with all data preserved as PID `57176`, readiness is green, and all 120 listeners accept connections. The old server is recoverable from `runtime/backups/server/DNF90Server.before-adventure-20260728-160213.exe`.
- The exact offline `control.bat check --skip-database` still reports the pre-existing client compatibility mismatch: current Release/package/manifest expect `90CN.dll` SHA256 `380015CCDA3E7C4C1D89FB912EEF86A1A0578E5FB6527BDA3A0913B0292FF370`, while the open client has the earlier `C708380052EC1D5992AF7C3998BF3707D3E6FD6AAFDAA80B19BFD5D42A69532A`. No running client was killed and no client file was overwritten; server readiness/status preflight passes independently.

## 2026-07-28 - commit real card equipment rewards and restore the working town/appearance packet boundaries

- Fresh settlement evidence isolated the shared reward/exit failure before any town transition: both free and paid op71 selections logged `dungeon reward is invalid`, `settlementCardRewardCommitted` stayed false, and op72 was consequently blocked. The generated equipment reward already carried a runtime-PVF inventory page, but the domain grant was called with `MainSlots=0` and rejected every item bundle before the transaction. Card reward plan v2 now freezes each item's PVF-proven slot range, the dungeon owner allocates or merges only inside that range, and no synthetic bag capacity is introduced.
- A successful card item transaction now emits the repository-backed class0/op14 row for the exact changed main-inventory slot after the gold snapshot. Regression coverage requires the item to exist in the durable slot, appear in the incremental packet, mark the free reward committed, and allow the immediately following op72 return-to-town route to complete and detach the dungeon runtime. The op71 terminal/ghost marker is restored to zero, matching the working card reader boundary.
- The supplied July 5 working source disproved the two most recent town/appearance packet changes. Ordinary op36 and previous-village movement again emit their single authoritative op24 transition instead of the later-added op22/op23 prelude, and op36 no longer forces a deferred scene tail immediately before movement. This removes the duplicate actor lifecycle that let the controlled actor run outside the camera.
- Ordinary equipment and avatar moves again follow the working source's `op19 ACK -> authoritative containers -> typed mode0 appearance snapshot` boundary. The mode0 snapshot is repository-backed and contains all fourteen appearance rows, including explicit empty rows after unequip; the later full-mode1 replacement is no longer used for this live mutation. Creature slot 26 keeps its separate rebuild/state path.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, production build, BAT-only offline preflight, and runtime preflight pass. Database data was preserved across the controlled restart; portable MySQL is READY as PID `56500`, DNF90Server SHA256 `FF7FCCD0D82D55F987BC9D741E93453CF0CC878D76E3253A44163FFB3628B2A8` is READY as PID `21708`, all 120 listeners accept connections, and the fresh stderr log is empty.

## 2026-07-28 - preserve equipment quality across inventory-to-worn mode1 rebuilds

- Live op19 evidence reproduced an equipment swap followed by the repository-backed container refresh and full mode1 actor-equipment rebuild. The durable equipment row retained its 0x77 quality seed, but `sub_1D77560`'s create-row v74 grade/instance field was always projected as zero; this made the already-live actor redraw the equipped item as top quality even when the same item showed a lower grade in the bag.
- Runtime-moved current 0x77 equipment now carries its authoritative quality seed into that mode1 create field. Historical PVF starter rows keep their proven zero initialization value, and explicit `mode1_v74` compatibility overrides still take precedence.
- Focused projection and end-to-end post-op19 transport regressions require the same nonzero quality seed in the durable item row and the emitted mode1 create row. The existing PVF starter and zero-grade compatibility cases remain locked.
- Restored the missing repository import in the newly synchronized dungeon-card reward runtime so the combined source tree builds with its existing main-inventory reward refresh.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, and required `go vet` pass. Candidate DNF90Server SHA256 `6CBCE89F58D3F37E2122D9C4981CF915A36E2EC18BA6F3CDDBCDDD68F6AFCE66` is built under `runtime/bin`; runtime replacement and BAT preflight are deferred because DNF PID `58312` is still active.

## 2026-07-28 - complete the runtime-PVF Growth Contract effects

- The reported Growth Contract gap was real but narrower than a missing entitlement: live dungeon evidence already proved that active type `84` added `255` and `624` clear experience in two recent settlements, and a base monster award of `14` advanced durable character experience by `16`. The missing coverage was quest experience and the two PVF-owned drop-rate effects; the monster-award event also logged only its base value, which made the committed bonus easy to miss.
- The active `etc/premiumlist_new.etc` is now the effect authority as well as the activation-item authority. Its type-84 block parses and regression-checks `[bonus exp] = 20`, `[quest item drop rate] = 20`, and `[independent drop rate] = 20/23/27/30`; no hard-coded compatibility multiplier is used when the runtime PVF effect block is unavailable.
- Active Growth Contract now applies the PVF percentage to quest completion experience inside the atomic finish owner, stores base/bonus/total in the durable replay receipt, and returns the total in the current op34 finish ACK. Reconnect or retry replays the same committed total without granting it twice, while an expired contract contributes zero.
- Quest-item rolls combine Growth Contract's PVF percentage with the existing quest-helper service and cap at certainty. Runtime independent-drop rolls apply the PVF party-sized percentage; the current local dungeon runtime owns one actor and therefore uses the authoritative first value. Logs expose the effective Growth Contract percentage on both drop paths.

## 2026-07-28 - restore town camera binding and live equipment/avatar appearance refresh

- Live acceptance disproved the preceding duplicate-op24 repair as a complete camera fix: removing the unsolicited co-presence op24 was correct, but a normal client-authored op36 still emitted only op24. The working cold-login route emits class0/op22, class0/op23, then class0/op24. Rechecked current `NoPack.exe` directly: `sub_1D89590` stores the accepted local actor's town, area, position, direction, and state before rebinding that actor, while `sub_1D901D0` applies the following area roster. Ordinary op36 moves and the legacy previous-village return now preserve that exact `op22 -> op23 -> op24` sequence instead of sending the roster transition alone.
- The durable location mutation and all three writes are one failure boundary. If op22, op23, or op24 fails, the previous repository location is restored; regressions validate the exact nine-byte op22 body, ten-byte op23 body, op24 row, order, single emission, and rollback at each write.
- Equipment and avatar unequip persistence was already correct, but the post-op19 appearance action replayed op2 mode0. Mode0 is an object-creation route and did not clear removed slots on the already-live town actor. Current `NoPack.exe` mode1 handler `sub_2008010` resolves that existing actor and calls `sub_1D77560` to replace its complete worn-equipment projection, so ordinary equipment/avatar moves now send the repository-backed full mode1 snapshot after the ACK and container refresh.
- Creature slot 26 remains an explicit mode0 object rebuild because it owns a separate scene-object lifecycle followed by op105/op102. A dedicated aligned post-action separates that path from normal equipment/avatar mode1 refreshes, preventing this fix from regressing pet creation, growth bars, or names. Regressions cover both an equipped mode1 row and an empty post-unequip mode1 snapshot that clears all removed slots.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production build, and BAT-only offline/runtime preflight pass. Database data was preserved; portable MySQL is READY as PID `30256`, DNF90Server SHA256 `18E81373C2019DD2C87B809307757CC6483B72C818A81A53C7BE5847062A7DA1` is READY as PID `54644`, all 120 listeners accept connections, and the fresh server stderr log is empty.

## 2026-07-28 - keep town camera ownership on the local actor

- Fresh live packet evidence for the first accepted town-area `op36` showed two identical class0/op24 packets containing the selected character: the required sequenced transition from `handleTownSetUserArea`, followed by a fixed-header `town_copresence_enter_0x0018_roster_to_newcomer` replay even though no peer player existed. The character could continue moving, but the camera stopped following and allowed the local actor to leave the screen.
- Rechecked current `NoPack.exe` `sub_1D901D0` through the active IDA database. Op24 reads `u8 town, u8 area, u16 count` plus eight-byte actor rows and takes a special scene/local-player branch when a row ID matches the selected character. It is a scene transition, not a repeatable co-presence roster notification.
- Removed the co-presence module's second op24 entirely. The one authoritative op36 transition continues to contain the local actor exactly once; peer visibility continues through the current-EXE actor creation/binding sequence (`op2 mode0`, full `mode1`) followed by the per-actor `op23` area state in both directions.
- Added regressions requiring a manager-backed solo op36 to emit only one op24, a peer join to end after remote mode0/mode1/op23 without trailing transition bytes, and an empty peer set to write no co-presence packet.
- Focused and complete bridge tests, `go list`, every DNF module test, control tests, required `go vet`, `gofmt`, `git diff --check`, production build, and BAT-only offline/runtime preflight pass. Database data was preserved; portable MySQL is READY as PID `55392`, DNF90Server is READY as PID `20748`, all 120 listeners accept connections, fresh stderr logs are empty, and the deployed server SHA256 is `5454C9043BD6E74355C97B5F7F7D9E32DB677C7B404D0704DE854086912E13EA`.

## 2026-07-28 - commit the cold-login scene tail before the first town-area reset

- Live packet evidence showed that the main cold-login initialization was not globally missing: the selected-character route sent all six op13 container snapshots, mode0/full mode1, the op105 creature table, and the equipped-creature op102 projection. The failure was the remaining `sceneBootstrapTailDeferred` lifecycle. The client completed the typed op24 transition and immediately sent op36 for its first ordinary town-area change; op36 reset `initialTownRouteCharacterID` and `initialTownRouteStage` before any heartbeat committed the tail. Every later retry consequently logged `actor_binding_or_scene_transition_incomplete`, leaving the scene-ready container rehydrate, creature projection, rental wallet, and premium state absent for the rest of that session.
- The current EXE's first accepted op36 is now the scene-ready ownership boundary for that pending tail. After any pending dungeon return is committed and before the town route lock/reset, the bridge idempotently commits `sendDeferredSelectSceneTail`. If actor/transition readiness is still unproved, op36 is rejected without clearing the pending route instead of silently losing initialization state. Ordinary town movement after an already-completed tail remains unchanged.
- Added a regression that reproduces a finalized initial actor followed directly by op36 with no intervening heartbeat. It requires the repository-backed main-container refresh to precede the typed op24 area response, the tail to finish exactly once, and only then the old initial route to be cleared.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production build, and BAT-only offline preflight pass. DNF90Server SHA256 `71DB3FB30EF07B41D5C896C64BD17F880D6CBA22819D5B2D6519F12F10DF83BD` is deployed as PID `32664`; portable MySQL restarted with all data preserved as PID `42172`. Live client acceptance must confirm that the first op36 logs `game-upper-select-character-scene-tail-send` followed by `game-select-scene-tail-active-finished`, with no later `actor_binding_or_scene_transition_incomplete` loop.

## 2026-07-28 - repair both dungeon-settlement card rows and durable rewards

- Re-audited the current `NoPack.exe` settlement flow and the supplied ServerS4A12 reference. Class0/op71 request `ValueA=0` selects the upper free row, `ValueA=1/2` selects the lower paid row, and `ValueB` is the party-member index rather than the visible card index; the clicked position among each row's four cards remains client-local. Replaced the incorrect eight-card-index interpretation and shared selection gate with independent free/paid row state, so selecting either row first no longer blocks the other and the response projects the selected member through `StateA`/`StateB` exactly as the client reader expects.
- Settlement reward plans are frozen once from runtime PVF data. The upper row now carries its real gold and item faces in op35 slots `0/1`; the lower row uses slots `4/5`, follows the reference RNG order and rarity thresholds, and returns its paid reward tuples through op71. Gold commits immediately, while item rewards use the existing atomic mailbox fallback because current evidence still does not establish a safe main-bag capacity; no inventory shape or item data is invented.
- Added the reference four-second upper-row auto flip on the shared gameplay time queue, with cancellation on manual selection and every dungeon/session transition. Manual op72 actions and completed op42 exits first finish the outstanding free reward, op72 focus remains ACK-only, and retry/select-other/return-to-town no longer become trapped behind an unselected card.
- Monster reward accounting now records champion, super-champion, and boss experience once after a successful award and writes those values into the settlement score packet instead of leaving all three at zero. Regression coverage includes paid-first/free-second selection, both paid request values, timer auto flip, manual timer cancellation, exit auto-completion, split card faces, score mapping, and saturating counters.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the bridge/control suites, required `go vet`, the production build, and BAT-only offline preflight pass. DNF90Server SHA256 `CB5C5611AB3489F1D5C961D60909DCB306CCB53C6CD834A90AE05D6F346C5648` is deployed as PID `18280`; portable MySQL restarted with all data preserved as PID `39036`, readiness is green, and all 120 listeners accept connections. Live client acceptance of both four-card rows remains required.

## 2026-07-28 - merge epic-production phase one and runtime PVF special drops

- Adapted the supplied epic-production implementation to the current split gameplay-module architecture instead of restoring its stale monolithic dispatch and town-route files. Class-default op1417 now selects a PVF-authorized job target, class-default op1419 atomically consumes the catalyst/material pair and persists charge/weekly state, and the actor-bound town bootstrap restores the selected target and progress. All ownership checks use the authenticated session account rather than the local-profile fallback account, so two-account sessions cannot cross-write a character.
- Connected the supplied `Etc/Independent_Drop.etc` and `Etc/WorldDrop.etc` parsers to the authoritative monster-death drop planner. Independent rules honor monster, difficulty, level, probability, repeat count, and inline weighted lists; WorldDrop uses the killed monster level and its configured weights. Both consume the existing room LCG and pass selected templates through the runtime PVF item resolver before any scene object is created.
- The supplied parser deliberately skips independent `listFlag=2` external-list references. This is visible in diagnostics rather than silently approximated. The production `Script.pvf` SHA256 `1F82563B9A84D1CBEB2B9885F5CC002BDF8C18AAD7371D69F30231E1D9DF75E7` contains no such rows: real-PVF smoke tests parsed 720 independent monsters / 1,904 entries and 199 WorldDrop levels / 13,064 weighted candidates; the epic-production catalog also passed its real-PVF smoke test.
- Added parser, probability, atomic transaction, packet-body, durable-state, target-validation, and gameplay-module ownership regressions. `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, and required `go vet` pass. A non-deployed verification server was built as `runtime/bin/DNF90Server.merge-verify.exe` at SHA256 `84BDFE41E1A5A6A32980C6AA834049C47BE937173C1C3C06948588C11AF59642`.
- Deployment was intentionally not performed during this source synchronization. BAT-only offline preflight correctly refused to rewrite generated runtime configuration because the currently deployed DNF90Server was still running as PID `33592`; portable MySQL remained PID `27528`, and no live process or database state was changed.

## 2026-07-28 - route all four premium-contract right clicks through the audited op44 writer

- Fresh Overlord Contract item `31` tracing disproved the earlier eligibility-gate diagnosis. The current item wrapper has an empty scene-restriction vector, and its native can-use cache entry is `1`; nevertheless, right-click reaches none of the three known contract callbacks and emits no class1/op44 packet. The missing behavior is therefore the current item class's dispatch into the contract writer, not a failed eligibility test.
- Added a current-EXE window-click-cycle fallback limited to the four `premiumlist_new.etc` contract families. The native window procedure runs first. Only when an entire right-button down/up cycle produces no native op44 does the fallback resolve the hovered allow-listed template, prefer the native group-39 selection, otherwise look up slots `3..359` through the current main-inventory manager and send the already-audited op44 body. It prefers the exact wrapper/item-data object under the mouse and permits a template-only fallback only when the main bag has one match. An op44 counter spans the full click cycle so DirectInput/native callbacks cannot race into a duplicate activation.
- The first live window-procedure build activated Overlord item `31` from main slot `69` and Expert item `44` from main slot `70`; each received its class1/op44 ACK, class0/op66 activation notification, and durable op14 decrement. It also exposed why a passive row-cache slot was insufficient: item `30` existed simultaneously in main slot `0:68` and warehouse slot `2:22`; the later-parsed warehouse row made the fallback send main-list slot `22`, which the durable owner safely rejected without consuming anything. The revised native-manager resolution excludes that cross-container collision instead of trusting parser order.
- Live Overlord use then exposed a separate count-projection defect: the owner transaction reduced the durable item-31 count to `108`, but the repository-backed op14 row still encoded the stale legacy `amount`/`amount_or_count` value `117`, so the backpack appeared not to consume the item and a relog would reproduce the same display. Main-inventory rows now preserve equipment quality-seed semantics first and otherwise encode authoritative `ItemStack.Count`; this repairs both incremental op14 refreshes and full container bootstrap without inventing client state. Unit and bridge transport regressions reproduce stale `RawEntry` and `Extra` mirrors and require the emitted count to be `108`.
- The refresh is deliberately shared by every successful single-item op44 use, not limited to premium contracts: ordinary PVF-proven consumables, all four visible contract families, and revive-coin consumables all enqueue the source list/slot after the durable mutation. A surviving stack emits its committed count, the last item emits the common deletion row, and blocked/failed uses emit no success refresh. Explicit ordinary-item, final-item, revive-wallet, and four-contract regressions lock that invariant.
- Removed the high-volume template-read diagnostic hook after it established the live wrapper, selection, and eligibility state. The revised Release/Win32 DLL builds with zero errors at SHA256 `C708380052EC1D5992AF7C3998BF3707D3E6FD6AAFDAA80B19BFD5D42A69532A`; Release output, packaged copy, runtime copy, installed client DLL, and the compatibility manifest match byte-for-byte. The preceding `3DF690A649C970D409703AA7BB4DD03E8BB6AD6BC543011F2650207DE3CC5DA6` runtime and client DLLs are recoverable from `runtime/backups/client-patch/20260728-130843_contract-main-inventory-resolver`. Live acceptance of the revised resolver and the remaining contract families is still required.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production build, and BAT-only offline preflight pass. DNF90Server SHA256 `E1786DC6D74990748021EB7F41838AE58B4C8B4116234BDF5023CB4ED0F7A2AC` is deployed as PID `50188`; portable MySQL restarted with data preserved as PID `55824`, readiness is green, and all 120 listeners accept connections.

## 2026-07-28 - allow real zero-value PVF items in player mailbox attachments

- The live class0/op94 request proved that the same-account recipient picker and GB18030 recipient decoding were already correct: sender `pouut` selected `去去去`, and the bridge decoded that exact recipient before rejection. The current client's Chinese “无法向自身发送邮件” popup was only its mapping for the server's shared error code 7, not evidence of a self-recipient comparison failure.
- The actual rejection was attachment item `3166` at list `0`, slot `125`, count `3`. It exists in the runtime PVF item index and its document parses successfully, but the document omits `[price]`; the previous mailbox adapter incorrectly required every sendable item to expose a nonnegative price and rejected the durable attachment before the owner transaction.
- A listed and successfully parsed PVF item with no `[price]` now contributes zero to the value-based postage component. Fixed per-attachment postage remains unchanged, and runtime-PVF existence, page range, stack limit, durable slot, item ID, count, ownership, and atomic sender/recipient transaction checks remain mandatory. No database column or migration is required.
- Added adapter and end-to-end owner regressions for the reproduced item/list/slot/count, including successful sender removal, fixed postage deduction, and recipient mailbox persistence. `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production build, and BAT-only offline preflight pass.
- Portable MySQL was stopped cleanly with all data preserved and restarted as PID `27528`. DNF90Server SHA256 `35768EB256528C209AA235445BAF98644A9DAAC722485CD19A2219562FD1A804` is deployed as PID `33592`; readiness is green and all 120 listeners accept connections.

## 2026-07-28 - repair the native dungeon pickup container and suppress dungeon personal-info mode3

- A read-only inspection of the live failing client pinned the pickup failure before the network boundary: the controlled actor was valid, but its native field at `actor+0x646C` was null. Every audited manual pickup route checks that field before calling `sub_232F810`, so highlighted drops never reached the client's class0/op43 writer. This explains why repeated server-side op43 parsing changes had no effect: no pickup request was being produced.
- Current `NoPack.exe` supplies the exact missing initialization rather than requiring an invented packet or object. `sub_249CCE0` allocates `260` bytes with `sub_759030`, constructs the object with `sub_233CFD0(memory, 0, 366)`, and stores it at `actor+0x646C`; native teardown already owns the same field. `90CN.dll` now repairs that object only when the controlled dungeon actor reaches either audited manual pickup entry with a null field, then resumes the unchanged native eligibility checks and op43 writer.
- Re-auditing `sub_2008600` also disproved the previous changelog claim that mode3's later owner mismatch must remain native. The first context-0 patch selected the local actor, but the second comparison still treated owner `0` versus the current scene channel as remote and called `sub_24A0920(..., 1)`. The compatibility unit now maps context 0 to local at both comparisons.
- Dungeon entry and finish-loading no longer emit mode3. It is a personal-information/equipment snapshot and visibly opens that panel in the current client; mode1 remains the dungeon actor/equipment initialization packet. Packet-order regressions require mode3 to remain absent and retain the established room placement and user-state order.
- Release/Win32 rebuilt with zero errors at SHA256 `D89E85073D7B4F65906AD5355307777EB89DA596BFDF30EB9B1881CAFF427B63`. Release output, packaged DLL, installed client DLL, and the strict compatibility profile now match byte-for-byte; the replaced DLL is recoverable from `runtime/backups/client-patch/20260728-1145_pickup-native-container`.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, and the production server build pass. DNF90Server SHA256 `242A017544A093667ED33CCDFA45FEE318DA56C3C22BDE208AB7CD9F074CBF6D` is running as PID `56012`; portable MySQL is PID `15076`, readiness is green, and all 120 listeners accept connections.

## 2026-07-28 - resolve the context-0 mode3 actor before its native runtime finalizer

- The first live test of the context-0 dungeon mode3 crashed the client at `10:57:27.895`. `90CN_trace.log` records `0xC0000005` at `DNF.exe+0x020A0926`, writing through `ECX=0`; the packet trace stops on class0/op2 mode3 before the remaining scene finalizers can run.
- Current `NoPack.exe` proves the cause rather than a body-length error. At `sub_2008600+0x137`, mode3 compares packet owner context `0` with the current scene channel `206`, takes the remote-actor lookup, and receives a null ability/runtime object from `sub_2002FC0`. The later mismatch branch calls `sub_24A0920` with that null object and faults at its first `this+0x6702` write.
- The existing compatibility DLL already maps the bridge's established context-0 sentinel to the current scene only in mode0. Added the same narrowly scoped mapping at mode3's actor-resolution comparison (`RVA 0x01C08737`, exact native bytes `3B CE 74 3F 8B B5 50 FB FF FF`). The later native mismatch test is deliberately unchanged, so the selected actor's real runtime receives the native context-difference flag instead of bypassing the finalizer.
- Release/Win32 rebuilt with zero errors. Release, packaged, and installed DLLs are byte-identical at SHA256 `166DF4FE5282049790A4C24B4DAE430D96D652DF404DFAD23B5E684A4029A7D9`; the strict compatibility profile pins that hash. The previous installed DLL is recoverable from `runtime/backups/client-patch/20260728-110924_mode3-owner-resolve`.
- DNF90Server PID `50180` and portable MySQL PID `17144` were not restarted. `STATUS.bat` reports the database ready and all 120 listeners accepting connections. Live acceptance remains required: the next dungeon entry must log `mode3-owner-resolve ... current-scene-context0-remap`, produce no exception at `0x024A0926`, and then emit C2S op43 when pickup is pressed.

## 2026-07-28 - correct the mailbox account-role request/response pair

- Re-audited the current `NoPack.exe` mailbox UI from its control IDs and handlers. Object offset `+380` is `IDC_TREE_MYCHARACTER`; expanding an empty server node sends class1/op789 with exactly one `u8 server_id`. The registered mailbox population handler is class0/op718, beside the mailbox op97/op98/op99 notification handlers, not the fixed 1200-byte op789 reader previously attributed to this UI.
- Removed the proactive mailbox-open op789 cache and its bridge-owned fixed-row projection. Mailbox open again emits only the op96 ACK and class0/op97 snapshot; an actual op789 expansion request now returns class0/op718 as `u8 server_id, u8 count, count * (u32 character_id, DSTR GB18030 name, u8 flag)`.
- Live trace at `11:16:51` proved the role query and body were already correct: the client sent op789/server `0` and received both other account roles with their exact GB18030 names. The tree remained empty because that first implementation sent op718 as class1, so the class0 mailbox notification table never invoked `sub_1D7FE60`; the response now uses the registered class0 route.
- The response is built from the existing durable character repository and excludes the selected character. No database field or migration is required: account ID, character ID, and name already provide every field consumed by the current client.
- Added domain, body-layout, malformed-request, selected-role exclusion, cross-account exclusion, and bridge transport regressions. `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production build, and BAT-only offline preflight pass.
- Portable MySQL was stopped cleanly with all data preserved, then restarted as PID `53868`. DNF90Server SHA256 `435857FA3E5185BCE878BC1E74713B55530B2ED7266D5A0EEF6F336C30B1F734` is deployed as PID `34424`; readiness is green and all 120 listeners accept connections.

## 2026-07-28 - match the pickup-proven dungeon context-0 actor tuple exactly

- Live testing of the one-shot mode3 with owner `206` still produced no client op43 and visibly opened the personal-information panel. The packet was in the right sequence but took the wrong current-EXE owner branch.
- Re-read the complete successful `game-000737` trace rather than inferring owner semantics: its dungeon mode0, pre-mode1, full mode1, one-shot mode3, and op9 all carry owner/context `0`. That route emitted and completed multiple op43 pickups. The server channel id is transport/session metadata and is not the dungeon actor context byte.
- Dungeon initialization now reproduces that tuple exactly while preserving the already-proven order `mode1 -> mode3 -> op359/op356/op124/op9/op120 -> op3`. A new explicit-context mode3 builder keeps this correction scoped to dungeons; town and explicit personal-information builders continue using their existing committed-owner behavior. The post-op37 mode3 replay remains absent.
- Updated actor-packet regressions to require context `0` even when the session channel is nonzero. Focused entry/pickup tests, `go list`, all DNF-module tests, the complete bridge/control suites, and required `go vet` pass.
- BAT-only offline preflight passed after server and portable MySQL stopped cleanly with database files preserved. DNF90Server SHA256 `44FA8E2ACEABB1FF5DF877D4DAC9FEECC8D5668924459960D29208D8EE7CD70C` is deployed as PID `39148`; portable MySQL restarted as PID `20320`, and all 120 listeners accept connections.

## 2026-07-28 - restore the one-shot dungeon mode3 runtime finalizer required by pickup

- The first owner correction was present in the fresh failing trace: dungeon mode0, mode1, and op9 all carried the committed channel owner `206`, but the client still emitted no op43 pickup request. This disproved owner mismatch as the remaining pickup gate.
- Compared the complete failing sequence with historical session `game-000737`, which produced and completed multiple op43 pickups. The successful route contains exactly one full mode3 after the post-op29 mode1 and before op359/op356/op124/op9/op120/op3. Current `NoPack.exe` `sub_2008600` performs actor runtime/stat finalization not provided by mode1; removing it left drops visible while the native pickup writer stayed disabled.
- Restored only that first owned-dungeon mode3, using the committed channel owner. The later mode3 replay after the client's op37 finish-loading request remains prohibited; that replay, rather than the one-shot runtime initializer, was the route that reopened the personal-information panel.
- Packet regressions now require `mode1 -> mode3 -> op359/op356/op124/op9/op120 -> op3`, validate the mode3 object key and committed owner, reject mode3 for stale/unowned scenes, and retain the no-post-finish-replay boundary. Focused entry/pickup tests, `go list`, all DNF-module tests, the complete bridge/control suites, and required `go vet` pass.
- BAT-only offline preflight passed after both managed processes stopped cleanly with database files preserved. DNF90Server SHA256 `9D06FC441124972DA015351FDC9C3E3C9E3EADE87A4EFAE97220313F338E74E0` is deployed as PID `47084`; portable MySQL restarted as PID `38468`, and all 120 listeners accept connections.

## 2026-07-28 - restore Crystal Contract and aura-skin expansion from their authoritative owners

- Crystal Contract selection no longer searches the selected character's ordinary main bag. The current client owns black, white, red, blue, clear, and gold cubes in the account-shared crystal warehouse at list-0 slots 354 through 359; selection, login projection, and one-cube consumption now validate those exact PVF-derived item/slot pairs.
- Crystal consumption now uses the account-inventory/character-asset unit of work, so decrementing the shared cube stack and clearing the final selected-cube state commit or roll back together. Surviving stack raw counts and metadata stay synchronized with the durable count.
- Added the signed relational `premium_crystal_selection` mirror column with default `-1`. The previous map-only value passed memory tests but was discarded by the MySQL allow-list, which explained why every login returned op898 with selection `-1`.
- The aura-skin expansion already persisted correctly as `aura_flag=1`; the missing half was current-client restoration. After the town actor and current containers are ready, an open character now receives the IDA-proven op863 success marker, whose current NoPack handler unlocks the aura-skin slot without reading any business payload. Closed characters receive no unlock packet.
- Live relog evidence then showed the relational crystal selection and scene-tail op898 were both correct while the later-opened material panel still displayed no contract. Current NoPack `sub_1E7FAD0` only calls `sub_1E7F810` when inventory panel owner `0xBE` already exists; an earlier notification is discarded rather than cached. The bridge now replays the durable op898 state after the client's first successful op36 town-area confirmation, the first client-authored boundary after the town UI becomes interactive.
- Added domain, relational repository, schema-migration, bridge transport, relog, last-cube, wrong-owner, and ordinary-main-bag rejection regressions. `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, the production build, and BAT-only offline preflight pass.
- The first startup audit caught that create-table DDL alone did not alter the existing character table, so deployment was not accepted until an additive signed-column migration was added and rerun. MySQL now reports `premium_crystal_selection` as `TINYINT NOT NULL DEFAULT -1`, the existing open character retains `aura_flag=1`, and no character/inventory data was cleared.
- DNF90Server SHA256 `E09D338014FCEB120643C64EFC1E48FBCDFAE937E59C8A2162065F36605B8CEB` is deployed as PID `55460`; portable MySQL is PID `18308`, readiness is green, and all 120 listeners accept connections.

## 2026-07-28 - bind dungeon packets to the committed local owner so the client emits pickup

- A fresh failing trace on channel `206` showed drops arriving through op38 but no client op43 pickup request at all. The earlier op3-after-op120 ordering change was therefore insufficient: dungeon mode0, mode1, and op9 still identified the player with owner `0` instead of the committed channel owner.
- Current `NoPack.exe` IDA evidence explains the failure. The mode0 handler creates the selected local actor only when the row owner matches the client's committed owner; an owner mismatch enters the remote-actor creation branch. Mode1 then resolves only an existing local actor, so the client could display the room and drops while never enabling its native pickup writer.
- Dungeon-only mode0, mode1, and op9 now use `currentCommittedChannelOwnerOrDefault`. Town and explicit user-information routes are unchanged. Mode3 remains excluded from dungeon initialization because it is the visible personal-information panel route; the correction no longer depends on mode3 accidentally rebuilding the local actor.
- Added packet regressions that require all three dungeon actor packets to carry the committed owner while preserving the existing `op120 -> op3` activation order and rejecting mode3. Focused pickup/entry tests, `go list`, all DNF-module tests, the complete bridge/control suites, and required `go vet` pass.
- BAT-only offline preflight passed after portable MySQL was stopped cleanly with all data preserved. DNF90Server SHA256 `7B86332B745D179573F5180B8B8684AE0A9BCCCDAC141CB12D83E73137DD5E03` is deployed as PID `54172`; portable MySQL restarted as PID `24156`, and all 120 listeners accept connections.

## 2026-07-28 - restore current-client dungeon pickup activation order

- Compared the failing route with the last runtime trace that actually accepted class0/op43 pickup requests. The successful client commits the room placement with op120 before class0/op3 marks the selected local actor active; the regressed route sent op3 before op124/op9/op120, so drops remained visible but the client never enabled its native pickup writer.
- Dungeon initialization now keeps class0/op2 mode3 completely excluded (so the personal-information panel is not opened), sends the existing mode1 actor/equipment state, commits the scene through op120, and only then sends op3 for ordinary owned dungeons. PVF tutorial scenes retain their proven finish-loading deferral.
- Updated the exact packet-order regression to require `mode1 -> op359/op356/op124/op9/op120 -> op3` and continued to reject mode3. Focused entry/pickup tests, `go list`, every DNF-module test, the complete bridge/control suites, required `go vet`, and the production build pass.
- The production executable includes both this correction and the concurrent mailbox transport correction at SHA256 `9666495A5D9B323407710A71D73E15EE2147CF498CE9C9A175A1675A8B1EF643`. The current verified processes are DNF90Server PID `51400` and portable MySQL PID `55524`; startup preflight reports all 120 listeners accepting connections and database files were preserved.

## 2026-07-28 - send the mailbox account-role cache on its IDA-proven raw-upper route

- Live mailbox-open reproduction produced `CrashDNF2.cra` at `10:05:46`. The game process faulted at `0x03B107CA` while writing across the `0x09141000` page boundary immediately after the server sent op789 through the 28-byte latest-game transport; the compatibility protocol logger saw op96/op97 but no raw-upper op789. The earlier direct-request experiment had the same failure shape at `0x03B25D26`.
- Current `NoPack.exe` IDA confirms op789 is registered to `sub_A6E590` in the raw-upper dispatcher. That handler immediately calls `upper_pkt_read_u32`, copies the declared raw block into its fixed 1200-byte stack table, then reads the u32 role count. The recipient body was correct, but the latest-game transport header shifted those reads and caused the overflow.
- Same-account recipient projection now sends the unchanged `u32(1200) + 100 * 12-byte rows + u32(count)` body as class1 raw upper op789 with the production 16-byte server header. Mailbox ordering remains op96 ACK, class0/op97 snapshot, then class1/op789 cache. Regressions reject the old latest-game framing and verify both empty and populated account rosters.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the full bridge/control suites, required `go vet`, the production build, and BAT-only offline preflight pass. No old binary was restored: the forward-fixed DNF90Server SHA256 `9666495A5D9B323407710A71D73E15EE2147CF498CE9C9A175A1675A8B1EF643` is running under the resident launcher as PID `51400`; portable MySQL data was preserved and restarted as PID `55524`, and all 120 listeners accept connections.

## 2026-07-28 - recover contract right-click before the native eligibility gate

- Fresh client logs proved that right-clicking a premium contract reached none of the three previously covered op44 callbacks. Current-EXE call-site evidence places `sub_275A060` and `sub_275A140` earlier in the item-action route; the latter can return from its native `0x7D4` eligibility check before the active-coupon writer is called.
- The compatibility DLL now covers both pre-panel item-action callbacks. It always preserves the complete native callback first, then sends the already-audited class1/op44 body only when the current main-bag selection is an allow-listed `premiumlist_new.etc` contract and no native op44 was observed. The existing recursion and packet-count guards prevent a lower callback from duplicating the fallback.
- Rebuilt and installed the Win32 Release DLL into the runtime copy and verified client directory. SHA256 `7290352C6A5EF6D3BE31B0EB59E25E0C296C254A340CD97C24B08D9D61E2305B` matches the compatibility manifest. A clean launcher start installed both new current-EXE hooks at `0x0275A060` and `0x0275A140` with `contract-use compatibility result=1`; the focused control/release suites pass.

## 2026-07-28 - isolate client wallet, legacy-equipment, and dungeon-panel compatibility repairs

- Main-list slot `0` is now treated as the current client's gold-wallet reservation. If a historical ordinary item owns it, the bridge resolves that item's active-PVF page and the inventory owner atomically moves the complete durable stack to the first empty slot in that page. No occupied row is overwritten; an unresolved or full PVF page remains fail-closed and is logged instead of hiding the conflict. The gold wallet can then be projected from durable character gold again.
- Equipment move ownership now repairs only a raw-less historical equipment row with complete durable evidence: `item_kind=equipment`, an `equipment/` PVF path, valid quality seed/durability, a single-item count, and the current client's nonzero op19 source identity. It materializes the existing 0x77 representation in the same character-item transaction before the normal placement validation. Incomplete rows still return the existing safe missing-raw error.
- Dungeon entry no longer emits class0/op2 mode3. Current client evidence shows that mode3 is the visible personal-information route; pre-op27 mode0 plus post-op29 mode1 provide the local actor binding, then ordinary scenes send op3 before placement. Regression coverage asserts that neither ordinary nor tutorial dungeon initialization adds a mode3 panel packet.
- Added inventory, equipment, and bridge regressions for preservation of relocated stack metadata, no-overwrite failure behavior, exact raw reconstruction/seal consumption, and wallet-page selection. `gofmt`, `go list`, every DNF-module test, the complete bridge/control suites, and the required vet set pass. The production executable SHA256 is `F182D71DD66BA27D5760D0674E959DCC6D14680D1D8262B3A6017D8EB16D1174`; after replacing a verified stale hot-swap process, the BAT controller started PID `54524` with MySQL PID `6468` preserved and all 120 listeners accepting connections.

## 2026-07-28 - implement same-account mailbox recipient selection and verified send/receive

- The current `NoPack.exe` mailbox recipient picker (`op789`) now reads the active account's durable character list and returns its required 100 fixed 12-byte entries (character ID to character slot). It excludes the currently selected role while preserving slot `0`; friend and guild sources stay empty because this local profile has no durable social roster to fabricate.
- Fresh live traffic proved that expanding `账号内角色` emits no client request: the current EXE only reads a cache that must already exist. A successful mailbox op96 open now sends the normal op96 ACK and class0/op97 snapshot first, then proactively installs the op789 same-account roster; the speculative legacy C2S op789 dispatch was removed.
- Added transport-level coverage for the picker and an end-to-end same-account mailbox regression: send gold and an attachment to another role, open the recipient mailbox, claim it into the correct item page, then verify the mail's gold and attachments are durably cleared.
- The earlier client access violation occurred while entering town before any mailbox/open-recipient traffic, so it did not exercise this handler. The new mailbox-open ordering regression requires op96, op97, then op789 and verifies the empty-account and populated-account forms.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the full bridge/control suites, required `go vet`, the production build, and BAT-only offline preflight pass. The controller preserved all database files, restarted portable MySQL as PID `51792`, and deployed DNF90Server SHA256 `D082AD89001B9286DF89D60FC1B01A42C2A9DA82D9C984A15616835870682084` as PID `54412`; all 120 listeners are accepting connections.

## 2026-07-28 - repair all four coupon contracts from the active-coupon panel

- Reversed the current `NoPack.exe` active-coupon callback `sub_2331CE0`. It accepts the exact `(item ID, main-bag slot)` pair and writes the same class1/op44 body as the ordinary inventory paths, but its global UI gate can return before that write. The compatibility DLL now covers this third path as well as the two normal right-click callbacks: it executes the native callback first and only emits the already-proven op44 through the native writers when the packet logger confirms no native op44 was sent. The allow-list remains limited to current `premiumlist_new.etc` contract items, so ordinary stackables are never bypassed.
- The current runtime PVF maps Black Diamond activation items `193` and `901` to premium type `29`, not the old profile's `1/17`. Black Diamond clear-experience rewards now recognize type `29`; legacy type-1/type-17 account metadata remains readable so already-active local accounts retain their benefit.
- Added regressions that require Black Diamond, Overlord, Expert, and Growth activation to send the current class0/op66 activation notification used by the client to update the active-coupon count. The select-character premium section now has an explicit four-family count regression, and the real-PVF smoke test pins Black Diamond item `193` to type `29`.
- Rebuilt the Release/Win32 compatibility DLL and installed it into the verified local client while no client process was running. Packaged and installed SHA256: `F93ED5DBDA23C1B700E4350275676C435FAC24BBDE11C21ED169C01C7B7A16C8`; the compatibility manifest was updated as the same profile unit. `go list`, every DNF-module test, the full bridge/control suites, required `go vet`, production server build, and focused real-PVF smoke pass. The rebuilt server SHA256 is `74B2F8038D3938975F3BEEEC5020FF0912F0C7CD084C54781A46880277FD0F64`. BAT preflight was not allowed to rewrite/restart because DNF90 was already running as PID `51348`; no service or database state was changed.

## 2026-07-28 - persist Crystal Contract and aura-slot state and keep title equip single-owned

- Registered the current client's legacy op535/op338 Crystal Contract commands with the premium gameplay module in addition to the upper transport. A crystal selection now reaches the durable owner, returns the proved op535 ACK/op898 refresh, and is restored from the character repository after a new login session.
- Reloaded the authoritative relational character Stats before building selected subtype0. The aura-skin expansion remains the existing `aura_flag` entitlement rather than an original aura item, and a successful op863 expansion now survives relog instead of falling back to the stale legacy zero.
- Closed the title-book equip handoff exposed by the live op413/op19 trace. A title-book reward with the exact durable provenance can rebuild the current 43-byte zero-durability title equipment summary after retrieval; op19 then removes the one inventory entity and persists it in worn slot 13. Unproven raw-less items still fail closed, so the client can no longer show one backpack title plus one optimistic phantom title.
- Added regressions for legacy Crystal Contract dispatch and relog restoration, repository-backed aura subtype0 restoration, title-book reward equip ownership conservation, and rejection of unrelated raw-less equipment. `gofmt`, `go list`, every DNF-module test, the complete bridge/control suites, and the required `go vet` set pass.
- After the client exited, the BAT controller stopped the old server and portable MySQL while preserving all database state, deployed SHA256 `74B2F8038D3938975F3BEEEC5020FF0912F0C7CD084C54781A46880277FD0F64`, passed the required offline `check --skip-database`, and restarted DNF90Server as PID `6472` with portable MySQL PID `6468`. Startup preflight reports all 120 listeners accepting connections.

## 2026-07-28 - stop reopening personal information during dungeon finish loading

- Kept the required post-op29 `mode3` local-actor initialization, but removed the second `mode3` replay after the client's op37 finish-loading request. That later full personal-information/equipment snapshot was reopening the character panel over an active dungeon and could replace live room actor state.
- The finish-loading boundary now sends only the skill refresh, current HP/MP placement, and the existing tutorial-only deferred op3 activation. The focused regression verifies placement follows skills directly, so a post-op37 mode3 cannot be reintroduced unnoticed.
- Repaired the unrelated Crystal Contract relog regression fixture to use the current session `accountID` field instead of the removed `clientAccountID` alias, restoring complete bridge-suite and vet validation for this build.
- `gofmt`, `git diff --check`, `go list`, all DNF-module tests, the full bridge/control suites, and the required vet set pass. The BAT controller stopped only managed DNF90Server PID `4508`, preserved portable MySQL PID `53588` and all data, deployed SHA256 `B05623B29B5B5577E82A77AADCD4648C3FB4A5C2049AC20F77A44675125308F7`, and started the verified server as PID `19520`; all 120 listeners pass online preflight.

## 2026-07-28 - place claimed mailbox attachments in their real item tab

- A successful op95 claim now clears the mail's durable gold and attachment fields in the same unit of work that grants them to the character. Opening a mailbox also repairs legacy claimed rows that retained already-transferred assets, so they cannot be re-projected or re-selected.
- op95 now returns the current class0/`0x61` clear-first mailbox snapshot immediately after its ACK. This removes stale attachment widgets for both single-claim and select-all paths while the existing inventory refresh updates the item tabs.
- Corrected mailbox claiming to stop treating op13 `main_list_param16` (the current EXE's `0/8/16/24` expansion marker) as a physical main-bag capacity. In particular, the normal initial value `0` no longer rejects every item attachment before its inventory transaction begins.
- op95 now resolves each attachment through the active runtime PVF and places it only in that item's real main-inventory category range: equipment, consumables, materials, quests, profession materials, or emblems. Existing compatible stacks in that page (and quick slots where applicable) are filled before a page-local empty slot is used; stack limits and equipment single-slot rules are preserved.
- Mail, inventory, and attached gold remain one transaction. If the item's own page is full, the claim fails and leaves every attachment and gold amount unclaimed; a successful insert is the only path that marks the mail claimed. Sender-only mailbox framing extras are removed before returned stackables are compared, so compatible recipient stacks can merge.
- The mailbox UI keeps claimed letters visible and includes them in “select receive”. op95 now skips those completed rows and processes pending rows from the same batch; a batch containing only previously claimed letters still reports the normal duplicate-claim error.
- New full-inventory system mail is Chinese end-to-end: sender `系统`, `背包已满：礼盒奖励` / `背包已满：通关奖励`, and a clear instruction to free space in the matching item tab before claiming. Existing overflow letters with the known durable source are translated only when projected to the mailbox UI, so their attachments and claim state remain untouched.
- Added regressions for initial expansion marker `0`, automatic material-tab placement, a full target page retaining both attachment and gold, Chinese overflow-mail prompts, select-all batches containing already claimed mail, durable legacy-attachment repair, and the post-op95 clear-first mailbox snapshot. `gofmt`, `git diff --check`, `go list`, all DNF-module tests, mailbox/bridge tests, control tests, the required vet set, and the production build pass. The BAT controller stopped the verified old server before replacing its executable and restarted the rebuilt service as PID `4508`. Portable MySQL remains READY as PID `53588`, and all 120 listeners pass online preflight.

## 2026-07-28 - complete current-client title-book progress and physical title flow

- Reversed the active `NoPack.exe` handlers for op358/op359/op412/op413/op417. Title-book rows now include the mandatory per-row `u32 raw_len`, op412/op413 success bodies contain exactly four `u32` values after the transport success byte, failure bodies contain exactly two `u32` values after the result/code bytes, and op417 returns the three remaining counters the client actually consumes.
- Replaced the former inventory-backed achievement counters with character-settlement-transactional `Quest.Progress` state. The first event now subtracts from the PVF `[check count]` target (for example 500 becomes 499) instead of treating the first delta as the target; op359 restores active/completed rows after relog, and zero op417 counters let the current EXE mark completion without sending the unrelated historical op360.
- Corrected `etc/titlebook.etc` parsing to its category/slot/marker/quest/count/title five-tuple layout and derives bounded targets from the already-loaded quest PVF catalog. Completion grants the mapped physical title exactly once in the same quest/inventory transaction.
- op358 now projects only durable list-100 title rows for its requested category; ordinary inventory is never misreported as a title page. Login precisely removes the former broken `100:900..999` progress rows only when their legacy signature matches, while real user titles are retained.
- Physical title put/get mutations now use the character settlement transaction, validate the five current categories and bounded indices, retain exact item metadata, and refresh with the proved row framing. Added domain and bridge regressions for 500-style remaining counters, relog snapshots, one-shot reward placement, legacy-row repair, mapping layout, raw-length framing, and exact ACK bodies.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the complete `dnfbridge` and control suites, required `go vet`, production build, and BAT-only online preflight pass. Portable MySQL restarted with data preserved as PID `2904`; DNF90Server is READY as PID `16736`, all 120 listeners accept connections, and the deployed SHA256 is `2B647C572CB61A481B72209CF8675C3E1CE615DB3666C49C01F8A50356308C8A`.

## 2026-07-28 - deliver full-inventory dungeon and magic-box rewards through mailbox

- Added a reusable transaction-scoped system-mail append helper. Overflow delivery stores the exact durable item snapshot (raw row, bind, expiry, and PVF-derived extras), has no expiry, and is committed in the same character/inventory/mailbox transaction as the source reward change; it is never a best-effort post-commit repair.
- Dungeon card rewards now keep gold in the normal settlement transaction and route item rows that cannot be placed in an evidenced main-inventory range to one system mail. Where the current bridge has no authoritative main-slot capacity yet, it deliberately uses that safe mailbox path instead of inventing writable slots. The bridge emits the proven class0/`0x63` unread-mail alarm after commit; notification failure is safe because op96 reloads the durable mailbox snapshot.
- Random-box rewards now detect unplaced main-inventory remainder instead of silently discarding it. The box/material consumption and the system mail commit atomically; only rewards with real inventory slots are included in the existing current-EXE 0x77 result window, so no nonexistent slot is fabricated. Non-main containers (for example creature inventory) still fail closed when their own placement evidence is full.
- Added regressions for dungeon-card overflow, magic-box overflow (including source consumption and attachment snapshot), and the post-ACK mailbox alarm request. Focused dungeon, package, repository, and bridge suites pass.

## 2026-07-28 - consume account-shared quest materials atomically

- Quest settlement now reads required materials from the authoritative account-owned crystal/soul warehouse slots (354–365) as well as ordinary character inventory, while ignoring stale character-owned copies of those shared slots.
- Account-owned materials are decremented, persisted, replay-checked, and returned in the post-commit snapshot within the same memory/MySQL character-settlement transaction. Failed reward allocation leaves both the quest and shared materials unchanged.
- The post-finish list-0 update now projects the committed account inventory for surviving shared stacks. Existing per-session account authorization and the current-client same-session replay suppression remain unchanged.
- Added regressions for account-shared material consumption, replay idempotency, allocation rollback, zero-count handling, and the 0x77 item refresh projection.
- `gofmt`, `git diff --check`, `go list`, every DNF-module test, the full `dnfbridge` and control suites, required `go vet`, and the production build pass. The rebuilt `runtime/bin/DNF90Server.exe` SHA256 is `871A90946074008D24F8FA4D6814CD14596CA02F8AFFCA09475BF358DE124E4B`; BAT preflight did not proceed because PID `53072` is an unmanaged `DNF90Server.exe~`, and no running service or database data was changed.

## 2026-07-28 - restore verified normal-dungeon pickup activation order

- The supplied working server baseline and a fresh failing-client trace agree that ordinary dungeon drops are still serialized exactly as the client expects, but their local activation order had regressed: `op3/state=1` was moved after `op120` and then replayed after finish loading. The current EXE displays those drops but no longer constructs C2S `op43`.
- Restored the baseline sequence only for ordinary owned dungeon rooms: `mode3 -> op3/state=1 -> op124/op9/op120`; removed the ordinary post-finish replay. PVF tutorial rooms retain their separately required deferred activation after finish loading, so the known tutorial room-manager crash boundary remains guarded.

## 2026-07-27 - refresh the Crystal Contract panel immediately after Cera-shop activation

- Fresh current-client traffic proved the account purchase and persistence were already correct: op64 activated premium type `97`, the account expiry remained valid, but the scene had previously received an inactive op898 and the client emitted no op535 selection after the purchase.
- Current `NoPack.exe` confirms the split lifecycle. `sub_1D61460` records the generic type-97 activation, while raw class1/op898 handler `sub_1E7FAD0` rebuilds the Crystal Contract UI and runs its dedicated refresh path. The missing post-purchase op898 left the panel locally disabled even though the database contract was active.
- Cera-shop checkout now detects a committed Crystal Contract activation and sends the repository-backed raw op898 state immediately after class0/op66 and before the balance update. Existing login/scene restoration, item-use activation, op535 selection persistence, and op338 dungeon cube consumption remain unchanged.
- Added an exact purchase-order regression requiring `op64 ACK -> class0/op66 type 97 -> raw class1/op898 -> Cera balance`, while retaining the existing Expert, Overlord, and Devil Contract packet expectations. Updated the stale bridge mailbox-open assertion to include its already-implemented empty class0/op97 snapshot; runtime mailbox behavior was not changed.
- `gofmt`, `git diff --check`, `go list`, every DNF module test, the complete `dnfbridge` and control suites, required `go vet`, production build, and BAT-only offline/online preflight pass. Database data was preserved. The deployed server SHA256 is `A282BA2BDF13B7CCD00BC59B08113A2DD5593A955AC9F4B88127D2E3A2160AB5`; portable MySQL is READY as PID `2084`, DNF90Server is READY as PID `11636`, all 120 listeners accept connections, and fresh stderr is empty.

## 2026-07-28 - complete current-client mailbox list, state, and online alarm flow

- Reversed the active `NoPack.exe` class0 mailbox readers rather than importing the reference server's packet layouts. `0x61` now follows `sub_1D913C0` exactly: summary-count/clear flag, sender and claim fields, the current `0x77` item row, deferred-page count, then detailed text/status rows. `0x62` is the proven `u32 count + u32 mail id[]` removal notification and `0x63` is the one-`u16` online mailbox alarm.
- Opening with op96 now sends its count ACK followed by the repository-backed `0x61` snapshot. Text-only, gold, and item mail all receive the required summary seed; GB18030 DSTR values, opaque current item rows, PVF-resolved creature/type-26 framing, mail type, creation time, and 1/2/3 letter state are all bounded and serialized from durable data. Item core fields are never invented from request values.
- Completed op134 state behavior: `0` deletes only after assets are claimed and emits `0x62`, `2` records read, and `3` preserves the letter in the client-visible saved store (bounded to ten). Player sends now carry the current normal 15-day lifetime; old records derive the same lifetime from creation time, while saved mail is retained. Claims and state changes reject expired non-saved mail.
- Successful sends now notify an already-online recipient through the existing character-to-game-session index only after the sender ACK succeeds. The bridge recomputes the durable unread count and sends class0/`0x63`; an offline or stale recipient remains safe because their next op96 produces the same authoritative snapshot.
- Added exact snapshot, raw attachment/type-26 framing, state-2/state-3, expiry, delete-notification, alarm-body, and online-recipient wire regressions. `go list`, all DNF-module tests, the full bridge/control suites, required `go vet`, production build, and BAT-only offline preflight pass. The rebuilt `runtime/bin/DNF90Server.exe` SHA256 is `A282BA2BDF13B7CCD00BC59B08113A2DD5593A955AC9F4B88127D2E3A2160AB5`; no runtime service was restarted and no mailbox/database data was changed.

## 2026-07-28 - restore post-op19 mode3 finalization for dungeon pickup input

- Fresh live pickup tracing showed that drops were correctly serialized and registered (the client received two real scene-drop rows), but pressing pickup emitted no class0/op43 at all. The earlier completion-path reduction had removed the post-op19 class0/op2 mode3 pass which the current client uses to finalize the local actor's interactive/input state.
- Restored only that current-EXE `sub_2008600` final mode3 recalculation in its proven position: `op19 skills -> mode3 final stats -> op120 placement/HPMP`. The earlier one-shot class0/op3 change remains intact: normal scenes keep their original post-op29 state send, and PVF tutorial scenes still defer exactly one state-1 activation until after op120 following op37.
- Regression coverage again pins the final mode3 body and preserves the op120-before-deferred-op3 ordering.

## 2026-07-28 - reactivate the completed local dungeon actor after final mode3

- The live post-restart trace proved that ground-drop op38 rows and their registered runtime objects were correct, while pickup still produced no C2S op43. The trace also showed the only state-1 op3 arrived before the final op19/mode3/op120 phase; after that mode3 rebuild the local wrapper was no longer in the active interaction state.
- Active dungeon entries now send one state-1 activation after the proven final sequence `op19 -> mode3 -> op120`, whether the initial state was ordinary or tutorial-deferred. It is limited to an owned active dungeon runtime; initial town, return-town, and no-runtime finish-loading routes cannot receive the extra state. Tutorial activation continues to use the pre-recorded owner key and is cleared after the one send.

## 2026-07-28 - complete Crystal Contract and verify Expert/Overlord contracts

- Reversed the current `NoPack.exe` Crystal Contract protocol instead of reusing a legacy layout. Class1/op535 is exactly `[u8 source][u8 selection]`, its success ACK echoes those two bytes, class1/op898 is a raw two-byte state notification `[0, selection]`, and dungeon cube consumption class1/op338 is `[u16 slot][u32 item id][u16 client count]` with the current success response `[u32 item id][u16 consumed][u32 remaining][u8 refresh]`.
- The runtime PVF now supplies the authoritative Crystal Contract cube catalog from `stackable/stackable.lst`. The current six native selections resolve in client order to black `3033`, white `3034`, red `3035`, blue `3036`, clear `3037`, and gold `3262`; no item ID or selection order is invented when the PVF dependency is absent.
- Added character-scoped Crystal Contract ownership and persistence for premium type `97`. Selection requires an active contract and a matching owned cube, is restored on scene activation, and is cleared when the selected cube is exhausted or the contract expires. Dungeon use validates the current owned runtime, slot, template, account, character, and stack before atomically consuming exactly one cube and returning a bounded inventory refresh.
- Existing Expert Contract type `27` and Overlord Contract type `22` paths were audited and retained: Expert Contract raises effective learned-skill level by `+5`, performs dependent-skill cascade cleanup and SP/TP refund on expiry, while Overlord Contract raises the equipment level gate and rental effective level by `+10`. Their PVF 7/30-day activation and renewal continue extending from `max(now, current expiry)`.
- Added domain, wire-layout, scene-restoration, transaction, last-cube, multi-stack, mismatch, expiry, registration, and real-runtime-PVF regressions. `gofmt`, `go list`, every DNF module test, the complete `dnfbridge` and control suites, required `go vet`, production build, and BAT-only offline/online preflight pass.
- Database data was preserved. The deployed server SHA256 is `32BACB57B80215522C97993F2E15C417D93CD7FF03DBDF3164B33356C6E49F86`; portable MySQL is READY as PID `31948`, DNF90Server is READY as PID `52748`, all 120 managed listeners accept connections, and the fresh stderr logs are empty.

## 2026-07-28 - defer tutorial actor activation and stop replaying personal info after finish-loading

- Live current-client traffic after the dungeon-entry crash guard showed the player reached the tutorial room, but the bridge sent a second complete class0/op2 mode3 personal-information snapshot after the client's op37 finish-loading request. That replay overwrote live dungeon-owned player state after mode1/mode3 had already committed it immediately after op29.
- The same trace proved the broken pickup was client-side gating rather than an inventory transaction failure: the highlighted ground items produced no class0/op43 request at all. PVF tutorial entry had permanently suppressed class0/op3 state 1 to avoid the earlier nil room-object-manager crash, leaving the local wrapper outside the active/interactable room state.
- Post-finish-loading no longer replays complete mode3 personal information. Tutorial scenes record the exact selected actor key at post-op29 placement, then send the deferred state transition only after the client's real finish-loading request, with the safe order `op19 skills -> op120 placement/HPMP -> op3 active state`. Ordinary dungeon and initial-town state boundaries remain unchanged, and all entry/return resets clear the deferred key.
- Added regressions for tutorial-only deferral, stale-scene and ordinary-dungeon exclusion, exact op120-before-op3 finish-loading order, one-shot clearing, and absence of the post-finish mode3 replay. Focused bridge tests and formatting checks pass.
- `go list`, every DNF module test, control tests, test-free required `go vet`, and the production build pass. The complete bridge test command is currently blocked by an unrelated untracked crystal-contract test that references the nonexistent `dnfproto.GameServerUpperPacket` type; no crystal-contract source or test was changed here. The rebuilt server SHA256 is `8644D2D630BC70AF15230EAFAEFE49B14A123730571688B75BD1D2D920C2E47B`; MySQL data was preserved, DNF90Server restarted as PID `55252`, and all 120 managed listeners are ready.

## 2026-07-27 - guard the op903 auto-repair auxiliary durability scan during dungeon entry

- Two fresh dungeon-entry crashes reproduced the same current-client access violation at `0x034DBD93`, reading address `0x00000044`. The stack resolves through `sub_1D68000 -> sub_20695F0 -> sub_2326860`: the class0/op903 premium-service handler enables auto repair and immediately scans durability after the ordinary equipment pass.
- Current `sub_20695F0` obtains the optional auxiliary equipment container through `sub_24A1BC0`, but its slots `3..8` loop is the only audited caller that passes the result directly to `sub_2326860` without checking null. The live controlled actor legitimately had `player+0x646C == 0` in the dungeon-entry window; other current-EXE users of the same getter already treat that container as optional.
- The version-locked compatibility DLL now guards only the exact seven-byte lookup sequence at RVA `0x01C6977F`. A present container executes the original lookup unchanged; a missing container discards the already-pushed slot argument and skips only the auxiliary scan after the ordinary durability pass has completed. The first bounded hits are logged for live acceptance.
- Release/Win32 rebuilt with zero errors. The release, packaged, and installed DLLs are byte-identical at SHA256 `C6F058E0AFFADC0B9C73AFA429A199B79DF7D7ADAD525B53763B1FA6C316A956`, and the strict compatibility manifest pins the same hash. MySQL and DNF90Server were not restarted; status reports both ready with all 120 listeners accepting connections.

## 2026-07-27 - restore absolute op903 deadlines after native server-clock initialization

- A fresh login packet trace proved the premium-service state was already pushed as class1/op903 with all eight slots populated; the empty `再次购买` state was therefore not a missing-send problem.
- The earlier relative-duration workaround became invalid once the current client's native server clock was initialized. The op903 UI subtracts that clock from each slot value, so a value such as seven remaining days is interpreted as a Unix timestamp in 1970 and immediately rendered expired.
- Class1/op903 now serializes each active slot's absolute Unix expiry again, while expired slots remain zero and daily usage counters retain their existing offsets. This supersedes the earlier remaining-seconds interpretation; no premium purchase or database row is cleared or rewritten.

## 2026-07-27 - fix selected-character dungeon entry and make pet rename update the live object

- The latest client exception was an access violation at current `NoPack.exe` VA `0x024A0926`, reached from `0x02008863`: class0/op2 mode3 selected the remote-actor path, failed to resolve the local actor, then called `sub_24A0920` with `this == 0` and wrote through offset `0x6702`.
- The selected-character mode3 row had been serialized with owner context `0` even after channel `206` was committed. It now carries the session's validated committed `u8` channel owner, matching the value cached by the current EXE before `sub_2008600` chooses the local/remote branch. Nil, uncommitted, and nonresident-channel fallbacks remain unchanged.
- Added a focused dungeon-entry regression requiring the mode3 row to carry the committed channel owner. The complete pet and bridge suites pass with the new contract.
- `go list`, every DNF module test, the complete `dnfbridge` and control suites, required `go vet`, the Release/Win32 compatibility-DLL rebuild, the production server build, and BAT-only preflight passed. Database data was preserved; portable MySQL is READY as PID `20560`, DNF90Server is READY as PID `16288`, all 120 listeners accept connections, current stderr is empty, and the deployed server SHA256 is `FFB9AE04F0483A5CE1AFEDDF53F7F0A5B492040C29D8B3A39259DC7FF56D20B7`.

## 2026-07-27 - initialize the current client server clock after a compatible channel connection

- Traced the reported Cera-shop item durations through the current `NoPack.exe`. Values such as `20686天` are the absolute Unix day for `2026-08-21`, proving the client was subtracting a zero server-time baseline rather than displaying a corrupt item duration.
- Current handler `sub_1D70130` initializes three obfuscated clock values from CHANNELINFO field 11, `_time32`, and `timeGetTime`; current getter `sub_2276FE0` then returns the server Unix base plus elapsed ticks. The compatibility connection path already committed the verified resident channel and HUD state but did not run this clock initialization.
- After a successful, port-validated native channel connection, the compatibility DLL now uses the EXE's own alignment-specific stores `sub_34DB910` and `sub_34DBA10` to initialize the exact server-time, local-time, and tick-base globals. No PVF expiration date, purchased-item deadline, database row, or server packet layout is rewritten.
- Release/Win32 rebuilt with zero errors. The compatibility manifest and packaged DLL now use SHA256 `A5E3D4FD1527D803F482316A9F01DC913A1DCE9A60D7CCAC4A9D565404499C42`.

## 2026-07-27 - implement all eight Devil Contract services with 7-day and 30-day tiers

- Parsed the runtime PVF's complete `[selectable character premium]` table as 16 independent Cera products: every one of the eight 魔王之契约 services has a 7-day and a 30-day commodity, both duration tiers activate the same functional slot, and renewals extend from `max(now, current expiry)` instead of replacing remaining time.
- Corrected class1/op903 service-state serialization to send remaining seconds instead of the repository's absolute Unix expiry. The client treats this field as a duration; sending the absolute timestamp rendered active services as roughly 20,745 days. Expired values now clamp to zero and persisted expiry data requires no cleanup.
- Implemented the current `NoPack.exe` class1/op903 state contract: eight exact expiry/usage slots, a 06:00 Beijing service-day reset, immediate refresh after purchase and scene entry, and per-character persisted counters. The current-PVF limits are enforced for gold cards (10), double jars (8), seven buffs (10), free weakness recovery (10), and auto repair (6); HP/MP recovery remains once per dungeon and fast jar opening remains uncapped as the client defines them.
- Closed the actual effects behind those slots. Gold-card settlement rewards, double legacy-jar rewards, quest-item drop assistance, free auto repair, seven-buff use accounting, and free weakness recovery accounting now commit through repository transactions. The current client retains its native HP/MP recovery, seven-buff application, and fast-jar timing after receiving the authoritative op903 state.
- Added the current legacy-jar two-phase op27/op217 transaction, doubled rewards without duplicating source consumption, and bounded inventory refreshes. Added exact 7/30 runtime-PVF mapping checks, expiry stacking, daily-reset/limit, op903, op9 free-weakness, repair, gold-card, and double-jar regressions.

## 2026-07-27 - restore creature names and implement the current pet rename card

- Current `NoPack.exe` and live op100 evidence corrected the rename boundary: the request sends a main-inventory slot, the fixed creature semantic value `7`, and the new DSTR. That `7` is not a durable inventory collection. The pet owner now loads the card from main inventory list `0`, then resolves the equipped creature from equipment slot `26` and its durable serial before mutating state.
- Implemented the version-matched `宠物换名卡` (`item 25`, `stackable/cash/creature/rename_card.stk`, `[creature]`) as one character-scoped inventory/equipment/pet transaction. A successful request decrements exactly one main-inventory card, persists the GB18030 name against the equipped creature, returns the proved three-byte slot/semantic ACK, sends the current EXE's class0/op101 in-place name notification, and refreshes only main-inventory source row `0:<slot>`. Missing cards, mismatched PVF identity, absent slot-26 serials, and inconsistent pet records fail without consumption.
- Live protocol evidence isolated the literal `null` to the final class0/op2 mode0 actor row: creature template `400990199` and alive state `1` were present, but its embedded creature-name DSTR length was `0`. Empty legacy/imported pet names are now projected from the equipped creature's PVF `[name]` as GB18030 both in that final scene-object snapshot and at the op105 wire boundary. Durable custom names remain authoritative and the fallback is not written into the database.
- Live rename capture proved the database, op105 row, and rebuilt mode0 row all carried the newest name while three client views still retained different object generations. Current op105 allocates and replaces map entries instead of updating UI-held objects, and a repeated mode0 rebuild likewise does not repair those retained references. Post-rename op105 and mode0 replays were therefore removed; cold login still uses the established authoritative op105/mode0 bootstrap.
- Current handler `sub_1D84FE0` is the native in-place rename path and updates actor and live creature objects first. Its final optional map copy did not null-check `sub_25FD8D0` and crashed at `0x034DE96E` with `this == 0x14` when the map entry was absent. The version-locked compatibility DLL now skips only that final copy on null, preserves all earlier native name updates, and fails closed when the expected EXE bytes do not match.
- Added exact request/ACK/op101 tests, main-inventory card and equipped-creature mismatch coverage, concurrent single-card consumption coverage, Chinese PVF-name projection coverage, and regressions rejecting post-rename table/scene rebuilds. Release/Win32 rebuilt with zero errors; the packaged and installed DLLs are byte-identical at SHA256 `79AB25CA94C150CDF21E2AE242F44E2CFADE130683237EA81364C0F8AC6035DC`, and the strict compatibility manifest pins that hash.

## 2026-07-27 - restore cold-login equipment sockets and creature growth bars

- Confirmed in the current EXE that mode1 equipment creation `sub_1D77560` reads `sub_225CCA0` as `u8 count + repeated u32`, then applies that vector through equipment vtable method `+280`, exactly like the op14 path after `sub_225CD00`. The bridge previously emitted an empty mode1 vector even when the durable `raw+0x3C` socket state was populated.
- Mode1 equipment rows now derive the `sub_225CCA0` vector from the same persisted current-item bytes used by op14, while retaining an explicit mode1-extra override when present. Open-empty `0xFFFFFFFF` entries and attached emblem IDs are therefore visible on first login without wearing the item again.
- Current EXE `sub_1D57AB0` and `sub_1D5AF60` reconfirm the repository-backed class0/op105 creature table and class0/op102 equipped-growth layouts. The cold-login player-state flow sends the absolute creature table and equipped creature level/experience immediately after full mode1 creates the creature object and before typed op24 makes the town visible.
- The first live acceptance proved a second lifecycle boundary: the later scene-ready class0/op2 appearance refresh destroys and recreates slot-26, while the session idempotency flag incorrectly suppressed the following op105. The bridge now forcibly replays op105 only after that final object rebuild, then sends op102, so the current object is rebound to its name/level/experience/satiety state instead of rendering `null`. Ordinary duplicate op105 calls remain suppressed.
- Added focused regressions for first-login mode1 socket projection, pre-scene-tail creature satiety/level/experience delivery, and the exact case where op105 was already sent before the final slot-26 rebuild.
- Focused cold-login regressions, `go list`, every DNF module test, the complete `dnfbridge` and control suites, required `go vet`, production build, and BAT-only preflight passed. Database data was preserved; portable MySQL is READY as PID `4092`, DNF90Server is READY as PID `52888`, all 120 listeners accept connections, stderr is empty, and the deployed server SHA256 is `C528C46DBE24F0E09FE60ED40F418C151B1799522D9C1BADB876FBE892E7E71B`.

## 2026-07-27 - keep equipment socket refresh incremental

- Confirmed the reported popup item was template `20002` (`精纺的棉布项圈`), while the actual op914 target and committed socket state were template `100130117` (`传承 : 天马守护神的铂金护腿`). The transaction and target selection were correct; the unrelated item came from the trailing full main-inventory op13 rebuild.
- Rechecked current handler `sub_1D73120`: every class0/op14 row is decoded through `sub_225CD00`, and the same-template equipment branch applies the extracted ordinary-emblem vector through vtable method `+280`. The earlier assumption that op14 could not rebuild socket state was incorrect.
- Removed the redundant full op13 snapshot after both op914 socket opening and op913 emblem attachment. Their wire order is now only `ACK -> target op14 -> consumed-item op14 row(s)`, which updates the socket state without traversing the entire backpack or raising an unrelated obtained-item popup.
- Updated regressions to reject any trailing packet after the bounded incremental refreshes.
- Focused socket tests, the complete DNF module and `dnfbridge` suites, control tests, `go list`, required `go vet`, production build, and BAT-only preflight passed. Database data was preserved; portable MySQL is READY as PID `55120`, DNF90Server is READY as PID `52700`, all 120 listeners accept connections, and the deployed server SHA256 is `2593283B84A3112FF5B4A99E23A24FAFE749816A91CA37EF882B2CF82335D07D`.

## 2026-07-27 - handle the current client's equipment-emblem op913

- Captured the reported no-response click as class1/op913 `ENUM_CMDPACKET_USE_EMBLEM_FOR_EQUIPMENT`, not the unrelated no-body op796 control route. The live 21-byte request decoded exactly as target slot `14`, template `100240013`, two emblem rows at inventory slots `296/295`, and socket indexes `0/1`.
- Registered op913 in the equipment-emblem gameplay module and connected it to the existing transactional owner. Current handler `sub_1D20750` proves the success body is `u8 count` followed by repeated `[u16 emblem slot][u32 emblem template]`; the bridge returns that exact body and refreshes the target and consumed rows.
- Added captured-layout parsing, exact ACK, durable two-emblem mutation, consumption, and incremental-refresh regressions. The no-body op796 route remains a separate acknowledgement and cannot mutate equipment.
- Focused and complete `dnfbridge` tests, `go list`, all DNF module tests, control tests, required `go vet`, production build, and BAT-only offline/online preflight passed. The two already-disconnected clients were closed so the canonical compatibility DLL could be installed at its manifest-pinned SHA256 `90554BDC4FA8A755B3011C04781D827AA99AFFA66809A4850C53CF3C36F72420`; database data was preserved. Portable MySQL is READY as PID `25516`, DNF90Server is READY as PID `54652`, all 120 listeners accept connections, and the deployed server SHA256 is `83875FC9BDBBC516A58EAE1B2F902821F216EBA1CA78C84666C2FAFAA5D2BF92`.

## 2026-07-27 - remap the proved PVP query sites and correct the native HUD indices

- Replaced the failed type-24 admission hypothesis with live current-EXE call evidence. The seven ETC PVP objects already exist and `sub_1898C30` returns all seven when queried with type `24`; the active Duel Arena builders instead called it from five exact return RVAs using types `8`, `31`, and `41`. The patch now remaps only those five proved caller/type pairs to `24`, leaving every other channel query unchanged. A live temporary equivalent exposed `CH.501` through `CH.507` under `传统决斗场`.
- Corrected the native HUD field interpretation in `sub_1F941B0`. The encrypted pair at `0x51B0EE4/0x51B0EE8` is `ServerIndex` and uses the case-4 writer; the pair at `0x51B0EF0/0x51B0EF4` is `ChannelIndex` and uses the case-0 writer. The prior implementation stored the values in reverse order, producing `ch.00-未知`. Swapping the same two decoded values live resolved the native lookup and rendered `ch.251-异次元裂缝`.
- Corrected the failed type-24 admission conclusion with live collection evidence. A read-only snapshot of `dword_5188FDC` proved that runtime objects 501 through 507 were already present at indexes 103 through 109 with native type `24`; `sub_2264550` is an unrelated dedicated-category predicate and no classifier hook is required. The actual zero-row cause was the query proxy declaring `sub_1898C30`'s second argument as an 8-bit `char` even though the current EXE passes and compares a complete 32-bit type slot. The proxy now preserves that 32-bit ABI and remaps only the five proved caller/type pairs.
- The first fresh HUD build exposed a separate scene-context dependency. With the real channel index `253` restored, the server's established mode-0 owner sentinel `0` no longer matched the current EXE's channel comparison, so the selected actor took the remote branch, `current_actor` stayed null, and op124 crashed at `0x034DBE73`. The existing exact compare hook now translates only `owner context 0` to a nonzero current scene; every nonzero context retains the native comparison. A fresh trace then showed `current-scene-context0-remap`, successful local actor creation, and the same client surviving subsequent channel changes without an exception.
- The final Release/Win32 DLL uses SHA256 `FEC0F0D4204FBD1AABBACC336E802D1BC714F2AA2A5B7F5941B7B5DB97E05B27`. Fresh PID `45116` loaded all seven native type-24 objects, remapped the three page-initialization queries and the active page query to full-width type `24`, and returned `count=7` at every site; login, current-actor creation, the corrected `ch.206-地轨中心` HUD, and process responsiveness remained intact with no exception. `go list`, every DNF module test, the complete `dnfbridge`, control and release suites, required `go vet`, and an isolated production server build passed. Online status verifies MySQL PID `25516`, server PID `54652`, and all 120 listeners; the database-free configuration preflight correctly refused to rewrite runtime configuration while the server is running.

## 2026-07-27 - correct the ordinary-equipment emblem vector to raw+0x3C

- Reversed the complete current-client row application path after the first op914 build consumed the opener but still displayed no holes. `sub_7BA900` initializes the fixed 0x77 row, `sub_225CD00` extracts the ordinary-equipment emblem count at `raw+0x3C` and two u32 values at `+0x3D/+0x41`, and vtable method `sub_22D2280` installs every nonzero entry in the equipment wrapper. `0xFFFFFFFF` remains the client's open-and-empty sentinel because the tooltip keeps the socket entry while skipping positive emblem lookup.
- Removed the incorrect socket projection at `raw+0x27..+0x37`; that range belongs to a separate two-record collection parsed through `sub_21792A0`, not ordinary equipment emblems. Added a bounded migration that clears only the exact old server-generated byte pattern and preserves every nonmatching record.
- Updated raw-row decoding, inventory/equipment projection, incremental op14 and full op13 regressions to require `02 FFFFFFFF FFFFFFFF` at `raw+0x3C`. This correction supersedes the earlier `raw+0x27` interpretation recorded below.
- Focused socket/emblem tests, `go list`, all DNF module tests, the complete `dnfbridge` and control suites, required `go vet`, production build, and BAT-only database-free plus online preflight passed. Portable MySQL data was preserved and restarted as PID `37492`; DNF90Server is READY as PID `13100`, all 120 listeners accept connections, and the deployed server SHA256 is `77F2167976A04AE879452DA8F3E317C178EA4E4487FDDBAB9CC60AA24ECEB756`.

## 2026-07-27 - admit the complete ETC's PVP channels and commit the real HUD state

- Replaced the unsuccessful old-PVP-builder hypothesis with the current EXE's actual channel-object admission path. `sub_18A2780` loaded all seven ETC records `501` through `507` as type `24`, but the virtual predicate reached `sub_2264550`, whose accepted type set omitted `24`; each record therefore returned a null runtime object before any UI builder could query it.
- Added one bounded type-classifier hook that preserves every native result and returns true only for type `24`. The active channel query remains instrumented for acceptance, while the unused old PVP builder, tab-switch, row-visibility, and screenshot-label hooks were removed so they cannot overlap future current-client work. The supplied complete ETC remains unchanged and authoritative.
- Corrected the separate `ch.00-未知` state at its native source. The live HUD function `sub_1F941B0` decrypts the channel pair at `0x51B0EE4/0x51B0EE8` and server pair at `0x51B0EF0/0x51B0EF4`; both stayed zero because the corresponding current-client channel-info packet path was absent. After a successful, port-validated connection, the patch now calls the EXE's own spin locks and alignment-specific obfuscated writers `sub_34DB990` and `sub_34DB910`, rather than writing plaintext into encrypted globals or overriding rendered text.
- Release/Win32 builds with zero errors. The package, IDA copy, and installed game-root DLL now all match SHA256 `FBEDE8BA9CCA976380CC20D99B2514DDAE47964EFDBB08F12F0F2D34E09E3499`; the previous installed DLL is recoverable from `runtime/backups/client/20260727-185424_pre_type24_hud_state`. DNF module, `dnfbridge`, control, release, vet, server-build, and live runtime-status checks passed. Fresh-process visual acceptance remains pending.

## 2026-07-27 - correct the channel HUD state and instrument the empty Origin PVP page

- Confirmed against the current `NoPack.exe` that the Duel Arena tab does not use the ordinary channel-category table. Its dedicated builder `sub_1E98390` queries exact channel type `24`, and the complete ETC's seven records `501` through `507` are present in the live client channel-object vector with their resolved names.
- Kept the supplied complete ETC authoritative: PVP remains `501-507 / type 24 / [pvp]`; ordinary, special, and raid mappings were not changed, no display number was written into an internal channel ID, and no block-style comment was added to the ETC.
- The first row-visibility hypothesis was not sufficient: a live Origin screenshot still showed an empty Duel Arena page. Added bounded hooks at the actual type-24 query, PVP tab switch, and dedicated builder so the next click records whether the active UI reaches the builder and how many rows it receives, instead of changing the ETC again.
- Corrected a separate unsafe HUD workaround. RVA `0x04D88FA0` is a lazily allocated channel-state object pointer, not a resident channel ID; the previous patch wrote an integer channel ID into that pointer. The write and the unrelated `sub_189AF50` pseudo-commit path were removed. The town label builder now uses the channel name and number captured only after a successful connection, while the native scene commit remains protocol-owned.
- Added a server regression that requires the online script to preserve all seven PVP IDs, type `24`, `[pvp]` group, and exact `chn_channel_info_039` through `045` name keys. Release/Win32 builds successfully and the packaged DLL/compatibility manifest now use SHA256 `4B4C1F414663712917C092E1ABF2FC7E714D16EE4D5545E38B329C9F154445B0`. Live Duel Arena and HUD acceptance is pending installation into a fresh client process.

## 2026-07-27 - rebuild equipment socket state after the op914 mutation

- Traced the reported “socket did not change” case through the live op914 request, packet log, MySQL row, and current `NoPack.exe`. Character `1` main-inventory slot `14`, template `100240013`, was already persisted correctly as `02 FFFFFFFF FFFFFFFF` followed by eight zero auxiliary bytes; the material was consumed once, so this was not a failed transaction or a wrong raw socket layout.
- IDA evidence isolated the stale display to the current client's class0/op14 handler `sub_1D73120`: when the target slot still contains the same template ID, its in-place branch updates basic state but does not rebuild the equipment wrapper/socket vector decoded by `sub_21792A0`.
- Kept the incremental target/material rows and added a trailing authoritative main-inventory class0/op13 snapshot after a successful op914 ACK. The wire order is now `op914 ACK -> target op14 -> material op14 -> main op13`, which rebuilds the existing equipment object without repeating the transaction or consuming another opener.
- Added regression coverage for that exact packet order and for the opened target row inside the final snapshot. `gofmt`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the complete `dnfbridge` and control suites, required `go vet`, production build, and BAT-only offline/online preflight passed.
- Portable MySQL data was preserved and is READY as PID `52504`; DNF90Server is READY as PID `39324`, all 120 listeners accept connections, and the persisted belt still has two empty sockets. Deployed server SHA256 is `9DF3A7AE251B5D905D843C9A26D2ED9E59BE3679C3F50C0E14ADD75BCFC9732A`.

## 2026-07-27 - keep the complete ETC's PVF channel mappings authoritative

- Fixed the reported channel-selection PVF mismatch after adopting the complete `channel_info.etc`. The 90CN online-script projection was still replacing every source channel name with the reduced catalog's hard-coded ID mapping; channel `19`, for example, was changed from the supplied `chn_channel_info_028` / `granfloris` route into legacy `chn_channel_info_012`, which displayed the wrong old-dimensional channel name.
- Removed all online name and group rewrites. The client download now preserves each complete ETC record's exact name key and area key while changing only the proved advertised server ID and online channel type. The complete ETC is therefore authoritative for both the raw game catalog and the downloaded client table.
- Removed the reduced catalog's raid-ID assumptions. Channel `201` is now correctly treated as its source type-11 `metro` route instead of being hidden by the old `HiddenRaidChannelID=201` constant; raid filtering now follows the ETC's type `23`/`32` and group values. The bridge test resource was replaced with the same 17,503-byte complete ETC so old test data cannot mask future regressions.
- Regression coverage pins channel `19` to `chn_channel_info_028` / `granfloris` / type `11`, channel `201` to ordinary `metro`, channel `241` to type-32 `luke_raid`, all 118 online records, and rejection of the removed legacy channel-19 mapping. Validation passed `gofmt`, `go list`, every DNF module test against the complete resource, focused and full `dnfbridge`, control, required `go vet`, production build, strict client validation, and BAT offline/online preflight.
- Portable MySQL data was preserved and is READY as PID `18356`; DNF90Server is READY as PID `49260`, all 120 listeners accept connections, and fresh stdout/stderr are empty. Deployed server SHA256 is `541C05AC8431D7211A0E139A7DDADFB1FDEA445F8DFBA6CC519D233BD66267AA`.

## 2026-07-27 - promote the active local character and grant requested gold

- Confirmed the active session belonged to account `dnf:1`, character ID `1`, name `pouut`, before changing any runtime data.
- Stopped only DNF90Server while preserving portable MySQL, then atomically changed the character from level `5` to level `90` and added exactly `300,000,000` gold (`203` -> `300,000,203`) in `dnf_local.dnf_characters`.
- Left the persisted experience and learned-skill state intact. The existing login/reselect repair will derive the level-90 SP/TP ledger from the runtime PVF while preserving already-spent points, rather than hand-writing invented progression values.
- Verified the committed row after the transaction and restarted DNF90Server READY as PID `35680`; portable MySQL remained online as PID `4160`.

## 2026-07-27 - close the current-EXE equipment-socket flow and correct its raw vector

- Closed the complete current `NoPack.exe` equipment-opening path through IDA MCP. Material action `sub_222EEC0` records the opener's main-inventory index and opens the panel; `sub_2112CF0` stores the selected equipment wrapper; `sub_2112D50` sends class1/op914 as exact `[u16 target slot][u32 target template][u16 material slot]`. The registered success handler `sub_1D1A130` reads only `[u16 target slot][u16 material slot]`, decrements/removes the opener locally, raises target event `0x2FD`, and refreshes the main-inventory manager.
- Corrected the server's normal-equipment socket row to the layout actually parsed by `sub_21792A0`: `raw+0x27` count, contiguous u32 emblem IDs at `+0x28/+0x2C`, and eight auxiliary bytes at `+0x30..+0x37`. The old projection misplaced the second emblem at `+0x30` and wrote the count into `+0x2F`, corrupting the second socket ID. Historical live rows for templates `100220519` and `100270515` independently match `02 FFFFFFFF FFFFFFFF` followed by eight zero bytes.
- Added current-row socket decoding so a persisted 0x77 row remains authoritative even without legacy Extra aliases. Removed the fabricated list-3 JewelSocket/expansion trailers: current `sub_225C960/sub_225C9B0` gate those blobs on item-table type `<= 11`, while normal worn equipment begins at type 12 and the captured target templates returned a false gate. List-1 avatar blobs remain unchanged.
- Strengthened regressions for the exact op914 ACK, list-0 target op14 row, material-deletion op14 row, raw-only restoration, duplicate-template exact-slot selection, and list-3 guardian-medal refresh boundaries.
- Validation passed `gofmt`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the complete `dnfbridge` and control suites, required `go vet`, controller-managed production build, and BAT-only database-free plus online preflight. Portable MySQL data was preserved and restarted as PID `4160`; DNF90Server is READY as PID `20176`, all 120 configured listeners accept connections, and fresh server stdout/stderr are empty. Deployed server SHA256 is `E826AD3F4886DCC1EF2B4E6BEF65B488FEEA9E365F99BDB0B6FBC92879BAE348`.

## 2026-07-27 - adopt the complete version-matched channel_info.etc

- Replaced the reduced 4,930-byte channel asset with the supplied complete 17,503-byte version-matched `channel_info.etc`. The deployment asset and generated runtime copy now both have SHA256 `B1741336A9F91646ECE865C799D187D6C36460BC826BF9EFBC1B850B39A71A36`; the asset manifest was updated as the same release unit, so later BAT configuration generation cannot restore the old file.
- Extended the shared channel parser to accept the native inline server header `[server] 1` in addition to the existing split `[server]` / `1` form. Applied the same header parser to the 90CN online channel-script projection so startup catalog construction and the client download path consume the identical source format. Invalid inline server IDs still fail closed.
- Added parser and online-script regressions for inline headers, native resource keys, decimal raw fields, bootstrap channel preservation, and invalid server IDs. The supplied file now resolves server group `1` to 118 unique channels and 118 exact game ports without fabricated channel records.
- Validation passed `gofmt`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test using the supplied file as the real-resource fixture, the complete `dnfbridge` and control suites, the required `go vet` set, controller-managed binary rebuild, strict client validation, and BAT-only offline/online preflight. Portable MySQL data was preserved and is READY as PID `13688`; DNF90Server is READY as PID `12448`, all 120 managed listeners accept connections, and fresh server stdout/stderr are empty. Deployed server SHA256 is `D945FFF0EB4F3B83406922D480136114E999A414A6FFD112C1DBDF2D9062DA27`.

## 2026-07-27 - acknowledge same-town peer requests so the player context menu can open

- Captured the reported no-response click from both live clients as class1/op10 `ENUM_CMDPACKET_REQUEST_PEER`, with the remote character/object ID in the first u16 and mode `0x0F`. The bridge classified every packet correctly but left it unhandled, so the client remained in its pending peer-request state and never completed the remote-player context-menu flow.
- Verified the complete request and response contract in the current `NoPack.exe` through IDA MCP. `sub_325F9D0` sends exactly `u16 target,u8 mode,u32 value0,u16 value1,u32 value2`; the class1 decoder `sub_2261E30` consumes the response envelope's success byte, and the registered op10 handler `sub_1D11A80` immediately returns on nonzero success. The correct minimum ACK is therefore same opcode/class with body `01`; no speculative player, inventory, or network fields were added.
- Added the exact 13-byte request decoder, a one-byte success builder, and bridge handling that acknowledges only a non-self target currently registered in the same concrete town/area. Malformed, stale, self, offline, and cross-area targets are consumed without a fabricated success response. The op11 party invitation path remains separate.
- Admitted the IDA-verified op10/op11 handlers through the bounded current-scene client route and retained the one-time dispatch-registry trace for live acceptance evidence. Rebuilt and installed the compatibility DLL, updated the `90cn-decode-bypass-v1` manifest as one unit, and preserved the existing database.
- Validation passed `gofmt`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the complete `dnfbridge` and control suites, required `go vet`, Release/Win32 client-patch rebuild, production server build, strict client validation, and online BAT preflight. Installed/package DLL SHA256 is `BA4C9F2FB13B9690BCD989679A0AF735FA89BD11111576D0192753CA7CFD2BC5`; deployed server SHA256 is `A0D458B4EDB275A1454CC4506044295C6C819AB3C4ADE16EF14FA3A55B81A4D1`. MySQL remains PID `53484`; DNF90Server is READY as PID `53072` with all 30 listeners accepting connections. Final two-client visual/menu acceptance is pending.

## 2026-07-27 - project the selected channel into the town HUD safely

- Traced the town HUD's `ch.00 未知` state against the current EXE while the same session was connected to game listener `10021` and the channel-selection dialog correctly exposed `CH.21`. The selected-channel object held channel ID `21` at its proved `+0x70` field, while the resident-channel ID global at RVA `0x04D88FA0` remained zero; a live write of that exact selected value into the resident global succeeded in both running clients without sending a packet or invoking the native channel commit.
- Extended the existing lightweight resident-channel snapshot in `90CN.dll` to copy the selected channel ID together with the already copied server ID, name, address, and port. The copy is fail-closed: it requires a positive ID, the established `10000 + channel ID` listener mapping, and agreement between the selected and connected ports before changing resident state.
- Kept the current-client compatibility boundary intact. The patch does not restore class0/op1 and does not call the native scene/channel commit, both of which are excluded because the current EXE evidence shows that path re-arms the 600-second Error2 watchdog. The Release Win32 DLL and packaged DLL both build to SHA256 `D8FFB129B0BA996FD3511BD76607361C2358A550F18B377696A53D1CB766E232`; the compatibility manifest was updated as the same profile unit.
- Validation passed the Release Win32 rebuild, packaged/manifest SHA256 equality, the controller and release suites, and `git diff --check`. Both running clients retained resident channel ID `21` after the live compatibility write; their loaded on-disk DLL remains untouched until the game processes are closed for the permanent replacement.

## 2026-07-27 - bind two launcher slots to distinct live accounts and create remote town actors

- Completed the local multi-account boundary without adding a client login packet that the current EXE does not send. The authenticated launcher registers each new `DNF.exe` PID and internal account ID through the protected loopback admin endpoint; accepted game connections resolve their peer PID from the Windows TCP owner table and bind every repository access to that session account. `server.accountId` remains only a compatibility fallback for a directly launched or unregistered client.
- Corrected the Windows TCP-owner lookup after a live check exposed the server-side accepted socket PID as the wrong tuple. The bridge now resolves the reverse client-source/server-listener tuple, and a Windows regression uses a separate child TCP process to require the returned owner PID to be the peer process.
- Completed the existing same-town state manager. The previous path sent movement, area state, and roster packets but never created a remote actor object, so two online characters could not render each other. A live two-client check disproved the first class0/op9 display attempt: both clients received op9/op23/op24 but still had no peer actor. Town entry now uses the current EXE's real class0/op2 object path, sending mode 0 object creation and mode 1 state/equipment binding before class0/op23 state and class0/op24 roster data.
- Fixed a launcher regression found during live multi-account testing: the main login button still executed `stop --keep-database` and switched the global account before starting a client, which disconnected the first window. Both login actions now keep an already-READY server running and perform only authentication, multi-instance client startup, PID/account registration, and the five-second process survival check.
- Fixed the last account-scope leak found by the second live account: adventure-group projection now resolves the requesting session instead of silently using the compatibility account. Repository-backed character, inventory, quest, wallet, and adventure-group reads therefore remain isolated for both simultaneous clients.
- Replaced the launcher's single credential form and redundant second-client/hide/show actions with two independent account/password forms. Each slot has its own registration and login buttons plus its own protected Windows Credential Manager target. Standard dialog navigation now handles Tab and Shift+Tab across both forms, the remember checkbox, and the action buttons.
- Added account-registry, session-account override, adventure-group isolation, Windows TCP-owner, launcher no-restart/two-slot, and mode0/mode1 town actor-order coverage. The currently deployed server is READY as PID `51916` with SHA256 `5CE9E4B01C17E01737F30148CECEE3F21647F97612E2781188FB03C58F17F0EF`; the two-slot launcher is PID `50252` with SHA256 `2A0B2080F2A030C8C2BE417A5825D696DD1EBFE64221B0E015A6E36396D1BABA`; portable MySQL data remains preserved under PID `53484`. Final two-client visual acceptance, complete validation, and the source one-click archive are still pending.

## 2026-07-27 - restore tutorial room activation after the opening cinematic

- Traced the Gunblader tutorial dungeon `7145` live. The server entered start room `0:0` / map `70576`, loaded the real PVF route to `1:0`, and classified the room as clear, but the current client never emitted op45 when the player reached the right exit. The client did emit the proved op191 `state 0/type 1 -> state 1/type 1` opening-cinematic boundary.
- Kept the unsafe tutorial op3 suppressed at the early post-op120 placement boundary, where the current EXE has previously crashed with a nil active-room object manager. After replying to the later op191 state-1/type-1 resume, the bridge now sends the tutorial actor's class0/op3 state-1 packet exactly once, provided post-op29 placement and FINISH_LOADING are both complete. Ordinary dungeons and malformed, early, duplicate, or unowned op191 traffic retain their existing behavior.
- Added regressions for the op170-then-op3 order, scene-readiness guard, and one-shot replay guard. Added a real-PVF smoke assertion that Gunblader map `70576` has the right-hand `1:0` neighbor and its basic action ends by moving map.
- Validation passed the real-PVF Gunblader smoke, `go list`, every `internal/modules/dnf/...` test, the complete `dnfbridge` and control suites, the required `go vet` set, `git diff --check`, and BAT-only offline/online preflight. MySQL data was preserved as PID `53484`; DNF90Server is READY as PID `48844`, all 30 managed listeners accept connections, and the deployed SHA256 is `36B1BD30B0CFDF254FECA6448CB78B8223B792910C3924CCA0D36B8D23E7FB49`. The previous server is recoverable from `runtime/backups/server/20260727-145137_pre_gunblader_tutorial_resume_op3`.

## 2026-07-27 - add safe game-window hide/show and second-client launch

- Extended the native login GUI with “再开一个客户端（当前活动账号）”, “隐藏游戏窗口”, and “显示游戏窗口”. The second-client action calls the existing strict `launch-client` path without stopping or restarting DNF90Server, so it does not deliberately disconnect the first client.
- Window control is limited to visible unowned top-level windows whose owning process resolves to the exact configured `client.directory` `DNF.exe` or `NoPack.exe`. Each hidden window is retained with its HWND, PID, full executable path, and process creation time; restore revalidates all four properties so a reused handle or PID cannot expose an unrelated window.
- Normal launcher close restores every window hidden during that launcher session. Closing is refused while a background action is in flight, preventing the hide task from racing launcher shutdown. This is display control only: it does not hide a process from Task Manager, install a driver, or add a process-enumeration bypass.
- Kept the current protocol boundary explicit: both simultaneous clients still use the one active account loaded by DNF90Server. Different login accounts cannot be mapped to different concurrent client sessions until the current EXE provides and the bridge implements a proved per-session login identity.
- Added a native Win32 regression that creates a real owned top-level window, hides it through the executable-path filter, verifies it is no longer visible, restores it, and verifies visibility. Launcher/control/release tests, `go list`, the required `go vet` set, and `git diff --check` pass. Deployed launcher PID `14272` is responsive and exposes all three new controls; SHA256 is `DF7DAD0B696730632E9D9FD0E700C4ACCD11FBBD51E1492AD7352239CD774211`. The launcher-only deployment did not issue a server restart; final observed runtime status is MySQL PID `53484` and READY DNF90Server PID `52940`, with all 30 managed listeners accepting connections.

## 2026-07-27 - add the local account launcher and protected remembered credentials

- Added a standalone native Windows login GUI and the top-level `LOGIN.bat` entry. It supports account registration, username/password authentication, one-click server/client startup, and an optional “remember account and password” checkbox without adding a PowerShell deployment dependency.
- Added the loopback MySQL `dnf_login_accounts` identity table. Login names map to the existing DNF `account_id` boundary, passwords are stored only as bcrypt hashes, and the controller accepts passwords only from standard input. The first registered login claims the instance's current active account so upgraded `dnf:1` character data remains reachable; later registrations receive independent random account IDs.
- Kept the required local profile boundary: one DNF90Server process has one active account. A successful launcher login stops the existing local service when needed, authenticates the selected account, updates the active account ID through the controller, then starts the service and compatible client. This adds sequential multi-account switching without pretending the current protocol has simultaneous per-session account mapping.
- Remembered plaintext login credentials are stored only in the current Windows user's Credential Manager under a project-specific generic-credential target. They are not written to `instance.json`, MySQL, logs, command arguments, source, or release archives; a successful login with the checkbox cleared removes the saved credential.
- New release instances generate a random initial account ID instead of hard-coding `dnf:1`. Existing instances are not rewritten until an authenticated account switch, and the first registration preserves their current account data. The release builder, runtime build, documentation, and source-package whitelist now include `DNF90Launcher.exe` and `LOGIN.bat`.
- Validation passed `gofmt`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the complete `dnfbridge`, control, launcher, and release suites, Windows Credential Manager write/read/delete coverage, and the required `go vet` set. BAT-only offline preflight, strict client validation, production four-binary rebuild, login-table initialization, online database checks, and all 30 listeners passed. Existing MySQL data was preserved and is READY as PID `53484`; DNF90Server is READY as PID `52644`. Deployed launcher SHA256 is `6497A245C95F4033EA2C761958DD6DB6204D860704E6DDBE576C98C74B80840C`.

## 2026-07-27 - refresh booster source stacks after every use

- Traced the reported 华丽的徽章随机礼盒 stale count on the live connection. Each class1/op160 transaction durably committed the correct `source_remaining` sequence (`4`, `3`, `2`, `1`), but the bridge sent only the op160 result ACK and full op13 container snapshots. The current client used the ACK to open the reward window but did not mutate the source item row, so the displayed stack stayed unchanged until the final deletion.
- Reused the established repository-backed incremental item sender already used by op44. Booster success now sends the native class1/op160 ACK, then one class0/op14 source-slot row, then the existing authoritative op13 reward-container refreshes. A remaining stack carries its new count immediately; the last unit carries the proved `0xFFFFFFFF` deletion row. Reward selection, PVF rolls, durable transactions, and result-window bodies were not changed.
- Added route-level regressions for both a decrement from two units to one and final-unit deletion, and updated the multi-selection, creature, and pet-artifact flows to lock the new `op160 -> op14 -> op13` order.
- Validation passed `gofmt`, focused and complete `dnfbridge` tests, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the control suite, the required `go vet` set, a production controller build, strict client validation, and BAT-only offline/online preflight. Portable MySQL data was preserved and restarted as PID `22384`; DNF90Server is READY as PID `51508`, all 30 managed listeners accept connections, and fresh server stdout/stderr are empty. Deployed server SHA256 is `F5A3C50D2F782380D6659E90BE26E1AE20B47C411E07A655412987752BC1880B`; the previous server SHA256 `F9DE36C8347CB9B7CE8D113088E494C865E6FB9BDC62D01EB51B7CA4F2DBF479` is recoverable from `runtime/backups/server/20260727-134215_pre_booster_op14`.

## 2026-07-27 - serialize game-session state through a per-connection event loop

- Added one framework event loop per live game connection. Initial bootstrap, native DPROTO open, every decoded client packet, disconnect cleanup, and the first asynchronous gameplay callbacks now enter the same FIFO session owner instead of mutating `gameSession` from the connection goroutine and scheduler goroutine at the same time.
- Migrated both callbacks currently owned by the shared gameplay time queue: pet satiety recovery/decay ticks and the ten-second dungeon-death return. Their scheduler callbacks now only submit a typed session event. The existing pet-tick and dungeon-run generations remain as feature-level replay guards.
- Added a session-level character generation. Selecting a different character and returning to character select advance it; each character-scoped timer captures both character id and generation. A queued callback for an old character is discarded before it can touch dungeon, town, pet, repository, or wire state. Closing a connection first disables asynchronous delivery, serializes DPROTO/online/timer cleanup, then stops and rejects the mailbox.
- Added regressions proving an inbound packet and timer callback cannot overlap, an old-character callback cannot cross a selection boundary, a stopped session rejects later timer work, and both real death-return and pet-growth timers wait behind the session owner. The complete `dnfbridge` suite passes with Go's race detector.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the complete `dnfbridge` and control suites, the required `go vet` set, and a production build. Deployed server SHA256 `F9DE36C8347CB9B7CE8D113088E494C865E6FB9BDC62D01EB51B7CA4F2DBF479`; the previous wire-serialization server SHA256 `2A7D3E300D5F940AC1603AA7185CE3F4258DA98A386986F52A31C2562216F5A1` is recoverable from `runtime/backups/server/20260727-132117_pre_session_events`. Portable MySQL remained online as PID `47564`, preserving all data. The new server is READY as PID `51284`, strict client validation passes, all 30 managed listeners accept connections, and its fresh stdout/stderr files are empty.

## 2026-07-27 - serialize the complete per-session game wire boundary

- Fixed a framework-level race between gameplay/time-queue sends and the game connection reader. The per-session wire lock now owns transport/upper sequence allocation, packet construction, DPROTO state transitions, and the corresponding socket write as one atomic operation; a later sender can no longer allocate an earlier sequence and then reach the socket after another packet.
- Routed ordinary latest-game transport, fixed-15 compatibility transport, character-roster upper, ordinary upper, native DPROTO decode/control output, and protected upper output through that same boundary. Fixed-header upper packets that deliberately do not consume a sequence retain their current wire layout and are still serialized at write time.
- Made the diagnostic game packet counter atomic because receive logging and timer-driven send logging can run concurrently. This changes only log ordering metadata and does not change a protocol body, database record, PVF rule, or gameplay response.
- Added concurrent regressions with 128 simultaneous latest-transport sends and 128 simultaneous upper sends. They parse the resulting byte stream and require exact wire-order sequences `0..127`; a separate 256-way regression verifies the packet-log counter. The focused suite also passes with Go's race detector.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the complete `dnfbridge` and control suites, the requested `go vet` set, and a production server build. Deployment also exposed a stale strict-manifest value: the installed client DLL and source-owned `client-patch/bin/90CN.dll` both had the already validated contract-fix SHA256 `60BF6FEF43C0528F2BD8729C8EBF4603ABF98043636BE476F94A630AA0C3C32C`, while `client-compatibility.json` still named an earlier DLL. The manifest now pins the matching compatibility unit and strict client validation passes again.
- Deployed server SHA256 `2A7D3E300D5F940AC1603AA7185CE3F4258DA98A386986F52A31C2562216F5A1`. The previous server SHA256 `3F5090ED3FA570B421365483D04D9027CB5B0E68CC602C450A05A5C907ABF17A` is recoverable from `runtime/backups/server/20260727-130150_pre_wire_session`. Portable MySQL remained online as PID `47564`, preserving all data; the new server is READY as PID `13584`, all 30 managed listeners accept connections, and its fresh stdout/stderr files are empty.

## 2026-07-27 - preserve rerolled grade and use row-level inventory updates

- Traced the repeated “最下级” result through live server events and wire captures. Ordinary grade adjustment generated different seeds (`124865009`, `572024372`, `428379598`, and `996561680`), but every target op14 row still carried raw `+0x06 = 1`. Legacy inventory equipment lacked `item_kind=equipment`, so the bridge rejected its explicit `quality_seed` and fell back to the ordinary stack count.
- The item-grade owner now durably normalizes every adjusted target as equipment. Current item projection also treats an explicit valid `quality_seed` as authoritative for legacy rows even before that identity is repaired; ambiguous legacy amount/instance aliases and raw data remain gated by equipment identity. Regression coverage starts with an untagged legacy target and locks the persisted identity plus the exact random/gold seed in the returned op14 row.
- Stackable-use success now declares the exact durable slots it changed. After the native class1/op44 ACK, the bridge reloads only those slots and sends one class0/op14 incremental update: an ordinary or premium consumable refreshes one source row, the last unit emits the proved `0xFFFFFFFF` deletion row, and a revive-coin consumable emits only the source row plus fixed wallet slot 1. The previous revive-coin path no longer rebuilds all selected item containers through multiple op13 packets.
- Explicit stack merges between the backpack and personal warehouse now commit the summed destination count and source deletion, then return the native op19 ACK followed by exactly two op14 rows. They no longer rebuild both containers with competing full op13 snapshots, which made an already-stacked destination appear to lose the transferred items in either direction.
- Fixed one malformed-row contributor to right-click use being rejected locally for relational stackables imported without a historical `instance_value`. Live PID `51184` emitted no op44 for contract right-clicks, and its post-login op13 rows for items `30`, `31`, and `10000388` still carried `raw+0x0E = 0` after the first candidate fix. MySQL proved their `item_kind=stackable`, PVF paths, and counts were present; `amount_or_count` was being mistaken for an equipment quality seed before the stackable fallback ran. Quality aliases are now equipment-only unless an explicit `quality_seed` exists. Login/full-list and op14 incremental encoders preserve an explicit identity first and otherwise project the authoritative item id as the stable nonzero stackable wire identity without changing durable inventory contents.
- Fresh PID `51668` disproved the assumption that nonzero `raw+0x0E` alone restores contract use: its login rows correctly carried identities `30`, `31`, and `44`, but right-clicking still emitted no class1/op44. Static current-EXE analysis then pinned the two main-inventory op44 callbacks at VA `0x015280C0` and `0x01B72FD0`, plus the native writer sequence `[u16 opcode=44][i16 slot][u8 list=0][u32 identity][u32 item][u32 reserved=0]`. `90CN.dll` now wraps only those exact callbacks, lets the native path run first, and emits the same 15-byte request through the EXE's own scalar writers only when no native op44 was produced and the selected row is an exact `premiumlist_new.etc [item]` id from the pinned runtime PVF. Ordinary stackables, boxes, equipment, warehouses, and non-premium items cannot enter the fallback.
- The first candidate build passed automated validation and was deployed as PID `12236`, but the restarted client disproved its contract fix through the zero-identity op13 capture above. That live failure is retained as the regression fixture's exact `item_kind + amount + amount_or_count + pvf_path` metadata combination rather than being treated as a successful release.
- Corrected the op208 single/display magic-box header from the running current EXE rather than the contradictory earlier notes. After the class1 dispatcher consumes success, `sub_1D05A60` always reads `clientType` and `doubleFlag`; type 0 reads its two u16 source slots at `0x01D05B8B/0x01D05B97` and enters the reward reader at `0x01D05DD3`, while the reported Seria type 4 reads them at `0x01D06815/0x01D06821` and enters the reward reader at `0x01D0698A`. op208 is therefore `[u8 success=1][u8 clientType][u8 doubleFlag][i16 boxSlot][i16 materialSlot][u16 rewardCount][0x77 rows]`. Both ordinary zero-flag and Seria type-4 paths now have exact header regressions, including the live single-open request `04 55 00 FF FF`.
- Contract activation continues to consume one item and atomically upsert `max(now, existing expiry) + PVF duration` at account scope, followed by op44, premium notification, and one-row op14 refresh. Real `Script.pvf` verification pins the reported item `31` (霸王之契约 3天) to type 22 and 259200 seconds, plus the three-day 达人/成长/晶 contract rows. Select-character restoration now also emits every active runtime-PVF u8 contract type instead of silently dropping the additional 29/80/88/92/100 families; internal 魔王 slots remain folded into native type 58.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the complete `dnfbridge` and control suites, the requested `go vet` set, the production build, strict client-profile validation, and BAT-only offline/online preflight. The contract compatibility addition also passed the Release/Win32 rebuild with zero errors, installed client SHA256 `60BF6FEF43C0528F2BD8729C8EBF4603ABF98043636BE476F94A630AA0C3C32C`, and started fresh client PID `49292`; its startup trace confirms the two audited callbacks and shared item-row cache are installed. The previous DLL is recoverable from `runtime/backups/client-patch/20260727-025318_contract-op44`. The BAT controller stopped MySQL normally only because offline preflight refuses a running portable instance; relational data was preserved and restarted under MySQL PID `47564` plus DNF90Server PID `51036`. All 30 listeners accept connections, fresh MySQL/server stderr files are empty, and deployed server SHA256 is `3F5090ED3FA570B421365483D04D9027CB5B0E68CC602C450A05A5C907ABF17A`.

## 2026-07-27 - restore the native item-grade result protocol

- Live retesting disproved the earlier op81 sentinel/result-type workaround. Result type `1` invokes current-EXE `sub_1D16480`'s `0x298` announcement with DSTR `1699`, which this client renders as the unrelated “已成功封装该物品” message; it is not a gold-quality result. Result types `2/3` share the grade-adjust result message path.
- The x32dbg breakpoint at VA `0x021792A0` stopped inside the class0/op13 item-object constructor, with caller `0x01D72ECE`. This proved that the new 8.9 KB full-inventory op13 sent before op81 was rebuilding the client inventory and destroying the existing grade-adjust UI/object state.
- Corrected the op81 success body to its actual material contract: `[success][u16 consumed material slot][u32 post-consume amount][u16 grade result=2]`. The bridge now returns the real adjustment-box slot and remaining stack, then sends one class0/op14 row for the still-present target equipment. It no longer sends the `0xFFFF` sentinel, result type `1`, or a full op13 refresh.
- Ordinary boxes still persist an unbiased full random quality seed and gold boxes still persist the top-quality sentinel. Both now use the native grade-result protocol; regression coverage locks remaining-stack and removed-stack ACKs, exact `op81 -> op14` order, the single target row, and the committed seed.
- Kept the evidence-only class1/op81 dispatch trace in `90CN.dll`; installed and pinned client SHA256 is `55FDF2E37A07C86A846572D18961DAEE85843BE48AC8F635636E2E8CB3022DA9`.
- Validation passed `gofmt`, focused item-grade/item-quality tests, the complete `dnfbridge` suite, `git diff --check`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the control suite, required `go vet`, production build, strict client-profile validation, and BAT-only offline/online preflight. MySQL data was preserved; portable MySQL PID `31724` and DNF90Server PID `23200` are READY with all 30 listeners accepting connections. Deployed server SHA256 is `238BE01971F7BEA4EAAC0F75C7DACE064A38F3D19881CF930253B66BA3540B05`.

## 2026-07-27 - correct Seria op1128 header alignment from live x32dbg evidence

- Captured the failed ten-open in x32dbg before restarting. Current DNF PID `48448` stopped on a first-chance access violation at VA `0x01D07679` (`DNF.exe+0x01907679`, `mov edx,[esi]`) with `ESI=0`. The matching protocol trace showed the server body began `04 01 0A 00 41 00 FF FF 0F 00`; the current handler consumes the leading `04` as its success argument, then expects a separate `variant`, `doubleFlag`, `openCount`, box slot, and material slot. The missing explicit variant byte shifted every later field and made the client resolve invalid slots before the crash.
- Corrected the native Seria batch body to begin `04 04 <doubleFlag> <u16 openCount> <i16 boxSlot> <i16 materialSlot>`. The captured ten-open shape is now `04 04 01 0A 00 41 00 FF FF ...`, so variant `4` follows the verified branch directly to the native Seria result construction instead of entering either local slot-validation path. The package regression locks the complete corrected body, both reward lists, real slots, and `0x77` rows.
- Removed the earlier RVA `0x01907577` null-source jump patch from `90CN.dll`; the breakpoint proved that bypass unsafe because later variant-1 code still dereferences the invalid slot object. The current EXE's original null guard is intact again. Retained only the independently verified NOP at RVA `0x019081CF` for the stale legacy follow-up warning, and retained the server-side removal of the extra op208 overlay.
- Validation passed `gofmt`, focused and complete DNF module tests, `go list`, the full `dnfbridge` and control suites, required `go vet`, Release/Win32 client-patch rebuild with zero errors, production server build, strict client profile check, offline preflight, and online 30-listener preflight. Installed client SHA256 `100F9A64DA9F469AB79728F962BC28436AF5A08474CA83865A890055798C32E2` and server SHA256 `42C2EB2E6BE1911906B877C2ADF367BCB98895A8B14B18CD02AE22AC05FE8939`; MySQL PID `40320`, server PID `50708`, and fresh client PID `15456` are running with the existing database preserved. The fresh client trace confirms the obsolete null-guard patch is absent and only the toast compatibility patch was installed.

## 2026-07-26 - consolidate four client compatibility modules into one 90CN.dll

- Consolidated the responsibilities of the supplied `antidebug_bypass.cpp`, `block_minimize_all.cpp`, `tensdk_bypass.cpp`, and `tersafe_stub.cpp` under a one-target/one-owner rule. The files were not concatenated and no competing trampoline is installed over an existing hook.
- Compiled `debug-compat/debug_compat.cpp` directly into production `90CN.dll`. The patch worker now calls the embedded installer without `LoadLibrary` or `GetProcAddress`; debug compatibility defaults to enabled and can be disabled only with game-root `90CN.ini` `[debug] enabled=0`. The game client no longer needs or loads `90CN-debug.dll`.
- Kept the four proven native-query owners: current-process debug port/object/flags, `ThreadHideFromDebugger`, kernel-debugger state, and returned debug-register values. Deliberately did not merge the downloaded `NtSetContextThread` hook because it would clear the DR state x32dbg is attempting to install. Debugger-process hiding and broad global Win32 export hooks also remain excluded.
- Retained the three audited minimize branches as the sole main-EXE IAT owners for `ShowWindow`, `SendMessageW`, and `CoCreateInstance`. Retained the six current TCLS helpers as the sole TenSDK owner; the old unconditional patch at current VA `0x01EF7C40` is not applied because it is the same entry as the required TCLS login trampoline. Retained the real TerSafe object and bounded native DPROTO route instead of installing the allow-all fake vtable.
- Release/Win32 rebuilt successfully with the embedded component. The x86 DLL is `365056` bytes, exports `Install90CNDebugCompat`, has no dependency on `90CN-debug.dll`, and has SHA256 `D3D3E2DC7E973E83624812A7C7B9B678206E956CCFBA59CFCDDDEB02CA3BCB7E`. The isolated harness build and complete 32-bit debug-compat smoke test passed.
- Backed up the prior client DLLs and strict manifest under `D:\BaiduNetdiskDownload\23.4.15.0\地下城与勇士\_codex_backups\20260726_235035_pre_embedded_four_module`, installed only the new `90CN.dll`, moved the obsolete companion DLL out of the client root, and updated the strict client hash. Client launch passed the strict `90cn-decode-bypass-v1` profile.
- Live PID `48448` installed the four native-query hooks, three audited IAT hooks, six TCLS helpers, and bounded DPROTO route in that order without a pre-hook/unknown-byte rejection. x32dbg then kept a real fast-resume hardware read/write breakpoint active on the process PEB debug byte for more than 20 seconds; it reached `1548` hits while the client remained responsive and no protection error `4026` occurred. The test hardware breakpoint was removed and x32dbg remained attached.
- No server, MySQL, database row/schema, PVF, generated runtime configuration, or IDB was changed.

## 2026-07-26 - Seria's Luck ten-open uses the native two-pane result

- Fixed the reported Seria's Luck (`2682272`) ten-open result overlay. Live packet evidence showed that one successful request emitted the native class1/op1128 batch result and then an extra class1/op208 single-open display; the latter created the incorrect three-row "obtained items" window over the intended two-pane result.
- Seria expand success now emits exactly one native op1128 batch response. The existing variant `4`, double flag, open count, base-draw list, doubled-round list, real reward slots, `0x77` row shape, transactional grants, SeriaLuck progression, and post-commit container refresh remain unchanged. Non-Seria expand behavior remains on its established op208 display path.
- Current-client trace for the reported request also proved that op1128 exited at error `11159` before the `0x36B` two-pane call when ten boxes consumed the complete local stack. The compatibility DLL now narrowly redirects only the null-source conditional jump at current-EXE RVA `0x01907577` past the obsolete local count check; non-null source validation remains intact and the authoritative server transaction plus following op13 refresh still align inventory.
- Suppressed the op1128 handler's unconditional follow-up toast call at current-EXE RVA `0x019081CF`. Its legacy string ids `70156/70157` map to an unrelated cross-class purchase warning in this client's 2018 `dstr.dat`; the ten-open result needs no separate announcement.
- Updated the package regression to require exactly one Seria batch response and forbid the op208 overlay. Client-patch Release/Win32 rebuilt successfully with zero errors; full `go list`, every `internal/modules/dnf/...` test, complete `dnfbridge` and control suites, the requested `go vet` set, the production server build, and BAT-only offline/online preflight passed.
- Installed client compatibility SHA256 `BA13C3C06DD8BED081A1CBB31AD13F9520392B6368AE0172E262AB9AFB7D61C4` and server SHA256 `CD904ED59B82EEB4E1E25A885B91D52732697A68FF522D0C9CCB18FD2640F043`. Portable MySQL PID `11104`, DNF90Server PID `29172`, and DNF client PID `21848` started successfully; all 30 managed listeners accept connections, the database was preserved, and the fresh client trace confirms the two narrow Seria compatibility patches installed at their verified addresses.

## 2026-07-26 - consolidate over-split bridge adapters

- Applied the cohesion audit and removed eight bridge-only thin files that had no independently changing responsibility. This consolidation deliberately permits cohesive files above 300 lines; file length is no longer used as an architectural boundary.
- Merged `scene_skill_owner.go` into `scene_skill_mutation.go` (158 lines), `guardian_gem_owner.go` into `gameplay_module_guardian_gem.go` (179 lines), and `game_select_name_tag_owner.go` into `game_select_state.go` (73 lines). The real `skill`, `guardiangem`, and `equip` domain Owners remain separate and unchanged.
- Merged `initial_town_actor_scene_snapshot.go` into `initial_town_route_packets.go` (294 lines) because the body builder and sender share the same actor-packet offsets and lifecycle. Merged `character_roster_legacy.go` into `character_roster_body.go` (157 lines) because the slots/chars/count helpers only compose that roster protocol family.
- Merged `npc_shop_owner.go` into `npc_shop_projection.go` (402 lines) and `booster_owner.go` into `gameplay_module_booster.go` (317 lines). Their `npcshop.Owner` and `booster.Owner` domain implementations remain independent; only bridge construction, context, error mapping, and the uniquely consumed read/commit adapters moved beside their callers.
- Merged `userinfo_legacy_registry.go` into `userinfo_legacy_dispatch.go` (351 lines). Preserved the complete legacy notification ID order and `csharpLegacyUserInfoInitPackets` compatibility evidence instead of deleting the currently test-referenced initializer.
- Updated boundary tests to require the eight obsolete thin files to stay absent and to pin moved functions to their cohesive owners. Removed all remaining `strings.Count` line-ceiling gates from bridge boundary tests; architecture tests now enforce real ownership, domain delegation, absence of direct repository writes, and absence of private timers without encouraging cosmetic splitting.
- No protocol body, current-client behavior, gameplay registration, domain transaction, PVF authority, or time-queue behavior changed. The consolidation only moved existing declarations and imports.
- Validation passed `gofmt`, declaration-preservation and obsolete-file checks, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `6160` and DNF90Server PID `49124` reached READY with all 30 managed listeners accepting connections; the fresh instance produced no server stderr file. Production server SHA256 is `42E77A82F446B5F4068DBA593677BCDD626B6A9AE3BE117701015B4B0447C3C7`.

## 2026-07-26 - parallel scene, Boss-death, and initial-equipment cohesion split

- Completed three independent bridge splits in parallel and integrated them as one release. Removed the mixed 1,271-line `game_select_scene.go`, 828-line `dungeon_boss_die_check.go`, and 805-line `initial_equipment.go`.
- Character-select scene lifecycle now has seven cohesive components: entry orchestration, shared transport, object projection/diagnostics, deferred tail, selected USERINFO, runtime gates, and request handlers. The largest is 282 lines. Preserved all 32 production declarations and the existing post-scene rental-point replay repair byte-for-byte; the component remains shared selection/scene infrastructure rather than a fake gameplay.
- Boss-death handling now has five state-machine components: op117/op39 state and protocol, clear decision, settlement handshake, ordinary final-room completion, and cross-component actor/death evidence. The largest is 246 lines. Short protocol writers were deliberately merged back into the state file after a cohesion review. Preserved the existing `dungeon-combat-drop` gameplay registration, cross-room death-owner validation, replay behavior, tutorial CMT evidence, completion barriers, ordinary final-room path, and failure retry semantics.
- Initial equipment now has five cohesive components: PVF catalog/cache, initial plan and starter-slot rules, item metadata/model layers, durable equipment projection, and roster/scene appearance projection. The largest is 208 lines. Preserved all 40 production declarations, current starter-item authority, durability/type metadata, raw equipment entries, weapon appearance priority, and final persistence through the existing atomic `characterdata.Creator`.
- Audited persistence and timing boundaries for all three groups. They perform synchronous reads/projection or delegate durable work to existing domain Owners/Creator; none directly saves repositories or owns a delayed callback/private timer, so no new Owner, fake gameplay, or time-queue job was introduced.
- Applied the user's cohesion rule after the parallel split: files are separated only when they have an independently changing lifecycle or shared responsibility. The three new boundary suites keep old mixed files absent, pin representative function ownership, reject direct bridge writes and private timers, and intentionally do not enforce a mechanical line-count ceiling.
- Performed a read-only cohesion audit of earlier splits. Domain Owners, independent `gameplay_module_*` entries, current-item-list, item-expiration, USERINFO authority layers, and cross-gameplay projections remain correctly separated. Eight bridge-thin-layer merge candidates were identified for a later focused cleanup: scene-skill owner adapter, guardian-gem bridge owner adapter, select name-tag adapter, initial-town actor snapshot, NPC-shop bridge owner adapter, booster bridge owner adapter, legacy roster body, and legacy USERINFO registry/dispatch. No stable historical code was rewritten during this audit.
- Validation passed `gofmt`, declaration/body preservation checks, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `6160` and DNF90Server PID `39796` reached READY with all 30 managed listeners accepting connections; the fresh instance produced no server stderr file. Production server SHA256 is `FC6CA409B2948A9DAE34C9C5616BCE6B5D6939C0353B47E5DD7C0F5849D3D0C6`.

## 2026-07-26 - split and descend character lifecycle infrastructure

- Removed the mixed 1,743-line `dnfbridge/character.go`. Shared character lifecycle is now separated into create/name-check transport, create request parsing, creation orchestration, repository reads, creation-owner adapter, slot-change handling, roster send/body/entry/default/value/legacy projection, current scene-object body/tail projection, shared repository access, protocol constants, and repository timeout policy. Character selection and account bootstrap remain shared lifecycle infrastructure rather than fake standalone gameplays.
- Added `characterdata.RosterOwner` as the durable character-slot mutation boundary. The bridge validates the exact eight-byte op295 request and delegates the account-scoped exchange to the owner; MySQL keeps its locked three-update transaction, memory storage preserves the same occupied-slot-only behavior, and the bridge no longer calls `dnfrepo.SwapCharacterSlots` directly.
- Kept new-character persistence behind the existing `characterdata.Creator` transaction. PVF-derived initialization, account bootstrap, character insertion, inventory, equipment, skill, quest, pet, and settings records still commit or roll back as one aggregate.
- Preserved current working-tree compatibility fixes: NoPack create/name-check DSTR bytes are decoded as the Windows GB18030 code page even when the bytes are also valid UTF-8; the exact empty mode2 roster arms the class1/op679 completion handshake only after a successful send; non-empty rosters clear that probe; and all current roster, scene mode0, equipment-summary, name-tag, creature, UTF-16, route, slot, and object-key layouts remain unchanged.
- The 17 production bridge components are now bounded to 271 physical lines or fewer. Boundary tests keep `character.go` absent, pin representative functions to responsibility-specific files, require `RosterOwner` and `Creator` delegation, reject direct bridge writes and private timers, and enforce the file-size ceiling.
- Audited the timing boundary. Character lifecycle uses only bounded repository contexts and synchronous UTC timestamps; it has no delayed callback or background task. Repository deadlines now live in `repository_timeouts.go`, while no artificial time-queue job was introduced.
- Added domain tests for account-scoped occupied-slot exchange, cross-account preservation, empty-slot preservation, invalid-slot rejection, and missing-owner rejection. Existing create, name-check, slot-change, GB18030 name, empty-roster handshake, roster wire, and current scene-object tests remain green.
- Validation passed `gofmt`, function/type-set comparison, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `17848` and DNF90Server PID `47140` reached READY with all 30 managed listeners accepting connections; the fresh instance produced no server stderr file. Production server SHA256 is `45B5FA8767CA0BD5F89F8C3652E41A4C36DA0AFFDB2352CB06789F821AA992FF`.

## 2026-07-26 - split current USERINFO scene projection

- Removed the mixed 1,816-line `dnfbridge/userinfo2.go`. Shared selected-character and scene USERINFO projection is now separated into entry orchestration, mode1/mode3 object bodies, equipment projection, equipment eligibility rules, equipment wire encoding, subtype0 appearance, subtype1 stats/equipment, stat-blob wire encoding, authoritative stat resolution, and generic typed stat readers. It remains shared selection, reconnect, return-town, quest-refresh, and scene lifecycle infrastructure rather than a fake standalone gameplay.
- Preserved the current working-tree compatibility fixes while splitting: mode1 and mode3 carry authoritative cumulative character EXP, mode3 carries the scene route/owner channel, selected actor binding can explicitly include equipment, and mode1 refreshes retain the verified stat-blob offsets and equipment object ordering.
- Preserved existing current-client behavior for subtype0/subtype1, mode1/mode3 actor construction, adventure level, object keys, equipped-creature tails, current-EXE worn-slot mapping, create/deferred equipment rows, indexed state, raw/vector blocks, durability branches, appearance filtering, PVF-backed stat precedence, legacy fallback rejection, GB name encoding, and missing-state diagnostics.
- The 10 production components are now bounded to 253 lines or fewer. A bridge boundary test keeps `userinfo2.go` absent, pins representative functions to their responsibility-specific files, enforces the production-file size ceiling, and rejects direct repository writes or private timers.
- Audited the persistence and timing boundaries. This infrastructure only reads character, equipment, PVF, and legacy projections and synchronously encodes packets; it performs no durable mutation and has no delayed callback or background task, so no domain owner or artificial time-queue job was added.
- Validation passed `gofmt`, function/type-set comparison, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `32012` and DNF90Server PID `9236` reached READY with all 30 managed listeners accepting connections; the fresh instance produced no server stderr file. Production server SHA256 is `D731DD07CCADFFD09B73F1223E82801E59E31B6EF6D3465F0E04870CD5313C55`.

## 2026-07-26 - split legacy USERINFO compatibility infrastructure

- Removed the mixed 2,903-line `dnfbridge/userinfo_legacy.go`. The shared character-selection and scene-entry compatibility library is now separated into registry, subtype dispatch, repository-row reader, the complex type `0x23` body, seven bounded notification-builder groups, raw-notification registry, raw `0x374/0x379/0x37A` projection, raw readers, and collection-key helpers. It remains bridge-owned shared transport infrastructure instead of being registered as a fake gameplay.
- Preserved all 156 production declarations and the existing current-client wire behavior: notification registry order and IDs, subtype bodies, missing-repository and missing-row suppression, mode-dependent `0x22E` branches, type `0xB6` twelve-value layout, selected USERINFO subtype 0/1 integration, GB/wide-string encoding, raw fixed/list validation, and the verified `0x17C`, `0x287`, `0x29F`, `0x327`, `0x374`, and `0x37A` layouts.
- The 15 production components are now bounded to 302 lines or fewer. A bridge boundary test keeps the old mixed file absent, pins representative functions to their responsibility-specific files, enforces the production-file size ceiling, and rejects direct repository writes or private timers.
- Audited the persistence and timing boundaries. This library only reads repository projections and synchronously encodes notifications; it performs no durable mutation and has no delayed callback or background task, so no domain owner or artificial time-queue task was introduced.
- Validation passed `gofmt`, declaration-set comparison, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `1580` and DNF90Server PID `18248` reached READY with all 30 managed listeners accepting connections; the fresh instance produced no server stderr file. Production server SHA256 is `AAB4C49F26AC6863E669FCFB2EC1ECE2C2414E75EC91429B1A2E767A47F54B92`.

## 2026-07-26 - split and descended adventure-info gameplay

- Removed the mixed 493-line `dnfbridge/account_adventure_info.go`. The independently registered `adventure-info` gameplay module now directly owns its class1/op1403 handler and request validation, while class0/op1340/op1346 account pushes, verified selector-slot persistence, wire projection, identity/date resolution, and constants are separated as shared lifecycle components.
- Added `adventuregroup.Owner.RememberSelectorSlot` as the durable selector-state boundary. It rereads the authoritative account, validates the bridge-proven slot range, and writes only `selector_adventure_info_slot`; the bridge no longer calls `Account.Save`.
- Added repository `AccountMetadataEntryWriter` support. MySQL now upserts the single normalized `account_metadata` row and updates the owning account timestamp in one database transaction; memory storage provides the same key-level behavior, and the fallback preserves compatibility with other repositories. Unrelated account fields and metadata keys are no longer replaced by selector-slot persistence.
- Preserved the current 16-byte protected and two-byte in-scene op1403 requests, selected-character/account ownership checks, PVF-derived account summary, op1340 direct payload, op1346 actor-bound refresh, hidden-probe one-shot address, remembered-slot roster verification, 7,420-byte raw object offsets, GB18030 roster names, represent-account identity, YYYYMMDD creation date, and neutral unowned growth-capsule fields.
- Audited the timing boundary. Adventure-info uses only bounded synchronous repository contexts and date parsing; it has no delayed callback or background task. Boundary tests reject direct repository writes and private `After`, `AfterFunc`, `Timer`, `Ticker`, or `Sleep` use so future delayed behavior must use the unified gameplay time queue.
- The six production bridge components are now bounded to 175 lines or fewer: shared pushes 175, gameplay module 142, protocol 130, selector owner adapter 58, identity 58, and constants 30. The new adventure-group domain owner is 99 lines.
- Added domain tests for key-only selector-slot persistence, unchanged-value preservation, invalid-slot rejection, and missing-account rejection. A repository SQL-shape test verifies that MySQL performs only the account timestamp update and owned metadata-row upsert without a map-wide delete. Bridge boundary tests keep the old mixed file absent, pin key functions to their owned files, require domain-owner delegation, and require op1403 to remain in the `adventure-info` gameplay module.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `48328` and DNF90Server PID `28668` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `853F44C4A067CFBF1E563E1D9A5A2C1D842AD325DCA1A398CDED2F60E6169F90`.

## 2026-07-26 - split and descended character-select request infrastructure

- Removed the mixed 520-line `dnfbridge/game_select_request.go`. Character-select parsing, authoritative character resolution, enter-select-dungeon lifecycle, current-client response bodies, expired name-tag ownership, and the legacy select-state ACK now live in six responsibility-specific files. Character selection remains shared connection/scene lifecycle infrastructure rather than being registered as a fake standalone gameplay.
- Added `equip.Owner.CleanupExpiredNameTag` as the durable login cleanup boundary. The owner rereads the selected character and verifies account ownership inside the character-assets transaction, clears expired character name-tag fields, and removes worn slot 30 atomically; the bridge no longer performs separate character and equipment saves.
- Preserved plain 16-byte, key4-protected 16-byte, current 11-byte, and legacy two-byte selection parsing; slot-first ambiguity handling; authoritative account roster resolution; equipment/PVF projection; op15 ACK and fatigue ordering; dungeon permission/town context gating; duplicate/retry handling; return-to-town suppression; and the exact legacy type-3 11-byte ACK echo.
- Audited the timing boundary. Selection uses bounded synchronous repository contexts and synchronous expiration comparisons only; it has no delayed callback or background gameplay task. Boundary tests reject direct repository writes and private `After`, `AfterFunc`, `Timer`, `Ticker`, or `Sleep` use so future delayed behavior must use the unified gameplay time queue.
- The six production bridge components are now bounded to 157 lines or fewer: character resolution 157, enter-select lifecycle 125, response bodies 104, request parsing 97, name-tag owner adapter 47, and legacy state ACK 32. The new domain owner is 112 lines.
- Added domain tests for atomic expired-card cleanup, live-card preservation, and cross-account rejection. Bridge boundary tests keep the old mixed file absent, pin key functions to their owned files, require `equip.Owner` delegation, and reject direct writes or standalone timers.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `29824` and DNF90Server PID `14092` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `AE6E09C29E7AA6639B0CC442458DC8348849C090369BC4DE0362D3D56377D176`.

## 2026-07-26 - split and descended initial-town scene lifecycle

- Removed the mixed 1,335-line `dnfbridge/initial_town_route.go`. The shared character-selection, return-select, channel-reconnect, and dungeon-return town lifecycle is now separated into route state, reconnect entry, scene readiness, player-state ordering, route progression, actor/transition packets, persisted-transition resolution, and login-location ownership; it remains shared infrastructure instead of being registered as a fake standalone gameplay.
- Added `internal/modules/dnf/town.Owner` as the durable login-location boundary. The owner rereads the selected character and verifies account ownership inside the existing character-assets transaction, applies the runtime-PVF Seria-room projection, and saves only character stats; the bridge no longer calls `Character.Save` directly.
- Preserved the runtime-PVF town/area/gate authority, previous-village origin capture and persistence, character-list login Seria projection, ordinary select/reselect progression, channel-reconnect route, dungeon-return transition, mode0/mode1 actor ordering, pre-op24 player-state sequence, scene readiness gates, and typed coordinate/state validation.
- Audited the timing boundary. The initial-town entry delay is fixed to `time.Duration(0)` and only exposes an injected synchronous test seam; there is no real delayed callback or background job to enqueue. Boundary tests reject private `After`, `AfterFunc`, `Timer`, `Ticker`, and `Sleep` use so any future timed behavior must use the unified gameplay time queue.
- The eight production bridge components are now bounded to 286 lines or fewer: player-state ordering 286, route progression 254, packet sequence 243, persisted transition 160, reconnect 132, route state 120, login owner adapter 120, and scene readiness 72. The new domain owner is 108 lines.
- Added domain tests for transactional projection, projector rollback, and cross-account rejection. Bridge boundary tests keep the old mixed file absent, pin key functions to their owned files, require `town.Owner` delegation, and reject direct repository writes or standalone timers.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `29824` and DNF90Server PID `6596` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `4418E6288D47E923CB054092533F4DBC03ED3BA1019305989821C56853AB23DB`.

## 2026-07-26 - split and descended shared current-item-list infrastructure

- Removed the mixed 1,294-line `dnfbridge/current_item_list.go`. The shared op13/op14 infrastructure is now separated into selected-session send ordering, session snapshot assembly, account cargo/wallet projection, container-state ownership, equipment snapshot/projection, protocol bodies, repository reads, stack projection, row helpers, and wire types; it is deliberately not registered as a gameplay because many independent gameplays consume it.
- Added `inventory.ContainerStateOwner` as the missing-only durable boundary for current-client container headers. The bridge still supplies the evidence-backed initial row, while the owner validates the exact character scope and typed main/avatar/personal-cargo values before saving and reloading it; the bridge no longer calls `Settings.Save` directly.
- Preserved current-client compatibility for list types 0/1/2/3/7/12/38, op13 and op14 packet ordering, account-shared slot merging, the slot-0 character gold wallet, account cargo metadata, avatar optional blobs, personal-cargo trailer, equipment 0x77 rows, expiration offsets, equipment quality seeds, guardian-gem socket vectors, pet list-7 serial placement, and authoritative zero clearing at raw+0x0E/raw+0x38.
- Audited the time boundary. The two remaining `time.Now` reads only calculate stackable remaining seconds during synchronous row projection; there is no delayed callback or background scheduler in this infrastructure. Boundary tests reject direct repository writes and private `After`, `Timer`, or `Ticker` use so any future delayed work must use the unified gameplay time queue.
- The 11 production bridge components are now bounded to 229 lines or fewer: send orchestration 229, session snapshot 200, equipment projection 159, row helpers 156, stack projection 137, account projection 111, equipment snapshot 109, repository reads 88, protocol 77, container adapter 60, and types 32. The domain owner is 101 lines.
- Added domain tests for validated missing-state creation, existing-state preservation, wrong-scope rejection, and invalid-value rejection. Bridge boundary tests keep the old mixed file absent, pin key functions to their owned files, require `ContainerStateOwner` delegation, and reject direct writes or standalone timers.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `49556` and DNF90Server PID `15932` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `FD20121B3032E8E46DD004C4C8B40CDCDBC5F38A74022EA25356AF100CB7021A`.

## 2026-07-26 - split and descended shared item-expiration infrastructure

- Removed the mixed 631-line `dnfbridge/item_expiration.go`. The shared infrastructure is now separated into PVF catalog loading, grant/deadline rules, durable-stack projection, domain-owner orchestration, and current-client wire projection; it is deliberately not registered as a fake gameplay because several real gameplays consume the same expiration rules.
- Added `internal/modules/dnf/itemexpiration.Owner` as the durable reconciliation boundary. Character inventory, personal warehouse, account-shared inventory, and worn equipment expiration repairs now commit or roll back together through the existing account-character-assets transaction instead of performing three independent saves from the bridge.
- Preserved current-client compatibility: relative PVF `[usable period]` is resolved once at grant time, nested rewards can inherit a live source deadline, per-instance deadlines override expired static event dates, the 0x77 expiration timestamp remains at the proven offset, model 26..29 remaining seconds stay capped to `0xFFFF`, and only the superseded `runtime_pvf` tail projection is cleaned.
- Audited the timing boundary. Item expiration contains no background callback, private timer, or scheduler: grant, reconciliation, and packet projection synchronously evaluate an injected/current timestamp, while the client consumes the projected remaining-seconds field. No artificial queue task was added; a bridge boundary regression rejects future `After`, `Timer`, or `Ticker` use so any real delayed work must use the unified gameplay time queue.
- Production components are now bounded to a 242-line durable/raw projection, 181-line domain owner, 124-line bridge owner adapter, 115-line grant/deadline rules, 85-line wire projection, and 65-line catalog loader.
- Added domain tests for one-transaction four-container commit, projector rollback, and cross-account character rejection inside the transaction. Bridge boundary tests keep the old mixed file absent, pin the key functions to their owned files, require `itemexpiration.Owner` delegation, and reject direct repository writes or standalone timers.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `4376` and DNF90Server PID `42508` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `8B971C69BB4D3309035B22D3880657C45F15E58055DB3D505FA9F31417DA6346`.

## 2026-07-26 - split and descended guardian-gem gameplay

- Removed the mixed 737-line `dnfbridge/guild_medal.go`. The already independently registered op829 `guardian-gem` gameplay now owns its handler and commit adapter, while request grammar, PVF validation, raw socket projection, and current-client refresh/replay are separate files.
- Added `internal/modules/dnf/guardiangem.Owner` as the durable mutation boundary. Exact list-38 source consumption and worn-medal raw socket changes now commit atomically through the character-items transaction; no guardian-gem bridge file directly opens a repository transaction or saves inventory/equipment aggregates.
- Preserved the exact current 11-byte request, list-38 guardian-gem page range, runtime-PVF `[flag gem]` and `[flag]` authority, grade/enchant-family validation, actor slot 32 targeting, 0x77 offsets 101..108, occupied-socket rejection, adjacent raw byte preservation, exact source-slot consumption, and the deliberate absence of a fabricated op829 ACK.
- Preserved client refresh ordering: the committed worn list-3 op14 row is followed by the authoritative list-38 op13 rebuild, and selected-character rehydration still replays persisted raw socket words after mode1/mode3 actor creation.
- Reduced production components to a 195-line raw projection, 158-line gameplay entry, 144-line PVF resolver, 140-line refresh adapter, 137-line protocol, 134-line domain owner, and 28-line owner adapter.
- Added owner tests for atomic inventory/equipment commit, projector rollback, and missing-inventory rejection. Bridge boundary tests keep `guild_medal.go` absent, pin every function to its owned file, require `guardiangem.Owner` delegation, and reject direct repository writes or standalone timers.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `8756` and DNF90Server PID `17332` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `9A3059D8A0826D2897A86205D28E7C4B8656C360A8E36D0D80CDB76D4174CEC1`.

## 2026-07-26 - split and descended scene skill projection

- Descended all scene-skill repair writes into `internal/modules/dnf/skill.SceneOwner`. Missing skill-record backfill, SP/TP ledger synchronization, and missing quick-layout persistence now use the existing character-skill transaction without any direct transaction or `SaveSkillFields` call in `dnfbridge`.
- Kept op504 skill-layout change, op505 skill purchase, and op506 skill reset owned by the existing independent `skill` gameplay module. `scene_skill_info` remains that gameplay's login/reselect/final-scene projection rather than registering a duplicate gameplay boundary.
- Split the former 412-line `scene_skill_info.go` into an 88-line scene-send orchestrator, 147-line mutation adapter, 88-line protobuf protocol builder, and 17-line domain-owner adapter. Current-client transport, protobuf field layout, two-tree projection, logging, and send ordering remain in the bridge.
- Preserved runtime-PVF initial skill authority, per-job initial records, cooldown carry-forward, already-learned skill preservation, spent-SP/TP accounting, level-synchronized totals, initial/current quick-layout selection, concurrent existing-layout preservation, direct persisted skill ranks, and the current class0/op19 body.
- Added domain tests for initial backfill with cooldown preservation, SP/TP repair, one-time layout persistence, concurrent existing-layout preservation, and builder rollback. Bridge boundary tests pin function ownership, require `SceneOwner` delegation, and reject direct repository writes or standalone timers in all scene-skill files.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `47428` and DNF90Server PID `37556` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `A9AC1BDA585637F9AEAA818ACC19CE97E1434F72D2C5B5E73C74B98C6B56AD0E`.

## 2026-07-26 - split and descended equipment-rental gameplay

- Removed the 403-line `dnfbridge/rental_asset.go`. Rental-point charging, equipment rental, and expired-rental cleanup now delegate through `internal/modules/dnf/rental.Owner`; no rental bridge file directly opens the four-aggregate transaction or saves account, character, inventory, or equipment rows.
- Moved account/character authorization, rental-point parsing and limits, gold charging, point debits, and atomic commit/rollback into the domain owner. PVF group/tier/job validation, current-client raw equipment projection, result codes, and packet order remain in `dnfbridge`.
- Split the former 492-line `rental_protocol.go`: op999/op1000 handlers now live in the independently registered rental gameplay file, request/response bodies remain in a 74-line protocol file, login/scene rental state is isolated in `rental_state.go`, and the shared op14 item-update builder moved to `current_item_update_protocol.go`.
- Preserved current-EXE 21-byte request bodies, packed job/tier validation, over-equip contract level behavior, PVF point costs and item authority, 24-hour rental refresh, permanent same-item isolation, equipped-item refresh, inventory-full result code, active-rental state rows, and op999/op1000 response ordering.
- Production rental components are now bounded to the 363-line domain owner, 289-line PVF catalog, 237-line raw projection, 219-line state adapter, 215-line gameplay entry, and 74-line protocol file. Boundary tests keep `rental_asset.go` absent, pin function ownership, require Owner delegation, and reject direct repository writes or standalone gameplay timers.
- Added domain tests for point/gold charging, projected inventory/equipment commit, projector rollback, cross-account rejection, and cleanup persistence. Existing bridge coverage continues to verify atomic rental behavior and permanent-equipment isolation.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted with the existing MySQL data preserved. Portable MySQL PID `13964` and DNF90Server PID `25200` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `A35B0C836223B4C4B3A7A8AA7F1C04811C439439B7CFCC1E79634FC5E4407F35`.

## 2026-07-26 - split and descended NPC-shop gameplay

- Removed the mixed 830-line `dnfbridge/npc_shop.go`. The independently registered `npc-shop` gameplay now owns buy/sell request orchestration, while current-client compatibility code is split into protocol, PVF catalog/pricing, mutation projection, and domain-owner adapter files.
- Added `internal/modules/dnf/npcshop.Owner` as the durable boundary for gold purchases, material exchanges, and sales. Account/character authorization is repeated inside the existing account-character-assets transaction, and account warehouse, character wallet, and character inventory changes commit or roll back together.
- Closed the material-exchange authorization gap where character ownership was previously checked before the account-items transaction. All three mutation paths now use one transaction shape, and no NPC-shop bridge file directly opens a repository transaction or saves an aggregate.
- Preserved op21/op22 current-EXE request layouts and ACK bodies, PVF shop listing and pricing authority, equipment sell rates, stack limits, equipment locks, gold overflow rules, material exchange costs, fixed cube-fragment warehouse cells, backpack overflow, raw item rows, and incremental op14 update ordering.
- Reduced the largest production NPC-shop bridge component to the 368-line mutation projection; PVF rules are 211 lines, protocol is 135 lines, gameplay entry is 117 lines, and the old mixed file is guarded absent.
- Added owner tests for multi-aggregate commit, projection rollback, and cross-account rejection. Bridge boundary tests pin each function to its owned file, require `npcshop.Owner` delegation, and reject direct repository writes or standalone gameplay timers.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online runtime preflight.
- Rebuilt and restarted without clearing MySQL data. Portable MySQL PID `17564` stayed running and DNF90Server PID `12140` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `B458BFEEC72FF748017E6B5FABE630DDDF94B6F2616199DB0AAF85C241D9937F`.

## 2026-07-26 - split and descended booster-item gameplay

- Removed the mixed 1,102-line `dnfbridge/booster_item.go`. The independently registered booster gameplay now owns its handler and commit adapter, while shared current-client code is split into protocol, read-only owner/preflight, PVF/random rules, and reward projection files.
- Added `internal/modules/dnf/booster.Owner` as the durable mutation boundary. Character/account authorization, account-shared crystal/soul storage, and character inventory now commit through the existing account-character-assets transaction; no booster bridge file directly opens a repository transaction or saves an aggregate.
- Reduced the largest booster bridge component to the 435-line PVF rule file. The gameplay implementation is 231 lines, protocol is 215 lines, projection is 182 lines, and read-only owner/preflight assembly is 97 lines.
- Preserved current op160 random, single-selection, and multi-selection request layouts; runtime-PVF categories and weighted rolls; explicit and magic-box material consumption; instance/static expiration checks and nested deadline inheritance; main/avatar/pet/pet-artifact/pet-consumable placement; fixed account warehouse rewards; ACK bytes; and committed list-refresh ordering.
- Added owner tests for atomic account/character inventory commits, rollback, and cross-account rejection. Bridge boundary tests keep the old mixed file absent, pin the handler/commit/protocol/PVF/projection functions to their owned files, require domain-owner delegation, and reject direct repository writes or standalone gameplay timers.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online preflight.
- Rebuilt and restarted without clearing MySQL data. Portable MySQL PID `17564` and DNF90Server PID `25808` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only its normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `515760A624C5AC1403458E758CD8FFB44E0B09929B5F49EC093D0D5845DEF16E`.

## 2026-07-26 - split and descended disjoint/compound gameplays

- Removed the mixed 1,192-line `dnfbridge/disjoint_compound.go`. Equipment disjoint, avatar disjoint, and emblem compound now keep their request handlers and commit adapters in their three independently registered gameplay files.
- Added `internal/modules/dnf/disjoint.Owner` as the durable boundary for all three gameplays. Character/account ownership validation, account crystal/soul storage, and character inventory now run inside the existing account-character-assets transaction instead of performing the character ownership read before the account-items transaction.
- Split shared compatibility code into protocol, domain-owner adapter, reward projection, and PVF/random-rule files. The largest remaining shared component is the 541-line PVF rule reader rather than a multi-gameplay transaction file, and no disjoint/compound bridge file directly opens a repository transaction or saves an aggregate.
- Preserved current op26 equipment-disjoint, op202 avatar-disjoint, and op256 emblem-compound behavior: runtime PVF authority, equipment/avatar classification, fixed crystal/soul warehouse slots, backpack overflow, locked/expired source rejection, repeated emblem-stack inputs, real booster rolls, popup bodies, and op14 -> op256 ACK -> full main-inventory refresh ordering.
- Added owner tests for atomic account/character inventory commits, rollback, cross-account rejection, and all three domain entrypoints. Bridge boundary tests keep the old mixed file absent, pin every handler and commit adapter to its gameplay file, require domain-owner delegation, and reject direct repository writes.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online preflight.
- Rebuilt and restarted without clearing MySQL data. Portable MySQL PID `9844` and DNF90Server PID `9976` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only its normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `7408F6845BF5BE5845A40CAC7331023024F07EE9A27B0E1DB2CCCE3567E1C44D`.

## 2026-07-26 - split and descended socket/emblem gameplays

- Removed the mixed 1,483-line `dnfbridge/inventory_socket_emblem.go`. Equipment socket opening, equipment-emblem control, avatar socket opening, and avatar-emblem attachment now keep their handlers and mutation adapters in their four independently registered gameplay files.
- Added `internal/modules/dnf/socketemblem.Owner` as the single durable boundary for the four gameplays. Inventory and worn-equipment mutations now run through the existing character-items unit of work and commit atomically; `dnfbridge` retains current-EXE packet layouts, PVF interpretation, raw-row projection, session logging, and refresh order.
- Split shared compatibility code into explicit protocol, PVF, projection, owner-adapter, and refresh files. The largest remaining shared file is the 687-line raw projection component rather than a multi-gameplay transaction file.
- Preserved op914 equipment-opening behavior, op206 avatar opening, op201 avatar emblem attachment, and the evidence-backed no-state op796 control ACK. Existing client-key fallback, worn-slot compatibility, socket type checks, material/emblem consumption, raw vectors, optional avatar blobs, ACK bodies, and visible slot refresh ordering remain covered.
- Added owner tests for atomic inventory/equipment commits, rollback on projector failure, and all four domain entrypoints. Bridge boundary tests keep the old mixed file absent, pin every handler/commit adapter to its gameplay file, require delegation to the domain owner, and reject direct repository writes from the socket/emblem bridge files.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/online preflight.
- Rebuilt and restarted without clearing MySQL data. Portable MySQL PID `28540` and DNF90Server PID `43588` reached READY with all 30 managed listeners accepting connections; fresh server stderr is empty, and MySQL stderr contains only its normal startup, deprecated-option, and self-signed-CA notices before `ready for connections`. Production server SHA256 is `0509A3475369441D82E538E5C3008A83984F50887B0A4F03D3D2B3B506392A77`.

## 2026-07-26 - descended and split Cera-shop checkout

- Removed the mixed 1,845-line `dnfbridge/cera_shop.go`. Static PVF product resolution now lives in `cera_shop_catalog.go`, checkout orchestration in `cera_shop_purchase.go`, current-client inventory projection in `cera_shop_grant.go`, and response bodies in `cera_shop_protocol.go`.
- Added `internal/modules/dnf/cerashop.Owner` and a dedicated repository `CeraShopAssetUnitOfWork`. Cera debit, character state, inventory, worn equipment, and the exact character container-settings scope now commit through one MySQL transaction instead of saving Settings first and compensating after a rejected settlement.
- Moved name-tag card character/equipment persistence into the same checkout transaction. The bridge still resolves PVF duration and builds the current-client raw equipment entry, while the owner commits that projection together with the account wallet; the old post-checkout direct-save function was removed.
- Preserved the existing PVF authority and behavior for ordinary products, packages, avatars, avatar coupons, pets, pet consumables, one-plus-one bonuses, main/personal/account cargo upgrades, devil contracts, premium packages, expiration projection, ACK order, item updates, and balance refresh.
- Added owner tests covering full multi-aggregate commit, insufficient-Cera rejection, and projection rollback; repository coverage rejects cross-character Settings scopes; a bridge source-boundary test keeps the old mixed file absent, pins implementation functions to their owned files, and bans direct repository writes from every Cera-shop/name-tag adapter.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, and the requested `go vet` set.
- Rebuilt and restarted without clearing MySQL data. Portable MySQL PID `8952` and DNF90Server PID `35560` reached READY with all 30 managed listeners accepting connections; the fresh server and MySQL stderr logs are empty. Production server SHA256 is `67ECA5ACC88CCF810656431A0D41CFB12A5E9046852E5CE8B28DCF0BEA63514F`.

## 2026-07-26 - descended and split dungeon runtime gameplay boundaries

- Removed the mixed 1,493-line `dnfbridge/dungeon_runtime.go`. Dungeon entry and room movement now live in `dungeon_navigation_runtime.go`, monster-death combat progression lives in `dungeon_combat_runtime.go`, and session/PVF/repository-read assembly remains centralized in `dungeon_runtime_state.go`.
- Added `internal/modules/dnf/dungeon/runtime_policy.go` as the reusable pure-rule owner for PVF entry eligibility, fatigue/party/level enforcement, selected-character runtime ownership, and idempotent current-client op16 request identity.
- Kept transport parsing, current-client packet order, session locks, runtime room state, PVF catalog access, and packet emission in `dnfbridge`. Movement legality continues to use the existing `worldmap` state machine, durable mutations continue through dungeon owners, and delayed death/tutorial work remains on the unified gameplay time queue.
- Added domain tests for entry acceptance/rejection, no-fatigue rules, party limits, request replay identity, and character ownership. A bridge source-boundary regression requires the old mixed file to stay absent, pins navigation/combat handlers to their owned files, and requires bridge adapters to delegate the descended rules.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, and the requested `go vet` set.
- Rebuilt and restarted without clearing MySQL data. Portable MySQL PID `29124` and DNF90Server PID `6508` reached READY with all 30 managed listeners accepting connections; the fresh server and MySQL stderr logs are empty. Production server SHA256 is `3A7177CB50D9640BDAD25464D5A367BC686E7AA04ABAF395253F451375BA9796`.

## 2026-07-26 - split dungeon dispatch into independent gameplay modules

- Replaced the single dungeon gameplay registration file with five independently registered boundaries: `dungeon-navigation`, `dungeon-combat-drop`, `dungeon-death-revive`, `dungeon-settlement-card`, and `dungeon-tutorial-story`.
- Preserved all 20 existing dungeon opcodes across legacy and upper transports. Each module still calls the original bridge handler, so packet parsing, classification guards, aligned-command fallback, current-client compatibility, session state, packet order, and the unified gameplay time queue remain unchanged.
- Moved the shared default-class handler into the gameplay registry core because it is used by multiple gameplay modules; no protocol behavior moved into the registry.
- Expanded registry regression coverage to assert every dungeon legacy handler, upper handler, and legacy normalizer has the exact intended module owner. Duplicate module names and duplicate transport/opcode ownership continue to fail closed.
- Validation passed `gofmt`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, and the requested `go vet` set.
- Rebuilt and restarted without clearing MySQL data. Portable MySQL PID `42164` and DNF90Server PID `46172` reached READY with all 30 managed listeners accepting connections; the fresh server and MySQL stderr logs are empty. Production server SHA256 is `7828E41EF7331F1D6F4747BE0AE56F9522ED1F59908BE19E4671AEFB5607019F`.

## 2026-07-26 - completed dungeon repository-boundary descent

- Extended `internal/modules/dnf/dungeon.Owner` with separate pickup, tutorial-reward, revive-coin, settlement, and mutation owner files. The domain now owns dungeon inventory placement, character/account inventory transactions, tutorial grant idempotency, tutorial next-login completion state, lucky-star state, card rewards, gold pickup, revive-coin consumption, and settlement progression/receipts.
- Moved ordinary dungeon item pickup into the character-items transaction while retaining PVF resolution, equipment-quality generation, expiration raw projection, and op14 construction in `dnfbridge`. Quick-slot preference, PVF slot ranges, stack limits, merge overflow checks, and full-bag rejection are now reusable dungeon-domain rules.
- Moved tutorial reward delivery into one character-assets transaction. The progress marker and all item grants commit or roll back together, repeated op143 requests remain idempotent, and current-client expiration projection stays behind a typed callback.
- Replaced revive coin's old read-clone-then-save flow with a character-items transaction that rereads authoritative inventory, chooses the lowest valid main-list slot, preserves the permanent wallet slot at zero, removes depleted ordinary stacks, and commits the projected raw row atomically before the death timer is cancelled.
- Routed quest monster-drop inventory commits and discard/restore account-or-character inventory commits through dungeon owners. Discard still validates runtime/PVF tradeability, binding, lock, expiration, scene object state, and current-client item rows in the bridge; the owner validates account/character ownership and provides the atomic repository boundary.
- Tutorial completion persistence now delegates to the owner while keeping the proven tutorial handshake and runtime scene gates in the bridge. A dynamic regression scans every production `dungeon*.go` bridge file and fails if any direct `Within*`, `Save*`, or repository `.Save` write returns.
- Added domain regression coverage for pickup placement and persistence, tutorial grant idempotency, revive-wallet consumption, account/character ownership checks, tutorial completion replay, card rollback, settlement replay, lucky-star cap, and gold pickup.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production build, and BAT-only offline/runtime preflight.
- Rebuilt and restarted without clearing MySQL data. Portable MySQL PID `9560` and DNF90Server PID `9832` reached READY with all 30 managed listeners accepting connections; fresh server/MySQL stderr logs are empty. Production server SHA256 is `99D3101875211145DE75DD51E7C4691539B2C706DFE906B418A235A88ADE45D1`.

## 2026-07-26 - op470/mode0 compatibility fixes and dungeon owner descent

- Surgically synced the two supplied current-client fixes into the modular bridge instead of replacing newer dispatcher snapshots. Personal teleport op470 is now owned by the town gameplay module in both legacy and upper dispatch maps, parses the exact 15-byte body, derives the source town from the selected character's persisted location, converts the request to the existing op36 area transition, and reuses the proven same-town/cross-town PVF route. A successful transition emits op24 and deliberately sends no invented op470 ACK.
- Mode0 actor appearance now projects weapon reinforcement only for worn weapon slot 12. The current-EXE packed byte is clamped to `0x7f` and shifted left by one for `sub_20026C0`; coat/title and other non-weapon rows remain zero. Regression tests cover reinforced, absent, bounded, and non-weapon cases.
- Added `internal/modules/dnf/dungeon.Owner` as the durable mutation boundary for complex dungeon flows. Card gold plus inventory rewards remain one character-assets transaction and roll back together on a full bag; ground-gold pickup uses the same owner; suitable-dungeon lucky stars are account-owned with the existing 999 cap; settlement experience, level, SP/TP, and the completion receipt now commit through one character-settlement transaction with replay idempotency.
- Kept current-client raw-entry expiration projection, PVF/runtime state, packet parsing, session ownership, logging, notifications, and op34 -> op37 -> op35 ordering in `dnfbridge`. A source-boundary test prevents the migrated bridge adapters from regaining direct repository writes.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` suites, the requested `go vet` set, the production server build, and BAT-only offline/runtime preflight.
- Rebuilt and restarted through the BAT-only controller without clearing database data. Portable MySQL PID `43000` and DNF90Server PID `19440` reached READY with all 30 managed listeners accepting connections; fresh server/MySQL stderr logs are empty. Production server SHA256 is `022ACB8823920B11643890105A584D20FCC9662C1773CAAA4D1482D7CF4DD168`.

## 2026-07-26 - opt-in 90CN debugger compatibility module

- Added an explicit, default-off `90CN.dll -> 90CN-debug.dll` loader. The normal patch worker reads game-root `90CN.ini`; only `[debug] enabled=1` loads the absolute game-root DLL and calls the versioned `Install90CNDebugCompat` export. Loading is never performed inside `DllMain`, and missing/disabled/rejected modules do not block the normal client patch.
- Added a separate Win32 `90CN-debug.vcxproj`. The module validates each current x86 `ntdll` entry as an unmodified `MOV EAX, imm32` syscall stub before installing a five-byte trampoline. All four entries are prepared first, other process threads are suspended only for the bounded write transaction, and partial writes are rolled back before threads resume.
- The initial compatibility scope is limited to current-process `NtQueryInformationProcess` debug port/object/flags, `NtSetInformationThread(ThreadHideFromDebugger)`, `NtQuerySystemInformation(SystemKernelDebuggerInformation)`, and debug-register values returned to in-process callers by `NtGetContextThread`. A low-frequency worker clears only `PEB.BeingDebugged` and the `0x70` debug-heap bits in `NtGlobalFlag`.
- Explicitly excluded debugger-process enumeration hiding, `NtSetContextThread` mutation, kernel drivers, TenSDK byte patches, the TerSafe allow-all stub, game protocol changes, packet changes, and database/PVF/server behavior. The debug module is not loaded when `90CN.ini` is absent or disabled.
- Added an isolated x86 smoke executable. Three consecutive runs passed pseudo-handle and real-handle process queries, `ThreadHideFromDebugger`, kernel-debugger state, simulated PEB attach flags, and a suspended thread with real `DR0/DR7` values whose in-process query was sanitized without intercepting `SetThreadContext`.
- Clean `Release|Win32` builds passed for `90CN.vcxproj`, `90CN-debug.vcxproj`, and `90CN-debug-smoke.vcxproj`. Build intermediate directories are isolated. The final production loader SHA256 is `226F257FD30263DD7C4EC4E5DE9A4526394CA9F52F5E4DA98B3A59B846A483AC`; the optional debug module SHA256 is `F2DC7FE294638DB4842D0F990AAB2A761E598D4B8F63024CF13E2E5E91AC7A68`. The smoke executable passed again after the final loader build.
- Installed both DLLs into the configured client root, enabled the module explicitly through game-root `90CN.ini`, updated the strict client manifest, and confirmed a live client loaded all four native query hooks plus the PEB scrubber before the normal 90CN compatibility hooks. A narrow log-format correction removed wide-string paths from the narrow trace logger, then the corrected loader was rebuilt and redeployed after closing the residual client/debugger processes. Server, database, PVF, generated runtime configuration, and IDB were not changed.
- Live validation completed against client PID `32336` with x32dbg attached after the debug module reported ready. A single fast-resume x86 hardware read/write breakpoint at the current process PEB `BeingDebugged` byte remained active for more than 30 seconds and reached `3076` hits; the client stayed responsive and protection error `4026` did not occur. The test hardware breakpoint was then removed, the client resumed normally, and x32dbg remained attached for continued debugging.

## 2026-07-24

- Created the independent `D:\DNF` project without modifying the original source tree.
- Extracted the exact DNF90 build/test dependency closure: 121 local package directories, 10.1 MB of Go source and embedded fixtures.
- Excluded unrelated services, old configs, compiled backups, logs, dumps, IDA/Codex artifacts, and temporary deployment copies.
- Split delivery into `go-server`, `deploy`, `runtime`, `docs`, and `releases`.
- Added a shared `internal/app/dnf90` composition root and compatible `dnf90`/`dnfbridge` entries.
- Added `DNFBRIDGE_GAME_LISTEN_HOST`; the local profile binds all game ports to `127.0.0.1`.
- Added a runtime doctor for config, PVF, channel catalog, single read/write database route, root MySQL access, schema state, and all listener ports.
- Added one-instance config generation. Runtime now has only three generated configs and uses one explicit `dnf_local` read/write database.
- Redis is disabled. The generated MySQL DSN uses the root account and a plaintext locally generated password, as requested.
- Added strict PVF/channel/client hashes and the `90cn-decode-bypass-v1` protocol profile.
- Added Windows install/start/stop/status/rebuild/client-launch flows and DB-only Docker Compose on loopback port 13306.
- Validation: the earlier temporary deployment scripts and JSON parsed successfully; no-database install/preflight passed; 29 channel game ports were derived and checked.
- Validation: `go list -buildvcs=false ./...` passed for 126 packages.
- Validation: all 39 packages under `internal/modules/dnf/...` passed tests.

## 2026-07-24 - BAT-only deployment control

- Replaced every Windows deployment entry with BAT plus the native `DNF90Control.exe`; removed all PowerShell files and calls.
- Added native start, stop, status, build, offline check, Docker MySQL, readiness, rollback, and client-launch workflows.
- The local profile now rejects non-loopback channel/admin/advertise/database addresses, non-root database users, protocol drift, nonzero initial game port, and a disabled client hook.
- Added per-installation Docker project isolation and process ownership checks using installation ID, PID, exact process creation time, executable path, and executable SHA256.
- Confirmed the 86JP one-click database is SQLite and cannot be reused as this MySQL repository's physical database.
- Final audit fixed startup rollback so PID state and database remain available whenever verified process termination cannot be confirmed.
- Final validation passed `go list` for 125 packages, the complete requested DNF/control/server test set, and `go vet` for all relevant packages.
- `REBUILD.bat` successfully rebuilt `DNF90Control.exe`, `DNF90Server.exe`, and `DNF90Doctor.exe`.
- BAT-only offline preflight passed with the configured Chinese-path client, strict client/PVF/channel hashes, one `dnf_local` read/write route, Redis disabled, and all 31 listener addresses available.
- Final delivery contains six BAT files, zero PowerShell files, zero PowerShell calls, exactly three generated service configs, and no compiled artifacts inside `go-server`.
- Full database-backed server startup was not run because Docker is not installed on this computer; the verified failure path exits nonzero without leaving a server PID/state.

## 2026-07-24 - portable MySQL open-box release supersedes Docker workflow

- Removed the Docker controller and Compose definition. The Windows local profile now uses the bundled official MySQL Community Server 8.4.10 Windows x64 archive directly; Redis, Docker, PowerShell scripts, network downloads, and a system-wide VC++ runtime installation are not required.
- Added SHA256/size manifests for the MySQL archive and three Microsoft x64 application-local runtime DLLs. Startup verifies both manifests, extracts MySQL under this installation, copies the verified DLLs next to `mysqld.exe`, and validates the server binary with `mysqld.exe --version`.
- First-start initialization now creates an installation ID, plaintext local root password, one loopback `dnf_local` read/write database, runtime configs, and schema state. The controller owns database identity with server UUID, port, data directory, process creation time, executable hash, PID file, and listener PID checks.
- Hardened portable database lifecycle with a cross-process controller lock, atomic state writes, external data ownership state, interrupted-initialization preservation, launch-configuration digests, validated SQL identity before privileged operations, saved active credentials for safe stop after config drift, and graceful SQL `SHUTDOWN`. MySQL is launched with `--no-monitor`, so one managed PID equals the PID file and port listener.
- Hardened multi-binary rebuilds with random temporary paths and a hash-verified transaction journal. A complete candidate set is committed after interruption; a partial set is rolled back to the exact previous hashes. Unknown files and paths outside `runtime/bin` are never guessed or removed.
- Fixed the Windows graceful-stop exit race observed by the extracted-package acceptance test. When the managed process exits between the handle state check and executable query, the controller now waits briefly for the already-open synchronized handle; it reports stopped only after that handle is signaled. A reused or protected live PID still fails closed.
- A real clean first start extracted and initialized MySQL, prepared the schema, opened all 31 listeners, and reached `DNF90 is READY`. A stop preserved data, a second start reused it, and the final `--no-monitor` run proved one `mysqld.exe` with matching state/PID-file/listener identity. Repeated start was idempotent and final stop left both services and all managed listeners stopped.
- Synced the latest six dungeon discard/pickup source and regression-test files from the original project, including the final scene-mode rollback at 09:45 CST. Exact source hashes match the original tree.
- Final validation passed `gofmt`, the complete requested control/server/doctor/bridge/DNF module test set, `go vet`, and BAT `REBUILD`. The rebuilt runtime contains `DNF90Control.exe`, `DNF90Server.exe`, and `DNF90Doctor.exe`.
- Release packaging is whitelist-only. It includes the verified EXEs, source, MySQL archive, application-local DLLs, PVF, channel catalog, templates, and docs; it excludes `.git`, local instance credentials, generated configs, MySQL server/data, roles, keys, logs, state, dumps, and previous/temp artifacts.

## 2026-07-24 - 90cn channel reconnect lifecycle

- Ported the validated 90cn channel-transfer lifecycle into `internal/services/dnfbridge`: native client op2/31 and op1/598 lifecycle bodies are recognized before block-codec decoding, op8 records a normal roster login, and op4 without a roster immediately rebuilds the selected actor in the connected target channel.
- The reconnect route now commits the target channel with class0/op1 before and after the repository-backed town route, preserves the character's persisted town/area/position without running the ordinary persisted-area need-quest mutator, carries owner `{0,targetChannel}` through mode0, mode1, and op9, keeps op1276 ACK-only, and performs the 598-byte display rebind exactly once.
- Added regression coverage for native decode bypass, roster guarding, exact immediate packet order, target owner bytes, unchanged durable location, ACK-only op1276, and one-shot mode1/op800/op9/op120 rebind.
- Validation: `gofmt` completed and `go test -buildvcs=false -count=1 ./internal/services/dnfbridge` passed.

## 2026-07-24 - exact 90cn channel identity, resources, and Patch closure

- Used clean reference commit `98dd92f8f4200a0d54a5eb9e50b5c19d2d188c8e` from `D:\BaiduNetdiskDownload\90cn\new_dfo_server` as the requested channel baseline. The raw PVF/catalog remains source server `1`; the downloaded online script, dynamic directory, and current-channel op1 advertise server `0`. Bootstrap remains channel `19`, while channel-update resident selection is channel `11`.
- Added cached online-script derivation, exact legacy startup/direct-refresh lifecycle behavior, ordinary initial-town and return-select `op24 -> final op1`, and immediate reconnect `pre-op1 -> target-owner town route -> post-op1`. Reconnect op1276 is ACK-only, the 598-byte actor-display rebind is one-shot, and its persisted town/area/position and NeedQuest state are read-only.
- Replaced all three bundled/runtime channel resources with the exact 90cn source file: `runtime/data/dnf/channel_info.etc`, `deploy/assets/channel_info.etc`, and bridge testdata are each `4930` bytes with SHA256 `DC43465023960328FD71786F23A6D7D633CBE73AEB6AEAEF00834FCE57A33624`. The asset manifest now locks that exact file, including source channel `201` type `23`; the old extra channel `38` resource row is gone.
- Precisely merged only the reference channel/scene diagnostic closure into the existing client Patch source at `D:\BaiduNetdiskDownload\23.4.15.0\地下城与勇士\Patch\90CN.cpp`; existing quest, socket, codec, anti-debug, TerSafe, and UI functions were retained. Source SHA256 is `7645FF1971681E580D1701026662ACBF46A72C68BDA5AE3795A2ED67EFC5D134`.
- Win32 Release compilation produced `Patch\Release\90CN.dll`, `361472` bytes, SHA256 `7EB808E9B7377FA021E60A1C1202B3FE802D40ECF6CF45E5765E7AF34FCE4FB2`. The current client-root `90CN.dll` has the exact same bytes, so `deploy/assets/client-compatibility.json` now locks that hash. Patch documentation records the default `10019 -> 10011` redirect and its two environment overrides.
- Final validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, the full `go test -buildvcs=false -count=1 ./...` repository suite, focused DNF/bridge/control tests, `go vet`, the native BAT rebuild, Patch Release/Win32 build, and `deploy\windows\control.bat check --skip-database`. The offline preflight resolved 28 exact-source channels and found all 30 managed listener ports available.
- Final runtime executable SHA256 values: control `EC3BBB73FB5B383718B90FE3E66C58D50E04362611085E192D70ACD707AF7AA0`, server `BED3A8D0CDDD0EB2E6DA07F526BB4F7EB81EF259C8DA5472BDB1D6EA309CB3C6`, doctor `5B36D90325199BC35775A85F56D97A1EC881955C9D6147BD36FB4366A9823979`.
- No DNF90 server, MySQL instance, or game client was started. No database row/schema, PVF, remote service, or production process was changed.
- Aligned the remaining 90cn town-entry boundary: cold completed-character entry and op7 -> op8 -> op4 return-select re-entry now send the current-channel class0/op1 immediately after typed op24. Channel reconnect keeps its existing explicit pre/post op1 pair and does not gain a third notice.
- Extended exact-order regression coverage for cold entry, return-select re-entry, and reconnect. Reconnect fixtures retain source `ServerID=1` while asserting advertised protobuf field 7 remains `0`; focused and full `dnfbridge` tests passed.

## 2026-07-24 - audited necessary client-module integration

- Audited `antidebug_bypass.cpp`, `block_minimize_all.cpp`, `tensdk_bypass.cpp`, and `tersafe_stub.cpp` from `C:\Users\Administrator\Downloads\新建文件夹` against the exact current `DNF.exe` (`82620416` bytes, SHA256 `8A7AF614769D0B47D0647B77C33BCE725F2F20258651C01FC6C22AC1DB44AA4D`) and the existing TCLS/DPROTO Patch paths.
- Removed the hand-merged TenSDK, TerSafe allow-all, global anti-debug, and broad Win32 export-hook implementations from `Patch\90CN.cpp`. TenSDK shared RVA `0x01AF7C40` with the required TCLS FetchLogin hook and its OnlineModeInit patch did not create the connection records checked by the caller. The TerSafe stub bypassed the bounded native fallback predicate. The global anti-debug hooks were unnecessary for normal play and used an unsafe trampoline/thread lifecycle.
- Kept only the required desktop-minimize behavior. `SendMessageW`, `ShowWindow`, and `CoCreateInstance` now use the existing main-executable IAT hook and require the exact audited return RVAs `0x01A02629`, `0x01A02528`, and `0x01A0227F`. The live mode-1 `WM_COMMAND/0x19F` branch was blocked; unrelated UI, window, and COM calls remain native.
- Removed `HookInterface.h` from the Patch compilation surface and enabled `/utf-8` for both Win32 configurations. Updated the Patch README with the evidence and explicit exclusions.
- A clean Release/Win32 rebuild produced `90CN.dll`, `339968` bytes, SHA256 `6D328E561399698DF8E24F9AC7825A2F8FFB73377E19C0BB86A97B6DC34C2657`. The client-root DLL matches exactly, and `deploy/assets/client-compatibility.json` now pins that hash.
- Live launch validation passed with client PID `17668`: all three IAT hooks installed, the exact SendMessageW branch was blocked, all six TCLS helpers installed, channel diagnostics installed, the compatibility DLL completed, and the process remained responsive. No anti-debug, TenSDK, or TerSafe-stub code/string/log event was present. MySQL PID `13048` and DNF90Server PID `36392` remained running and were not restarted; no database or schema data changed.
- This module cleanup did not resolve the separate channel-selection stall: the client continued reloading the 3974-byte channel script and did not invoke the runtime channel-connect proxy during this observation window. Continue that diagnosis independently; do not re-enable the rejected TenSDK patches as a workaround.

## 2026-07-24 - exact 90cn Patch DLL rebuild and client install

- Rebuilt the user-selected `D:\BaiduNetdiskDownload\90cn\Patch\90CN.vcxproj` from source SHA256 `A71C01AF99ADA444D2FD4BE303ACA740F999688C409A6A9362478344AC99C9C5` with Visual Studio 2019 Build Tools, `Release|Win32`, toolset v142. The rebuild completed successfully with only existing unsafe-CRT deprecation warnings.
- The project deliberately sets `TargetName=86JP`, so the fresh x86 output was `Patch\Release\86JP.dll`; the stale `Patch\Release\90CN.dll` was not used. The fresh output is `333824` bytes with SHA256 `364D920E1F5F9834820087AE6BFB82B67B0019712309643A7AE7A0248A63FC40`.
- Backed up the prior client-root `90CN.dll` (`347648` bytes, SHA256 `824F10E5E528673C30DB597CB1D05F5CB6396C42EC976B3EB7DD1DAEC682B561`) under `D:\BaiduNetdiskDownload\23.4.15.0\地下城与勇士\_codex_backups\20260724_181512_pre_90cn_patch_install`, then installed the fresh `86JP.dll` bytes as client-root `90CN.dll`.
- Updated `deploy/assets/client-compatibility.json` to pin the installed DLL hash. `LAUNCH_CLIENT.bat` accepted all four strict client hashes and started `DNF.exe` PID `17060`.
- Live evidence confirms this exact DLL executed: `cipher_passthrough_dll.log` contains fresh PID `17060` initialization/channel/dispatch entries, the client window remained responsive, and it connected to the local channel endpoint. MySQL PID `39676` and DNF90Server PID `27656` remained READY and were not restarted; no database or schema data changed.

## 2026-07-24 - client-owned DLL exact 90cn channel implementation

- Compared the 90cn reference channel block (`D:\BaiduNetdiskDownload\90cn\Patch\90CN.cpp`, lines 776-1107) with the client-owned Patch source (`D:\BaiduNetdiskDownload\23.4.15.0\地下城与勇士\Patch\90CN.cpp`, lines 1489-1820). All 332 lines are exactly equal and both normalized blocks have SHA256 `A22CD26BC2DF8D3C336E80829DB428569939B0DAEC0717809D62EE0CE8D4CCE1`; all 16 channel RVAs, resident/server identifiers, and bootstrap/game-port constants match.
- Rebuilt the client-owned `90CN.vcxproj` as `Release|Win32` and installed its output directly, without retaining a new backup as requested. `Patch\Release\90CN.dll` and the client-root `90CN.dll` are both `347648` bytes with SHA256 `8C65970389EDE742ECD02B6F614CFC7A6DE742E6A2D3E8530F41B83900F36ACE`; the strict client manifest now pins this hash.
- Preserved the current-EXE class-0/op-1 endpoint layout and its 24-byte account-state tail. IDA/current-client evidence shows that this tail is not channel-directory data; security-card flags at tail offsets 2 and 3 remain `1/1` to suppress the account-protection modal, while passpad/resting/Seria-lock flags remain zero.
- Rebuilt and restarted the BAT-only local runtime. MySQL PID `23348` and DNF90Server PID `42180` reached READY with 28 source channels and all 30 managed listeners accepting connections. Runtime server SHA256 is `FB7C2DBB8CCA6C9FEA073E3108964EF7D19236030A5B68236F7677CCA282CE83`.
- Live client PID `30060` loaded the exact installed DLL and remained responsive. Fresh `90CN_trace.log` evidence shows all six channel hooks installed, a `3974`-byte online script loaded successfully, the complete server-0 directory applied with each runtime channel returning success, and the native channel connect completing on port `10017`. TCP state confirmed PID `30060` connected to PID `42180` on `192.168.1.113:10017`.
- Validation passed `go test -buildvcs=false -count=1 ./internal/services/dnfbridge`, every package under `internal/modules/dnf/...`, and `go list -buildvcs=false ./...`.

## 2026-07-24 - current-client create and new-character start-game disconnect

- Fixed current-client character creation framing in `internal/modules/dnf/protocol/stream.go`: the short native op5 create body is now recognized dynamically instead of being forced through the incompatible legacy block-codec boundary. Regression coverage was added in `stream_test.go`.
- Live creation validation succeeded at 20:08:31: the client sent op5 with a 17-byte body, the server replied with the 11-byte op5 result and a refreshed op2 roster, and the new slot-1 character was persisted. Existing character and database data were retained.
- Diagnosed the subsequent start-game disconnect from current frontend protocol/trace evidence. The client sends GET_USERINFO op8 as the real 3-byte body `FF FF 02`; that path used the legacy dispatcher, rebuilt the roster, but failed to set `session.rosterRequested`. The following op4 was therefore misclassified as a channel reconnect and injected class0/op1 resident notices around op24. Current EXE completed actor creation and op24, then closed the game socket at that false lifecycle boundary.
- Updated the shared GET_USERINFO bootstrap in `internal/services/dnfbridge/game_bootstrap.go` to mark the roster request regardless of whether op8 arrived through the upper or legacy dispatcher. Added a regression test proving the real 3-byte op8 followed by op4 does not arm reconnect or emit class0/op1.
- Removed the ordinary cold-login/return-select class0/op1 after op24 from `initial_town_route.go` and its obsolete ordering assertions. The explicit pre/post class0/op1 pair remains only in the genuine channel-reconnect route.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, `go test -buildvcs=false -count=1 ./internal/modules/dnf/...`, and `go test -buildvcs=false -count=1 ./internal/services/dnfbridge`.
- Rebuilt and restarted through the BAT-only controller. Portable MySQL PID `34960` and DNF90Server PID `6876` reached READY; no database cleanup or schema/data reset was performed.
- Live client PID `42148` entered the new-character intro successfully. The fixed stream contains no class0/op1 around op24, continues through op16/op27/op28/op29/op37, exchanges tutorial op1516/op1518 traffic, and receives repeated op1276 heartbeats through at least 20:28:27. The client remained responsive with an established game connection to local port `10014`.
- Evidence screenshots: `runtime/logs/client-42148-after-start-fixed.png` and `runtime/logs/client-42148-tutorial-playable-fixed.png`.

## 2026-07-25 - fixed the exact 600-second Error2 watchdog

- Reproduced the channel-session failure exactly at 600 seconds and captured `runtime/logs/client-35768-channel13-after-600s.png`. At popup time the game TCP connection was still `Established`, op1276/op1516/op1518 traffic continued, and the backend had not disconnected the client; this proved the visible Error2 was a client watchdog rather than a transport timeout.
- Current-EXE analysis identified the timer at `owner+0x70` with duration `600000` ms. A class0/op1 resident notice makes the client send class1/op1 and arms that timer. The first S2C class1/op1 initializes the endpoint-ready gate at `[owner+0xB0]+0x90`; a later duplicate S2C class1/op1 returns through the already-ready branch before clearing the timer. The gate also owns UDP/NAT and periodic op1276 state, so forcing it back to zero is not a valid fix.
- Removed all post-initialization class0/op1 notices from cold town entry, return-select entry, and channel reconnect. Each game TCP now receives exactly one connection-level S2C class1/op1. Genuine channel reconnect op4 immediately executes the complete target-owner town route; stale native op1/590 and op1/598 probes are logged and ignored, with no delay and no reply. Removed the obsolete reconnect endpoint-wait/display-pending state.
- Strengthened bridge regression coverage to forbid op1 anywhere in an initialized town/reconnect route, require target owner bytes and exactly one reconnect mode1/op800/op9/op120 sequence, preserve persisted location and NeedQuest state, keep op1276 ACK-only, and verify stale 590/598 probes cannot emit a response.
- Validation passed `gofmt`, `git diff --check`, focused bridge/DNF/server tests, `go vet`, and the production server build. The BAT-only controller rebuilt and restarted the local runtime; portable MySQL PID `45784` and DNF90Server PID `24312` reached READY without changing database data.
- Live client PID `42900` connected to channel 21 at `04:47:48`, entered town at `04:55:47`, and remained responsive for 705 seconds. TCP `192.168.1.113:57421 -> 192.168.1.113:10021` stayed `Established`; the entire session contained one initial S2C op1, zero C2S op1, and zero class0/op1. op1276/op1516/op1518 continued after the former deadline, and the server had no runtime disconnect, read/write error, panic, or fatal.
- Passing screenshot: `runtime/logs/client-42900-after-fix-620s.png` shows the live town scene after the former 600-second deadline with no Error2 popup.

## 2026-07-25 - restored original non-channel behavior around the watchdog fix

- Created the recoverable checkpoint `stash@{0}: codex-before-original-baseline-channel-only-20260725` before restoration.
- Compared the current bridge implementation with the original source tree and removed later changes that leaked channel ownership into ordinary gameplay. Cold login, normal return-select, dungeon return/entry, pet/creature refresh, mode0/mode1/mode3/op9, and scene reset again use the original `context0`; only the explicit channel-reconnect route uses the target channel owner.
- Restored the original character-selector behavior: op679 is silent when no database-backed slot-extension state exists, and normal return-select again emits its original lightweight mode1 followed by the full equipment-bearing mode1.
- Restored the validated endpoint boundary: one class1/op1 at game TCP open, write-silent pre-bootstrap, no class0/op1 in town/reconnect routes, and stale client op1 bodies (including 590/598-byte probes) are logged and ignored. Reconnect remains isolated, reads persisted location without mutation, skips reconnect-only op124 and duplicate full mode1, and keeps op1276 ACK-only.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, the complete bridge and DNF module tests, control tests, and the requested `go vet` set.
- Rebuilt and restarted through the BAT-only controller. Portable MySQL PID `19576` and DNF90Server PID `43684` reached READY with all 28 source channels and 30 managed listeners available. Database data was preserved; no DLL, PVF, role, inventory, quest, or schema data was changed.
- Live role-selector, channel-switch, and post-600-second client validation remain the next checks.

## 2026-07-25 - extracted seven current-client protocol repairs

- Extracted the repair bundle from `C:\Users\Administrator\Downloads\协议修复` without replacing newer bridge snapshots wholesale. Current inventory delete/sort requests now accept the length-prefixed protobuf envelope used by the current EXE, enforce strict field and entry limits, and return one protobuf ACK for the complete operation.
- Added the three real Cera-shop main-inventory expansion stages. Each stage atomically advances both the main and personal capacity fields, consumes its exact PVF-backed upgrade item even when it has no ordinary bag row, refreshes list 0, and rolls back account and inventory state on failure.
- Added repository-backed typed creature reconciliation before equipment and list-7 moves. Pet inventory is enforced as three fixed segments (creatures `0-139`, artifacts `140-188`, consumables `189-238`); cross-segment moves are rejected and direct grants reconcile equipped creature state transactionally.
- Current mode1 and mode3 actor refreshes now carry the character's authoritative cumulative EXP in the current EXE `sub_2004AE0` field, so dungeon entry and repeated mode1 refreshes no longer reset the baseline used by the first monster EXP delta.
- Dungeon monster-death and boss-death ownership checks now accept an announced, non-hostile PVF APC retained by a previously visited room while still rejecting hostile, planned, defeated, stale, or unrelated object keys.
- Random-option armor lookup now derives the normalized armor slot and material and accepts both verified PVF directory orders, including all five armor slots for cloth, leather, light armor, heavy armor, and plate.
- Rental tier selection now honors an active over-equipment premium contract as an effective `level + 10`. After the scene actor and typed creature state are committed, the bridge forcibly replays the absolute op985 rental/lucky-star snapshot so it follows op105 and cannot retain a stale pre-actor counter.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` test suites, the requested `go vet` set, and the production server build. Real `Script.pvf` smoke tests passed for all three main-inventory stages, all 25 armor material/slot combinations, and the rental catalog.
- The initial BAT preflight attempt without database access stopped before listener checks because the already-running server image was still mapped by Windows as `DNF90Server.exe~` after the new binary was built. No process was stopped or restarted during that merge-validation phase.
- Deployment then verified the mapped image against the controller's recorded SHA256 and process creation time, restored its managed filename, and stopped it through the BAT controller. The controller preserved the database, then performed its required full portable-MySQL stop and transaction-safe rebuild; database data remained intact.
- Final offline preflight passed the strict client profile, PVF and channel assets, configuration, 28-channel catalog, and all 30 listener ports. BAT startup brought portable MySQL PID `17752` and DNF90Server PID `15032` to READY; the production server SHA256 is `F79D5BE183870A82B39C72163CB0FD6F28A19080C75C8ABC7F1D27688125729B`, MySQL/schema checks passed, and all 30 managed listeners accepted connections.

## 2026-07-25 - Cera-shop one-plus-one IPG bonus grants

- Fixed the missing 充满爱慕的信 purchase bonus. The runtime `etc/chn_cerashop_multibonus.etc` `[one plus one bonus item info ipg]` blocks (commodities 102925-102930 grant 电动小马达泰迪礼盒 10007368 plus 幸运魔锤 10007367 in matching counts; 104426/104427 grant 490701730) were never parsed, so checkout delivered only the base product.
- `newPVFCeraShopCatalog` now loads the optional multi-bonus document into `bonusByCommodity`; a missing document means no gifts, while a malformed row fails catalog load closed. Repeated flat `[bonus item]` blocks are walked through the document section list because `Document.Section` only returns the first match.
- `commitCurrentCeraShopPurchase` grants configured bonus items inside the same rental-assets transaction as the purchased product, resolving each bonus through the PVF item catalog with the same grant/expiration path as ordinary products. Bonus inventory rows keep the parent commodity in provenance and add `last_cera_shop_bonus=one_plus_one_ipg`.
- Coverage: parser unit tests (repeated blocks, missing document, malformed rows), a commit test proving product plus both bonus items land with one Cera debit, and a real `Script.pvf` smoke test locking the 102925/102929/102930 bonus rows.
- Validation passed `gofmt`, `go list -buildvcs=false ./...`, the full `internal/services/dnfbridge` and `internal/modules/dnf/...` and `cmd/server/control` test suites, the requested `go vet` set, and the production `DNF90Server.exe` build. Real-PVF smoke tests ran with `DNFBRIDGE_REAL_PVF_SMOKE` against the runtime `Script.pvf`. Deployment preflight was not rerun because the server was live; restart through the BAT controller is required to load the new binary.

## 2026-07-25 - enchant success popup preserves the target item

- Captured the current-client op272 enchant request for main-inventory target slot 10 and confirmed the target was `精心制作的武士刀` (`101010438`). The server persisted the original item ID and its `490702358` enchant-card field correctly.
- The successful response sent the required main-inventory op13 refresh and four-byte op272 ACK, but a generic post-action then sent a second full op13 refresh after the ACK. The current EXE success handler resolves the ACK slot to an item pointer before publishing the result-popup event; the second refresh destroyed and recreated those item objects, allowing the popup's stale pointer to render the unrelated `街舞达人` item from slot 3.
- Removed only the duplicate post-ACK selected-container refresh. The authoritative pre-ACK main-inventory refresh and exact op272 target-list/target-slot ACK remain unchanged, as do the transaction and stored enchant result.
- Updated the enchant regression to require exactly the explicit refresh followed by the ACK, with no pointer-invalidating post-action.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every package under `internal/modules/dnf/...`, the complete `internal/services/dnfbridge` and `cmd/server/control` test suites, and the requested `go vet` set. The BAT-only controller preserved database data, passed offline and online preflight, rebuilt the production server, and started portable MySQL PID `2244` plus DNF90Server PID `25916` to READY with all 30 managed listeners accepting connections. The deployed server SHA256 is `DD68C61FA05F3F664EA08653CC997D425B9ABE4923F61DF1DA73D4FE4C5A374D`.

## 2026-07-25 - random-box expand open (op1128) + managed rebuild correction

- Corrected the earlier multibonus deployment: the 13:27 manual `go build -o runtime/bin/DNF90Server.exe` was rolled back by the controller's hash-verified transaction journal at the next BAT start, so the 14:04 purchase still ran the old binary (packet capture proved the op14 update carried only the letters). Rebuilt through `REBUILD.bat` instead; the managed binary now contains the one-plus-one grants.
- Implemented `CmdPacketUseRandomboxItemExpand` (0x0468) end to end. The 15-byte C2S layout (u8 listType, u16 boxSlot, u32 boxItemID, u16 materialSlot, u32 materialItemID, u16 openCount) was captured live from the current NoPack client while opening 100 泰迪礼盒, and is locked by a decode test using the captured bytes.
- `packageitem` now shares one transaction core between single open (0x00D0) and expand open: request/box/material counts converge to the minimum, N weighted draws aggregate into one grant pass, boxes and per-open material are consumed atomically, and client-reported item ids are validated against repository stacks and the PVF resolution (mismatch fails closed with the family `{0x00}` ack). The expand ACK reuses the 86JP single-ack family layout with aggregated rewards; the registered NoPack 1128 S2C handler only closes the wait dialog.
- Registered the opcode in the aligned command table (package domain) and extended the bridge magic-box resolver gate to both opcodes.
- Enabled `packetLog` in `runtime/config/instance.json` to capture current-client evidence; purchase/open traffic now lands in `runtime/logs/packet_log.txt`.
- Validation passed `gofmt`, `go test` for `internal/modules/dnf/...` (packageitem expand decode/owner/handler tests included), the full `internal/services/dnfbridge` suite, and the requested `go vet` set. Rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-25 - purchase-result bonus display rows

- The op64 purchase-success ACK now appends the one-plus-one bonus rows (u16 count + u32 itemID/u32 count pairs) sourced from the runtime multibonus catalog, so the 购买结果 dialog can render the 商城购物奖励 panel alongside the purchased product. Row layout is marked pending live-client confirmation in code; delivery of the actual items remains owned by the op14 updates.
- Validation passed `gofmt`, full `internal/services/dnfbridge` and `packageitem` suites, real `Script.pvf` smoke tests, and the requested `go vet` set.

## 2026-07-25 - expand-open client crash root cause and 0x77 ack rows

- Live verification of op1128 succeeded server-side (opens=100, box and 100 hammers consumed, 200 瞬移药剂 + 100 赛丽亚的幸运 granted atomically) but the client crashed reading the ACK. The 90CN_trace.log exception (0xC0000005, stack frame `0x77` + return address 0x1D0585F) pinned the crash to current-EXE `sub_1D057B0`, which reads a u16 count followed by count × 0x77 full item entries; the initial expand ACK reused the 31-byte single-open summary rows, so the second 0x77 read ran past the packet buffer.
- The expand ACK now emits 0x77 wire entries (u16 slot, u32 itemID, u32 count, zero tail) with the real granted bag slots tracked per reward (`MagicBoxGrantedReward.Slot`). Single-open (0x00D0) keeps the proven 31-byte family rows.
- Validation passed `gofmt`, the full `packageitem` and `internal/services/dnfbridge` suites, and the requested `go vet` set.

## 2026-07-25 - expand ack real header: u16 openCount plus trailing second list

- The second live crash (same reader, same 0xC0000005) disproved the single-ack header reuse. Decompiling the current EXE S2C handler `sub_1D074C0` shows the real read order: u8 result, u8 clientType, u16 openCount (drives the client-side material check `perUse * openCount`), u16 boxSlot, u16 materialSlot, then reward list one (u16 count + count × 0x77 entries), one u16 field, and reward list two. The missing u16 openCount shifted every later field by two bytes, so the client read a garbage entry count and overran again.
- `buildMagicBoxExpandAck` now emits the full layout with openCount, the granted reward list, a zero u16, and an empty second list. Test updated to the exact byte layout.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites.

## 2026-07-25 - expand ack variant byte: lottery UI for material-less boxes

- Live verification showed the teddy-box expand grant lands, but the 赛丽亚的幸运 十连 drew the wrong client behavior: an error announcement and no lottery page. Decompiling the rest of current-EXE `sub_1D074C0` pinned the variant byte contract: variant 4 marks a material-less box (skips the client-side material deduction check that fired error 11031 against slot 0xffff) and, with a nonzero clientType, opens the 0x36B ten-draw flip UI fed by reward list one/two; variant 1 keeps the material-consuming path where a nonzero clientType shows reward list two via 0x1FC and a zero clientType shows list one via 0x145.
- `buildMagicBoxExpandAck` now emits variant 4 whenever the resolved box has no material requirement; rewards stay in list one and list two remains empty. Regression test locks the 赛丽亚 captured body (listType 4, slot 67, count 10) to variant 4 with the consumed stack.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, and the requested `go vet` set.

## 2026-07-25 - grade-adjust notification and reward-container routing

- Corrected the op81 equipment-grade success response. Current EXE `sub_1D16480` applies the nonzero u32 grade directly to the target item; the old zero value removed the target and the following op14 recreated it, which made the client announce the existing equipment as newly obtained. The response now carries the committed grade `1..5`, preserves the target object, and refreshes only the consumed adjustment-box slot.
- Routed booster rewards with fixed crystal/soul identities through the account-shared inventory in the same account+character transaction. The observed `490701716` artifact-soul bag reward `10099774 x5` now stacks in account soul slot `363` instead of a character consumable slot; overflow still falls back without reward loss.
- Extended runtime-PVF random-box reward metadata with its target list. `[creature]` equipment rewards now use pet list `7`, body slots `0..139`, retain creature metadata, and no longer enter the ordinary item bag.
- Added regression coverage for op81's nonzero in-place grade and material-only op14, account-shared artifact-soul grants, and creature rewards landing in pet inventory. Focused `packageitem` and complete `dnfbridge` tests passed before the full release validation.
- Repaired the already-misplaced local data during the stopped-server deployment transaction: `10099774 x9` moved from character slot `0:123` to account soul slot `0:363`, while creature eggs `2681470` and `63089` moved from `0:10/0:11` to pet slots `7:0/7:1`. Post-start verification confirmed all three old character slots are absent.
- Full validation passed `git diff --check`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the complete `dnfbridge` and control tests, the requested `go vet` set, and BAT offline/online preflight. The BAT controller preserved MySQL data, rebuilt the managed server, and started MySQL PID `46372` plus DNF90Server PID `48636`; all 30 listeners are accepting connections and the deployed server SHA256 is `0F6C26523FCA6C3A868096C96B9E50F764289AABEB105256B46E5CF945EC1AD7`.

## 2026-07-25 - permanent pet rows, pet-feed routing, and full equipment-quality seeds

- Corrected the op81 interpretation from the preceding deployment. Values `1..5` are not current-client grade enums; they are invalidly tiny quality seeds, which made every adjustment resolve near the bottom grade. Standard boxes now persist a cryptographically generated seed in `1..999999997`, while gold boxes persist the `999999998` top-quality sentinel.
- Current EXE `sub_1D16480` applies op81's nonzero u32 through the inventory object's stack mutation path, explaining the visible number added to the target after every reset. The success ACK now uses zero to discard that stale object and follows with a full class0/op13 main-container refresh. This reconstructs the committed equipment silently, avoids both phantom stacking and the incremental-op14 false “obtained equipment” announcement, and includes the consumed material state in the same refresh.
- Fixed list-7 row construction so a creature serial/handle stays only at raw `+0x06`; it is no longer mirrored into raw `+0x0E`, where the current client interpreted serial `3` as three days and serial `19` as nineteen hours. Permanent pet rows also authoritatively clear stale `+0x0E` and expiration fields, while explicit values and real expirations remain supported.
- Cera-package rewards now route PVF `[creature]` equipment to pet body slots `0..139` and `[feed]` stackables to pet-consumable slots `189..238`. Magic-box reward resolution applies the same `[feed]` rule. Real `Script.pvf` coverage confirms package `490702439` grants item `24 x300` as `[feed]`.
- During the stopped-server migration, the existing item `24 x300` was moved from character main slot `0:69` to the empty pet-consumable slot `7:189`. Equipment `16403` in `0:11`, left with invalid seed `4` by the preceding op81 implementation, was restored to its preserved pre-reset full seed `596592351`; its raw row and typed metadata now agree.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every `internal/modules/dnf/...` test, the complete `dnfbridge` and control suites, the requested `go vet` set, the production build, and real-PVF summer-package smoke coverage.
- The BAT controller preserved MySQL data, completed a managed rebuild plus offline/online preflight, and started portable MySQL PID `25096` with DNF90Server PID `38248`. All 30 managed listeners accept connections; the deployed server SHA256 is `1F97F5894D869C219E924DCBE75602B52393F691D0C2FDF853A65B407DED3082`. Post-start database verification confirmed old slot `0:69` is absent, `7:189` holds item `24 x300`, and equipment `0:11` retains seed `596592351`.

## 2026-07-25 - expand ack: force 0x145 result window for material-less boxes

- IDA tracing closed the right-click dispatch (`sub_2233FE0`): the current EXE opens a plain confirm dialog for 赛丽亚的幸运 ×10 and its button handler (`sub_2231D00`) sends op1128; the 875 flip window is legacy code with no live entry, so routing the expand ack into the 0x36B flip branch (variant=4 + nonzero clientType) displayed nothing at all.
- `buildMagicBoxExpandAck` now forces clientType=0 for material-less boxes, which the current-EXE S2C handler (`sub_1D074C0`) routes into the 0x145 result window fed by reward list one. The material-box path (variant=1) is unchanged. Test updated to lock the forced clientType.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-25 - expand ack routed to the on-demand 0x1FC result window

- Deeper IDA tracing corrected the window model: protobuf windows 0x145 and 0x36B are lookup-only (factory arg4=0xFFFF) with no live creation entry in this client build, which is why both earlier variants showed nothing or a stray dialog. The single-open result window family 0x1FC is created on demand (arg4=0) and is what the official two-pane ten-open result uses.
- `buildMagicBoxExpandAck` now always emits variant=1 + clientType=4 with rewards in list two, so the 1128 handler (`sub_1D074C0`) creates and fills the 0x1FC window. For material-less boxes the material slot field is backfilled with the box slot, letting the client-side perUse×openCount partial-consume check pass (the following op14 refresh re-aligns counts); the bogus 11031 error is gone by construction.
- Tests lock both header shapes (teddy with real material slot, seria with backfilled slot). Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-25 - Patch scene-route allow-list admits op1128

- Root cause of the missing ten-open result UI: the client patch's `IsSceneRouteOpcodeAllowed` (90CN.cpp) admitted op208 but not op1128, so the S2C expand result fell back to the native predicate and a generic announcement instead of the 0x1FC result window. The user called this twice; packet and IDA evidence confirm it.
- Added `case 1128` to the allow-list, rebuilt `Patch\Release\90CN.dll` with MSBuild 2019 (Win32 Release), and updated the locked hash in `deploy/assets/client-compatibility.json` to `9646829A24E670217493D17A15DB13DF58A790E890D3C52DFCDBFBE1377F45C4`.

## 2026-07-25 - grade-adjust result popup survives inventory synchronization

- Corrected the op81 packet order introduced by the full equipment-quality-seed repair. Sending the silent full op13 inventory refresh after the op81 success ACK immediately rebuilt the client item objects and dismissed the newly opened grade-adjust result popup.
- The bridge now sends the committed full main-inventory refresh before the op81 success ACK. The ACK uses slot `0xFFFF`, which the current EXE treats as a missing item and therefore skips both its zero-value delete path and nonzero stack-mutation path; result type `1/2` still opens the normal gold/standard grade result popup.
- Regression coverage locks the exact `op13 -> op81` order, the refreshed full quality seed and consumed material state, the no-mutation slot, zero amount, and standard result type. `gofmt`, `go list`, all DNF module tests, the complete bridge/control suites, and the requested `go vet` set passed.
- The BAT controller preserved database data, completed a managed rebuild plus offline/online preflight, and started portable MySQL PID `30044` with DNF90Server PID `16732`. All 30 managed listeners accept connections; the deployed server SHA256 is `E05B99D26A508C28B15C4140344C659ADCF34B65E15AF70E2F8735DD584A5563`.

## 2026-07-25 - Patch scene-route allow-list admits op81

- Live packet evidence from the failed retest showed the server committed quality seed `919533966`, sent the full op13 inventory refresh, and then sent the op81 success body, but the client trace contained no op81 dispatch. Current-EXE `sub_1D16480` is the registered handler that opens result message `1699/1700`; the installed Patch scene-route allow-list admitted neighboring op80/op94 but omitted op81, so the success packet never reached that handler.
- Added `case 81` to `IsSceneRouteOpcodeAllowed`, rebuilt `Patch\Release\90CN.dll` with Visual Studio 2019 Build Tools (`Release|Win32`, zero errors), and installed the exact output in the client root. The output and installed DLL are both `348672` bytes with SHA256 `921BDC3FD8D11BCF2238E732A7CC29F6C72335D4BB980EE55BC3DB53775FC56A`; the strict `90cn-decode-bypass-v1` client manifest now pins that hash.
- The BAT controller validated all four client files and launched DNF PID `17604`. The new DLL trace initialized successfully and completed the channel-directory/connect hooks. DNF90Server PID `17152` and portable MySQL PID `50144` remained READY with all 30 listeners accepting connections.

## 2026-07-25 - purchase-result main-product count field

- The purchase-result dialog rendered bonus rows but no quantities. The first trailing u32 after the commodity in the op64 success body is the dialog's main-product count; it was hardcoded zero. `buildCurrentCeraShopPurchaseSuccessBodyWithCount` now carries `product.Count` there (bonus rows already carry their own u32 count). Existing tests were moved to the counted builder with their fixture counts.
- Validation passed `gofmt`, full `internal/services/dnfbridge` and `packageitem` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-25 - revert op64 trailing-u32 count experiment (client hang)

- Writing the purchased quantity into the first trailing u32 of the op64 success body made the result dialog hang (client unclickable). Reverted to the proven zero shape. Note: the official purchase-result dialog also lists item names without visible xN counts, so the zero shape matches official rendering.
- Validation passed the full `internal/services/dnfbridge` suite; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-25 - reproducible source one-click release

- Added `PACKAGE_RELEASE.bat` and the source-backed `cmd/server/release` packager. It rebuilds `DNF90Control.exe`, `DNF90Doctor.exe`, and `DNF90Server.exe`, validates the pinned MySQL/VC++/PVF/channel/client-patch assets, copies only the explicit source/runtime whitelist, emits `SOURCE_MANIFEST.sha256`, and creates a self-contained ZIP under `releases`.
- Imported the maintained 90CN Patch project and its exact pinned DLL under `client-patch`. Added `INSTALL_CLIENT_PATCH.bat`; the commercial DNF client executables remain excluded.
- Established `D:\DNF\client-patch` as the canonical Patch source and release input. `REBUILD_CLIENT_PATCH.bat` rebuilds its `Release|Win32` project into `client-patch\bin\90CN.dll`; game-directory Patch files are no longer release inputs, and the game-root DLL remains only the installed runtime copy.
- Added a Chinese source-release tutorial covering publisher packaging, ZIP verification, first startup, the loopback database connection, client-patch installation, client-directory configuration, normal stop/start, backup/restore, source rebuilds, and common failures.
- Fixed the local release template and the existing managed MySQL instance to the user-selected plaintext root credential. The generated DSN was verified to contain the configured plaintext value, MySQL remains bound only to `127.0.0.1:13306`, and the existing character database was preserved.
- The release never embeds the current computer's generated `runtime/config`, generated TOML/MySQL configuration, MySQL server/data, roles, logs, PID/process state, backups, dumps, or debug artifacts. Dynamic game-channel advertisements remain automatically bound to the target computer's private LAN IPv4; no source-machine `192.168.x.x` address is written into the release.
- Acceptance extracted the generated archive into an independent empty directory, verified all manifest hashes, completed a real first-run MySQL extraction/initialization and `START.bat -> STATUS.bat -> STOP.bat` lifecycle, and confirmed the fresh loopback database credential and all 30 listeners. The original database-backed instance was then restored to READY without data reset.
- Validation passed `gofmt`, `go list`, every DNF module test, the complete bridge/control/release test suites, and the requested `go vet` set.

## 2026-07-25 - local runtime archive reset

- At the user's request, stopped the managed server and portable MySQL, then created the recoverable physical backup `runtime/backups/wipe-20260725-193805` containing the prior MySQL data directory, data ownership state, and matching instance configuration. The backup contains 413 database files totaling 226,284,421 bytes.
- Removed only the stopped runtime MySQL data directory and its ownership state, then allowed the BAT controller to initialize a fresh `dnf_local` database. Source, binaries, PVF, client patch, release archives, and generated instance identity were retained.
- The clean bootstrap recreated only the required fixed-account shell and empty account-cargo state. Exact post-start counts are zero for characters, account character slots, character inventories, character items, equipped entries, and character skills.
- Portable MySQL PID `2196` and DNF90Server PID `38484` reached READY with all 30 listeners accepting connections.

## 2026-07-25 - empty-selector handshake and first-character crash

- Repaired the exact empty-roster selector lifecycle. A mode-2 roster with zero entries now arms only the current EXE's immediate class1/op679 `FFFF` slot probe, and that probe receives the required two-byte failure envelope. Ordinary op679 behavior remains unchanged. This restores the selector-ready flag so the create-character button opens reliably after a clean archive reset.
- Diagnosed the current post-create Start Game crash from the current-client exception, dispatch, and plaintext protocol traces. Full mode3 returned normally; the next S2C packet was class0/op3 body `01 02 00 01`, followed immediately by `0xC0000005` at `E44ED6` with a nil room-object-manager pointer.
- Current-EXE `sub_1D88A10` parses op3 as `count, u16 object key, u8 state`. A nonzero state immediately resolves the active room object manager and calls `sub_25B69E0`; current `sub_1D41DA0` creates and places that actor only while handling class0/op120. The bridge had sent op3 before op120, so the manager was not initialized.
- Moved the owned-dungeon op3 state transition after op9 actor display and op120 placement. Added exact-order regression coverage requiring op3 to follow op120. Removed the disproven temporary character-ID floor so allocation again follows the repository's real next ID.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, the requested `go vet` set, the production build, and BAT offline/online preflight.
- Per the user's retest request, stopped both managed processes and moved the current database plus ownership state into the recoverable backup `runtime/backups/wipe-20260725-203010`. A fresh database then initialized with zero characters, inventories, equipment rows, pets, quests, skills, and dungeon permissions. Portable MySQL PID `28644` and DNF90Server PID `21572` are READY with all 30 listeners accepting connections; deployed server SHA256 is `F41C3F0E473C988032093DCB0154BDA183BD38587631E0AD4B55B2F980A8F14C`.
- After the current crash-order correction, the focused scene/create tests, full DNF module and bridge/control suites, `go list`, `go vet`, production build, and offline/online BAT preflight all passed again. The exact-order scene test now rejects any op3 sent before op120.
- Deployed the corrected server without clearing or replacing the current database. Portable MySQL PID `7288` and DNF90Server PID `26692` reached READY with all 30 listeners accepting connections; the deployed server SHA256 is `12EB1A72EB68565270C5C7AB56A700AA249F9B4256DE1CDA50EC21CC9F198472`.

## 2026-07-25 - current-client Chinese character-name decoding

- Diagnosed the question-mark character names from current create packets and stored rows. The client sent valid GB18030 bytes such as `D2 A1 D2 BB D2 A1` for `摇一摇` and `C2 A5 C5 B6` for `楼哦`; both byte sequences also happen to satisfy UTF-8 syntax, so the bridge's UTF-8 shortcut stored unrelated Cyrillic/Latin code points and the roster's later GB18030 encoding rendered question marks.
- Removed the UTF-8 shortcut from current NoPack create/name-check decoding. These DSTR fields now always use the current EXE's Windows GB18030 code page; ASCII remains byte-identical. Added regression cases for both real UTF-8-shaped GB18030 names and required their roster bytes to round-trip exactly.
- Repaired only the two confirmed affected current rows, guarded by character ID and exact old UTF-8 bytes: character `2` is now `摇一摇` and character `3` is now `楼哦`. No other character, inventory, equipment, quest, skill, pet, or account data was changed.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, `go vet`, production build, and BAT offline/online preflight. Portable MySQL PID `48460` and DNF90Server PID `49336` reached READY with all 30 listeners accepting connections; deployed server SHA256 is `88D950F486CB784D6D181FC33FDCBC9CFCBD6A2BE034D2D796FBCC3BB8DDD614`.
- Live post-deployment creation also passed: the client sent `BB B7 BE B3` for `环境`, the op5 success ACK returned the exact same four name bytes, and character `4` is stored as `环境` in the UTF-8 database.

## 2026-07-25 - JSON-free relational MySQL repository

- Replaced every runtime DNF repository JSON aggregate with MySQL relationship tables. Account metadata, account inventory, character extension stats/location/roster, inventory and warehouse items, equipment, pets and artifacts, quests, skills/points/layouts/cooldowns, mail and attachments, packet metadata, and settings now persist as typed parent/child rows. Current-client raw item evidence remains byte-exact in `LONGBLOB` columns.
- Added an idempotent first-start upgrader. It detects each legacy JSON column, decodes existing values through the authoritative Go record types, replaces the corresponding relational rows transactionally, and drops the old column only after every import succeeds. C# compatibility `extra_json` and `payload_json` text fields are also migrated into key/value or typed JSON-pointer path rows, so neither MySQL JSON columns nor JSON-named columns remain.
- Reworked field-level repository saves to retain their existing aggregate transaction boundaries while replacing only the selected relational collection. Added relational character location/roster persistence, dynamic character-stat rows, ordered pet idempotency tokens, ordered roster/mail rows, deterministic inserts, and migration coverage for primitive and nested legacy payloads.
- Regenerated the checked-in MySQL schema example without runtime repository JSON columns and updated transaction, schema, repository, and migration tests for the new physical model.
- A clean isolated MySQL schema passed round-trip CRUD for every repository aggregate and reported zero JSON columns. A cloned copy of the pre-migration `dnf_local` database then migrated successfully with 3 characters, 5 inventory rows, 9 equipment rows, 102 quest rows, 19 skill rows, and 114 settings rows still readable.
- Before changing the live database, stopped both managed processes and created the recoverable physical backup `runtime/backups/relational-json-20260725-215234` containing 415 files and 228,160,525 bytes: the full MySQL data directory, data ownership state, and matching instance configuration.
- The live migration completed without clearing player data. Post-migration `information_schema` reports zero JSON-typed columns and zero column names containing `json`; the same 3 characters, 5 inventory rows, 9 equipment rows, 102 quest rows, 19 skill rows, and 114 settings rows remain.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, the requested `go vet` set, the production build, and BAT offline/online preflight. Portable MySQL PID `28392` and DNF90Server PID `20536` are READY with all 30 listeners accepting connections; deployed server SHA256 is `A8823A21A26A6162B1E11B9E0DD0B7112B77392CE0F636ECA484D0D3FE2AF136`.

## 2026-07-25 - fresh relational database placed into official use

- At the user's request, stopped the managed server and portable MySQL and removed the migrated player database from the active runtime. The stopped data directory, matching data ownership state, and instance configuration were moved into the recoverable archive `runtime/backups/official-wipe-20260725-221119` (443 database files, 231,642,409 bytes).
- The BAT controller initialized a completely new portable MySQL data directory, and the current server created the 274-table JSON-free relational schema from source. `information_schema` reports zero JSON-typed columns and zero column names containing `json`.
- The official fresh instance has zero repository accounts, characters, inventory items, equipped entries, pets, quest states, skill states, mails, and setting values. Only the required fixed local-account shell and empty account-cargo state exist in the legacy compatibility schema.
- Portable MySQL PID `33856` and DNF90Server PID `47296` are READY with all 30 managed listeners accepting connections. The deployed server SHA256 remains `A8823A21A26A6162B1E11B9E0DD0B7112B77392CE0F636ECA484D0D3FE2AF136`.

## 2026-07-25 - character-slot exchange and tutorial cold-entry crash

- Fixed current-client character-position exchange op295 stream classification. Its exact eight-byte two-u32 body now selects the current upper protocol before the overlapping legacy header parser, so the repository swap and the response use the same current-client framing instead of committing the swap and then disconnecting on a legacy ACK.
- Corrected the earlier assumption that tutorial class0/op3 was safe immediately after op120. Live current-EXE evidence still crashed at `E44ED6` after op120 because the op3 state-1 handler resolved a nil active-room object manager. PVF tutorial scenes now defer op3 until the client sends its op143 progress-30 scene-ready checkpoint; the server writes the op143 ACK first and then the one-shot deferred op3. Ordinary owned dungeon scenes retain their post-op120 op3 flow.
- Added exact stream-boundary regression coverage for op295, tutorial/ordinary scene-state timing coverage, and an op143 ACK-then-op3 integration regression. No database rows, character slots, client patch, PVF, or generated configuration were changed by this source fix.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, the requested `go vet` set, the production server build, and BAT offline/online preflight. The BAT controller preserved the active database and restarted portable MySQL PID `24464` plus DNF90Server PID `26548`; all 30 listeners accept connections and the deployed server SHA256 is `B913E7ABF7A4D396037343AFB8FD7E2E559DA3651877B2096E4923C499AE7374`.

## 2026-07-25 - suppress unsafe tutorial op3 after live retry

- The live retry disproved op143 progress 30 as a current-client scene-ready boundary. The client transmitted that request before its dispatch queue processed op120, so the server's deferred class0/op3 state-1 packet was still queued directly behind op120 and reproduced the same `0xC0000005` at `E44ED6` through a nil active-room object manager.
- PVF tutorial entry no longer synthesizes class0/op3 state 1 at either post-op120 placement or the op143 progress-30 acknowledgement. The normal op143 success ACK remains unchanged, and ordinary non-tutorial owned dungeon scenes retain their established post-op120 state transition.
- Regressions now require female-Slayer and Knight tutorial entry to contain no op3 and require progress-30 routing to emit only its ACK. The stale-scene-owner and ordinary-dungeon cases remain covered separately. This is a tutorial-runtime correction, not a character-slot special case; no database, roster position, PVF, client patch, or generated configuration is changed.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, the requested `go vet` set, the production build, and BAT offline/online preflight. The BAT controller preserved the active database and started portable MySQL PID `47840` plus DNF90Server PID `31080`; all 30 listeners accept connections, both fresh stderr logs are empty, and the deployed server SHA256 is `E37A9C49FE77FD94143F0855F911F3DF0DB883C171CEADEC2965826705EA6484`.

## 2026-07-25 - confirmed mid-tutorial exit persists completion

- Live packet evidence identified the reported dialog as bodyless op42 `GiveupGame`, not merely the system-menu op132 path. Character `1` explicitly left active PVF tutorial dungeon `7118`, received typed op24, completed the town-side op36 scene commit, switched characters, and still re-entered with `tutorial_completed=0`.
- A confirmed active PVF tutorial exit now persists the same durable completion and standard birth-town coordinates as a normal clear before constructing the town transition. Both explicit current-client boundaries are covered: in-dungeon op42 give-up and system-menu op132 back-to-village. Ordinary dungeons, completed settlement rewards, malformed requests, and automatic death return do not use this path.
- Regression coverage executes each explicit exit, checks the op24 target, repository and runtime completion flags, then immediately switches characters before any later town request and verifies the marker remains durable.
- Repaired the already-confirmed live event for character `1` only. The guarded transaction changed one row from tutorial incomplete to complete and set town `38:1`, position `450,234`, direction `5`, area state `3`; slot `0`, job `11`, level `1`, equipment, inventory, quests, and every other character remained unchanged.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, all DNF module tests, complete bridge/control/release suites, the requested `go vet` set, the production build, and BAT offline/online preflight. Portable MySQL PID `19920` and DNF90Server PID `26076` are READY with all 30 listeners accepting connections; the fresh stderr log is empty and the deployed server SHA256 is `4F76CD75A55E1E3158989F9E7D2CBB948A05F93B512D0266B309B38A7B658FC1`.
- Rebuilt the source one-click release as `releases/DNF90-source-oneclick-20260725-224747.zip` (480,146,554 bytes, SHA256 `D27FDAACB7248F18321D29B8AEAE131E9E0BD6C19B91EB2EAE6A08B841D6B143`). Independent in-archive verification matched all 1,642 payload entries against `SOURCE_MANIFEST.sha256`; generated database state, characters, logs, and local runtime configuration remain excluded.

## 2026-07-25 - same-session finish-quest replay suppression (client crash fix)

- Root cause of the 23:10 game exit: the client sent op34 finish for quest 3157 twice (retry/double click). The server committed once and then answered the duplicate with the durable replay receipt in the same session. The current EXE finish handler (sub_1D32120) removes the quest UI entry on the first receipt and NULL-derefs `v63[0x694]` on the second (0xC0000005 at 0x1D3340F), killing the client; the server only observed the socket drop. The same crash also blocked the mainline chain continuation dialog the player expects after a finish.
- `handleCurrentFinishQuestWithResources` now suppresses same-session duplicate replay receipts via a session-scoped answered set (`questFinishAnswered`). Reconnect recovery is preserved: a fresh session has an empty set and still receives the durable replay receipt. First-commit receipts and the post-commit op574/op21 flow (which drives the next-chain quest presentation) are unchanged.
- Updated the replay test to the new contract: first finish sends the full flow, same-session duplicate sends nothing, reconnect receives the replay; rewards/character deltas stay single-committed.
- Validation passed `gofmt`, full `internal/services/dnfbridge` and `internal/modules/dnf/quest` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-25 - expand-open result display via proven 208 channel (plan B)

- The current-EXE 1128 result-window branch (0x1FC fed from list two) never materializes a window in this client build despite the ACK matching the handler's read layout, while the single-open 208 handler reliably shows the result window. The expand-open success now emits two ordered responses: the well-formed 1128 ack (closes the client's expand wait state), then a 208-shaped display ack (`buildMagicBoxSingleAck`) carrying the aggregated rewards, so the player gets the "您已获得" window through the proven channel. Failure handling (`{0x00}`) is unchanged.
- Tests updated: expand success now expects exactly two responses (1128 ack then 208 display ack) with the aggregated reward rows; the material-less variant keeps its header assertions on the first response.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-25 - 86JP magic-box flow: seria batch ack, legacy popup, SeriaLuck doubling

- Adopted the 86JP reference flow (Desktop 86JP 最新整合版, `MagicBoxOpenAckBuilder` + `InventoryHandler.Package.cs` + `InventoryPackageStore.cs`), with packet structure pinned to current-EXE IDA evidence wherever the two disagree:
  - Seria source (2682272) expand (0x0468): native batch ack `[u8 variant=4][u8 doubleFlag][u16 openCount][i16 boxSlot][i16 materialSlot=-1][list1: u16 n + n×0x77 rows][u16 0][list2: doubles]`. variant=4 matches `sub_1D074C0` (skips the bogus material check that fired error 11031; the double list feeds the seria two-pane result branch).
  - Non-seria expand (teddy etc.): 0x00A0 legacy obtained-items popup (`[u8 1][i16 boxSlot][i32 0][i32 0][u16 n][(i32 item, i32 count) rows]`), replacing the earlier 1128+208 two-packet workaround.
  - SeriaLuck mechanic (86JP `SeriaLuckValueMax=8`): account-persisted `seria_luck_value` metadata; each open with value>=8 doubles that round's rewards (granted twice + DoubleRewards) and resets to 0, then +1 capped. Applies to both single (208) and expand opens; the single ack's doubleFlag byte now reflects it.
- Owner wiring: `packageitem.Owner` now holds the account repository; magic-box commands carry AccountID; account save failure rolls the character-item transaction back so rewards and luck never desync.
- Tests: seria batch layout (10 opens from luck 0 doubles exactly once at round 9), teddy 0x00A0 popup bytes, account-persisted luck progression (7 -> double -> 1), failure ack unchanged.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-26 - dispatch trace now observes 0x00A0/0x0468 (debugger-less evidence path)

- x32dbg/x64dbg attach attempts failed on two fronts: hardware breakpoints trip the client's anti-debug (silent exit code 0), and piped headless sessions left the client paused on TLS callbacks. Dropped the debugger route.
- `IsObservedDispatchOpcode` in `Patch/90CN.cpp` now also logs class1 opcodes 160 (0x00A0 random-box popup) and 1128 (0x0468 ten-open) through the patch's own dispatch-state trace, so the exact client handler callback for these packets is captured in `90CN_trace.log` without any debugger. Rebuilt and deployed `90CN.dll` (hash `25DFC301A3A52C89F0112D62BD55996B5E645DF080596D0DF1748E9B6F70C186`, locked in `deploy/assets/client-compatibility.json`).

## 2026-07-26 - expand display unified on the proven 208 channel; dispatch evidence confirms handler delivery

- Patch dispatch trace (newly observed opcodes) proves both the 0x0468 expand ack and the 0x00A0 popup reach their registered client handlers (RVA 0x19074C0/0x192BFF0 -> VA 0x1D074C0/0x1D2BFF0) and run to completion, so the display failure was inside the window layer, not packet routing.
- 0x00A0/0x145 and the 0x36B seria window do not materialize renderable rows in this client build, so expand display is unified on the proven single-open channel: every expand now sends its native ack (seria batch variant=4 for 2682272, ordinary variant=1 ack otherwise) followed by the 208-shaped display ack with aggregated rewards. SeriaLuck doubling, material checks, and the container refresh are unchanged.
- Tests: teddy expects [1128 expand ack, 208 display]; seria expects [1128 batch ack, 208 display with doubleFlag=1 and the doubled round aggregated]; failure ack unchanged.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-26 - single/display ack reward rows are 0x77 wire entries, not 31-byte summaries

- Root cause of the one-row result window: the current EXE reads every magic-box reward list with `sub_1D057B0`, which is hardwired to 0x77-byte item entries. The 86JP BuildSingle 31-byte summary rows parsed as one valid entry followed by garbage, so the window only ever rendered the first reward (teddy showed potions without seria; single opens showed only row one).
- `buildMagicBoxSingleAck` now emits 0x77 wire entries (u16 slot, u32 itemID, u32 count, zero tail) with real granted slots; the 208 single-open and the expand display acks share this shape. The seria batch ack already used 0x77 rows and its double pane renders correctly (red 双倍 window confirmed live).
- Tests updated to 0x77-row expectations for single, teddy-expand, and seria-batch flows.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-26 - seria two-pane window: left = base draws, right = doubled round

- The seria double window rendered only the red (double) pane because the left list carried the aggregated rewards *including* the doubled round, duplicating the right list; the client merge emptied the left pane. Per 86JP `GetPrimaryRewards` semantics, the batch ack's first list is now `DisplayRewards` (base draws only) and the second list stays `DoubleRewards` (the triggered round), so both panes render.
- Owner tracks base-draw aggregation separately (`result.DisplayRewards`) without changing grant totals (doubled rounds are still granted twice into inventory).
- Tests updated: 10 opens from luck 0 -> left list 20, double list 2, doubleFlag=1.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-26 - non-seria expand drops the native 1128 ack (kills the bogus announcement)

- The 1128 handler (`sub_1D074C0`) unconditionally ends with a follow-up announcement (strings 70156/70157, rendered as a shop-cross-class warning by the 2018 `dstr.dat`). Non-seria expand opens don't need that packet at all: the result window is fully carried by the 208 display ack, and the client showed no wait-state hang when 1128 was previously unimplemented. Teddy and other non-seria expands now receive only the 208 display ack, so the result window appears without the bogus dialog. Seria keeps the native batch ack (required for its two-pane window) plus the 208 display ack; its follow-up text remains a known string-table issue to fix separately.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-26 - 208 single ack has no doubleFlag byte (current-EXE read layout)

- The current EXE single-open handler (`sub_1D05A60`) reads two u8s (result, clientType) then u16 slots — there is no doubleFlag byte. Writing one shifted every later field, so any open with a triggered seria double (doubleFlag=1) made the client look up a garbage box slot and the normal result window never appeared. Removed the byte; the ack is now `[u8 1][u8 clientType][u16 boxSlot][u16 materialSlot][u16 count][count × 0x77 rows]`. DoubleFlag=0 cases accidentally worked before because the byte aliased the slot's high byte.
- Validation passed `gofmt`, `packageitem` and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-26 - magic-box probe hooks in 90CN.dll (evidence only)

- Added temporary inline-hook probes in `Patch/90CN.cpp` tracing the current-EXE magic-box S2C chain: 1128/208 handler entries, 0x36B window call, feed function (pane 0/1), follow-up toast, and the 11009/11031/11159 early-error sites. Hook prologues are whole instructions (10-11 bytes; an 8-byte patch split `push imm32`/`call rel32` and caused a 0xC0000005 crash at the trampoline — fixed by widening to full instruction boundaries).
- Deployed `90CN.dll` hash `6BA19D620D199ED1E14B04CC54EC4C944731D71D22422514B475357D3D8BED80` (locked in `deploy/assets/client-compatibility.json`). Remove the probe before the final release build.

## 2026-07-26 - equipment magic-box rewards: top-quality seed and PVF durability (86JP ItemQuality)

- The result window rendered equipment reward rows as "加载中" because the rows carried a stack count where the client expects equipment instance data. Per 86JP `ItemQuality.TopQualitySeed` (999999998) and `GenerateInstanceValue`, equipment magic-box rewards now grant with `quality_seed=999999998` plus PVF `[durability]` in the stack extras (count forced to 1), and display rows write the quality seed at 0x06 and durability at 0x0B instead of a count. The resolver now extracts `[durability]` from the reward item's PVF (`MagicBoxRewardItem.Durability`).
- Seria pool group-3 equipment draws (25xxxxxx) therefore land in the bag with proper equipment metadata and render in the result window.
- Validation passed `gofmt`, `packageitem`, `alignedcmd`, and full `internal/services/dnfbridge` suites, `go vet`; rebuilt via `REBUILD.bat` and restarted through the BAT controller.

## 2026-07-26 - gameplay file modules and unified gameplay time queue

- Split gameplay entry routing out of the legacy and upper transport switches into 17 independently registered gameplay files: NPC shop, Cera shop/package, inventory, skill, disjoint/emblem, booster, rental, equipment upgrades/sockets, aura skin, quest, dungeon, town, adventure info, title, achievement, emotion, and collectbox. The central dispatchers now retain only connection, login, roster, character lifecycle, compatibility probes, and fallback transport responsibilities.
- Each gameplay module owns its legacy/upper opcode routes and any legacy transport-body normalization. The registry rejects duplicate module names, duplicate opcode ownership, and duplicate normalizer ownership at startup; regression coverage pins representative owners and normalization boundaries.
- Replaced the dungeon-only timer adapter with one process-wide gameplay time queue. The ten-second death return, 750 ms tutorial-final grace, and pet satiety 60-second dungeon decay/360-second town recovery ticks now schedule, replace, and cancel through that queue. Dungeon start, clear, settlement, hunt completion, and pet elapsed calculations use the same injectable queue clock. Durable item, rental, premium, mail, and skill-cooldown deadlines remain authoritative MySQL timestamps and are evaluated by their owners; network deadlines, database timeouts, process supervision, and publisher retries remain infrastructure timers outside the gameplay queue.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, the requested `go vet` set, the production build, and BAT offline/online preflight. The BAT controller preserved the active relational database and restarted portable MySQL PID `4500` plus DNF90Server PID `39888`; all 30 listeners accept connections, fresh server stderr is empty, and the deployed server SHA256 is `10F1526893E5FF629C3AAE16D5E74F024A96FBC52B4F5B4DCB3B1D9584D1294B`.

## 2026-07-26 - one gameplay per registration module

- Removed the remaining composite gameplay registrations. The bridge now composes 28 single-purpose gameplay modules: Cera purchase and Cera package opening, equipment/avatar disjoint and emblem compound, guardian gem, equipment socket, equipment-emblem control, avatar socket, avatar emblem, item-grade adjustment, equipment effect, title book, clone title, ordinary dungeon, and Death Tower all have distinct module files in addition to the already independent gameplay modules.
- Moved the request-entry handlers for equipment/avatar disjoint, emblem compound, equipment/avatar sockets, avatar emblem attachment, equipment-emblem control, achievement, and clone title into their owning module files. The former large mixed files retain only shared parsing, PVF, transaction, wire-projection, and persistence helpers; shared algorithms are not registered as gameplay modules.
- Registry regression coverage pins the exact 28-module composition, verifies that every module owns at least one handler or normalizer, verifies representative legacy/upper/normalizer ownership, and continues to reject duplicate names and duplicate opcode ownership.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, the requested `go vet` set, the production build, and BAT offline/online preflight. The BAT controller preserved the active relational database and restarted portable MySQL PID `39468` plus DNF90Server PID `25320`; all 30 listeners accept connections, fresh server and MySQL stderr files are empty, and the deployed server SHA256 is `CCF76BA57B7DDAE33956207880F9A13CA7A56759FC1D547417040542E40C304B`.

## 2026-07-26 - first gameplay domain-owner descent

- Moved the durable rules and repository writes for six independently registered gameplay modules out of `dnfbridge`: emotion selection, clone-title selection, aura-skin-slot opening, item-grade adjustment, achievement progress/title reward, and title-book put/get now delegate to dedicated owners under `internal/modules/dnf/<gameplay>`.
- The aura-skin owner now owns the runtime-PVF ticket rule and the atomic character/inventory transaction. The achievement owner commits progress, one-shot completion, and the optional title grant as one inventory update instead of the previous sequence of best-effort saves. The title-book owner owns source/target validation, free-slot selection, moves, and persistence. Bridge files retain current-EXE body decoding, session identity, logging, packet bytes, and response ordering only.
- Extracted the equipment quality seed sentinel, unbiased random generation, validation, and item mutation into the shared `itemquality` domain component; item-grade adjustment owns material classification/consumption and the committed quality mutation.
- Added owner tests for all descended gameplay modules, bridge tests that pin emotion/clone-title/title-book wire bytes and ordering, and a source-boundary regression test that rejects direct repository writes returning to the migrated bridge files.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, and the requested `go vet` set. The production binary passed BAT offline/online preflight; the BAT controller preserved the relational database and restarted portable MySQL PID `7564` plus DNF90Server PID `25852`. All 30 listeners accept connections, fresh server and MySQL stderr files are empty, and the deployed server SHA256 is `B154FA9B57AA5F03E4AB5D7FEE22774B1F9AE44649AF48579D9F81D386AF54C0`.

## 2026-07-27 - correct current-EXE 208 single-open header

- Corrected the previous 208 single-open conclusion after tracing the complete `sub_1D05A60` success branch: the current EXE reads `[u8 result][u8 clientType][u8 doubleFlag]` before the two u16 source slots and the 0x77-byte reward rows. Omitting `doubleFlag` shifted every later field, so Seria single-open results remained on "loading".
- `buildMagicBoxSingleAck` now always writes the byte and derives it from the persisted SeriaLuck trigger. Regression coverage pins both zero and one flag layouts.

## 2026-07-27 - legacy coin_general stacks migrate into the revive-coin wallet

- Live reproduction proved that right-clicking the legacy item-42 stack stalled inside the current client before class1/op44 was sent; server, MySQL, and protocol heartbeats remained healthy. The affected MySQL row was the obsolete backpack representation of runtime-PVF `stackable/cash/coin_general.stk`, while current reward paths already route each item-42 unit directly into the fixed item-1/slot-1 revive-coin wallet.
- Main-inventory snapshot migration now atomically converts only item-42 rows carrying that exact stored PVF path, combines their complete count with every item-1 wallet row, repairs canonical wallet metadata, deletes the obsolete backpack rows, and persists the result before any list-0 bytes reach the client. The normal op44 one-at-a-time owner remains unchanged for valid request-bearing consumables.
- Regression coverage pins the reported 30-wallet + 856-legacy-stack case to one slot-1 wallet with 886 coins, proves the invalid backpack row is absent from both MySQL-backed state and the client snapshot, preserves non-matching item-42 rows, and proves the migration is idempotent.
- Production login evidence confirmed the guarded migration for character `1`: one 856-unit consumable row plus the existing 30-unit wallet became one 886-unit slot-1 wallet before the list-0 snapshot was sent. The obsolete backpack row was not exposed to the fresh client.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, the requested `go vet` set, production build, and BAT-only offline/online preflight. The existing relational database was preserved; portable MySQL PID `49708`, DNF90Server PID `28596`, and fresh responsive client PID `25320` are running with all 30 listeners accepting connections. Deployed server SHA256 is `6E4B83E519638A939F77E0FFA92B5DE7B709BC2102B61510E80F77FE39796A3E`.

## 2026-07-27 - current-EXE mailbox handlers and atomic asset transfer

- Reviewed 86JP merge request 46 as a domain/reference implementation, but kept every handler and wire layout pinned to the active NoPack.exe IDA database. Current requests are now decoded exactly for op94/op315 send, op95 batch extraction, op96 open, op134 state change, and op324 character query; responses follow the matching current handlers instead of the reference server's generic ACK assumptions.
- Mail send now resolves attachments from the sender's durable list/slot, validates the client item ID/count, uses runtime-PVF `[price]`, applies the IDA-proven `100 + floor(gold/20) + 1000*n + floor(total item price/20)` postage, and atomically debits gold/inventory while creating the recipient mail. Extraction atomically grants every selected mail's gold/items and marks the mails claimed/read; concurrent duplicate claims commit exactly once.
- Added one mailbox asset unit of work for memory and MySQL repositories, uint32-compatible mail IDs, safe deletion for op134 status 0, repository-backed op324 projections, strict malformed/trailing-byte rejection, and fail-closed handling for unsupported special/global sends or missing PVF/character evidence.
- Regression coverage pins the exact packet bodies, multi-attachment transfer, forged-slot rejection without mutation, postage calculation, atomic batch claim, concurrent replay rejection, op134 persistence, op324 layout, and malformed request failures.
- Class0 op0x61 nonempty mailbox snapshots and op0x62/op0x63 client notifications remain deliberately deferred: the current attachment-row type-26/PVF branch and full 0x77 projection still need runtime evidence. MR-level per-attachment CAS and request-token idempotency were not copied because this client identifies op95 claims by mail ID and sends no idempotency token.
- Validation passed `gofmt`, `go list -buildvcs=false ./...`, all DNF module tests, the complete bridge/control suites, the requested `go vet` set, and the production build (SHA256 `8644D2D630BC70AF15230EAFAEFE49B14A123730571688B75BD1D2D920C2E47B`). BAT offline preflight remains blocked because live PID `54960` is executing the locked prior image exposed as `runtime\bin\DNF90Server.exe~`; the running service was not restarted.

## 2026-07-28 - current-EXE title-book animation replacement uses op568 and mode0 slot 13

- Fixed the title-book “替换称号动画” route after live traffic proved that the button sends class-1 command `568/0x0238` with one `u32` title item ID. The clone-title gameplay module had incorrectly registered decimal `569/0x0239`, so the correct request missed the module and fell through to the obsolete blocked-migration rule.
- Current NoPack IDA evidence pins `sub_104D910`, `sub_104D940`, and `sub_104E0D0` as the four-byte op568 senders. Its S2C command table registers op568 as `google::protobuf::DoNothing`, so the server no longer fabricates the old op569 success body. After committing the selection it rebuilds the authoritative class0/op2 mode0 actor projection and substitutes only equipped title appearance slot 13.
- Clone-title selection is durable per character, accepts zero to cancel, and now rejects client-supplied item IDs unless the character actually owns that title in repository list 100 (the title book). Re-login and later town actor creation reuse the same persisted projection instead of keeping a session-only visual.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, all DNF module tests, the full `dnfbridge` and control suites, and the requested `go vet` set. BAT-only deployment preserved the relational database, rebuilt the server, and restarted portable MySQL PID `53588` plus DNF90Server PID `32344`; all 120 listeners accept connections, both fresh stderr logs are empty, and the deployed server SHA256 is `3F30C3FD34A8DBA949852B75973DC54313276D35E06CF6E1F4518034849847DA`.

## 2026-07-28 - town movement and wear/unequip return to one-shot current-EXE updates

- Fresh client/server traces separated bootstrap traffic from interaction traffic. Character bootstrap sent one mode0 actor row followed by the two established mode1 projections; the later 27 mode0 rows and 120 of the reported op13 rows were generated by 24 ordinary wear/unequip operations, because every successful op19 ACK was followed by five full container snapshots and another complete mode0 actor row.
- Equipment and avatar endpoint moves now stop after the exact class1/op19 incremental ACK. Pet equipment moves use the same boundary; slot 26 additionally keeps only its dedicated op105/op102 creature-state projection. The post-move full `[0,1,2,7,38]` op13 replay and mode0 actor reconstruction were removed because they destroy/rebuild live client managers after the native incremental handler and leave town visuals stale.
- Town op36 already emits the authoritative typed op24 transition. `broadcastTownPlayerEnter` now exchanges only peer op17 state and no longer sends a second self-containing op24 roster, so a solo transition reaches the current EXE exactly once and does not reset the camera owner immediately after binding it.
- Regression coverage pins zero post-actions for ordinary equipment/avatar endpoint moves, only the creature-state post-action for slot-26 pet changes, no solo co-presence packet, no duplicate newcomer transition, and one op24 from the town-move workflow.
- Validation passed `gofmt`, `go list -buildvcs=false ./...`, every DNF module test, the full `dnfbridge` and control suites, the requested `go vet` set, production build, and BAT-only startup/runtime preflight. Portable MySQL data was preserved and remains READY as PID `15772`; DNF90Server SHA256 `F6D39D04715069C303A018FB0CDD206DB0717E754E5FB2CC39278A6CFBD3452E` is READY as PID `58528`, and all 120 listeners accept connections.

## 2026-07-29 - restore known-good town self binding and post-wear scene projection

- Corrected the 2026-07-28 one-shot hypothesis after live current-client testing showed that the reduced sequence broke every town-resident projection: the camera stopped following after a room transition, while equipment, avatar, creature, and aura changes became visible only after a character reload. The login bootstrap itself still completed through mode0/mode1 player-state packets and op24; the first reproducible divergence from the known-good 2026-07-05 server was after bootstrap.
- Runtime town transitions again send the authoritative op24 transition followed by the complete op24 area roster with the selected character's self row last. Runtime op36 and legacy previous-village transitions no longer inject the experimental op22/op23 pair; those location notifications remain only in the established initial-login route.
- Successful equipment/avatar moves again order the exact op19 ACK, repository-backed item-container snapshots, and a full class0/op2 mode0 selected-actor projection. Pet slot-26 moves likewise restore list 7 before the creature-state and actor projections. Regression coverage pins the self-containing roster, solo transition behavior, mode0 object key, and restored post-action order.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete `dnfbridge` and control suites, the requested `go vet` set, the production build, and BAT-only offline/online preflight. The relational database was preserved; portable MySQL PID `56404` and DNF90Server PID `17436` are READY, and all 120 listeners accept connections. Deployed server SHA256 is `EF112B4A99A9890E4CE39DF249BCDE8F162B05B83C4BE9CA4A2E9FA92811074F`.

## 2026-07-29 - scene-ready skill initialization and current-actor appearance refresh

- Fresh client traffic proved that the complete repository-backed class0/op19 skill projection was sent once immediately after typed op24 but before the client emitted its type1345 scene-ready acknowledgement; the server did not send a duplicate or empty replacement. The prepared skill/quick-slot body is now retained through op24 and flushed exactly once only after the deferred actor-binding tail handles type1345, so later bootstrap packets cannot rebuild the HUD over it.
- Live wear/unequip traffic proved that class1/op19 ACKs, durable equipment commits, full mode1 equipment rows, and op357 all targeted the same current actor, yet the client still kept stale equipment and avatar rendering. Current NoPack IDA shows mode0 `sub_20036C0` first resolves and reuses the existing `sub_2699A30` actor by key. The post-move chain is therefore reduced to `ACK -> one current-context mode0 self projection`; the ineffective mode1/op357 pair is no longer sent.
- Full mode0 refresh is restricted to the finalized selected town actor in local scene context zero and is suppressed in dungeons or across stale/remote scene ownership. It reads the post-transaction repository state, carries all fourteen explicit appearance slots so unequip clears stale rows, and does not alter the typed op24 town-movement/camera route.
- Pet slot 26 keeps its dedicated absolute state chain and now follows `ACK -> op105 -> optional op102 -> current-context mode0`, while pet artifacts remain on their separate endpoint path. The full actor projection carries the authoritative equipped creature and emits an empty creature state after unequip without resurrecting a stale `PetRecord`.
- Regression coverage pins the pre-type1345 pending skill state, one post-barrier skill packet, duplicate-barrier silence, exact ACK-to-mode0 order with no mode1/op357 tail, explicit worn and empty slots, local-self scene gating, pet post-action order, and pet state/growth before the actor projection.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete `dnfbridge` and control suites, the requested `go vet` set, the production build, and BAT-only offline/online preflight. The relational database was preserved; portable MySQL PID `63236` and DNF90Server PID `62948` are READY with all 120 listeners accepting connections. Deployed server SHA256 is `DF2A862672E460376AB6247EECDA099649B35AF4F5FC5C8C31B3C89D26AA36F2`; the transport-only client DLL remains unchanged at `7C331F5194751CFC86EFC38D6C33E32C7531C85674BDA3E0E8BE98669B41C43D`.
## 2026-08-03 - joust shop crash fix and all-day event activation

- The Hendon Myre joust NPC no longer points at unregistered item-shop ID `101262`, which caused the current client to crash before a shop request reached the server. The existing indexed slot `400000022` is retained, its shop script now carries the 2017 joust goods, and the shop NPC owner is patched from `20325` to the map-owned NPC `20449`. The NPC remains between Aisha and Grandis at `(2183,156)`.
- Current NoPack op108 login parsing is now followed exactly for a two-row event catalog: spend-time event `2347` keeps its extended route, while joust event `2365` is installed as a generic base row and active row with twelve zero parameters and no historical schedule extension. This makes the activity active for the full server uptime. Empty current-client op1291 info and op1293 history queries now receive their exact success layouts; the fixed 500-row history payload uses the native zero-sequence empty sentinel.
- The source-only PVF utility now removes only the historical visibility gates, rewrites the target NPC/shop references at token level, accepts both the original and already-positioned town tuple, verifies the unchanged item-shop index, and repacks a candidate instead of modifying any active PVF in place. The synchronized runtime, IDB-client, and configured game PVFs are `111095477` bytes with SHA256 `ED28D2C1C83B04339F58E8A193F2C3C9CEF6AD54C32B29020A837E4A6A2AFD8D`; the pre-deployment copies are under `runtime/backups/pvf/20260803-joust-shop-all-day-predeploy`.
- Validation passed `gofmt`, `go list -buildvcs=false ./...`, all DNF module tests, the complete `dnfbridge` and control suites, the requested `go vet` set, and the production build. Deployed server SHA256 is `D8F28CA04BE15B58DA6AB9219A455809836D535C3225568A844F01C15B3BD7AF`.

## 2026-08-03 - joust NPC click initializes the native event window

- Current NoPack tracing confirmed that the NPC action and all-day event gate already reach owner `609`; the apparent no-op happened after its empty op1291 query because a success reply alone does not populate that owner. The client separately consumes class-0 op1241 in `sub_E72490` as one fixed 90-byte opening roster: a `u16` round plus eight packed 11-byte knight records.
- The op1291 handler now preserves its proved zero-result success reply and immediately follows it with the required op1241 roster. Opening knight indexes `0..7` come from the twelve indexed entries in `event/chn_event/chn_joust.evt`; all unproved counters and statuses stay at the native zero waiting state. Regression coverage pins packet order, class/message IDs, the exact 90-byte body, all eight indexes, and zero-filled state fields.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, all DNF module tests, the complete `dnfbridge` and control suites, the requested `go vet` set, and the production build. BAT-only deployment preserved the relational database; the previous server is backed up under `runtime/backups/server/20260803-joust-roster-push-predeploy`, and the deployed server SHA256 is `BF29A5F053B27CDF885642B34D3CC41E3A76F0073DB8E50ECAD6BF7F466EC8E9`.

## 2026-08-04 - preserve native joust NPC event gates

- Live current-client evidence corrected the earlier visibility-gate assumption. Two clicks produced no op1291 request; owner `609` stayed in the fallback registry with zero opening rows, while event type `73` existed and its live minimum level was `50`. The blocker was therefore before the server protocol path.
- The target NPC had lost `[open visible event] 2365` and `[event open] 2365 -1 -1`. The latter is required by the current client to enable the `[joust event]` role action, so both native sections are now preserved. All-day availability remains owned by the server's active event-2365 op108 catalog; the shop replacement and Hendon Myre placement are unchanged.
- The source-only PVF utility now rejects a candidate missing either native event section. The synchronized runtime, IDB-client, and configured game PVFs are `111095486` bytes with SHA256 `2D6847A6C2F02AEFF8A83A668464CB249F76AD680CF5D9149E06C6B03C4F34F1`; pre-deployment copies are under `runtime/backups/pvf/20260804-joust-click-gate-predeploy`.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, the PVF utility compile check, all DNF module tests, the complete `dnfbridge` and control suites, and the requested `go vet` set.

## 2026-08-04 - initialize the joust roster before the NPC click

- A fresh current-client retry with the restored PVF gates still produced no op1291 request. Live memory proved that event type `73` was installed with minimum level `50`, but owner `609` remained in its fallback registry and all three roster vectors were empty. The NPC action therefore stopped locally before any server request.
- The login activity chain now sends the native class-0 op1241 opening roster immediately after the process-first op108 event catalog and before op1206 progress. Later character selections in the same client process repeat op1241, allowing a reconnected session to recover without illegally replaying the process-lifetime op108 descriptor. The op1291 success path keeps the same roster push as an idempotent recovery route.
- Regression coverage pins the protected op108 -> class-0 op1241 -> class-0 op1206 order, the exact 90-byte eight-knight roster, and the partial-write retry path. Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, every DNF module test, the complete bridge/control suites, the requested `go vet` set, and the production build. BAT-only deployment preserved the relational database; the prior server is backed up under `runtime/backups/server/20260804-joust-login-roster-predeploy`, portable MySQL PID `18692` and DNF90Server PID `23132` are READY, all 120 listeners accept connections, fresh stderr is empty, and the deployed server is `46196736` bytes with SHA256 `18A8C65A90D9D2BCA7B7BA6ECB79586CD1A2FD0BB4E7AA95E563E887C4F5B614`.

## 2026-08-04 - fix joust match profile keys that crashed town login

- A current-client town-login crash at `09:45:14` mapped `0xC0000005` through the EXE's deliberate null-write abort stub to the type-73 match updater `sub_F2EE20`. The failing round `51420` began with rider 4 defeating rider 8 while the server sent generated profile keys `5/3`; the current EXE has only four PVF-backed profile vectors and the absent key 5 produced an empty timeline followed by the native invalid-parameter abort.
- The seven monotonic values in each PVF battle group are client timeline percentages, not seven independently selectable action IDs. Tournament match records now send the opponent attack-type group keys proved by the EXE/PVF pairing: attack types `0`, `1`, `27`, and `28` map to profile keys `0..3`. Round 51420's first match is consequently the valid `4/1` versus `8/1` pair, and deterministic winners, bracket progression, odds, betting, history, and settlement remain unchanged.
- Regression coverage checks every `uint16` round against each participant's opponent attack type and pins the real-PVF crash round. Validation passed `gofmt`, `go list -buildvcs=false ./...`, all DNF module tests, focused Joust/bridge tests, the control suite, the requested `go vet` set, the real runtime PVF smoke test, the production build, and BAT-only offline/online preflight. The full bridge suite still reports four pre-existing town teleport/crystal packet-order failures outside this change. The relational database was preserved; the prior server is backed up under `runtime/backups/server/20260804-joust-action-profile-login-crash-predeploy`, portable MySQL PID `27664` and DNF90Server PID `24896` are READY, all 120 listeners accept connections, fresh stderr is empty, and the deployed server SHA256 is `03B1816DB9D361527B06B60F34F94535603B32B2F703106A66E5AEF52C2B2295`.

## 2026-08-05 - current EXE weapon-effect rune application

- Current NoPack live traffic for `CmdPacketAddEquipmentEffect` (951) is now handled as its proved 21-byte request: main-inventory target list at byte 12, target slot at byte 13, and source slot at byte 17. The success response preserves the exact native shape: one success byte followed by the original request body.
- Added the `equipmenteffect` domain owner. It transactionally validates a PVF `[equipment effect]` stackable and its `[int data]` effect ID, verifies that the target is an unsealed un-equipped PVF weapon above normal/high grade, consumes one rune, and records the effect durably. A historical client row-slot mismatch is recovered only when there is exactly one real effect rune in the main inventory; ambiguous state fails closed, so an unrelated material is never consumed.
- The original projection location stated here (`raw 0x38`) was later disproved by the current EXE reader: it is the expiration timestamp, not a rune field. That implementation is superseded by the corrected `raw 0x45` mapping documented below. The request transaction and persistence behavior remain valid.

## 2026-08-05 - project weapon-effect rune into equipped-item tooltips

- The first effect-rune deployment correctly stored `equipment_effect_id=1` on the target weapon and consumed the peach rune, but only the main-inventory 0x77 projection wrote the tail rune field. After the player wore the weapon, list-3/mode1 used `currentItemListEntryFromEquipment` and therefore the native tooltip had no effect line.
- The equipped projection work recorded here used the same subsequently disproved `raw 0x38` location and is superseded by the corrected mapping below.
- Validation passed `gofmt`, focused bridge and equipment-effect owner tests, `go vet ./internal/services/dnfbridge`, and the production build. BAT-only deployment preserved the relational database; the prior image is under `runtime/backups/server/20260805-equipment-effect-equipped-tooltip-predeploy`, portable MySQL PID `30996` and DNF90Server PID `33672` are READY, all 120 listeners accept connections, fresh stderr is empty, and deployed server SHA256 is `9E006611BEB41B8A2EA466866B7A9306CADD979BE1D485EF4FBE2153C1AEAB15`.

## 2026-08-05 - correct current-client weapon-effect rune hydration

- The missing 桃花 tooltip line was traced to two independent current-client mistakes: the current `90CN.dll` scene-route allowlist excluded `CmdPacketAddEquipmentEffect` (951), so its native success handler never ran; and the prior server projection wrote raw `0x38`, which the current 0x77 reader uses for expiration.
- The durable rune ID now maps to raw `0x45..0x46`: the proved current emblem vector occupies raw `0x3C..0x44`, and mode1 forwards the following word into its ordinary-equipment state. Both bag and worn-equipment 0x77 projection preserve raw `0x38` expiration and set/clear only this rune word. After mode1 creates equipment objects, a single list-3/op14 refresh includes only equipped entries that actually have `equipment_effect_id`.
- The Release/Win32 DLL admits opcode 951 to the native route. Validation passed `gofmt`, package listing, every DNF-module test, the complete bridge/control suites, the requested `go vet` set, the focused rune regression, a Release/Win32 DLL build, and a server candidate build (`runtime/tmp/DNF90Server-equipment-effect-rune-0x45-candidate.exe`, SHA256 `7491F8F2371A3CA361D60164C7DF5E4A55ECCD5D7DC3CA629A6280F28EB02211`). After the game was closed, the prior runtime DLL and EXE were preserved under `runtime/backups/20260805-equipment-effect-rune-0x45-predeploy`; the deployed DLL and server both match their verified candidates (DLL SHA256 `33BA46E898D592CACAD5B3774A682660BF1194DADB80332456C4773A781652CE`, EXE SHA256 `7491F8F2371A3CA361D60164C7DF5E4A55ECCD5D7DC3CA629A6280F28EB02211`). BAT-only startup reports MySQL PID `30996` and DNF90Server PID `33344` READY with all 120 listeners accepting connections. No database state was modified during deployment.

## 2026-08-05 - Cera package overflow delivery

- The 2018 Beast Guardian Cera package (`490701964`) is now opened through the mailbox asset transaction. If a normal PVF reward category is full, its complete reward row becomes a durable, non-expiring system-mail attachment while the source package, all direct rewards, and the mail commit atomically.
- Direct placement is first probed on a cloned inventory. This prevents the former all-or-nothing rejection and also prevents a partial stack merge from being duplicated when the remainder must be mailed. Avatar, creature, and pet-consumable placement keeps its existing authoritative category handling.
- The current client receives its normal op518 success and inventory refresh, then a mailbox alarm; delayed alarm delivery is harmless because the saved mailbox snapshot remains authoritative. Tests cover atomic main-bag overflow with preserved item expiry/raw state and the live PVF's 28 rows, 996 total units, eight avatar choices, coupon, material, and pet-feed rows.
- Validation passed `gofmt`, `git diff --check`, `go list -buildvcs=false ./...`, all DNF module tests, the full bridge and control suites, the requested `go vet` set, and a real-PVF smoke test. The deployed server SHA256 is `7491F8F2371A3CA361D60164C7DF5E4A55ECCD5D7DC3CA629A6280F28EB02211`; the previous image is backed up under `runtime/backups/server/20260805-cera-package-overflow-predeploy`. BAT preflight passed, the readiness endpoint reports HTTP 200, portable MySQL remains PID `30996`, and DNF90Server is READY as PID `33344`.

## 2026-08-05 - party leave authority and return-town co-presence recovery

- A party membership mutation that leaves one manager-side member no longer immediately reprojects that singleton through the client party packets. The central manager keeps the short-lived invite anchor, but the remaining client receives the native empty party projection, preventing the stale non-leader dungeon-entry gate after leave, kick, disconnect, or cross-party join.
- Leave packets are consumed even when the old client cache cannot be imported. The bridge sends the successful leave acknowledgement and clears that stale projection instead of falling through to the legacy handler, so an already-desynchronized client returns to a solo state deterministically.
- After a party returns from the dungeon-selection page, each client waits for every same-area member to consume its own op132 scene-finalizer. It then reconstructs remote actors and rebinds the realtime roster in native order, restoring town visibility and the party/minimap roster without injecting actors into a peer that is still on the selector page.
- Focused bridge regression coverage now pins leader leave, kick, and cross-party-join residual singleton clearing, alongside the existing selector synchronization and runtime-party authority tests. This entry documents source changes only; no running server has been replaced while players remain connected.
